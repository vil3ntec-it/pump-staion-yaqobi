# دستورهای دائمی این ریپو (برای Claude)

این دستورها یک‌بار توسط صاحب ریپو داده شده و برای همهٔ سیزن‌های بعدی معتبر است:

## جریان کار اتومات — بدون سوال از کاربر

بعد از تمام شدن هر تغییر (تاییدشده در تاریخ 2026-07-13 توسط صاحب ریپو):

1. تغییرات را روی برنچ کاری کامیت و پوش کن.
2. **اتومات Pull Request به `main` بساز** — بدون این که کاربر بگوید.
3. **اتومات PR را مرج کن (squash)** — بدون این که کاربر بگوید.
4. دیپلوی خودکار است: با مرج شدن به `main`، ورک‌فلو `deploy-pages.yml` سایت را روی GitHub Pages می‌برد.

فقط وقتی مرج نکن که تغییر ناقص/خراب باشد یا خود کاربر در همان سیزن چیز دیگری گفته باشد.

## ⚡ قانونِ همیشگیِ سرعت — پیش از هر مرج، هر سیزن، برای همیشه

خواستهٔ صریحِ صاحب ریپو (۱۴۰۵/۰۶/۲۷): «این‌هایی که باعثِ کندی می‌شدند دیگر
برنامه را کند نکنند. با هر آپدیت باید برنامه سریع بماند — این را ثبت کن که در
هر سیزنی که آمدم برقرار باشد.»

پس این بخش **بالای همهٔ بخش‌های دیگر** است و هر سیزنی که به این ریپو دست
می‌زند، پیش از کامیت باید از آن رد شود. کندی یک باگ است، نه سلیقه.

### ۱) هفت چیزی که تا امروز برنامه را کند کرده‌اند — همه با عدد

| کاری که کردیم | چه شد | کجا ثابت شد |
|---|---|---|
| ساختنِ همهٔ ردیف‌های یک جدول | ۶۰۰ ردیف = ۷٬۷۱۴ms و ۶۵۰MB | `bigtable` |
| جدولی که هم‌قدِ ردیف‌هایش بلند شود (مجازی‌سازی می‌میرد) | ۲۰۰ ردیف = ۴٬۶۵۲ms | `ledgerperf` |
| سایهٔ **محو** روی کادرِ صفحه‌قد | ۶۰٪ وقتِ هر فریمِ اسکرول | `scrollperf why` |
| `LayoutUpdated` بی سنجشِ `IsEffectivelyVisible` | ۱۳٪ وقتِ نخِ رابط | `scrollperf trace` |
| خواندنِ همهٔ ردیف‌ها برای یک جمع (`ListAsync(null)`) | با **هر** ویرایشِ هر خانه | `years` |
| کارِ دوره‌ای بی سنجشِ `PumpDbContext.Version` | نوار با هر رفتن به بخش همه‌چیز را می‌خواند | `years` |
| انیمیشنِ `INFINITE` روی کنترلِ پنهان | CPU دائمی، کامپیوتر داغ | `startup` (بی‌کاری) |

### ۲) ممنوع‌ها — این‌ها را برنگردانید

- ⛔ **پنجرهٔ چسبان را برندارید** (`ExcelGrid.StickyRowLimit`). بی آن، شمارِ
  ردیفِ زنده دوباره با شمارِ ردیف‌های داده بالا می‌رود و «ده سال داده» می‌میرد.
- ⛔ **`GrowsToContent` را روی جدولِ بلند روشن نکنید.** فقط ورق و پارچه، که
  سقفشان ۸۰ ردیف است.
- ⛔ **سایهٔ محو (`blur`) روی هیچ کادرِ صفحه‌قدی نگذارید** — `SectionShadow`
  عمداً بی‌محو است. محو فقط برای کارت‌های کوچک.
- ⛔ **هیچ شنوندهٔ `LayoutUpdated`ی بی `if (!IsEffectivelyVisible) return;`**.
- ⛔ **هیچ کارِ دوره‌ای بی ترمزِ `Version`** (یا `Pending`ِ خودش).
- ⛔ **هیچ انیمیشنِ بی‌پایانی بی کلاسی که خاموشش کند.**
- ⛔ **`SUM`/`CAST` را به SQLite ندهید** برای مبلغ‌ها (متن ذخیره می‌شوند) —
  ولی «همهٔ ردیف‌ها را بخوان و در C# جمع بزن» هم ممنوع است؛ همان یک ستون را
  بخوانید (`LedgerService.SumAsync`).
- ⛔ **بخشِ پنهان حق ندارد یک دستورِ دیتابیس بزند یا یک ردیفِ زنده داشته باشد.**

### ۳) سنجش‌های اجباری — CI هم همین‌ها را می‌زند

#### یک دستور برای همه‌شان — `fullscan` (از ۱۴۰۵/۰۶/۳۰)

خواستهٔ صریحِ صاحب ریپو: «بعد هر اپدیت همه برنامه از اول تا اخر اسکن بشه که
کند که نشده و لگ و باگی نگرفته باشه.» پس **پیش از هر مرج** این یکی می‌دود:

```
dotnet run --project PumpYaqobi.UiTests -c Release -- fullscan        همهٔ نوزده سنجه
dotnet run --project PumpYaqobi.UiTests -c Release -- fullscan quick   بی سه سنجهٔ سنگین
```

ترتیبش «از اول تا آخر»ِ خودِ برنامه است: باز شدن ← بی‌کاری ← رنگ ← دفترها ←
حساب‌ها ← اسکرول ← جدولِ بزرگ ← کارت‌ها ← ورق ← داشبورد ← چاپ ← پنج سال داده
← رفتارِ کاربر. آخرش یک جدولِ سبز/سرخ چاپ می‌کند و کدِ بازگشتش صفر است فقط
اگر همه سبز باشند.

- ⚠️ **هر سنجه در یک فرآیندِ جدا** می‌دود و این عمدی است: آوالونیا در هر
  فرآیند یک بار راه‌اندازی می‌شود، و چند سنجه در یک فرآیند یعنی عددهایی که از
  کَش و حافظهٔ سنجهٔ قبلی آلوده‌اند.
- ⚠️ `cloudlogin` و `themeflip` داخلش نیستند (اولی اینترنت می‌خواهد، دومی
  همان کارِ نیمه‌تمامِ پایین) — آن دو جداگانه.
- ⚠️ عددِ کند را با **خودِ `main`** بسنجید (`git worktree`)، وگرنه نوسانِ
  ماشین را به گردنِ تغییرِ خودتان می‌اندازید. همین اسکن یک بار ۴۴٪ CPUِ
  بی‌کاری نشان داد و روی `main` ۵۱٪ بود.


```
dotnet test PumpYaqobi.Tests -c Release                  ۱٬۰۱۱ آزمون
dotnet run --project PumpYaqobi.UiTests -c Release -- bigtable    اندازهٔ جدول بی‌اثر + درستیِ ترتیبِ ردیف‌ها
                                                     -- ledgerperf   دفترها زیرِ ۴۰۰ms
                                                     -- scrollperf   گامِ چرخ زیرِ ۱۲۰ms
                                                     -- gridperf     یک میلیون ردیف، ردیفِ زنده زیرِ ۱۲۰
                                                     -- idle         بخشِ پنهان: صفرِ مطلق
                                                     -- printperf    پنجرهٔ چاپ زیرِ ۲۰۰ms باز شود
                                                     -- waraqperf    ورق زیرِ ۴۰۰ms
                                                     -- enterperf    داخل و بیرونِ حساب زیرِ ۴۰۰ms
                                                     -- scrollend    ته اسکرول می‌ایستد، پرپر نمی‌کند
                                                     -- themeflip    تعویضِ تم چیزی را جابه‌جا نکند
                                                     -- verify       شانزده رفتارِ کاربر
                                                     -- cloudlogin   ثبت‌نام، ورود، کد و اشتراک (ابرِ ساختگی)
                                                     -- inputchars   میانبر نویسه را نخورد
                                                     -- serverdot    چراغِ سرور با سرورِ واقعیِ محلی
                                                     -- loginart     نقشهٔ صفحهٔ ورود: تم، کیفیت، کَش و بی‌وزنی
```

⚠️ **سنجهٔ اصلیِ کارایی «ردیفِ زنده» و «شمارِ دستورِ دیتابیس» است، نه
میلی‌ثانیه.** وقت روی ماشینِ CI نوسان دارد؛ عددِ ساختاری دروغ نمی‌گوید.

⚠️ **`themeflip` عمداً در CI نیست — و یک کارِ نیمه‌تمام است، نه فراموشی.**
یک بار اضافه شد (۱۴۰۵/۰۶/۲۷) و روی رانرِ گیت‌هاب قرمز شد در حالی که محلی سبز
بود. بخشِ **ساختاری**اش آن‌جا هم صفر بود (هیچ سرستون/خط/نوشته‌ای جابه‌جا
نشده بود)؛ فقط سنجهٔ **پیکسلی** برای `shifts` و `invoices` ~۶۰٪ اختلاف داد،
در حالی که همان‌ها محلی زیرِ ۱٪ بودند و بقیهٔ بخش‌ها در همان اجرا زیرِ ۳٪.
گمانِ اول: `BrushTransition`ِ ۰٫۳ ثانیه‌ایِ `Border.card`/`shift-card`/`stat`
— روی ماشینِ کندتر عکس وسطِ همان انتقال گرفته می‌شود. هر کس این را تمام
می‌کند: اول پیش از هر عکس منتظرِ ته‌نشین شدنِ انتقال‌ها بماند، بعد دوباره به
CI اضافه‌اش کند. **سنجه را ضعیف نکنید تا سبز شود.**

### ۴) وقتی کند شد، این ترتیب را برو — حدس نزن

۱. **اول بساز که ببینی**: یک سنجشِ تازه در `PumpYaqobi.UiTests` که همان کارِ
   کاربر را با عدد انجام دهد. («۲۰۰ ردیف کند است» تا سنجیده نشود حرف است.)
۲. **بعد نمونه‌بردار**: `dotnet-trace collect --format speedscope … -- <سنجش> trace`.
۳. **بعد A/B**: مثلِ `scrollperf why` یک چیز را خاموش کن و دوباره بسنج.
۴. **در آخر ریشه را درست کن، نه نشانه را** — و همان سنجش را در CI بگذار تا
   فردا کسی دوباره خرابش نکند.

⚠️ و همیشه **پیش از اصلاح، عددِ خودِ `main` را هم بگیر** (یک `git worktree`ی
`HEAD`): یک بار سنجشِ `years` بعد از تغییر قرمز بود و معلوم شد پیش از تغییر
هم همان‌قدر قرمز بوده. بی این مقایسه، آدم تقصیر را گردنِ کارِ خودش می‌اندازد.

## 🔍 اسکنِ کاملِ ۱۴۰۵/۰۶/۳۰ — چه سرخ شد و حقیقتش چه بود

گزارشِ صاحب ریپو: «برنامه باز خیلی کند شده و اسکرول هم لگ داره و بعضی بخش‌ها
دیر باز میشه.» همهٔ نوزده سنجه دویدند (`fullscan`) و بعد **همان‌ها روی
`v3.1.113`** در یک `git worktree` دوباره دویدند. نتیجه، بی تعارف:

| سنجه | این نسخه | v3.1.113 | داوری |
|---|---|---|---|
| `years` — باز شدنِ پنجره | ۴٬۰۸۶ms | ۴٬۱۲۹ms | **کندیِ خودِ ماشین، نه تغییرِ ما** |
| `scrollperf` — گامِ بیشینهٔ کارت‌ها | ۱۲۷ms | ۱۳۲ms | همان‌جا، همان‌قدر |
| `enterperf` — حساب، بارِ اول | ۴۷ دستور | ۴۷ دستور | بارِ اولِ سرد؛ بارهای بعد **۸** |
| بقیهٔ ۱۷ سنجه | سبز | — | — |

⚠️ **درسِ ماندنی**: هر دو سرخ روی نسخهٔ چهار آپدیت قبل‌تر هم سرخ بودند. بی آن
`worktree`، چهار ساعت صرفِ «تقصیرِ تغییرِ خودم» می‌شد. و آن «۴۷ دستور» هم
بارِ اولِ سرد است (اسکیما و کَشِ EF)؛ با `PUMP_ENTER_DEBUG=1` دیده شد که بارِ
دوم و سوم ۸ دستور است.

⚠️ **و یک سرخِ باقی‌مانده که پنهانش نمی‌کنیم**: گامِ چرخ روی **کارت‌های
قرض‌داران** هر چند اجرا یک بار از ۱۲۰ms می‌گذرد — ۱۲۷ · ۱۱۳ · ۱۴۴ در سه
اجرا، و ۱۳۲ روی `v3.1.113`. همان چیزی است که از ۱۴۰۵/۰۶/۲۶ در همین فایل
نوشته شده: «ساختنِ کارت‌های تازهٔ `ItemsRepeater`… راهِ ریشه‌ای — مجازی‌سازیِ
واقعی با اسکرولِ صفحه — هنوز نوشته نشده.» p95ِ همان اجرا ۱۹ تا ۲۸ms است، پس
یک گامِ تنهاست، نه کندیِ پیوسته. ⛔ سقفِ سنجه را بالا نبرید؛ کارتِ سبک‌تر یا
مجازی‌سازیِ واقعی راهِ درست است.

ولی دو **زیانِ ساختاریِ** واقعی همان‌جا پیدا شد و هر دو بسته شد:

- ⛔ **نقشهٔ صفحهٔ ورود روی نخِ رابط تجزیه می‌شد** (~۱۱۰ms). `WarmUp` هر بخش
  را یک بار می‌چیند، پس این هزینه در گرم کردن هم بود و سرِ باز کردنِ صفحه هم.
  حالا تجزیه روی نخِ دیگر است و فقط **نشاندنِ** تصویرِ آماده روی نخِ رابط
  (`LoginArt.UiMs` باید ~صفر بماند؛ سنجهٔ ۱ب در `loginart` قفلش کرده).
  ⚠️ و ساختنش دیگر در `OnAttachedToVisualTree` نیست، در اولین
  `MeasureOverride` است — کنترلِ پنهان اندازه گرفته نمی‌شود، پس صفحهٔ نادیده
  هیچ هزینه‌ای ندارد و به `IsEffectivelyVisible` (که خبر نمی‌دهد) هم نیازی
  نیست.
- ⛔ **برای شمردنِ جدول‌های آرشیو، همهٔ آرشیوها خوانده می‌شدند** —
  `ListArchivesAsync(...).Count` در `PersonViewModel.LoadArchiveCountAsync` و
  `CompanySectionViewModel.RefreshMetaAsync`. یعنی `RowsJson`ِ هر جدولِ آرشیو
  (هر کدام صدها ردیفِ سریال‌شده) با **هر باز کردنِ حساب** از دیسک می‌آمد تا
  یک عدد نوشته شود. حالا `CountArchivesAsync` است: برای حساب یک `COUNT` و
  برای شرکت **یک** `GROUP BY x.Fuel` (نه دو `COUNT`ِ جدا — وگرنه شمارِ
  دستورِ دیتابیسِ باز کردنِ حسابِ شرکت از ۳ به ۴ می‌رفت و همان هم پس‌رفت
  بود؛ `enterperf` گرفتش). همان قاعدهٔ «برای یک جمع، همهٔ ردیف‌ها را نخوان».

## 🏗️ زیرساختِ اشتراکی و چندمشتری — قاعده‌های تازه (از ۱۴۰۵/۰۶/۳۰)

شرحِ کاملِ بررسی، یافته‌ها و عددهای واقعیِ تست‌ها: `native/docs/INFRA-fa.md`.
این‌جا فقط چیزهایی که **نباید برگردند**:

- ⛔ **هیچ درخواستی بی هویتِ نسخه نرود** — `CloudLink.Stamp(req)` تنها جای
  ساختنِ آن هدرهاست (`User-Agent: PumpYaqobi/<نسخه>` · `X-App-Version` ·
  `X-App-Platform`) و از **هر** مسیرِ HTTPِ تازه‌ای صدا زده می‌شود. بی این،
  سرور نمی‌تواند نسخهٔ قدیم را جدا کند و در لاگ هم معلوم نیست کدام نسخه خطا
  داده. ⚠️ و هیچ چیزِ شناسایی‌کنندهٔ کاربر در هدر نمی‌رود (نامِ ماشین، ایمیل،
  شناسهٔ دستگاه — هیچ‌کدام)؛ آزمون قدغنش کرده.
- ⛔ **شناسهٔ پمپ مثلِ کلیدِ عمومی قفل است.** `RefreshAsync` شناسهٔ متفاوت را
  رد می‌کند (`station_mismatch`) و تنظیمات را دست نمی‌زند. پیش از این هر چه
  سرور می‌گفت بی سنجش می‌نشست — یعنی عکسِ حساب‌های این پمپ می‌توانست به
  پوشهٔ پمپِ دیگری برود.
- ⛔ **جدا کردنِ دستگاه از پمپ فقط `CloudLink.ForgetStationAsync()`** است و
  **یک بیت از دفتر را لمس نمی‌کند** (آزمونِ `JodaKardan_…_DaftarDastNemikhorad`).
  خروج از حساب تنها توکنِ حساب را پاک می‌کند و این درست است؛ جابه‌جاییِ پمپ
  کارِ آن یکی است.
- ⛔ **`busy_timeout` روی هر اتصال** — `BusyWait` در `PumpDbFactory`. سنجیده
  شد که پیش از این صفر بود (`foreign_keys=1 busy_timeout=0 journal_mode=wal`)؛
  پراگما مالِ **هر اتصال** است، پس نوشتنش یک بار در `EnsureReady` بی‌اثر بود.
- ⛔ **پشتیبان جریانی می‌رود** (`StreamContent`)، نه `ReadAllBytesAsync`.
  دفترِ چندصد مگابایتی نباید هر شش ساعت همان‌قدر رم بخواهد.
- ⛔ **سقفِ نرخِ مسیرهای پمپ روی سرورِ خانگی دو سطل است** (ریپوی `server`،
  `src/index.js`): یکی به‌ازای **هر پمپ** (۱۲۰۰/دقیقه) تا پمپِ پرکار سهمِ
  همسایه را نخورد، و یکی به‌ازای **آی‌پی** (۳۰۰۰/دقیقه) تا کدِ ساختگیِ تازه
  نتواند سقف را دور بزند. هر دو لازم‌اند؛ یکی‌شان را برندارید.
- ⚠️ **تستِ فشارِ چندپمپی**: `npm run test:stations-load` در ریپوی `server`
  (پیش‌فرض ۲۵ پمپ؛ `STATIONS=200 ROUNDS=5` برای عددِ بزرگ‌تر). همین تست بود
  که ایرادِ «سهمِ آی‌پیِ مشترک» را گرفت.
- ⚠️ **بازگردانیِ پشتیبان از امروز آزمون دارد** و باید داشته باشد
  (`InfraTests.Bazgardani_…`): فقط داشتنِ فایلِ پشتیبان هیچ‌وقت کافی نیست.
- ⛔ و آن‌چه **عمداً دست نخورد**: منطقِ حساب‌ها، نقش‌ها و دسترسی‌ها، سیستمِ
  پلن و قیمت، صفحهٔ ورود (سیزنِ دیگری رویش کار می‌کند)، و مدلِ «آینهٔ حالت»ِ
  همگام‌سازی. صفِ پایدارِ آفلاین و رمزنگاریِ `settings.json` با DPAPI **پیشنهاد
  شده‌اند و ساخته نشده‌اند** — هر دو «سیستمِ تازه»اند، نه اصلاحِ باگ.

## 🔁 جابه‌جاییِ حساب و دستگاه — قاعده‌های تازه (از ۱۴۰۵/۰۶/۳۰)

صاحب ریپو: «قبلاً گفتم لاگین را کار نگیر؛ الان کارِ لاگین تمام شده — آن
پرامپت را ببین چه گفته برای لاگین، آن را هم درست بچین.» پس بندهای ورودِ
همان پرامپتِ زیرساخت (بندِ ۱، و بندهای ۸ · ۱۰ · ۱۲ در آن‌چه به ورود
می‌خورد) یک‌به‌یک روی خودِ کد سنجیده شدند. شرحِ کامل و جدولِ بند‌به‌بند:
`native/docs/ACCOUNT-fa.md` (بخشِ آخر). این‌جا فقط چیزهایی که **نباید
برگردند**:

- ⛔ **ورودِ حسابِ دیگر، بندهای پمپِ قبلی را باز می‌کند** —
  `CloudLink.SeatAsync` ⇒ `ReleaseIfOtherAccountAsync` ⇒ `ForgetStationAsync`.
  پیش از این توکنِ **دستگاه**، شناسهٔ پمپ، مجوز، کلیدِ عمومی، کدِ اپِ
  کارمندان، مُهرِ ارفاق و نشانی و رمزهای سرورِ خانگیِ **پمپِ قبلی** سرِ جا
  می‌ماندند: عکسِ حساب‌ها به پوشهٔ ابریِ او می‌رفت و اشتراکش خرجِ این یکی
  می‌شد.
  - ⚠️ **ملاک `user.id`ِ حساب است** (`AppSettings.CloudUserId`)، نه ایمیل؛
    ایمیل فقط برای نصبِ کهنه‌ای که شناسه ندارد. ایمیلِ عوض‌شدهٔ همان حساب
    جابه‌جایی نیست.
  - ⛔ ورودِ دوبارهٔ همان حساب، و نخستین ورودِ نصبِ تازه، **هیچ بندی را باز
    نمی‌کنند**. «مشتریِ امروزی نباید با یک به‌روزرسانی از پمپش جدا شود.»
  - ⛔ و **یک بیت از دفتر لمس نمی‌شود** (آزمونِ سورس + شمارشِ واقعیِ ردیف‌ها
    در بندِ ۱۷ی `cloudlogin`).
  - ⚠️ بی‌صدا نیست: `LoginStatus` می‌گوید چه شد. قفلِ بی‌توضیح باگ است.
- ⛔ **جدا کردنِ دستیِ دستگاه از پمپ فقط `ForgetPumpCommand`** است (تهِ
  کارتِ «مشخصاتِ پمپ»، فقط با `PumpBound`، با پرسشِ پیش از انجام). تا دیروز
  برنامه می‌گفت «دستگاه را جدا کنید» و **هیچ راهی نبود**.
- ⛔ **`register/start` و `password/forgot` ترمزِ ۶۰ ثانیه‌ای دارند**
  (`CloudLink.ResendWaitSeconds`) — هر زدنشان یک **ایمیل** است و سقفِ خودِ
  ابر پنج تا در پانزده دقیقه (`otpMax`)، پس زدنِ پشتِ سرِ هم خودِ کاربر را
  پانزده دقیقه از ثبت‌نامش بیرون می‌انداخت. ترمز **جای سنجشِ سرور را
  نمی‌گیرد** و فقط با موفقیت مهر می‌خورد (درخواستی که نرسید، ایمیلی
  نفرستاده).
- ⚠️ **سقفِ نرخِ ابر خوانده شد، نه حدس زده**: `otpMax` ۵ · `authMax` ۱۰ ·
  `generalMax` ۶۰۰ (هر ۱۵ دقیقه) + قفلِ ۸‌تلاشیِ حساب. پس بندِ ۱۰ی پرامپت
  برای ورود سمتِ سرور **کاری لازم نداشت**.
- ⚠️ **سنجهٔ «چند کلیک ⇒ یک درخواست»** بندِ ۱۵ در `cloudlogin` است. با ابرِ
  ساختگیِ **هم‌زمان** این پنجره هیچ‌وقت دیده نمی‌شود و سنجه سبزِ دروغ
  می‌دهد، پس ابرِ ساختگی عمداً مکث می‌کند (`_slowMs`). ⛔ آن مکث را برندارید.

## ⛔ سایت دیگر به‌روز نمی‌شود — فقط برنامهٔ نیتیو

تصمیمِ صاحب ریپو (۱۴۰۵/۶/۱۷): «سایت دیگه لازم نیست، اون خاموش باشه، همین برنامه
رو بچسپ». از این پس:

- **هیچ تغییری در `index.html`، `sw.js`، `payam/` و بقیهٔ فایل‌های سایت نده.**
  نه وصله، نه نسخه‌گذاری، نه بهبود — حتی اگر همان باگ در سایت هم باشد.
- **نسخه‌گذاریِ سه‌گانهٔ زیر دیگر لازم نیست.** برنامهٔ نیتیو شمارهٔ نسخه‌اش را
  خودش از شمارهٔ اجرای ورک‌فلو می‌گیرد (`build-native.yml`).
- سایت فقط یک چیز است: **مرجعِ معماری و ظاهر.** وقتی نمی‌دانی چیزی در برنامه
  باید چه شکلی باشد، `index.html` را بخوان و همان را در نیتیو بساز — ولی
  دست به خودش نزن.

## ⛔ فایلِ نصبِ اندروید فقط یکی است — از ۱۴۰۵/۰۶/۲۴

گزارشِ صاحب ریپو: «این چیه؟ چرا سایتِ قدیم را برنامه‌شو دادی؟… من اگر آن را
می‌خواستم خودم می‌گفتم. دیگر با سایتِ قدیم و برنامهٔ قدیم هیچ ارتباطی نداری مگر
گفته باشم در پرامپت.»

    ⛔ build-android.yml  ⇒ index.html ⇒ PumpYaqobi.apk      ← دیگر خودکار نیست
    ✅ build-kar-apk.yml  ⇒ kar/       ⇒ PumpYaqobiKar.apk   ← همین را بده

هر وقت «اپِ اندروید» خواسته شد، منظور `PumpYaqobiKar.apk` است (انتشارِ
`kar-latest`) — همان که زنده به برنامهٔ نیتیو وصل است. انتشارِ `android-latest`
مالِ سایتِ قدیم است و **نباید به کاربر داده شود**. `build-android.yml` پاک نشده
ولی دیگر با پوش اجرا نمی‌شود؛ فقط دستی.

## خبر دادن به کارمند — از ۱۴۰۵/۰۶/۲۴

خواستهٔ صریحِ صاحب ریپو: «وقتی که یک قرض‌دار اضافه برد یا کم مانده بود از حسابش،
برنامه یک پیام بدهد — حتی اگر گوشی خاموش یا حتی اگر توی برنامه نبود هم پیام برود
تا بفهمد.»

```
StationSnapshot.Alerts  ⇒ live.alerts  ⇒  ۱) کادرِ بالای kar/
                                          ۲) اعلانِ مرورگر (آیفون فقط همین)
                                          ۳) AlertJob اندروید — اپِ بسته هم
```

- ⚠️ **فهرست فقط یک جا ساخته می‌شود**: `StationSnapshot.Alerts`. «اضافه برد» و
  «کم مانده» همان `DebtStatus.Out` و `Low`ِ `DebtCalculationService.Status`
  هستند — همان چیزی که رنگِ کارتِ قرض‌دار از آن می‌آید. قاعدهٔ جدا ننویسید،
  وگرنه روزی کارت سرخ است و گوشی ساکت.
- ⚠️ **کلیدِ `k`**: گیرنده هر کلید را یک بار خبر می‌دهد. کلید حال را هم در خود
  دارد (`d7-stP-out`)، پس «کم مانده ⇒ تمام شد» خبرِ تازه می‌دهد، ولی همان حال
  دوباره نه. کلیدی که از فهرست بیفتد فراموش می‌شود تا حسابِ خراب‌شدهٔ دوباره
  ساکت نماند.
- ⚠️ **هیچ سرویسِ پیام‌رسانِ بیرونی‌ای داخلِ اپِ گوشی نیست و نباید بیاید.**
  (استثنای صریحِ صاحب ریپو از ۱۴۰۵/۰۷/۱۳: **باتِ تلگرام روی سرورِ حساب** —
  بخشِ «🤖 باتِ تلگرام» در انتهای همین فایل. آن روی سرور است، نه در اپ.) گوشی خودش هر ۱۵
  دقیقه از سرورِ خانگی می‌پرسد: `JobScheduler` + `HttpURLConnection` + `org.json`
  — هر سه خودِ اندروید. پروژهٔ گریدل عمداً هیچ وابستگی ندارد (androidx یک‌بار
  ساخت را شکست)؛ همین‌طور بماند.
- ⚠️ **«گوشی خاموش» شدنی نیست** — گوشیِ خاموش هیچ برنامه‌ای اجرا نمی‌کند. ولی
  `setPersisted(true)` یعنی همین که روشن شد، بی باز کردنِ برنامه، می‌پرسد.
- ⚠️ خبرِ پس‌زمینه از درِ **HTTP** می‌آید
  (`GET /api/stations/<کد>/live?token=<رمزِ خواندن>`)، نه وب‌سوکت — کارِ
  پس‌زمینه بی کتابخانه وب‌سوکت نمی‌تواند بزند. خودِ اپ هر دو در را دارد؛ فقط
  خبرِ پس‌زمینه سرورِ به‌روز می‌خواهد.
- تنظیمات از `localStorage`ِ صفحه به جاوا می‌رسد (پلِ `PumpAlerts.setup`)، چون
  جاوا نمی‌تواند `localStorage` را بخواند. هر بار که تنظیمات عوض شود دوباره
  صدا زده می‌شود.
- آزمون: بخشِ «خبرها» در `tools/check-kar-bot.mjs` و `StationAlertsTests`.

## صفحهٔ کیو‌آرِ مشتری (`view/`) — از ۱۴۰۵/۰۶/۲۴

- هر عکس حالا فهرستی از **دفترها** دارد (`AcctBook`)، نه یک جدولِ تخت. یک حسابِ
  قرض‌دار دو دفترِ جدا دارد (`FuelRows` و `MoneyRows`) و تا پیش از این فقط
  دفترِ فعال داخلِ کد می‌رفت — مشتری نیمِ حسابش را می‌دید.
- هر دفتر: خلاصه، **تفکیکِ پطرول/دیزل** (`f`/`fh`)، جدول، و **آرشیوِ ماه‌به‌ماه**
  (`g`). سال‌به‌سال را خودِ صفحه از روی `g` جمع می‌زند.
- ⚠️ آرشیو روی **همهٔ** ردیف‌ها حساب می‌شود، پیش از کم شدنِ ردیف‌ها برای جا شدن
  در کیو‌آر. وگرنه ماهی که ردیفش نرفته از چشمِ مشتری می‌افتاد.
- ⚠️ `Head`/`Rows`/`Summary`ی تخت برداشته **نشده‌اند**: کیو‌آرهایی که چاپ شده و
  دستِ مشتری است همان شکل‌اند و صفحه باید تا ابد بازشان کند.
- صفحه فقط‌خواندنی است. دکمه‌هایش (تب دفتر، تیل، ماه) فقط فیلترند و هیچ چیزی
  نمی‌نویسند.
- آزمون: `node tools/check-view.mjs` — صفحه را واقعاً با یک DOMِ کوچک اجرا
  می‌کند و روی خروجیِ واقعی می‌سنجد، و `AcctBooksTests` در `PumpYaqobi.Tests`.

## اسکریپتِ پس‌زمینه — فقط اگر روزی به سایت دست زدی

```
node tools/check-background.mjs
```

چون سایت دیگر عوض نمی‌شود، این عملاً اجرا نمی‌شود. اگر روزی استثنائاً به
`index.html` دست زدی، پیش از کامیت اجرایش کن و اگر شکست خورد **وصله نزن —
خودِ پیام را بخوان**؛ توضیح هر قاعده داخلش نوشته شده.

## سبک کامیت و PR

- پیام کامیت و عنوان PR به فارسی، مثل کامیت‌های قبلی، با ذکر شمارهٔ نسخه در پرانتز:
  «... (نسخه 3.1.x)»
- ⚠️ **شمارهٔ نسخه را از خودت نساز و دستی جلو نبر.** نسخهٔ واقعیِ برنامه را
  `build-native.yml` از شمارهٔ اجرای ورک‌فلو می‌سازد:
  `نسخه = MAJOR_MINOR . (شمارهٔ ساخت − RUN_OFFSET)` (فایلِ `native/VERSION`).
  پیش از نوشتنِ عنوان، آخرین انتشار را ببین (`releases/latest` — مثلاً
  `v3.1.102`) و عددِ بعدیِ همان را بنویس. عددی که با آخرین انتشار جور نباشد
  فقط برچسبِ غلط است؛ روی خودِ برنامه هیچ اثری ندارد ولی صاحب ریپو را گمراه
  می‌کند.

## نکته‌های فنی پروژه

- کل برنامه یک فایل است: `index.html` (HTML + CSS + JS داخلی) — PWA با `sw.js` و `manifest.json`.
- **هیچ فایربیسی در کار نیست و نباید برگردد.** تنها بک‌اند، سرورِ خانگیِ خودِ صاحب
  ریپو است (`window.__SELF_HOST_URL__`). لایهٔ داده `window.__pumpHome__` است و
  نام‌ها `_srvDb` / `_srvReady` / `_srvPullLatestOnce` و مانندِ آن. اسمِ «firebase»
  را در هیچ متغیر، تابع یا نوشته‌ای برنگردان — صاحب ریپو صریح گفته.
- منطق حساب قرض‌داران (مودال شخص) حساس است: «واحد پول» و «واحد تیل» دو دفتر کاملاً جدا هستند (`rows` و `moneyRows`). جمع‌های نمایشی نباید روی محاسبات الباقی اثر بگذارند.
- برای تست دستی: صفحه با قفل رمز باز می‌شود؛ در تست headless می‌توان `lockScreen` را مخفی و `app` را نمایان کرد (و برای کارهای مدیریتی `currentRole = 'admin'`).
- **پس‌زمینهٔ صفحه:** روی `<html>` هیچ رنگی گذاشته نمی‌شود — بوم عمداً شفاف است تا
  پس‌زمینهٔ `<body>` طبق استاندارد CSS به آن تسرّی پیدا کند و همیشه هم‌رنگِ صفحه باشد.
- **شبکهٔ شش‌ضلعیِ پس‌زمینه حذف شده** (خواستهٔ صریح صاحب ریپو). فقط «رعد و برق» و
  «ستارهٔ دنباله‌دار» مانده‌اند. برنگردانش.
- `payam/` یک PWAِ جداگانه با scope و سرویس‌ورکرِ خودش است (پیام‌رسان). نقشِ هر دستگاه
  با لینک/کیو‌آرِ فعال‌سازی تعیین می‌شود؛ کارمند و مشتری نباید بتوانند موضوع اضافه کنند.
- **پیام‌رسان جای جدا ندارد:** خودِ تبِ «چت تک‌نفره»ی رباتِ دستیار است
  (`#rbPane-dm` و توابعِ `_msgr*` در `index.html`). مودالِ جدا نساز — صاحب ریپو
  صریح گفت باید داخلِ همان ربات باشد. چتِ قدیمیِ Firebase پاک نشده: `#dmLegacy`
  پنهان مانده و پیام‌هایش با `_msgrLegacyMerge` فقط برای «خواندن» در همان
  گفت‌وگو نشان داده می‌شوند.
- پیام‌رسان روی همان
  سرورِ خانگی سوار است: هر گفت‌وگو یک «موضوع» است (`staff` و `p-<peer>`). موضوعِ هر
  قرض‌دار با `msgrTopicOfPerson(person, sub)` ساخته می‌شود و باید دقیقاً همانی بماند
  که کیو‌آرِ همان حساب به آن گوش می‌دهد (`_peerKey('debt-'+id[+'~'+subId])`). هرگز
  آن را دستی نساز. فرستنده در `title` می‌نشیند و شناسهٔ گفت‌وگو در `tags` — سرور
  فیلدِ اضافه پاس نمی‌دهد.
- **همگام‌سازی چیزی را که تازه تایپ شده پاک نکند:** `_applyServerDataGuarded` تنها
  راهِ اعمالِ خودکارِ عکسِ سرور است. مستقیم `_applyServerData` را در مسیرهای زنده
  صدا نزن. `save()` مهرِ «تایپِ کاربر» می‌زند؛ برای کارهای خودکارِ خودِ برنامه
  `_saveQuiet()` را به کار ببر.
- **فیصدیِ قرض‌داران دیگر یکی نیست:** هر حساب `percentP` (پطرول) و `percentD` (دیزل)
  دارد و این دو هیچ ربطی به هم ندارند. هرگز مستقیم `acct.percent` را نخوان — از
  `_acctPct(acct, 'petrol'|'diesel')` استفاده کن؛ خودش برای حساب‌های قدیمی که فقط
  `percent` دارند به همان برمی‌گردد. برای نوشتن `_setAcctPct(acct, fuel, val)` که
  `fuel` آن `'petrol'` یا `'diesel'` یا `'both'` (میان‌بُرِ کادرِ «هر دو») است.
  `acct.percent` فقط برای فایل‌های کپی و نسخه‌های قدیمی هم‌گام نگه داشته می‌شود.
- **ردیف‌های خودکارِ رسید/فیصدی داخل جدولِ شخص** (`tr.pm-auto-row`) نه `data-idx`
  دارند نه پشتشان داده‌ای هست — فقط نمایش‌اند و در هیچ جمعی شمرده نمی‌شوند. نوشتهٔ
  هر خانه‌شان داخل `<span class="xls-in pmar-box">` می‌نشیند تا اندازه/فاصله/فونتش
  مو‌به‌مو با کادرهای تایپیِ ردیف‌های دستی یکی باشد؛ اگر آن span را برداری، جدول
  دوباره ناهموار می‌شود.

## اپِ کارمندان و رباتِ جست‌وجو (`kar/`) — از ۱۴۰۵/۰۶/۲۲

خواستهٔ صاحب ریپو: «اپ برای اندروید برای کارمندان تا قرض‌داران را چک کنند که
موجودی دارند یا نه — که اضافه ندهند — و موجودیِ تیل در مخزن را ببینند… اول
رمزِ برنامهٔ نیتیوِ کامپیوتر را بخواهد… و یک رباتِ قدرتمند که هر شخص، هر بخش و
هر ماه را جواب بدهد.»

راهِ داده:

```
برنامهٔ نیتیو ──set live──▶ پوشهٔ همان پمپ ──sub live──▶ kar/ (گوشیِ کارمند)
              ◀─sub inbox──               ◀─post inbox─ اندروید · آیفون
```

**فقط برنامهٔ نیتیو `live` را می‌نویسد** و این باید همین بماند — هیچ گوشی‌ای
حسابی را عوض نمی‌کند. راهِ برگشت `inbox` است: گوشی‌ها فقط درخواست/یادداشت
می‌گذارند و برنامه همان لحظه می‌بیندشان.

- `native/PumpYaqobi.App/Services/StationSnapshot.cs` — عکسِ خواندنیِ همهٔ
  بخش‌ها. **هیچ منطقِ حسابی این‌جا نیست**؛ هر عددی از خودِ سرویس‌های برنامه
  می‌آید. اگر بخشِ تازه‌ای اضافه شد، همان‌جا یک `Section(...)` اضافه کنید —
  شکلِ `{t, head, rows, m, sum}` را عوض نکنید، ربات روی همان حساب می‌کند.
- `StationPublisher.cs` — حلقهٔ بیست‌ثانیه‌ای. فقط وقتی می‌فرستد که داده واقعاً
  عوض شده باشد (`HashOf` که `seq` و تاریخ را نمی‌شمارد).
- ⚠️ شاخهٔ `-live` عمداً جداست: نسخهٔ وبِ قدیمی کلِ `stations/<کد>` را یک‌جا
  `set` می‌کند و اگر هم‌جا می‌نوشتیم، هم را پاک می‌کردند.
- ⚠️ `RowBudget` در `StationSnapshot` ترمزِ همان چالهٔ همیشگی است: «همهٔ
  ردیف‌های همهٔ حساب‌ها را بخوان». بالای سقف، فقط جمع‌ها می‌روند (که با یک
  `GROUP BY`ی خودِ SQLite درمی‌آیند) و اپ خودش می‌گوید ردیف‌ها نیامده‌اند.
- **رمز**: هیچ‌وقت خودِ رمز منتشر نمی‌شود. فقط رشتهٔ `pbkdf2$sha256$…`ی
  `PasswordHasher` می‌رود و مرورگر همان را با WebCrypto می‌پزد و مقایسه می‌کند.
  پس رمزِ اپ همیشه همان رمزِ برنامه است. `tools/check-kar-bot.mjs` این
  سازگاری را قفل کرده — اگر شکلِ هش عوض شود، همان‌جا قرمز می‌شود.
- آزمون: `node tools/check-kar-bot.mjs` (ربات، ماه‌ها، قفلِ رمز، و دو درِ
  سرور) و `StationSnapshotTests` / `KarLinkTests` / `StationLinkTests` در
  `native/PumpYaqobi.Tests`.

## بخشِ پمپ‌بنزین‌ها روی سرور — از ۱۴۰۵/۰۶/۲۳

خواستهٔ صاحب ریپو: «برای برنامهٔ نیتیوِ کامپیوتر یک بخش تو سرور بساز… اگه ادرس
نداشت با ادرس براش بساز که کار کنه… از هر پمپ بنزین جداگانه باشه و برای هر کدوم
جدا پوشه درست بشه… بعدن چندین پمپ بنزین دیگه رو هم اضافه میکنم.»

سرور (`vil3ntec-it/server`) حالا برای هر پمپ بنزین **پوشه و دو رمزِ جدا** دارد —
`docs/STATIONS-fa.md` همان‌جا شرحش داده.

```
data/stations/<کد پمپ>/
  token.txt      رمزِ برنامهٔ کامپیوتر — می‌نویسد
  readkey.txt    رمزِ کارمند/گوشی — فقط می‌خواند
  live.json · inbox.json · station.json
```

سمتِ برنامه:

- `Services/ServerFinder.cs` — سرور را با یک بستهٔ UDP (پورت `4702`) در شبکهٔ
  خانگی پیدا می‌کند. ⚠️ نشانی از روی **همان آی‌پی‌ای که جواب داد** ساخته
  می‌شود، نه از روی `url`ی داخلِ کارت: سرورِ چندکارته چند نشانی دارد.
- `Services/StationLink.cs` — «اگر آدرس نداشت، برایش بساز»: پیدا کردن،
  `POST /api/stations/enroll`، و نوشتن در **هر دو** جای تنظیمات. رمزی که داریم
  را در همان درخواست می‌فرستیم تا سرور همان پوشه و همان رمز را پس بدهد؛ پس
  نصبِ دوبارهٔ برنامه دادهٔ پمپ را گم نمی‌کند.
- `Services/HomeSync.cs` — **دو در**: اول `‎/station?station=<کد>&token=<رمز>‎`
  (مسیرها `live` و `inbox`)، و اگر نشد درِ قدیمیِ `‎/?token=‎` با
  `stations/<کد>-live`. ⚠️ درِ قدیمی را برندار: سرورِ به‌روزنشده فقط همان را
  بلد است. `Mode` می‌گوید از کدام در وصل‌ایم و `StationPublisher` مسیرش را با
  همان هماهنگ می‌کند.
- **رمزِ کیو‌آرِ کارمند رمزِ فقط‌خواندنی است، نه رمزِ برنامه.** کیو‌آر روی کاغذ
  چاپ می‌شود و دستِ چند نفر می‌گردد. `KarLink.Build(host)` خودش `ReadKey` را
  برمی‌دارد و فقط اگر سرور رمزِ خواندن نداده باشد به رمزِ برنامه برمی‌گردد.
  سرور هم مستقلاً جلوی نوشتن را می‌گیرد (`read_only`).
- `kar/app.js` — همان دو در، با همان ترتیب (`doorsFor`). خطای یک در یعنی
  «درِ بعدی را امتحان کن»، و تنها وقتی «رمز غلط» گفته می‌شود که هر دو رد
  کرده باشند.
- آزمون: `StationLinkTests` در `native/PumpYaqobi.Tests` و بخشِ «دو در به
  سرور» در `tools/check-kar-bot.mjs`.

## اپِ کارمندان دیگر آدرس نمی‌پرسد — از ۱۴۰۵/۰۶/۲۴

خواستهٔ صریحِ صاحب ریپو: «برنامه پمپ یعقوبی کارمندان چرا ادرس اینترنتی
میخان؟ این رو نخان، توی اپ است و این‌جا لازم نباشه و اونو بردار.»

```
ورود با گوگل ──▶ POST /api/auth/google ──▶ توکنِ حساب
             ──▶ GET  /api/pump/me     ──▶ {url, readKey, station}
             ──▶ همان سرورِ خانگی، مثل همیشه (doorsFor بی‌تغییر)
```

- `kar/cloud.js` — لایهٔ ابر. **نشانیِ ابر (`api.vill3n.top`) در خودِ کد
  قفل است** و از تنظیمات یا نوارِ نشانی خوانده نمی‌شود؛ وگرنه هر کسی
  می‌توانست اپ را به سرورِ خودش ببرد و توکنِ گوگلِ کارمند را بردارد.
  همان قاعده‌ای که ریپوی `shop` برای `AppConfig.kt` و `api-config.js`
  دارد.
- شناسهٔ گوگل از `/api/config` می‌آید، نه از کد — عوض شدنش نسخهٔ تازهٔ اپ
  لازم ندارد.
- `authed()` توکنِ منقضی را **یک بار و فقط یک بار** تازه می‌کند. حلقه
  زدن جز پنهان کردنِ مشکل کاری نمی‌کند.
- ⚠️ **راهِ کیو‌آر برداشته نشد و نباید برداشته شود.** دو حالت هست که ابر
  به درد نمی‌خورد: پمپی که برنامه‌اش هنوز به‌روز نشده، و جایی که شبکهٔ
  پمپ هست ولی اینترنت نیست. دکمهٔ «اینترنت ندارم» همان را می‌آورد، و سه
  سنجه در `check-kar-bot.mjs` قفلش کرده‌اند.
- ⚠️ **سرویس‌ورکر برای درخواستِ بیرون از دامنه هیچ کاری نمی‌کند.** تا
  دیروز هر درخواستِ ناموفقی `index.html` می‌گرفت؛ با آمدنِ اسکریپتِ
  گوگل، مرورگر HTML را به جای جاوااسکریپت می‌خواند و اپ با
  «Unexpected token '<'» می‌شکست. صفحه فقط جوابِ `mode === 'navigate'`
  است، نه جوابِ هر چیزی.
- ⚠️ با هر بار عوض شدنِ فهرستِ `SHELL`، شمارهٔ `CACHE` هم باید بالا برود.

سمتِ سرور: `vil3ntec-it/shop` → `docs/PUMP-fa.md`.

⚠️ `kar/` با `view/` یکی نیست و نباید یکی شود:
`view/` مالِ **مشتری** است (دادهٔ یک حساب داخلِ خودِ کیو‌آر، بی سرور و بی رمز)،
`kar/` مالِ **کارمند و کارفرما** است (زنده از سرور، پشتِ رمزِ برنامه).

## جدول‌ها: «هم‌قدِ ردیف‌ها شدن» انتخابی است — از ۱۴۰۵/۰۶/۲۳

گزارشِ صاحب ریپو: «صرافی و مصارف و گاوصندوق خیلی دیر باز می‌شوند.»
اندازه‌گیری (`dotnet run --project PumpYaqobi.UiTests -- ledgerperf`) وارونهٔ
انتظار بود — هرچه ردیف **کمتر**، کندتر:

| بخش | ردیف | خواندنِ SQL | باز شدن | ردیفِ زنده |
|---|---|---|---|---|
| گاوصندوق | ۲۰۰ | ۳ms | ۴٬۶۵۲ms | ۲۰۰ |
| گاوصندوق | ۳٬۰۰۰ | ۱۸ms | ۹۱۶ms | ۱۷ |

دیتابیس بی‌گناه بود. `DataGrid` مجازی‌سازی‌اش را از **قابِ خودش** می‌گیرد، پس
جدولی که هم‌قدِ ردیف‌هایش بلند شود هر ردیفش را واقعاً می‌سازد.

- `ExcelGrid.GrowsToContent` پیش‌فرض **خاموش** است و باید بماند. دفترهای
  ماهانه سرِ یک صفحه می‌ایستند و می‌لغزند (چرخ زنجیره‌ای است، پس یک اسکرولِ
  پیوسته حس می‌شود) — ۲۸ تا ۱۶۰ میلی‌ثانیه به‌جای ۴٬۶۵۲.
- فقط **ورق و پارچه** روشنش می‌کنند: خواستهٔ صریحِ صاحب ریپو («ردیفِ ۴۰ و ۵۰
  و ۵۱ نباید در کادر گیر کند») دربارهٔ همان‌ها بود و ذاتاً کوتاه‌اند.
  `GrowRowLimit = 80` آن‌جا هم سقفِ ایمنی است.
- جدولِ کوتاه‌تر از یک صفحه همچنان دقیقاً هم‌قدِ ردیف‌هایش می‌ماند، وگرنه دفترِ
  سه‌ردیفی یک کادرِ ۸۰۰ پیکسلیِ خالی می‌شد.
- ⚠️ ستون‌های `Width="Auto"` پهنایشان را از محتوای ردیف‌ها می‌گیرند، پس تا
  `SpreadColumns` پهناها را سفت نکرده، هر ردیفِ تازه همهٔ ستون‌ها را دوباره
  اندازه می‌گیرد — **مربعی**، نه خطی. برای همین پاسِ اولِ اندازه‌گیری همیشه
  یک صفحه بیشتر نیست.
- ⚠️ و چرا `PerfAudit` سال‌ها این را نمی‌دید: دادهٔ آزمونش `MonthKey` نداشت و
  این بخش‌ها ماه‌به‌ماه فیلتر می‌شوند، پس **خالی** سنجیده می‌شدند. هر دادهٔ
  آزمونی که به بخش‌های دفتری می‌دهید باید ماهِ جاری را داشته باشد.
- «مصارفِ کل» هم با `ListAsync(null)` حساب می‌شد — همهٔ ردیف‌های همهٔ ماه‌ها به
  شیءِ کامل، با **هر ویرایشِ هر خانه**. حالا `LedgerService.SumAsync` فقط همان
  یک ستون را می‌خواند. ⚠️ `SUM` را به SQLite ندهید؛ مبلغ‌ها متن ذخیره می‌شوند
  و `CAST(... AS REAL)` برای حساب‌داری خطرناک است.

## اشتراکِ برنامهٔ نیتیو — از ۱۴۰۵/۰۶/۲۴

خواستهٔ صریحِ صاحب ریپو: «اون `api.vill3n.top` رو توی برنامه ذخیره کن و لازم
نباشه تو جای تنظیمات باشه، کاری کن توی خود برنامه باشه و دیده نشه، و اشتراک هم
از سرور بره براش، و کسی هم نتونه برنامه رو کرد کنه یا دور بزنه.»

```
کدِ شش‌رقمی ──▶ POST /api/pump/device/activate ──▶ توکنِ دستگاه + کلیدِ عمومی
            ──▶ GET  /api/pump/device/license  ──▶ مجوزِ امضاشده (ES256)
            ──▶ LicenseGuard.Check(...)        ──▶ باز/بسته، حتی آفلاین
```

- `Services/CloudConfig.cs` — نشانیِ ابر **در خودِ کد قفل است**
  (`BaseUrl = "https://api.vill3n.top"`) و نه از تنظیمات خوانده می‌شود نه از
  محیط. اگر از تنظیمات می‌آمد، هر کسی سرورِ خودش را می‌نوشت و مجوزِ خودش را
  امضا می‌کرد — یعنی قفل با یک کادرِ متنی دور می‌خورد.
- ⚠️ **ابر با سرورِ خانگی یکی نیست.** `AppSettings.ServerUrl` مالِ دفتر و دادهٔ
  زنده است و با هر بار روشن شدنِ مودم عوض می‌شود؛ ابر یکی و ثابت است و فقط
  حساب و اشتراک از آن می‌آید. قاطی‌شان نکنید.
- `Services/LicenseGuard.cs` — پنج قید، و **ترتیبشان مهم است**: اول امضا، بعد
  هر چیزِ دیگر. تا امضا سنجیده نشده هیچ فیلدی از `payload` قابلِ اعتماد نیست.
  - ⚠️ **قفلِ کلیدِ عمومی (TOFU) مهم‌ترین قید است.** کلید را از خودِ پاسخ
    کورکورانه نمی‌گیریم: اولین فعال‌سازی ذخیره‌اش می‌کند
    (`AppSettings.CloudPublicKey`) و از آن به بعد `CloudLink` کلیدِ متفاوت را
    رد می‌کند (`key_mismatch`). بی این، سرورِ ساختگی همهٔ قیدهای دیگر را
    بی‌اثر می‌کرد.
  - `aud` = `tohid-pump-app` — مجوزِ بخشِ دکان روی این برنامه نمی‌نشیند.
  - `duid` — کپیِ پوشهٔ برنامه روی کامپیوترِ دیگر اشتراک را با خودش نمی‌برد.
  - `stn` — مجوزِ پمپِ اشتراک‌دار روی پمپِ بی‌اشتراک نمی‌نشیند.
  - `nbf`/`exp` با ارفاقِ یک‌دقیقه‌ای، مثلِ خودِ سرور.
- ⚠️ **بخش‌های `core` حتی با مجوزِ نامعتبر هم بازند** (`LicenseGuard.Allows`).
  صاحبِ پمپ باید همیشه دفترِ خودش را ببیند؛ گروگان گرفتنِ داده سریع‌ترین راهِ
  از دست دادنِ اعتماد است. قفلِ واقعی روی سرور است: نوشتن روی پوشهٔ ابری هر
  بار همان‌جا سنجیده می‌شود.
- `Services/CloudLink.cs` — هیچ‌وقت استثنا پرتاب نمی‌کند؛ `CloudResult`
  برمی‌گرداند. برنامه بی‌اینترنت هم باید باز شود.
- `StationPublisher` بعد از هر انتشارِ محلیِ موفق، عکسِ خانه را با ترمزِ
  ده‌دقیقه‌ای به ابر هم می‌فرستد و هر خطایی را می‌بلعد.
## 👤 «حسابِ من» — و تنظیماتی که خالی شد (از ۱۴۰۵/۰۶/۲۵)

گزارشِ صاحب ریپو: «ادرس سرور از تو تنظیمات پاک بشه و دیده نشه… صفحهٔ لاگین
با جیمیل هم داخلِ اپ نیست و صفحهٔ پروفایل هم نیست… چرا اشتراک تو تنظیمات
است؟… مشخصاتِ پمپ رو هم از دیدِ کاربر حذف کن و رمز نخاد. مثلِ برنامهٔ شاپ
باشه که بدون این که من رمز یا چیزی بزنم، اطلاعات از حسابش به سرور بیاد.»

بخشِ بیستمِ نوار: **`AccountSectionViewModel`** — ورود با گوگل، پروفایل و
اشتراک، یک‌جا.

```
ورود با گوگل (PKCE، مرورگرِ سیستم) ──▶ POST /api/auth/google ──▶ توکنِ حساب
                                   ──▶ GET  /api/pump/me     ──▶ {url, readKey, station}
                                   ──▶ خودش در تنظیمات می‌نشیند
```

- ⛔ **این‌ها از تنظیمات برداشته شدند و نباید برگردند**: مشخصاتِ پمپ،
  نشانیِ سرورِ هم‌گام‌سازی، رمزِ سرور، کدِ پمپ، پینِ جفت‌شدن، نشانیِ صفحهٔ
  حساب، و کارتِ اشتراک. `CloudAddressLockTests` هر کدام را جدا قفل کرده.
- ⚠️ **نشانیِ صفحهٔ حساب (کیو‌آر) کادر نمی‌خواست و نباید بخواهد.**
  `AcctView.DefaultBase` از قبل `https://yaqobipump.top/view/` بود و
  `AcctView.Url` با ورودیِ خالی به همان برمی‌گردد. آن کادر فقط کاربر را
  می‌ترساند.
- ⚠️ **رمزِ گوگل هیچ‌وقت داخلِ برنامه تایپ نمی‌شود.** `GoogleSignIn` مرورگرِ
  خودِ سیستم را باز می‌کند و روی `127.0.0.1` جواب می‌گیرد — تنها راهی که
  گوگل برای برنامهٔ نصبی می‌پذیرد. **PKCE اجباری است و `client_secret`
  در کار نیست**: برنامه‌ای که روی کامپیوترِ مردم نصب می‌شود هیچ رازی
  نمی‌تواند نگه دارد.
- ⚠️ **پورتِ برگشت ثابت نیست** — از سیستم گرفته می‌شود، چون پورتِ ثابت روی
  کامپیوترِ کاربر ممکن است گرفته باشد.
- شناسهٔ Desktop جدا از شناسهٔ Web است (`googleDesktopClientId` در
  `/api/config`). شناسهٔ Web روی برنامهٔ نصبی `invalid_client` می‌دهد.
- ⚠️ «حسابِ من» **بیستمی** است و باید بماند: هجده‌تای اولِ نوار ترتیبشان
  قفل است (`NavOrderTests`).

- بخشِ اشتراک دیگر در تنظیمات نیست؛ **کادرِ نشانی هم هیچ‌جا نیست** و نباید
  اضافه شود.
- آزمون: `LicenseGuardTests` و `CloudAddressLockTests` در `PumpYaqobi.Tests`
  (دومی روی خودِ سورس می‌گردد و قفل بودنِ نشانی را ثابت می‌کند).

سمتِ سرور: `vil3ntec-it/shop` → `docs/PUMP-fa.md`.

## چاپ — کارگاهِ چاپ به شکلِ سایت و اکسل (از ۱۴۰۵/۰۶/۲۵)

گزارشِ صاحب ریپو: «بخشِ پرینت خیلی کم‌بودی داره و نه شبیهِ سایت و نه شبیهِ اکسل
است و طراحی‌اش هم خیلی خراب است.» مرجع: `pumpPrintStudio` در `index.html`
(شش حالتِ مقیاس، چهار تبِ تنظیمِ ورق، نوارِ سربرگ/پاورقی با توکن‌ها).

- **سربرگِ جدول فقط یک جا ساخته می‌شود**: `DocStyle.Head(t, cell => …)`. اگر
  «تکرارِ سربرگ در هر ورق» روشن باشد `t.Header(...)` می‌شود، وگرنه ردیفِ
  اول. ⚠️ در هیچ گزارشی مستقیم `t.Header(h => h.Cell()…)` ننویسید — سربرگی که
  همیشه تکرار شود روی هر ورق دو ردیف و یک نوارِ خالی می‌دزدد.
- **`DocStyle.Current` ([ThreadStatic])** تنظیمِ ورقِ همان سند است و فقط
  داخلِ `DocStyle.Compose` معتبر است. رنگ‌ها از `Paint(hex)` می‌آیند (رنگی /
  خاکستری / سیاه‌سفید) و خط‌ها از `Line(hex)` که با خاموش بودنِ
  «خطوطِ جدول» شفاف می‌شود. رنگِ خام در `*Report.cs` ننویسید.
- **مقیاس یک جا حل می‌شود**: `ScaleSolver.Solve(setup, build)`. حالت‌های
  «جا شدن در یک ورق / ردیف‌ها / N×M ورق» با رندرِ ۳۶dpi و جست‌وجوی دودویی
  حساب می‌شوند (همان `pickScale` سایت) و `PageSetup.WithResolvedScale` نتیجه را
  به `Custom` برمی‌گرداند. `DocumentPreview.BuildSolved` تنها راهِ ساختنِ سند
  است — `_build(s)` را مستقیم صدا نزنید؛ آزمونِ
  `ScalingIsAppliedInOnePlaceForEveryReport` قفلش کرده.
- نوارِ سربرگ/پاورقی در فاصلهٔ `MarginHeader`/`MarginFooter` از لبه می‌نشیند
  (`BandOffsets` هیچ‌وقت از خودِ حاشیه بزرگ‌تر نمی‌شود). قابِ نقطه‌چینِ قدیمیِ
  دورِ متن برداشته شد؛ نقطه‌چینِ پیش‌نمایش راهنمای حاشیه است، نه بخشی از PDF.
- ⚠️ کارت‌های کنارِ پیش‌نمایش `SettingCard` هستند ولی `StyleKeyOverride`شان
  `ComboBox` است، پس سلکتورِ `c|SettingCard` **هیچ‌وقت** نمی‌گیرد. سلکتور
  `ComboBox.card` است و هر کارت `Classes="card"` دارد. بی این، کارت‌ها باریک و
  یک‌خطی می‌شوند.
- تنظیمِ قدیمیِ ذخیره‌شده (`{"Paper":"A5","Scale":1,…}`) باید بی‌خطا با
  پیش‌فرض‌های تازه بار شود — `AnOldSavedSetup_StillLoadsWithTheNewDefaults`.
- عکسِ سند برای چشم: `dotnet run --project PumpYaqobi.UiTests -c Release -- printshot <پوشه>`
  (پیش‌نمایش + هر چهار تبِ تنظیمِ ورق).
- **«ورق‌ها: ۱ تا ۳» همیشه دیده می‌شود** — همان مدلِ سایت که صاحب ریپو نشان داد
  («۱ تا ۳، ۲ تا ۳، ۱ تا ۱»): دو کادر با ▲▼ که تا بازه انتخاب نشده کلِ گزارش را
  نشان می‌دهند (`SyncRangeBoxes`) و تایپ در آن‌ها خودش حالت را روی «بازهٔ
  ورق‌ها» می‌گذارد (`ToRange`). پنهان کردنش پشتِ حالتِ بازه اشتباه بود.
- **انتخابِ ورق مثلِ اکسل** (خواستهٔ صاحب ریپو: «از سه ورق فقط دومی، یا یک و
  سه و دومی نه»): حالتِ چهارمِ کادرِ «کدام ورق‌ها» — `PrintWhat.Pages` با
  `PageSetup.PagesText` («۱،۳» یا «2,4-6»؛ `PrintJob.ParsePages`) و یک تیک برای
  هر ورق (`PageChecks`). ⚠️ تیک‌ها و کادر یک چیزند و `RebuildPageChecks` از خودِ
  کادر می‌خواند، نه از `Setup` — وگرنه یک قدم عقب می‌ماند. سنجشِ رفتارش در
  `printshot` است (ویومدلِ پیش‌نمایش بی پلتفرمِ آوالونیا ساخته نمی‌شود).
- آزمون: `PrintPageTests` و `PageSetupTests` در `PumpYaqobi.Tests`.

## 📉 زیان ناشی از افزایش قیمت — و ⚙️ تنظیمات مدیرِ تیل امانت (از ۱۴۰۵/۰۶/۲۵)

خواستهٔ صاحب ریپو: «تمام برنامه رو با سایت بررسی کن و ببین چه کم‌بودی‌هایی
دارد؛ منطقِ برنامه را دست نزن، فقط منطق‌هایی که ندارد را پیدا و درست کن.»
سنجشِ دکمه‌به‌دکمهٔ سایت با برنامه دو منطقِ جاافتاده داشت:

- ⛔ **از ۱۴۰۵/۰۷/۱۲ این بند تاریخ است، نه قاعده**: به خواستهٔ صاحب ریپو محتوای
  «زیان ناشی از افزایش قیمت» برداشته شد و `PriceLossService` با آزمون‌هایش رفت
  (بخشِ «🔁 شیفت‌+عدد، برگشت و دوباره» در انتهای همین فایل). کادرش مانده.
- **`Application/Services/PriceLossService.cs`** — رونوشتِ ‎_plReportAll‎ و
  خانواده‌اش (‎_plAllocate‎ · ‎_plRowReport‎ · ‎_plRateOn‎ · ‎_plInvView‎).
  زیربخشِ «قرض‌داران» با شناسهٔ `priceloss` (`PriceLossSectionViewModel`).
  - ⚠️ **فقط گزارش است و هیچ چیزی نمی‌نویسد.** هیچ عددی از این‌جا وارد بدهی،
    الباقی یا موجودی نمی‌شود؛ آزمونِ `Build_DoesNotMutateAnything` قفلش کرده.
  - نرخِ روزِ رسید فقط از تاریخچهٔ اتحادیه (`Tools.RateHistoryAsync`) و فقط
    برای تاریخِ واقعیِ همان رسید خوانده می‌شود؛ امروز نرخِ همین لحظه خودش
    رکوردِ امروز است. نرخِ تخمینی هرگز ساخته نمی‌شود (`NoRateN`).
  - رسیدها از سه جا می‌آیند و هیچ‌کدام روی دیگری نمی‌افتد: ردیفِ بی‌لیترِ
    جدول (رسیدهای مهاجرت‌کرده و فاکتورها، با `InvoiceId`)، `RasidLog`، و برای
    حسابِ **مهاجرت‌نکرده** عددِ سربرگ منهای سهمِ فاکتورها. حسابِ
    مهاجرت‌کرده عددِ سربرگش را دوباره نمی‌شمارد.
  - `DebtorService.LoadAllAsync(withReceipts: true)` همان بارگذاریِ فقط‌خواندنی
    است به‌علاوهٔ `RasidLog` با یک پرس‌وجو. مسیرهای ویرایش همچنان `LoadFullAsync`.
  - نسخهٔ وب `lossPaidDate/lossPaidRate` روی ردیف مهر می‌زد؛ این‌جا نیست، چون
    رسیدِ نیتیو یک ردیفِ واقعیِ تاریخ‌دار است. رسیدِ داخلِ خودِ ردیف با تاریخِ
    همان ردیف حساب می‌شود — همان رفتارِ سایت برای ردیفِ مهرنخورده.
  - ⚠️ ستون‌های جدول‌هایش پهنای **صریح** دارند: ستونِ `Auto` پیش از رسیدنِ
    ردیف‌های زیربخش روی پهنای سربرگ سفت می‌شود و متنِ بلند «…» می‌شد. ردیف‌ها
    ۴۴ پیکسل‌اند، پس متنِ خانه یک‌خطی است (با «·»)، نه `\n`.
- **`AmanatSettingsEditorViewModel`** (در `AmanatSectionViewModel.cs`) —
  ‎#am-settings‎ی سایت: نُه ضریب که همان لحظه ذخیره می‌شوند و همهٔ حساب‌ها را
  از نو حساب می‌کنند، فرمول، جدولِ «تبخیر در هر گرما» و منابع. تا پیش از این
  برنامه ضریب‌ها را فقط می‌خواند. فرمول‌ها همچنان فقط در `AmanatService`.
- آزمون: `PriceLossTests` و `AmanatSettingsTests`؛ `SubSectionTests` جای
  `priceloss` را زیرِ «قرض‌داران» قفل کرده. عکس: `shots/23-sub-priceloss.png`،
  `33-priceloss-person.png` و `24-amanat-settings.png` از `dotnet run --project PumpYaqobi.UiTests -- <پوشه>`.

## شرکت‌ها تیل، آرشیوِ قرض‌داران و تم‌های نمونه (از ۱۴۰۵/۰۶/۲۵)

گزارشِ صاحب ریپو: «بخش شرکت‌ها و جدول‌های شرکت‌ها خیلی کم‌بودی داره… کیلو رو حذف کن…
بخش قرض‌داران آرشیو اون هم مشکل داره… چند نوع دیزاین بده تا ببینم.»

- **شرکت‌ها تیل** (`CompanySectionViewModel` + `CompanyPagesViewModel`): سه صفحهٔ
  رویی مثلِ سه مودالِ سایت — «📦 خریدها»، «🗂️ جدول‌های آرشیو»، «🔍 جستجوی خرید» —
  با `Overlay` روی حساب می‌نشینند. کادرِ کشوییِ «کارها» همان `cm-actions` است.
  - ردیفِ خریدِ مخزن (`SourcePurchaseId`) با نشانِ 📦 و **قفل** (`ILockedRow`؛
    `ExcelGrid` پیش از ویرایش می‌پرسد). حذفش آزاد است.
  - «جدول جدید» فقط جدولِ همان تیل را آرشیو می‌کند (`CompanyTableArchive`) و
    `PurchaseCheckpointPetrol/Diesel` را به آخرین شمارهٔ خرید می‌برد؛ خریدهای
    آرشیوشده با `MarkUnlinked` به جدولِ نو برنمی‌گردند. صفحهٔ خریدهای یک آرشیو
    فقط بازهٔ `PurchasesAfter < Id ≤ PurchasesBefore` را نشان می‌دهد.
  - «جستجوی خرید» در `CompanyPurchaseService` است و فقط می‌خواند: عدد هم کیلو
    است هم تن، ردیفِ بی‌مقدار با هیچ عددی جور نیست، هر خرید یک‌بار.
  - ⛔ ستونِ «کیلو» برنمی‌گردد. `Kg` در داده می‌ماند و `CompanyService.Ton` هر
    ردیفِ کهنه را به تن می‌دهد.
  - آزمون: `CompanyArchiveTests`. عکس‌ها: `21-company-page.png` و `21b…21e`.
- **آرشیوِ قرض‌داران** (`DebtArchivePageViewModel`): صفحهٔ جداگانه روی شخص
  (`DebtSectionViewModel.Overlay`)، نه آکاردئون. سربرگِ هر تیل از
  `DebtCalculationService.ArchiveFigures` (همان `_histFigures`: رسید = سربرگ +
  رسیدهای جدول؛ الباقی = برد + فیصدی − رسید). ردیف‌ها ویرایش‌شدنی‌اند و با
  `DebtorService.UpdateArchiveAsync` فقط در خودِ آرشیو (`RowsJson`) می‌نشینند —
  جدولِ زنده هرگز دست نمی‌خورد. ستون‌های رسید/رسید تیل/نوع تیل مثلِ `PersonView`
  از کد پنهان می‌شوند (`DebtArchiveView.axaml.cs`). آزمون: `DebtArchivePageTests`.
- **تم‌های نمونه**: `PumpTheme.All` پنج تمِ تازه دارد (کاغذ و زغال، شیرِ گرم،
  زیتون، شرابیِ پررنگ، گرافیت) که در تنظیمات انتخاب می‌شوند. قاعده‌شان: بوم و
  کارت دو پله از هم فاصله دارند، خطِ دورِ کارت پررنگ و نوشته تیره. عکسِ
  چهارتاییِ هر تم: `theme-<id>-4up.png` از `dotnet run --project PumpYaqobi.UiTests -- <پوشه>`
  (`ThemeShot.Compose`). پیش‌فرض همچنان «مرمرِ شرابی» است تا صاحب ریپو یکی را برگزیند.

## سه طبقه: بوم ← بدنهٔ بخش ← کارتِ داخلی ← کادرِ تایپ (از ۱۴۰۵/۰۶/۲۵)

گزارشِ صاحب ریپو با عکس: «کادرها باید متفاوت باشن با اون جاهای خالیِ بغلِ
کادر… برای همه جای برنامه همین مشکل است.» ریشه: بدنهٔ بخش (`SectionPage`)،
کارتِ داخلی (`.panel`/`.stat`) و کادرِ تایپ (`TextBox`) در تمِ روشن هر سه سفید
بودند.

- `PumpTheme.Section` (اختیاری؛ `SectionBg` پیش‌فرضش پنل است) → `Pump.Section`،
  فقط روی `Border.card.section` که قالبِ `SectionPage` می‌گیرد. `Pump.Input`
  در `ThemeManager` از پنل و بوم مخلوط می‌شود و روی `TextBox`، `ComboBox` و
  `Border.calc` می‌نشیند. ⚠️ `Pump.Panel` را دوباره روی کادرِ تایپ نگذارید.
- `LookAudit` (`dotnet run --project PumpYaqobi.UiTests -- look`) سه پله را
  می‌سنجد: بدنه/بوم، پنل/بدنه، تایپ/پنل — هر سه دستِ‌کم `MinLayer`. تمِ
  تازه‌ای که یکی را نداشته باشد همان‌جا قرمز می‌شود؛ بومِ روشن‌تر از `#dcd5d6`
  عملاً رد می‌شود.
- (تمِ «سه‌طبقه» و بقیهٔ نمونه‌ها بعد از انتخابِ صاحب ریپو برداشته شدند — پایین.)

## دو تم — لایتِ آبیِ سفید و دارکِ مشکی‌وزرد، با نورِ لبه (از ۱۴۰۵/۰۶/۲۵)

خواستهٔ صاحب ریپو با دو عکسِ مرجع و پالتِ دقیق. لایت: `#1E3A8A · #3B82F6 ·
#60A5FA · #E0F2FE · #FFFFFF` («سفیدِ خالص با کارتِ سفید و سایهٔ ظریف»). دارک:
`#0B0B0F · #1A1A1F · #FFD700 · #E6C800 · #FFF3C4` («مشکی، کارتِ خاکستریِ تیره،
زردِ طلایی و نورِ ظریف دورِ کادرها — یک Border ساده نمی‌خواهم»). دارکِ سرمه‌ای یک
بار ساخته و همان روز رد شد: «اون قبلیه اشتباه شد».

- `PumpTheme.All = { Blue, Gold }` — `blue` («آبیِ روشن»، پیش‌فرض) و `gold`
  («مشکی و زرد»). `ById` شناسه‌های قدیمی را به یکی از این دو می‌رساند. تمِ سوم
  اضافه نکنید؛ در دارک از آبی برای تاکید استفاده نکنید.
- **نورِ لبه + سایه** در خودِ تم است: `CardShadow` / `CardShadowHover` (رشتهٔ
  `BoxShadows`؛ لایه‌ها: هایلایتِ سفیدِ داخلی فقط در دارک، خطِ ۱ پیکسلیِ آبی،
  هالهٔ کم‌آلفای `#60A5FA`، سایهٔ بیرونی) → `Pump.CardShadow` روی `Border.card`،
  `.panel`، `.stat` و زیرِ ماوس `Pump.CardShadowHover`. ⚠️ `ClipToBounds` از
  `Border.card` برداشته شد چون سایه را می‌بُرید. `person-card` عمداً سایه ندارد
  (چهارده کارت در یک صفحه).
- ⚠️ **بومِ لایت سفیدِ خالص است و کارت هم سفید** — این عمدی است. `FlatCanvas`
  به `LookAudit` می‌گوید پلهٔ روشنایی را نسنجد و فقط لبه (`MinRim`) و وجودِ
  سایه را بخواهد. بومِ لایت را خاکستری نکنید تا سنجش سبز شود.
- تبِ فعالِ نوار از `Pump.NavActiveBg`/`NavActiveFg` می‌آید: لایت زمینهٔ
  `#E0F2FE` با نوشتهٔ `#1E3A8A`، دارک گرادیانِ تاکید با نوشتهٔ سفید.
- نمودارها: `Pump.ChartLine` / `Pump.ChartPoint` (`SparkChart.PointBrush`) /
  `Pump.ChartBarSel` — لایت `#3B82F6`/`#60A5FA`، دارک `#FFD700`/`#FFF3C4`.
  آیکون‌های نوار از `Pump.IconFg`/`Pump.IconBadge` رنگ می‌گیرند
  (`IconColorConverter`): آبی در لایت، طلایی در دارک — نه رنگِ جدا برای هر بخش. مسیرهای برداری در `Themes/Icons.axaml` (۲۴×۲۴، خطِ ۱٫۷۵).

## رشدِ تدریجیِ جدول — «کلِ صفحه به اندازهٔ جدول» بی لگ (از ۱۴۰۵/۰۶/۲۵)

گزارشِ صاحب ریپو با عکس: «جدول‌ها غیب شدن» (صفحهٔ خالی زیرِ جدولِ ۶۱ ردیفی) و
خواستهٔ چندباره‌اش: «سقفِ زیرِ جدول نباشد که جمله سرِ جا بماند و جدول‌های دیگر
از زیرش رد شوند… همه‌چیز اول تو لودینگ رندر بشه و جدول‌ها به ترتیبِ اسکرول
زود بیان و لگ نزنه.»

- ریشهٔ صفحهٔ خالی: `Settle` هر بار نوارِ لغزشِ گذرای جدول را می‌دید، کلِ «جای
  لغزش» را به بلندی اضافه می‌کرد و سقفی نداشت؛ و بلندیِ ردیف با `RowHeight`ِ ۴۴
  حساب می‌شد در حالی که ردیفِ چیده‌شده ۴۵ است. حالا `MeasuredRowHeight()` از خودِ
  ردیف می‌خواند و `Settle` فقط پس از رشدِ کامل و حداکثر `MaxPad` پیکسل.
- **هیچ جدولی دیگر سرِ «یک صفحه» نمی‌ایستد** (`PageRowLimit` بی‌اثر شد). همه
  **تدریجی** بلند می‌شوند: پاسِ اول یک صفحه (`FirstChunk`)، بعد هر فریم یک تکه
  با اولویتِ `Background` (`QueueGrow`؛ تکه هر بار دو برابر تا `GrowMax`). ⚠️ هر
  پاسِ رشد همهٔ ردیف‌های ساخته‌شده را دوباره اندازه می‌گیرد، پس تکهٔ ثابتِ کوچک
  هزینه را مربعی می‌کرد.
- ⚠️ `DataGrid` با اضافه/کم شدنِ ردیف خودش را دوباره اندازه **نمی‌گیرد**؛
  `OnRowsChanged` روی `CollectionChanged` همین را می‌کند و با `Reset` رشد از
  اول شروع می‌شود (وگرنه ماهِ بعدی یک‌جا ساخته می‌شد).
- `GrowRowLimit = 600`: بالاتر، مجازی‌سازی و نوارِ خودِ جدول برمی‌گردد
  (`gridperf` با ۱٬۰۰۰ ردیف همین را می‌خواهد).
- سنجش: `ledgerperf` حالا «بارِ دوم» را **اولین فریمِ قابلِ کار** می‌سنجد
  (`RunJobs(DispatcherPriority.Render)` تا تکه‌های رشد شمرده نشوند) و «رشد» را
  جدا با سقفِ `GrowGoal`. ⚠️ `RunJobs()`ی بی‌پارامتر زنجیرهٔ چیدمان ⇄ تکه را
  یک‌جا می‌دواند و «اولین فریم» را دروغ می‌کند. بازسازیِ باگ: `-- vanish`
  (`VanishProbe`).

## بقیهٔ فهرستِ ۱۴۰۵/۰۶/۲۵ — کجا چه شد

- **دالر هم‌وزنِ افغانی**: `TextBlock.num2` (۱۸، پررنگ، `Pump.Info`) روی سه کادرِ
  گاوصندوق، «الباقیِ دالر»ِ صفحهٔ شرکت و کفِ کارتِ شرکت. `muted` نگذارید.
- **تاریخچهٔ هر بخش**: `AppHost.OpenHistory` را `MainViewModel` می‌نشاند
  (`OpenHistoryAsync(kind)` = رفتن به «تاریخچه‌ها» + `OpenAsync(kind)`).
  گاوصندوق/صرافی/امانت `OpenHistoryCommand` دارند؛ کلیدها همان `HistoryService.Kinds`.
- **رسیدِ سربرگِ قرض‌دار**: در **اولین ردیفِ خالی** می‌نشیند (`IsBlank`)، نه تهِ
  جدول؛ نبود ⇒ ردیفِ تازه. کادرش با کلیکِ بیرون فوکوس را رها می‌کند
  (`PersonView`، ‎PointerPressed‎ی تونلی) و پس از `LostFocus` یک فریم بعد دوباره
  خبر می‌دهد (`RepaintHeadEdits`) چون اتصالِ دوطرفه خبرِ هم‌زمان را می‌بلعد.
- **آرشیوِ قرض‌دار**: دکمه «🗂️ آرشیو — جدول N» بغلِ «٪ فیصدی»؛ صفحه‌اش نوارهای
  رنگیِ کشویی است (`Button.arc-bar`، چهار رنگ چرخشی، `BarLabel`: «تاریخ ·
  روزِ هفته · ماه · جدول چندم»، شماره از ترتیبِ ساخت).
- **روز/شب و پطرول/دیزل**: دو دکمهٔ `seg` با `Classes.active`، نه `ToggleSwitch`
  (`SetNightCommand`، `SetFuelCommand`). `ToggleSwitch` دیگر جایی نیست.
  «کارمندِ شیفت» و «قرضِ پارچه» از صفحهٔ ورق برداشته شدند (داده‌شان می‌ماند).
- **پیشنهادِ خودکار** (`Controls/Suggest.cs`): یک پاپ‌آپ برای کلِ برنامه.
  `Suggest.Provide(key, …)` فهرستِ نام‌دار (staff، debtor) و همیشه مقدارهای
  همان ستون (`ColumnValues`). روی ستون: `c:Suggest.Key="staff"`؛ روی `TextBox`
  هم همان. Tab می‌چرخد و بعد از آخری بیرون می‌رود؛ ↑↓ گرفته نمی‌شوند؛ Enter
  برمی‌دارد. `Suggest.Showing` برای سنجش.
- **حذفِ ردیف**: راست‌کلیک + `Ctrl+Delete` (`RowDeleteCommand`ِ همان ردیف). ستونِ
  حذف برنمی‌گردد.
- **آیکون‌ها رنگِ خودشان را دارند** (`IconColor.<key>` در `Icons.axaml`) — قاعدهٔ
  «فقط آبی» همان روز پس گرفته شد: «تشخیص‌شون مشکله».
- **ماشین‌حساب**: اندازه در ویومدل (`Width/Height`، `Resize`، `ToggleSize` =
  ۲۸۶×۴۳۰ ⇄ ۴۰۰×۵۸۰ مثلِ سایت)، گوشهٔ بالا-چپ `Thumb`، در `AppSettings.Calc*`
  می‌ماند، قلمِ کلیدها با بلندی (`KeyFont`). با ماشین‌حسابِ باز، صفحه‌کلید هم
  می‌زند مگر داخلِ کادرِ تایپ (`Shortcuts.CalcKey`). کلیدها `Pump.Input` تا در
  تمِ تیره از کارت جدا باشند.
- ⚠️ **سنجشِ رفتاری `-- verify`** (`VerifyProbe`, در CI): هر هفت رفتارِ بالا را
  واقعاً انجام می‌دهد. بارِ اول سه باگ گرفت که از روی کد دیده نمی‌شد: ردیفِ
  خالیِ تازه تاریخِ امروز دارد (پس `IsBlank` تاریخ را نمی‌شمارد)؛ اتصالِ
  `LostFocus` مقدارِ «عوض‌نشده» را دوباره به کادر نمی‌فرستد (پس `RasidBoxLost`
  متنِ پیش از فوکوس را برمی‌گرداند)؛ و `Ctrl+Delete` را `Delete`ی خالی‌کننده
  می‌بلعید (ترتیبِ `case`ها). قاعدهٔ صاحب ریپو: «با تست بگو، نه با حدس».


## 📲 کیو‌آرِ زنده — «طرف داره با کیو‌آر چک می‌کنه، من دارم زنده تغییر می‌دم» (از ۱۴۰۵/۰۶/۲۵)

خواستهٔ صاحب ریپو: «من دارم زنده تغییرات میارم توی حسابِ طرف، طرف هم داره با
کیو‌آر حسابشو چک می‌کنه و می‌خوام درجا برای اون هم بره… که هر دقیقه بتونه چک
کنه و بفهمه.»

```
برنامهٔ نیتیو ──(هر تغییر)──▶ ابر  PUT /api/pump/device/files/acct-<id>   {v,k,at,d}
              ──(هر تغییر)──▶ خانگی  acct/<id>                          (همان پاکت)
گوشیِ مشتری   ◀──(هر ۶۰ث)──── GET /api/pump/public/<پمپ>/acct/<id>?k=…  {at,d}
```

- **کیو‌آر همان کیو‌آرِ قبلی است**: دادهٔ حساب داخلِ خودِ کد می‌ماند (بی‌اینترنت
  هم باز می‌شود) و فقط چهار پارامترِ کوتاه پیش از `d=` می‌نشیند:
  `s` (کدِ پمپ، به شکلی که ابر نگه می‌دارد — `AcctLive.CloudCode`)، `a`
  (`d<DebtAccount.Id>` یا `c<TilCompany.Id>`)، `k` (رمزِ همین یک حساب) و `t`
  (زمانِ ساختِ کد). `AcctView.Url(..., live)`؛ خالی ⇒ همان کدِ ایستا.
- ⚠️ **`QrKey` یک بار ساخته می‌شود و دیگر عوض نمی‌شود** (`AcctLive.EnsureAsync`
  هنگامِ ساختِ کیو‌آر): کاغذی که دستِ مشتری است باید تا ابد کار کند. حسابِ
  بی‌`QrKey` منتشر هم نمی‌شود — پمپی که کیو‌آر نداده هیچ هزینه‌ای نمی‌دهد
  (`DebtorService.LoadQrAccountsAsync` با کوئریِ خالی برمی‌گردد).
- ⚠️ **هیچ رمزِ پمپی در کیو‌آر نیست** — نه رمزِ برنامه، نه رمزِ خواندنِ سرور.
  `k` فقط همین یک حساب را باز می‌کند و سرور فقط `{at, d}` پس می‌دهد. آزمونِ
  `TheViewerPageExistsAndReadsTheSameFragment` واژهٔ `token` را در صفحه قدغن
  کرده — همین‌طور بماند.
- انتشار در `StationPublisher.PublishAccountsAsync` → `AcctLivePublisher`:
  هر حساب اثرِ انگشتِ خودش (`AcctLive.HashOf`، بی تاریخِ روز)، دو مقصد با دو
  حافظه (خانگی نرفت ⇒ فقط خانگی دوباره)، و `Pending` تا دورِ بعد هم بی تغییر
  دوباره بزند. ⚠️ **مقصدِ اصلی ابر است** — مشتری روی اینترنت است، نه در شبکهٔ
  پمپ؛ پس این بخش حتی با سرورِ خانگیِ خاموش هم می‌رود (`CloudActivated`).
  مقصدِ خانگی فقط پشتِ درِ تازه (`HomeSyncMode.Station`) است؛ درِ قدیمی شاخهٔ
  `acct` ندارد.
- صفحهٔ `view/`: `LIVE_API` **در خودِ کد قفل است** (همان قاعدهٔ `kar/cloud.js`)؛
  اول دادهٔ داخلِ کد، بعد هر ۶۰ ثانیه + هر بار که صفحه جلوی چشم بیاید + دکمهٔ
  «به‌روز کن»؛ فقط عکسِ `at > t` جای قبلی می‌نشیند. نشانِ زنده/آفلاین/«هنوز
  نیامده» در کادرِ بالا. کیو‌آرِ بی این پارامترها هیچ درخواستی نمی‌زند.
- سرورها: ابر `shop` → `src/routes/pump-public.js` (پیش از `/pump` سوار می‌شود،
  CORS `*`، ۴۰۴ برای هر سه حالتِ «نیست/غلط/نبود»)؛ خانگی `server` →
  `GET /api/stations/:code/acct/:id?k=` با همان قاعده.
- آزمون: `AcctLiveTests` (نشانی، رمز، پاکت، انتشار، دیتابیس)، بخشِ ۱۰ در
  `node tools/check-view.mjs` (با `fetch`ِ ساختگی)، `test/pump-public.test.js`
  در `shop` و بخشِ ۸ب در `test/stations.mjs`ِ `server`.

## 💬 چتِ پشتیبانی — مشتریِ کیو‌آر ↔ صاحبِ پمپ (از ۱۴۰۵/۰۶/۲۶)

خواستهٔ صاحب ریپو: «داخلِ کیو‌آر که اسکن می‌زند یک چتِ پشتیبانی با من داشته
باشد… عینِ واتساپ: اسمش دیده شود، پیام‌ها پاک شوند، عکس و ویدیو و ضبطِ صدا،
بتوانم بلاک کنم، و حتی مرورگرش بسته باشد پیام برایش برود. چتِ بخشِ پمپ را هم
ببرید بالا جای قفل.»

```
صفحهٔ view/ (رمزِ k)  ──▶ ابر  /api/pump/public/<پمپ>/acct/<id>/chat  ◀── برنامه (توکنِ دستگاه)
                          └──▶ web-push (view/sw.js) ◀── جوابِ صاحبِ پمپ
```

- **هویتِ مشتری همان `k`ِ کیو‌آرِ زنده است** — حساب نمی‌سازد، رمزِ پمپ ندارد.
  کیو‌آرِ ایستا (بی `s/a/k`) چت ندارد و صفحه هیچ درخواستی نمی‌زند.
- صفحهٔ `view/`: دکمهٔ شناورِ «💬 پشتیبانی» با شمارهٔ نخوانده؛ بارِ اول نام
  می‌پرسد (`localStorage`)؛ حباب‌ها، عکس/ویدیو/صدا inline، پاک کردنِ پیامِ
  خودش، هر ۸ ثانیه با چتِ باز و هر ۶۰ ثانیه با چتِ بسته می‌پرسد؛ «🔔 خبرم
  کن» اشتراکِ پوش می‌گیرد (`view/sw.js` فقط پوش است، هیچ کشی ندارد).
  `LIVE_API` همان نشانیِ قفل‌شده است. ⚠️ همه‌چیز پشتِ `try` و «اگر نبود، رد
  شو»: چت رفاه است، حساب اصل — `chatEnsure` بی `document.body` هیچ کاری نمی‌کند.
- برنامه: `ChatSectionViewModel` دو نوع گفت‌وگو دارد — شبکهٔ خانگی (مثلِ
  قبل، `HomeServer`) و پشتیبانی (`acct:<id>`، از ابر با `CloudLink.Chat*`، هر
  ۸ ثانیه صندوق). حالتِ خالص در `Services/SupportState.cs` (یکتایی با `Seq`،
  نخوانده = پیامِ مشتری بعد از `owner_seen_seq`، پاک‌شده جایش می‌ماند).
  عکس/ویدیو با `Dialogs.PickFileAsync`، صدا با `Services/WaveRecorder.cs`
  (winmm، فقط ویندوز، WAV ۱۶kHz — بی هیچ بسته‌ای؛ روی غیرِ ویندوز دکمه
  نیست)، ویدیو/صدا با برنامهٔ خودِ سیستم باز می‌شود. دکمهٔ «💬 پشتیبانی» در
  سربرگ (`MainViewModel.Chat`) با شمارهٔ نخوانده؛ بخشِ ۱۹ سرِ جایش ماند
  (`NavOrderTests`). پیامِ تازهٔ مشتری وقتی چت باز نیست، توست می‌شود.
- ⚠️ پشتیبانی فقط با برنامهٔ فعال‌شده (کدِ شش‌رقمی) کار می‌کند؛ بی آن همان
  پیام‌رسانِ خانگی است.
- سرور: `shop` → `docs/PUMP-fa.md` (بخشِ «چتِ پشتیبانی»)، `test/pump-chat.test.js`.
- آزمون: `SupportChatTests`، بخشِ ۱۱ در `node tools/check-view.mjs`.

## ⛔ مخزن: وزنِ خرید فقط به تن (از ۱۴۰۵/۰۶/۲۶)

«موقعِ خرید دو نوع اندازه است، یکی کیلو یکی تن؛ کیلو نباشد.» کادرِ فرم
«وزن (تن)» است (`BuyTon`، `StorageSectionViewModel.KgOf` = تن × ۱۰۰۰)، کارتِ
«وزن (کگ)» و ستونِ «وزن (کگ)»ِ PDF برداشته شدند. `Kg` در داده می‌ماند تا
فرمول‌ها (`PurchaseCalc.Compute(kg, …)`) و رکوردهای کهنه دست نخورند. کیلو را
به هیچ صفحه‌ای برنگردانید.

## پنج سال داده: کجا کند بود و چه شد (از ۱۴۰۵/۰۶/۲۶)

سنجشِ `years` پیش از اصلاح: تعویضِ تم ۱۴ ثانیه، تاریخچه ۴ ثانیه، هر زیربخشِ
قرض‌داران ۶ تا ۸ ثانیه، هر رفتن به بخش ~۱ ثانیه، عکسِ ایستگاه هر ۲۰ ثانیه ۱
ثانیه. ریشه‌ها و قاعده‌هایی که باید بمانند:

- **`PumpDbContext.Version`** — با هر `SaveChanges` یک واحد بالا می‌رود. هر
  کارِ دوره‌ای یا هر «تازه کن»ی که داده‌اش عوض نشده باشد باید با همین شماره
  رد شود: نوارِ بالای پنجره (`MainViewModel._bannerVersion`)، داشبورد
  (`_shownFor`)، و `StationPublisher` (`_lastVersion`؛ `BuildAsync` تنها
  وقتی می‌دود که شماره یا `Pending`ِ حساب‌ها عوض شده باشد). پیش از این نوار
  با هر رفتن به بخش همهٔ ردیف‌های شرکت‌ها و همهٔ گزارش‌ها را می‌خواند.
- **رشدِ جدول به دنبالِ اسکرول** (`ExcelGrid.NearViewport`/`HookPage`): تکهٔ
  بعدی فقط وقتی ساخته می‌شود که تهِ جدول به `Lookahead` برابرِ قابِ صفحه
  نزدیک باشد؛ جدولی که پایینِ صفحه است تا کاربر به آن نرسیده ردیف نمی‌سازد.
  همین است که تعویضِ تم و رفتن به بخش را از هزاران ردیفِ زنده رهانید.
- **تعویضِ تم یک خبر است، نه دو**: هر دو تم از همان راه‌اندازی ساخته و زیرِ
  `ThemeDictionaries` (`Light` = آبی، `Dark` = طلایی) **یک‌بار** ادغام می‌شوند
  (`ThemeManager.Installed`)؛ `Apply` در کارِ عادی فقط `RequestedThemeVariant`
  را عوض می‌کند. خطِ جدول‌ها با `TableStyle.Fill` داخلِ فرهنگِ هر تم است و
  `TableStyle.Apply` روی **هر دو** فرهنگ می‌نویسد، نه `app.Resources` (که روی
  فرهنگ‌های ادغام‌شده اولویت دارد و رنگِ تمِ بعدی را می‌پوشاند). ⚠️ «آبی =
  روشن، طلایی = تیره» قرارداد است (`PumpTheme.IsDark`)؛ دو تم با یک گونه
  نمی‌شود. آزمونِ `TheTableStyleIsAppliedAtStartupAndOnThemeChange` و
  `themeflip` قفلش کرده‌اند: جای هیچ سرستون/خط/نوشته‌ای پس از
  آبی⇒طلایی⇒آبی تکان نمی‌خورد (۰ جابه‌جایی)؛ تفاوتِ پیکسلیِ زیرِ ۵٪ فقط
  گزارش می‌شود، چون با `themediff` دیده شد لبهٔ یک‌پیکسلیِ کارت‌ها و نرمیِ
  حروف است، نه خط یا نوشتهٔ جابه‌جا. با پنج سال داده و بیست صفحهٔ گرم در
  درخت، هر خبر ~۴۰۰ میلی‌ثانیه است — تعویض از ۱۴ ثانیه به ~۲٫۵ رسید.
- **ریشهٔ «خط‌ها کج می‌شدند» پیدا و بسته شد**: ستونِ «#» (سرستونِ ردیفِ
  `DataGrid`) پهنای خودکار داشت؛ چیدمانِ اول آن را صفر می‌شمرد (۴۲ پیکسلِ
  خالی به ستونِ پُرکننده می‌رفت) و تعویضِ تم — اولین بی‌اعتبارسازیِ بزرگ —
  دوباره می‌شمرد و همهٔ خط‌های عمودی ۴۲ پیکسل می‌پریدند. `ExcelGrid.FixRowHeaderWidth`
  پهنا را از شمارِ ردیف‌ها صریح می‌گذارد. `themeflip` با
  `PUMP_THEMEFLIP_DEBUG=1` پهنای خانه‌های ردیفِ اول را پیش و پس از تم چاپ
  می‌کند — همان که ریشه را نشان داد. ⚠️ در `themeflip` فقط **جابه‌جایی** ایراد
  است؛ «گم شد» (نوشته‌ای که پس از تم در درخت نیست: ردیفِ هنوز ساخته‌نشدهٔ
  رشدِ تدریجی، کارتِ به‌روزرسانی وسطِ بررسی) و خانهٔ پُرکنندهٔ خودِ `DataGrid`
  (بیرونِ پهنای ستون‌ها) گزارش می‌شوند، نه ایراد.
- **ورود به بخش دو بار نمی‌خواند**: `GoAsync`/`OpenSubAsync` اول
  `EnsureLoadedAsync` و بعد `OnActivatedAsync` را می‌زنند؛ بخشی که هر دو را
  `RefreshAsync` کرده `ActivationRepeatsLoad => true` می‌گذارد تا پس از
  خواندنِ تازه، دومی رد شود (زیربخش‌های سنگین دقیقاً دو برابر طول می‌کشیدند).
  بخشی که در فعال‌سازی کارِ دیگری هم دارد (تاریخچه‌ها، قرض‌داران) روشنش
  نمی‌کند. سنجش‌ها منتظرِ `MainViewModel.LastSubOpen` می‌مانند، نه این‌که
  خودشان `OnActivatedAsync` بزنند.
- (سنجهٔ `plprof`ِ «زیان ناشی از افزایش قیمت» در ۱۴۰۵/۰۷/۱۲ با خودِ آن محاسبه رفت.)
- **کارت‌های تاریخچه** (`HistoryService.CardsAsync`) دیگر `FeedAsync` را صدا
  نمی‌زنند: هر دفتر فقط ستون‌های قاعدهٔ «این ردیف شمرده می‌شود؟» را می‌خواند
  (`Stamp`). آزمونِ `TheCardsMatchTheFeed` برابریِ عدد و تازه‌ترین تاریخ را با
  خودِ فهرست قفل کرده — قاعدهٔ رد کردنِ ردیف را در یکی عوض کردید، در دیگری هم.
- **زیان ناشی از افزایش قیمت**: `RateAt` جست‌وجوی دودویی است و در
  `Allocate` نشانگرِ `first` از ردیف‌های تسویه‌شده رد می‌شود؛ هر دو پیش از این
  مربعی بودند.
- «ورود و لودینگ» با پنج سال داده چند ثانیه است و عمدی: خواستهٔ صاحب ریپو
  «همه‌چیز اول تو لودینگ رندر بشه» (`WarmUpAsync` + `WarmDataAsync`). ارزان
  کردنِ هر بخش همان را کوتاه می‌کند؛ پرده را برندارید.

## باز شدنِ برنامه — «چرا برنامه‌های دیگر زود باز می‌شوند؟» (از ۱۴۰۵/۰۶/۲۶)

سنجشِ `dotnet run --project PumpYaqobi.UiTests -- startup` با دیتابیسِ **خالی**:
تا صفحهٔ رمز ۸٫۹ ثانیه بود — میزبان ۲٫۶، آوالونیا ۱٫۱، پنجره ۱٫۱، و پردهٔ
لودینگ ۴٫۱ (بیست‌ونه صفحه پشتِ سرِ هم). حالا ۳٫۳ (و روی ویندوز با کدِ
از پیش کامپایل‌شده کمتر). چهار قاعده که باید بمانند:

- **کدِ از پیش کامپایل‌شده**: `PublishReadyToRun` در `PumpYaqobi.App.csproj`.
  بی آن هر باز شدن یعنی JIT کردنِ آوالونیا، اسکیا، EF و خودِ برنامه روی
  کامپیوترِ کاربر. فقط روی `publish` با RID (ساختِ CI) اثر دارد.
- **میزبان هم‌زمان با آوالونیا**: `Program.Main` میزبان را روی نخِ دیگر شروع
  می‌کند و `App` با همان `AppHost.Start()` (که قفل دارد) منتظرش می‌ماند.
  سازندهٔ `AppHost` نباید به رابط دست بزند.
- **وصلهٔ دیتابیس یک بار برای هر ساخت**: `PumpDbFactory.EnsureReady` مهرِ
  `schema.stamp` (MVID اسمبلی + شمارِ جدول/ایندکسِ فایل) را در `Settings`
  می‌گذارد و تا همان است، `PatchTables/Columns/Indexes` نمی‌دوند. نسخهٔ تازه
  یا جدولِ افتاده مهر را عوض می‌کند.
- **پرده فقط بخشِ آغازین را گرم می‌کند**؛ بقیه پشتِ صفحهٔ قفل
  (`WarmRestAsync`، همان چند ثانیه‌ای که کاربر رمز می‌زند) و با اولین `Ready`
  می‌ایستد — وگرنه صفحهٔ در حالِ گرم شدن کنارِ صفحهٔ کاربر دیده می‌شد. برای
  همین `IsShellVisible` همیشه راست است و صفحهٔ رمز باید **مات و تمام‌قد و روی
  همه** باشد؛ `warm` هر سه را می‌سنجد. `WarmUp.RunAsync` دو بار صدا می‌شود و
  صفحهٔ گرم‌شده (`_warmed`) دوباره گرم نمی‌شود.

## بعد از رمز — و «کامپیوترم داغ می‌شود» (از ۱۴۰۵/۰۶/۲۶)

- **گذرِ دومِ داده (`WarmDataAsync`) دیگر پس از رمز اجرا نمی‌شود.** خواندنِ
  دادهٔ سی‌ودو بخش پشتِ سرِ هم، درست بعد از رمز، همان کندیِ بعد از ورود بود.
  هر بخش سرِ اولین دیدارِ خودش خوانده می‌شود. خودِ تابع برای سنجش‌ها هست؛
  دوباره به مسیرِ ورود برنگردانیدش.
- **انیمیشنِ بی‌پایانِ لوگوی پرده فقط با کلاسِ `pulse`** که به `IsStarting`
  بسته است. انیمیشنِ بی‌پایان روی کنترلِ پنهان هم می‌چرخد و هر فریم پنجره را
  از نو می‌کشد — CPU دائمی. هر انیمیشنِ `INFINITE`ی تازه باید به همین شکل
  با کلاس خاموش‌شدنی باشد.
- `StationPublisher.FirstDelay` (۱۲ ثانیه): اولین عکسِ ایستگاه بعد از ورود
  نمی‌رود، تا صفحهٔ اول بی رقیب بیاید.
- سنجشِ `startup` حالا «بی‌کاریِ ۳ ثانیه» را هم می‌سنجد: شمارِ چیدمان و CPU.
  برنامهٔ بی‌کار باید صفر چیدمان و زیرِ ۱۰٪ یک هسته باشد.
- **نیمه‌شب**: تیکِ ساعتِ پنجره روز را می‌پاید و `MainViewModel.DayChanged`
  تاریخِ سربرگ و نوار را از نو می‌سازد؛ کلیدِ نوار `Version` × روزِ سال است.
- چک‌لیستِ صدموردیِ تحویل با شاهدِ هر بند: `native/docs/CHECKLIST-fa.md`.

## چند اصلاحِ ظاهری با عکسِ صاحب ریپو (از ۱۴۰۵/۰۶/۲۶)

- **خریدهای شرکت** (`CompanyPurchasesView`): هر خرید کارتِ خواندنی است، همان
  شکلِ بخشِ مخزن و همان `cmp-card`ی سایت؛ فقط «ردیف‌های دستیِ جدول» جدول
  می‌مانند. جدول‌کردنِ خریدها خواستهٔ صاحب ریپو نبود.
- **کارتِ قرض‌دار**: نام ۱۹ سیاه، مبلغ ۲۶، برچسب‌ها ۱۳؛ کارت ۲۱۰×۲۱۵.
- **جست‌وجوی قرض‌داران** در همان نوارِ ابزار بعد از کارت‌های زیربخش
  (`SectionPage.Inline`)، نه ردیفِ جدا (`Filters`). `Inline` برای هر بخشی که
  کادرِ کوچکی کنارِ دکمه‌ها می‌خواهد.

## سنجش‌های تازه (از ۱۴۰۵/۰۶/۲۶)

- `dotnet run --project PumpYaqobi.UiTests -- themeflip` — «عوض کردنِ تم خط‌ها
  را کج می‌کند؟»: هر بخش، جای هر سرستون/خطِ جدول/نوشتهٔ خانه/کادرِ تایپ/نوارِ
  جمله پیش و پس از آبی⇒طلایی⇒آبی، و بخشِ تازه‌ساخته با تمِ دیگر؛ به‌علاوه
  سنجشِ پیکسلی (دو عکسِ یک بخش با یک تم باید یکی باشند؛ سربرگِ ساعت‌دار ماسک
  می‌شود، عکسِ اختلاف در `%TEMP%/pump-themeflip-shots`).
- `dotnet run --project PumpYaqobi.UiTests -- years` — «پنج سال استفاده»:
  دیتابیسِ پنج‌ساله (هر روز چند ردیف در هر دفتر، یک ورق با دو شیفت، ۶۰۰
  قرض‌دار با ۶۴ هزار ردیفِ پخش‌شده، شرکت‌ها، امانت، حاضری…) و همان کارهای
  روزمره با عدد: هر بخش و زیربخش، رفتن به ماهِ سه سال پیش و برگشت، حسابِ
  پنج‌ساله، ورقِ امروز و پنج سال پیش، تاریخچه‌ها، تعویضِ تم. سقف: ۱ ثانیه
  هدف، ۳ ثانیه شکست. ⚠️ ستون‌ها در `PerfAudit.Insert` حالا در گیومه‌اند —
  جدولِ حاضری ستونِ `In` دارد.

## 📲 کدِ پمپ — «هر کسی که برنامه را نصب می‌کند باید آن کد را بزند» (از ۱۴۰۵/۰۶/۲۶)

خواستهٔ صریحِ صاحب ریپو: «برای هر پمپ یک کد باشد که در برنامهٔ اندروید و آیفون
بزند، حساب‌های همان پمپ را نشان بدهد و با پمپ‌های دیگر قاطی نشود — این را خیلی
جدی بگیر… و در برنامهٔ نیتیو یک بخشِ جدید بالای صفحه بغلِ تم به اسمِ پروفایل
که لاگین با جیمیل و VIP و مدتش را ببیند.»

```
برنامهٔ نیتیو ──GET /api/pump/device/access-code──▶ K7PM-3XQ2   (پروفایل: کپی · کیو‌آر · عوض کردن)
گوشیِ کارمند  ──POST /api/pump/public/join {code}──▶ {url, readKey, station}  ──▶ همان دو در (doorsFor)
              ──GET  /api/pump/public/live?code=  ──▶ عکسِ ابری، وقتی سرورِ خانگی جواب نداد
```

- **صفحهٔ اولِ `kar/` کدِ پمپ است** (`#codePane`) — نه گوگل، نه فرمِ نشانی.
  گوگل («صاحبِ پمپ هستم») و کیو‌آر («اینترنت ندارم») دو لینکِ کوچکِ همان
  صفحه‌اند. ⚠️ راهِ کیو‌آر همچنان برداشته نشده و نباید بشود.
- **جداسازی**: هر چیزی که از یک پمپ در گوشی می‌ماند زیرِ کلیدِ همان پمپ است
  (`stnKey('snap'|'told')` = `pumpKar.v1.snap.<کد>`). `switchStation` عکس و
  خبرهای پمپِ قبلی را پاک می‌کند؛ `acceptSnapshot` تنها راهِ نشاندنِ عکس است
  (چه از خانه چه از ابر) و عکسِ کهنه را رد می‌کند. «پمپِ دیگر / خروج» =
  `forgetAll`. کدِ عوض‌شده (۴۰۴ سرِ باز شدن) گوشی را بیرون می‌اندازد.
- کد از ابر می‌آید و در `AppSettings.CloudAccessCode` می‌ماند؛ هیچ کادرِ تایپی
  برای کد در برنامهٔ نیتیو نیست (`ProfilePillTests`). `KarLink.ForCode` لینکِ
  `…/kar/?code=` می‌سازد — بی هیچ رمزِ سروری؛ اپ با `?code=` خودش کد را می‌زند.
- **دکمهٔ «پروفایل»** در سربرگ بعد از کادرِ تم و پیش از نشانِ نقش
  (`Button.pill-btn.profile`، رنگِ `Pump.Info`)؛ نوشته‌اش `Account.PillText`
  («VIP · ۴۲ روز» یا «پروفایل») و با `Phase == Ready` تازه می‌شود
  (`Account.RefreshAll`). بخشِ حساب همان بیستمی است، فقط عنوانش «پروفایل» شد.
- ⚠️ **cloud.js حالا داخلِ فایلِ نصبِ اندروید می‌رود** (`build-kar-apk.yml`).
  تا پیش از این جا مانده بود و اپِ نصبی نه گوگل داشت نه — از امروز — کدِ پمپ.
- سرورها: ابر `shop` → `docs/PUMP-fa.md` («کدِ پمپ») و
  `test/pump-access-code.test.js`؛ خانگی `server` → پنلِ «پمپ‌بنزین‌ها» با
  «جزئیات» و کارتِ ابرِ تب‌دار (`docs/STATIONS-fa.md`).
- آزمون: بخشِ «کدِ پمپ و جداسازیِ پمپ‌ها» در `node tools/check-kar-bot.mjs`
  (با `fetch`ِ ساختگی) و `ProfilePillTests` در `PumpYaqobi.Tests`.

## 🚪 دو در در اپِ کارمندان — «حساب‌های پمپ» و «کارمندان» (از ۱۴۰۵/۰۶/۲۶)

خواستهٔ صریحِ صاحب ریپو: «بعد از زدنِ کدِ پمپ وقتی اپ باز می‌شود دو بخش باشد:
یکی برای دیدنِ حساب‌های پمپ، یکی برای کارمندان تا افرادی که تیل دارند و ندارند
را با مخزن ببینند… و ببین دیگر چه‌ها لازم است، خودت کمبودی‌هایش را درست کن.»

- بعد از رمز، `#paneHome` با دو در. درِ انتخاب‌شده زیرِ کلیدِ همان پمپ می‌ماند
  (`stnKey('mode')`) و با «پمپِ دیگر» پاک می‌شود. `NAVS` نوارِ هر در را می‌گوید؛
  `#modeSeg` در سربرگ جابه‌جا می‌کند.
  - **حساب‌ها** (کارفرما): داشبورد با همان چهار عددِ نوارِ برنامهٔ کامپیوتر
    (`live.banner`، از `StationSnapshot.BannerAsync` = رونوشتِ `RefreshBannerAsync`)،
    مخزن، کاشیِ هر بخش ⇒ همان بخش؛ بخش‌ها، قرض‌داران با حساب‌های کامل، مخزن، ربات.
  - **کارمندان**: `#paneStaff` — چراغِ سرخ/زرد/سبزِ هر قرض‌دار (سرخ اول)، شمارنده‌ها
    و فیلتر، کلیک ⇒ فقط حال و الباقی (بی جدولِ حساب)؛ مخزن؛ ربات.
- **کمبودی‌های عکس پُر شد**: `parcha`، `waraq`، `debtrasid`، `parcharasid`،
  `attendance` حالا در `sections` هستند (همان شکلِ `{t, head, rows, m, sum}`؛
  `StationSnapshotTests` قفلشان کرده) و ربات واژه‌هایشان را دارد.
- ⚠️ **پردهٔ لودینگِ اندروید** فقط با `PumpAndroid.boot('ready')` برداشته می‌شود؛
  تا این‌جا هیچ‌جا صدا زده نمی‌شد و اپِ نصبی سی ثانیه روی پرده می‌ماند. حالا
  تهِ `boot()` صدا می‌خورد.
- آزمونِ مرورگری: `node tools/check-kar-ui.mjs` (کرومیومِ بی‌پنجرهٔ Playwright
  از نصبِ سراسری؛ ابر با `page.route` ساختگی) — کد، قفل، دو در، فیلترها، باز شدنِ
  دوباره و پاک شدن. بارِ اول دو باگ گرفت: ترتیبِ چراغ‌ها (`order.out === 0` با
  `||` سه می‌شد) و کلیدِ `mode` که با «پمپِ دیگر» نمی‌رفت.
- آیفون همان `kar/` است در Safari با «افزودن به صفحهٔ اصلی» — همان کد، همان دو در.

## 🔄 به‌روزرسانیِ خودکارِ اپِ کارمندان — «برنامه و سایت خودشان از گیت‌هاب آپدیت شوند» (از ۱۴۰۵/۰۶/۲۶)

- **یک منبعِ حقیقت**: `kar/version.json` (در ریپو نیست؛ `.gitignore`) که
  `deploy-pages.yml` سرِ هر انتشار می‌سازد: `v` = `git rev-list --count HEAD`،
  `apk` = `version.txt`ِ انتشارِ `kar-latest`. `build-kar-apk.yml` همان `v` را در
  `assets/www/version.json` داخلِ فایلِ نصب می‌گذارد. ⚠️ هر دو `fetch-depth: 0`
  می‌خواهند، وگرنه شمارِ کامیت‌ها ۱ است.
- `kar/update.js` (در `SHELL`ِ سرویس‌ورکر و در فهرستِ `cp`ِ فایلِ نصب): هر
  نیم ساعت + هر بار که صفحه جلوی چشم بیاید `version.json` را می‌خواند
  (سرویس‌ورکر عمداً آن را کش نمی‌کند). نشانیِ سایت `SITE` **در خودِ کد قفل**
  است — همان قاعدهٔ `cloud.js`.
  - مرورگر/آیفون: `v`ی تازه ⇒ نوارِ `#updBar` ⇒ `registration.update()` + `reload`.
  - اندروید: `v`ی تازه ⇒ پنج فایل (`FILES`) از سایت گرفته و با پلِ چندفایلهٔ
    پوسته نوشته می‌شوند (`updateFileBegin/Chunk/FileCommit` و آخر `updateFinish(v)`
    که `.files` را می‌نویسد)؛ بعد نوار «همین حالا سوار شود؟» ⇒ `restart()`.
    `apk`ی تازه‌تر از `info().app` ⇒ نوارِ «فایلِ نصبِ تازه» ⇒ `openUrl` به
    `kar-latest`. ⚠️ `updateOk()` سرِ هر بالا آمدنِ سالم صدا می‌خورد؛ بی آن
    پوسته بعد از دو بار آپدیت را دور می‌ریزد.
- پوسته (`MainActivity`): `bundledVersion()` اول `www/version.json` را می‌خواند و
  اگر نبود `versionName` (اپِ اصلیِ قدیمی همان راهِ تک‌فایلهٔ خودش را دارد و
  دست نخورده). آپدیتِ چندفایله فقط با نشانِ `.files` و همهٔ فایل‌هایش پذیرفته
  می‌شود (`multiComplete`)؛ `dropUpdate` کلِ پوشه را پاک می‌کند.
- ⚠️ همان اکشنِ شکستهٔ `setup-android` هر دو ورک‌فلوی اندروید را می‌انداخت؛ حالا
  SDKِ خودِ ماشین با `sdkmanager` آماده می‌شود. ساختِ سبزِ APK را بعد از هر
  مرجِ `kar/` یا `android/` واقعاً ببینید — «سبز نبود، پس گوشی قدیمی می‌ماند».
- آزمون: بخشِ «به‌روزرسانیِ خودکار» در `check-kar-bot.mjs` و سنجهٔ نوار در
  `check-kar-ui.mjs` (version.json ساختگی: ۱۰ بعد ۱۱).
- برنامهٔ کامپیوتر از قبل خودش به‌روز می‌شود (`Update/UpdateService.cs` از
  انتشارِ `latest`؛ کارتِ به‌روزرسانی در تنظیمات).

## 🪪 صفحهٔ پروفایل به شکلِ «پروفایلِ بیمار»ِ مرجع (از ۱۴۰۵/۰۶/۲۶)

گزارشِ صاحب ریپو با عکس: «نه پروفایلِ پمپ بنزین UI/UX است نه بخشِ سرورِ پمپ؛ باید
شبیهِ این عکس باشد، نه این که توی تنظیمات برود.»

- `AccountSectionView` سه‌ستونه است (`Grid 300,*,330`): **آواتار** (حرفِ اول، نامِ
  پمپ، کاربر و نقش، نشانِ VIP، ورود/خروجِ گوگل) · **مشخصاتِ پمپ** (نام، کدِ ابر،
  تلفن، نشانی، سرورِ خانگی، ابر، نسخه — همه فقط‌خواندنی؛ ⛔ هیچ کادرِ نشانی/رمز)
  و **کدِ اپِ کارمندان** · **اشتراکِ VIP** (پلن، پایان، مانده، وضعیت، کدِ
  شش‌رقمی). ردیفِ دوم: **تب‌ها** (کارمندان · تاریخچهٔ بخش‌ها · پشتیبان‌ها) با
  ردیف‌های رنگیِ `ProfileRow` (نوارِ رنگی از `Palette`، مثلِ «ویزیت»های مرجع) و
  **فایل‌های پشتیبان** (`BackupService.List`، دکمهٔ باز کردنِ پوشه).
- داده‌ها در `OnActivatedAsync` می‌آیند (`LoadRowsAsync`: `Attendance.StaffAsync`،
  `History.CardsAsync`، `Backup.List`) — هیچ حسابِ تازه‌ای این‌جا نیست.
- عکس: `20-account.png` از `dotnet run --project PumpYaqobi.UiTests -- <پوشه>`.
- مرکز فرمان همان طرح را دارد: `/stations/<کد>` (`StationProfile.tsx` در ریپوی
  `server`)، به‌جای مودالِ «جزئیات».

## 🐢 «موقعِ اسکرول لگ می‌زند» — ریشه‌ها با نمونه‌بردار (از ۱۴۰۵/۰۶/۲۶)

گزارشِ صاحب ریپو: «موقعِ اسکرول کادرها خیلی دیر می‌آیند، لگ می‌زند… اکسل خیلی
راحت بود ولی برنامهٔ من برعکس.» سنجش: `dotnet run --project PumpYaqobi.UiTests -c Release -- scrollperf`
(در `grid-perf.yml`)؛ ریشه‌یابی با `dotnet-trace collect … -- scrollperf trace`.
هر گامِ چرخ روی دفترِ ساخته‌شده ۹۴ میلی‌ثانیه بود، حالا ۱۱؛ حسابِ قرض‌دار ۱۱۳ ⇒ ۱۴.

- ⚠️ **سایهٔ محو روی کادرِ صفحه‌قد ممنوع.** ۶۰٪ هر فریمِ اسکرول سایهٔ محوِ
  `Border.card.section` بود که با هر فریم از نو کشیده می‌شد — هر شعاعِ محوی
  (۲، ۶، ۱۲) همان ~۳۰ میلی‌ثانیه؛ همان سایه بی محو (خط + `spread` + جابه‌جایی)
  صفر. پس `PumpTheme.SectionShadow` (بی `blur`) روی بدنهٔ بخش و `.panel`
  می‌نشیند — و زیرِ ماوس هم همان می‌ماند، چون ماوس همیشه روی صفحه است.
  `CardShadow`ِ محو فقط برای کارت‌های کوچک (`.stat`، `.card`ی معمولی).
- ⚠️ **`LayoutUpdated` برای هر چیدمانِ هر جای پنجره شلیک می‌شود** و همهٔ
  بخش‌ها در درخت می‌مانند. هر شنونده‌ای اول `IsEffectivelyVisible` را بسنجد
  (`ExcelGrid`، `TotalsStrip`). `TotalsStrip` نوارِ لغزشِ افقی را کَش می‌کند؛
  پیش از این در هر پاس کلِ درختِ جدول را می‌گشت — ۱۳٪ وقتِ نخِ رابط.
- **رشدِ جدول تکه‌های ۸ ردیفی** (`GrowChunk = GrowMax = 8`): ساختنِ هر ردیف ۱۰
  تا ۲۵ میلی‌ثانیه است، تکهٔ ۱۲۰تایی یک مکثِ ۱٫۳ ثانیه‌ای بود. «تکهٔ بزرگ‌تر
  ارزان‌تر» فقط با `TotalsStrip`ِ قدیمی درست بود.
- آنچه مانده و اندازه‌گیری شده: خودِ ساختنِ ردیف (ردیفِ گاوصندوق با دو دکمهٔ
  کپسول گران‌ترین است) و ساختنِ کارت‌های تازهٔ `ItemsRepeater` در قرض‌داران.
  راهِ ریشه‌ای — مجازی‌سازیِ واقعی با اسکرولِ صفحه — هنوز نوشته نشده.

## 🚫 بخشی که تویش نیستی هیچ مصرفی ندارد (از ۱۴۰۵/۰۶/۲۶)

خواستهٔ صریحِ صاحب ریپو: «اگر توی بخش نیستم آن بخش فعال نباشد و هیچ مصرفی
نداشته باشد — حتی یک درصد… توی یک حساب یا حسابِ شرکت یا ورقی هستم، نباید
ورق‌های دیگر و حساب‌های دیگر داده‌ای مصرف کنند… آن‌هایی که منطق دارند و از یک
بخش به بخشِ دیگر اطلاعات می‌رود، دست نزن.»

سنجش: `dotnet run --project PumpYaqobi.UiTests -c Release -- idle` —
با دادهٔ پنج‌ساله در هر بخش سه ثانیه بی‌کار می‌نشیند و سه چیز را می‌شمارد:
دستورِ دیتابیس (‎DbWatch‎)، ردیفِ زندهٔ بخش‌های پنهان، و کنترلِ نامرئیِ
چیده‌شده. **هر سه باید صفر باشند** و هستند.

- ⚠️ **`DbWatch`** (در `PumpDbFactory`) یک ‎DbCommandInterceptor‎ است که هر
  دستورِ رسیده به SQLite را می‌شمارد. همیشه روشن است (یک `Interlocked`)، و
  `Recording` فقط برای سنجش‌ها متنِ دستور را هم نگه می‌دارد.
- ⚠️ **جدولِ هم‌قدِ ردیف‌ها (ورق و پارچه) پارک نمی‌شود.** آن‌ها هیچ ردیفی را
  مجازی‌سازی نمی‌کنند، پس بازسازی یعنی ساختنِ دوبارهٔ هر سه جدولِ صفحه از صفر —
  `waraqperf` عددش را داد: باز کردنِ دوبارهٔ یک ورق از ~۳۰ میلی‌ثانیه به ۱٫۲
  ثانیه می‌رفت. سقفِ خودشان ۸۰ ردیف است، پس زنده ماندنشان ارزان است.
- ⚠️ **جدولِ نامرئی فهرستش را پارک می‌کند** (`ExcelGrid.OnShownChanged`):
  `ItemsSource` با `SetCurrentValue` برداشته می‌شود، `DataGrid` ردیف‌هایش را
  دور می‌ریزد، و با دیده شدنِ دوباره همان فهرست برمی‌گردد و تدریجی ساخته
  می‌شود. پیش از این هر بخشی که یک بار باز شده بود ردیف‌هایش تا همیشه زنده
  می‌ماندند (یک گشتِ ساده = ۲۴۵ ردیفِ نامرئی).
  - ⚠️ `SetCurrentValue` است نه `SetValue`: اتصالِ `{Binding Rows}` سرِ جایش
    می‌ماند و فهرستِ تازهٔ ویومدل (ماهِ دیگر، حسابِ دیگر) پارکِ کهنه را باطل
    می‌کند.
  - ⚠️ `IsEffectivelyVisible` در آوالونیا ۱۱ خبر نمی‌دهد، پس پوسته بعد از هر
    عوض شدنِ صفحه `ExcelGrid.NotifyPagesChanged()` را می‌زند (`SyncContent` و
    `OnContentPropertyChanged`).
- منطق‌های بین‌بخشی (`ShiftWaraqSync`، `WaraqPosting`، `ExchangeSync`،
  `StationPublisher`) دست نخورده‌اند و باید دست‌نخورده بمانند.

## 💾 پشتیبانِ هر شش ساعت روی سرور — سه روز (از ۱۴۰۵/۰۶/۲۶)

«هر روز بک‌آپ بگیرد و بفرستد به سرورِ برنامه هر ۶ ساعت و تا سه روز در سرور
بماند؛ اگر نرفت به مدیر پیام بدهد.»

```
هر ۶ ساعت ─▶ VACUUM INTO ─▶ POST /api/stations/<کد>/backup (بدنهٔ خام، رمزِ برنامه)
                              └▶ data/stations/<کد>/backups/  · سه روزِ تقویمی
```

- `Services/BackupPusher.cs` — هیچ‌وقت استثنا بیرون نمی‌دهد؛ هشدار از روی
  **زمانِ آخرین موفقیت** است (`WarnAfter` ۲۶ ساعت)، نه از روی یک بار نرفتن.
  `AppSettings.LastBackupSentAt` همان را نگه می‌دارد.
- سرور: `stations/backups.js` (‎KEEP_DAYS = 3‎، روزِ فایل از نامش) و دو مسیر در
  `routes/stations.js`. ⚠️ مسیرِ `backup` از میان‌افزارِ JSONِ `index.js`
  **مستثنا** است، وگرنه فایلِ SQLite را JSON می‌خواند.
- آزمون: بخشِ «۱۰ب) پشتیبانِ پمپ روی سرور» در `test/stations.mjs`.

## نوار، چراغِ سرور و پروفایل (از ۱۴۰۵/۰۶/۲۶)

- ⛔ **«پیام‌رسان» و «پروفایل» از نوار برداشته شدند** — هر دو دکمهٔ سربرگِ
  خودشان را دارند («دو جا هستند، این‌جا نمی‌خواهد»). خودِ بخش‌ها سرِ جایشان در
  `Sections` می‌مانند (نوزدهم و بیستم، `NavOrderTests`)؛ نوار از
  `MainViewModel.NavSections` می‌آید.
- **● چراغِ سرور** بغلِ «پمپ یعقوبی»: سبز = وصل، سرخ = جواب نمی‌دهد، خاکستری =
  تنظیم نشده. دلیل فقط در `ToolTip` و **هیچ نام/نشانیِ سروری** نوشته نمی‌شود
  (`TickServerDot`، با تیکِ ساعتِ پنجره).
- **کارتِ اشتراکِ پروفایل خالی نمی‌ماند**: `ShowSubscription` پیش از بازگشتِ
  «هنوز فعال نشده» هم `ShowSubDetails` را می‌زند. خانهٔ نداشته «—» است
  (`Dash`). آزمون: `ProfilePillTests`.
- **داشبورد** بدنهٔ رنگی دارد (`Pump.Section`) تا کارت‌ها و سایه‌شان از بوم جدا
  باشند.
- **کارت‌ها**: نامِ کارت ۱۹ سیاه است (`TextBlock.pname`) و عددِ درشت **ساده**
  — «الباقی:» و «AFN» و «افغانی» از خودِ عدد برداشته شدند و برچسبشان زیرِ عدد
  می‌نشیند (شرکت‌ها، امانت، ورق‌ها).
- **کیو‌آر**: نشانیِ بلند دیگر زیرِ کد نوشته نمی‌شود. ⚠️ جملهٔ زیرِ کد حقیقت را
  می‌گوید: تا برنامه با کدِ شش‌رقمی فعال نشده، عکسِ حساب به ابر نمی‌رود، پس
  به‌روزرسانیِ زنده و چتِ پشتیبانی روی گوشیِ مشتری نمی‌آید (`AcctLive.Hint`).

## ♾️ جدولِ چسبان — «بی‌نهایت هم باشد نباید افتی داشته باشد» (از ۱۴۰۵/۰۶/۲۷)

گزارشِ صاحب ریپو: «برنامه با اضافه شدنِ جدول یا زیاد شدنش خیلی کند می‌شود…
حساب دیر باز می‌شود، اسکرول نرم نیست و گیر گیر دارد… زیرِ ۱۰۰ یا نهایت ۲۰۰
میلی‌ثانیه… برنامهٔ من تا ده سال هم نباید کند شود، مثلِ برنامه‌های حرفه‌ای.»

سنجشِ تازه — `dotnet run --project PumpYaqobi.UiTests -c Release -- bigtable` —
یک دفتر را با شش اندازه می‌سنجد. پیش از اصلاح:

| ردیف | باز شدن | رشدِ کامل | ردیفِ زنده | حافظه |
|---|---|---|---|---|
| ۵۰ | ۶۹ms | ۴۳۵ms | ۵۰ | ۱۵۷MB |
| ۲۰۰ | ۱۱۸ms | ۳٬۸۰۵ms | ۲۰۰ | ۲۹۴MB |
| ۶۰۰ | ۱۶ms | ۷٬۷۱۴ms | ۶۰۰ | ۶۵۰MB |

یعنی نه باز شدن کند بود و نه خودِ اسکرول: **رشدِ تدریجی همهٔ ردیف‌ها را
می‌ساخت** و هر ردیف ~۱۲ میلی‌ثانیه CPU و نزدیک یک مگابایت حافظه می‌برد. با
این قاعده هیچ ترفندی «ده سال داده» را نجات نمی‌دهد. پس از اصلاح، همان جدول
با ۲۰٬۰۰۰ ردیف: باز شدن ۲۲ms، ۵۰ ردیفِ زنده، ۱۷۵MB — و با ۵۰ یا ۲۰٬۰۰۰ ردیف
یکسان.

- **`ExcelGrid` بالای `StickyRowLimit` (۶۰ ردیف) پنجرهٔ چسبان می‌گیرد**:
  `DataGrid` همان **فهرستِ کامل** را دارد (شماره، انتخاب، ویرایش، Enter و Tab
  همه دست‌نخورده) ولی قابش یک صفحه است، پس مجازی‌سازیِ خودش زنده است و فقط
  یک صفحه ردیف می‌سازد؛ در عوض جدول به اندازهٔ **همهٔ** ردیف‌هایش جا می‌گیرد،
  پس صفحه هم‌قدِ جدول بلند است و هیچ «کادرِ محدود»ی دیده نمی‌شود. با لغزشِ
  صفحه، آن قاب داخلِ جای خودش پایین می‌آید (`_stickyY`، با `RenderTransform`
  — بی هیچ پاسِ چیدمان) و به همان اندازه ردیف‌های داخلش می‌لغزند.
- ⚠️ **راهِ خودِ جدول را می‌رویم، نه میان‌بُر**: نوارِ لغزشِ درونی گذاشته
  می‌شود و `ProcessVerticalScroll(ThumbTrack)` صدا می‌خورد — همان دو خطی که
  `VerticalScrollBar_Scroll`ِ خودِ `DataGrid` می‌زند. یک بار با
  `ScrollSlotsByHeight`ِ مستقیم میان‌بُر زدیم و عکس‌ها ریشه را نشان دادند: با
  پرشِ بزرگ ردیف‌ها **وارونه** چیده می‌شدند (۳۰۷، ۳۰۶، ۳۰۵…) و یک ردیفِ کهنه
  ته جدول می‌ماند. قاعده: با حالتِ درونیِ یک کنترل، از درِ خودش وارد شو.
- ⚠️ قاب از **زیرِ نوارِ بخش‌ها** شروع می‌شود (`BlindTop`)؛ بی آن، سربرگِ جدول
  پشتِ نوارِ شناور پنهان می‌ماند — با عکس دیده شد.
- ⚠️ نوارِ لغزشِ خودِ جدول در این حالت دروغ است (گسترهٔ یک صفحه را نشان می‌دهد
  و کاربر را به اسکرولِ دوم دعوت می‌کند)، پس `DataGrid.sticky` پهنایش را صفر
  می‌کند. چرخِ ماوس هم همیشه مالِ صفحه است.
- ⚠️ **ورق و پارچه چسبان نمی‌شوند** (`GrowsToContent`): سقفشان ۸۰ ردیف است و
  رفتارِ امروزشان سنجیده شده (`waraqperf`). جدولِ زیرِ ۶۰ ردیف هم مثلِ قبل
  کاملاً ساخته می‌شود — پنجرهٔ چسبان برای جایی است که هزینه واقعاً بالا می‌رود.
- ⚠️ **سنجهٔ اصلی «ردیفِ زنده» است، نه میلی‌ثانیه.** وقت روی ماشینِ CI نوسان
  دارد؛ «چند ردیف ساخته شد» حقیقتِ ساختاری است. `ledgerperf` هم از امروز
  وارونهٔ قبل را می‌خواهد: بالای `StickyRowLimit` باید **کم** باشد.
- `bigtable` فقط سرعت نمی‌سنجد، **درستی** هم می‌سنجد (`Correctness`): در پنج
  جای اسکرول — هم با پرش هم گام‌به‌گام — شماره‌های ردیف‌های دیده‌شده باید
  یکی‌یکی بالا بروند و با پایین رفتنِ صفحه ردیفِ بالای جدول جلو برود. همان
  سنجه‌ای که باگِ «ردیف‌های وارونه» را می‌گیرد. عکسِ چشمی:
  `-- bigtable shot <پوشه>`.
- **قیمتِ هر ردیف مالِ ستون‌ها نیست**: `rowcost` همان ستون‌های واقعیِ دفتر
  (دو کپسول با مبدلِ رنگ و ToolTip) را جدا سنجید — ۰٫۳۸ تا ۰٫۵۲ میلی‌ثانیه
  برای هر ردیف. آن ~۱۲ میلی‌ثانیهٔ درونِ برنامه مالِ **چیدمانِ دوبارهٔ کلِ
  صفحه** در هر تکهٔ رشد بود، که با چسبان دیگر رخ نمی‌دهد. پس دنبالِ «سبک کردنِ
  خانه‌ها» نگردید؛ اندازه گرفته شده.
- تنها هزینه‌ای که هنوز با اندازهٔ ماه بالا می‌رود خواندنِ SQL است (۲۰٬۰۰۰ ردیف
  = ۱۶۳ms) و خطی است، نه مربعی.

## ⌨️ تکمیلِ خودکار داخلِ خودِ کادر · خطِ ستونِ اکسلی · پرینتی که منتظر نمی‌گذارد (از ۱۴۰۵/۰۶/۲۷)

### ۱) پیشنهادِ خودکار دیگر پاپ‌آپ نیست — تکمله داخلِ خودِ کادر است

گزارشِ صاحب ریپو با عکس: «این تکمیلِ خودکار جوری است که پیشنهاد پاپ‌آپ می‌آید —
نه. با همان «س» که دادم، داخلِ خودِ کادر تکمیلش را نشان بده که با زدنِ Tab یا
Enter آن جمله تکمیل شود و از پلیس‌هولدر بیاید بیرون.»

```
کاربر «س» می‌زند  ⇒  کادر: «س[لام من هارون هستم]»   ([…] انتخاب‌شده)
Tab یا Enter      ⇒  جمله می‌ماند، انتخاب برداشته می‌شود، در همان خانه
تایپِ ادامه       ⇒  حرفِ تازه جای تکمله می‌نشیند
Backspace / Esc   ⇒  تکمله می‌رود و همان چیزی می‌ماند که تایپ شده
```

- فهرستِ شناور (`Popup`) کاملاً برداشته شد؛ `Suggest` حالا فقط تکملهٔ درون‌خطی
  دارد. منبعِ واژه‌ها همان است: فهرستِ نام‌دارِ ستون + مقدارهای همان ستون.
- ⚠️ **تکملهٔ پذیرفته‌نشده هرگز در دفتر ذخیره نمی‌شود.** متنِ داخلِ کادر برای
  دیدن است؛ `Suggest.Settle()` (از `ExcelGrid.CellEditEnding`، یعنی درست پیش
  از نشستنِ مقدار) و `LostFocus` هر دو آن را به همان چیزی برمی‌گردانند که
  تایپ شده بود. بی این، یک «ح» می‌توانست «حوالهٔ ۲» ذخیره شود — این دیگر رفاه
  نیست، خرابیِ داده است. سنجهٔ ۶ در `verify` همین را قفل کرده.
- ⚠️ تکمله **یک تیک بعد** پیشنهاد می‌شود (`DispatcherPriority.Input`): `TextBox`
  اول متن را عوض می‌کند و بعد مکان‌نما را، پس پرسشِ «مکان‌نما تهِ متن است؟» در
  همان لحظه همیشه «نه» است و تکمیل هیچ‌وقت شروع نمی‌شد. (`verify` گرفتش.)
- ⚠️ ترتیبِ `CaretIndex` و بعد `SelectionStart/End`: `CaretIndex` انتخاب را جمع
  می‌کند، پس برعکسش یعنی تکمله‌ای که برجسته نیست.
- فقط از **سرِ** واژه تکمیل می‌شود (`StartsWith`)، و کوتاه‌ترین گزینه اول.

### ۲) دوبار-کلیک روی خطِ ستون = هم‌قدِ محتوا، مثلِ اکسل

خواستهٔ صاحب ریپو: «روی اندازه کردنِ جدول‌ها یک سیستم بگذار مثلِ اکسل که دوبار
روی خطِ همان جدول بزنی، خودکار اندازه‌اش شود.»

- `ExcelGrid.AutoFitAt(header, x)` + شنوندهٔ `DoubleTapped`. قاعده: **همان
  ستونی هم‌قد می‌شود که کشیدنِ همان خط پهنش می‌کرد** — لبهٔ راستِ سربرگ ⇒ خودش،
  لبهٔ چپ ⇒ ستونِ پیشین (به ترتیبِ نمایش). وسطِ سربرگ هیچ کاری نمی‌کند.
- ⚠️ **`DataGrid`ی آوالونیا برای خطِ ستون `Thumb` ندارد.** یک بار دنبالِ
  `PART_*HeaderGripper` گشتم و سنجه گرفتش: در درختِ سربرگ اصلاً `Thumb`ی نیست؛
  خودِ سربرگ نزدیکیِ لبه‌اش را «خطِ تغییرِ اندازه» می‌شمارد. پس تصمیم از روی
  **جای کلیک** گرفته می‌شود (`LineGrab = 8` پیکسل).
- ⚠️ `DoubleTapped` رویدادِ **حبابی** است؛ شنوندهٔ تونلی هرگز شلیک نمی‌شود.
- پس از هم‌قد شدن همهٔ ستون‌ها پیکسلی می‌شوند (`PinOnUserResize`) و پهنای تازه
  با `WidthKey` ذخیره می‌شود. آزمون: بندِ ۶ب در `verify`.

### ۳) پنجرهٔ چاپ دیگر منتظرِ ورق‌ها نمی‌ماند

گزارشِ صاحب ریپو: «بخشِ پی‌دی‌اف هنوز کند است و دیر باز می‌شود.» سنجشِ تازه
`dotnet run --project PumpYaqobi.UiTests -c Release -- printperf`:

| گزارش | ورق | پیش از این | حالا (باز شدنِ پنجره) | ورقِ اول |
|---|---|---|---|---|
| ۴۰ ردیف | ۲ | ۴۷۸ms | ۲۶ms | ۳۵۸ms |
| ۲۰۰ ردیف | ۸ | ۱٬۰۷۴ms | ۰ms | ۵۷۸ms |
| ۶۰۰ ردیف | ۲۳ | ۲٬۸۹۸ms | ۰ms | ۱٬۴۹۱ms |

- ⚠️ **`GenerateImages`ی QuestPDF تنبل نیست**: پیش از دادنِ ورقِ اول، همهٔ
  ورق‌ها را می‌سازد. پس «ورقِ اول را زود بده» چاره نبود؛ چارهٔ درست این بود که
  **پنجره منتظرِ هیچ ورقی نماند** — `StartPages(background: true)` در سازنده،
  و ورق‌ها که آمدند خودشان می‌نشینند (`Busy` تا آن لحظه پرده را نگه می‌دارد).
- ⚠️ **پیش‌نمایش با `PreviewDpi = 96` تصویر می‌شود، نه با `Setup.Dpi`.** هزینهٔ
  تصویر با **مربعِ** dpi بالا می‌رود و A4 در ۹۶ نقطه می‌شود ۷۹۴×۱۱۲۳ پیکسل —
  از قابِ پیش‌نمایش هم بزرگ‌تر. **ولی چاپ همان `Setup.Dpi`ِ کاربر است**:
  مسیرِ «فقط این ورق‌ها» (`PagesDocument`) ورق‌های برگزیده را همان‌جا دوباره و
  با کیفیتِ کامل می‌سازد. `GuideMargin` هم از `_pagesDpi` می‌خواند، نه از
  `Setup.Dpi`، وگرنه خط‌چینِ حاشیه جابه‌جا می‌افتد.
- ⚠️ `_renderGen` نگهبانِ «تنظیم وسطِ کار عوض شد» است: جریانِ کهنه ورق‌هایش را
  دور می‌ریزد. بی آن، ورق‌های تنظیمِ قبلی روی تنظیمِ تازه می‌نشستند.
- سنجش‌ها باید منتظرِ `AllRendered` بمانند (`printshot` همین‌طور شد).

## 🚪 داخل و بیرونِ حساب — «همان صفحه، دادهٔ تازه» (از ۱۴۰۵/۰۶/۲۷)

گزارشِ صاحب ریپو: «داخل رفتنِ حسابِ قرض‌داران، شرکت‌ها و ورق‌ها کندی داشت، و
بازگشت یا بیرون شدن از حساب هم همین‌طور.»

سنجشِ تازه — `dotnet run --project PumpYaqobi.UiTests -c Release -- enterperf` —
هر شش کار را با دادهٔ پنج‌ساله می‌سنجد (رفتن **و** برگشتن، که تا امروز هیچ
سنجه‌ای نداشت). پیش و پس:

| کار | پیش | حالا | دستورِ دیتابیس |
|---|---|---|---|
| حسابِ قرض‌دار — باز کردن | ۷۱۲ms | ۷۷ms | ۳۸ ⇒ ۷ |
| حسابِ قرض‌دار — بستن | ۱۵۱ms | ۵۸ms | ۰ |
| حسابِ شرکت — باز کردن | ۲۹۵ms | ۹۲ms | ۳ |
| حسابِ شرکت — بستن | ۱٬۴۳۷ms | ۱۹۵ms | ۲۶۷ ⇒ ۱ |
| ورقِ روزانه — بستن | ۵۸۷ms | ۲۰۸ms | ۱۱۳ ⇒ ۱۳ |

چهار ریشه، و چهار قاعده که باید بمانند:

- ⛔ **صفحهٔ حساب دور انداخته نمی‌شود.** `DebtSectionViewModel.PersonOpen` و
  `CompanySectionViewModel.PageOpen` جانشینِ «`Person is null`»ی قدیم‌اند —
  همان الگویی که «ورق‌ها» از پارسال دارد و ۱۰ میلی‌ثانیه باز می‌شود. با هر
  باز کردن، `PersonViewModel.Load` / `CompanyPageViewModel.Load` دادهٔ تازه
  را در **همان** ویومدل می‌نشانند، پس `Rows` همان شیء می‌ماند و `DataGrid`
  خانه‌ها را بازیافت می‌کند. نمونه‌بردار نشان داد ساختنِ دوبارهٔ خانه‌ها
  (سبک‌ها و `PropertyStore`) ۱۳٫۸ ثانیه CPUِ چیدمان در یک اجرا بود.
- ⛔ **جدولی که فقط صفحه‌اش بسته شده پارک نمی‌شود** — `ExcelGrid.SectionShown()`.
  «بخشِ پنهان صفر ردیفِ زنده» سرِ جایش است (سنجشِ `idle` قفلش کرده) ولی
  بیرون آمدن از یک حساب برای باز کردنِ حسابِ بعدی پارک نیست.
  ⚠️ این را از روی **ویومدل** بپرسید (`SectionViewModel.IsShown`)، نه از
  `IsEffectivelyVisible`ِ جدِ دیداری: `ContentPresenter`ی که `ItemsControl`
  دورِ هر بخش می‌سازد هیچ‌وقت پنهان نمی‌شود و یک بار همین، همهٔ پارک‌ها را
  بی‌اثر کرد (۱۶۶ ردیفِ زندهٔ بخشِ پنهان، `idle` گرفتش).
- ⛔ **ردیفِ دست‌نخورده ذخیره نمی‌شود** — `RowViewModel.IsDirty`. بستنِ صفحهٔ
  شرکت ۲۶۶ `UPDATE` می‌زد، فقط چون `FlushAsync` همهٔ ردیف‌ها را می‌نوشت. و
  زیانش فقط کندی نبود: هر نوشتن `PumpDbContext.Version` را بالا می‌برد و
  نوار و داشبورد و `StationPublisher` را از نو می‌دواند.
- ⛔ **مهاجرتِ رسیدها یک بار است** — `DebtorService.CommitReceiptMigrationAsync`.
  `MigrateReceiptsToRows` در حافظه نشانِ `ReceiptsMigrated` می‌گذارد و
  `RasidLog` را خالی می‌کند، ولی تا امروز هیچ‌کدام **ذخیره** نمی‌شدند: هر
  بار که همان حساب باز می‌شد، همان رسیدها دوباره ردیف می‌شدند (شمارِ
  ردیف‌ها ۱۰۰ ⇒ ۱۰۶ ⇒ ۱۱۲). این کندی نبود، **خرابیِ داده** بود.
- ⛔ `ExcelGrid.BlindTop()` نوار را کَش می‌کند؛ گشتنِ کلِ درختِ پنجره در مسیرِ
  چیدمان ۹۱۲ میلی‌ثانیه از یک نمونه بود.

## 🧾 شیفتِ پارچه ⇒ ورقِ همان روز (از ۱۴۰۵/۰۶/۲۷)

گزارشِ صاحب ریپو: «تو پارچه‌ها وقتی شیفتِ صبح یا شب را ثبت می‌کنم، در بخشِ
ورق‌ها ورق ساخته نمی‌شود.»

ورق **ساخته می‌شد** — `ShiftWaraqSyncService` سالم بود — ولی
`WaraqSectionViewModel` فهرستش را فقط یک بار می‌خواند (`EnsureLoadedAsync`)،
پس هر کسی که یک بار به ورق‌ها سر زده بود تا بسته شدنِ برنامه همان فهرستِ
کهنه را می‌دید. حالا `OnActivatedAsync` با ترمزِ `PumpDbContext.Version`
دوباره می‌خواند (داده عوض نشده ⇒ صفر پرس‌وجو).

⚠️ سنجه‌اش بخشِ «۳ب» در `dotnet run --project PumpYaqobi.UiTests -- parcha`
است و **عمداً اول یک بار به ورق‌ها سر می‌زند**؛ بی آن خط، باگ گرفته نمی‌شد.

## 🎨 بوم کمی تیره‌تر شد — و سنجهٔ `look` که توخالی بود (از ۱۴۰۵/۰۶/۲۷)

خواستهٔ صاحب ریپو: «بک‌گراند را کمی رنگش تیره کن، کمی که متفاوت باشد با
کادرها.» پس **خواستهٔ قبلیِ «بومِ سفیدِ خالص» پس گرفته شد**: بومِ تمِ آبی
`#d6e0ee` است، بدنهٔ بخش `#e8eff9`، کادرِ تایپ `#e4ecf8` و کارت سفید.
`FlatCanvas` هم برداشته شد، چون حالا پلهٔ واقعی هست.

⚠️ و همان‌جا معلوم شد `LookAudit` مدتی بود **بی‌صدا توخالی** شده بود:
`TryFindResource`ِ بی‌گونهٔ تم، از روزی که هر دو تم زیرِ `ThemeDictionaries`
ادغام شدند، هیچ رنگی پیدا نمی‌کرد — جدولش خالی چاپ می‌شد و همیشه سبز
می‌داد. حالا با `ActualThemeVariant` می‌پرسد. **سنجه‌ای که جدولش خالی است
سبز نیست، خراب است.**

## 📉 مقایسهٔ نرخ فاکتورها — جست‌وجو و انتخابِ یک ردیف (از ۱۴۰۵/۰۶/۲۷)

خواستهٔ صاحب ریپو: «ستونِ حال جا اضافه می‌گیرد… جملهٔ زیرِ جدول را حذف کن…
روی یکی از کادرها که زدم سربرگ اطلاعاتِ همان را بالا نشان بدهد و جای دیگر
که کلیک کردم از همه را… و یک جست‌وجو که شمارهٔ فاکتور یا اسم را پیدا کند.»

- ستونِ «حال» کوتاه شد («🟡 در صف — با نرخ امروز») و سقفِ پهنا گرفت.
- جملهٔ زیرِ جدول برداشته شد.
- `InvRateSectionViewModel.Selected` ⇒ شش کارتِ بالا فقط همان فاکتور را
  می‌شمارند و `ScopeText` می‌گوید دامنه کدام است. کلیکِ بیرونِ جدول انتخاب را
  برمی‌دارد (شنوندهٔ **تونلیِ** `InvRateSectionView`؛ حبابی هرگز شلیک
  نمی‌شود، چون خودِ جدول کلیک را مصرف می‌کند).
- `Search` روی شمارهٔ فاکتور، نامِ مشتری و تاریخ (با رقمِ فارسی).
- کادرِ «نام مشتری» و «به نام دیگر»ِ فرمِ فاکتور حالا تکملهٔ درون‌خطیِ
  `Suggest` با کلیدِ `debtor` دارند — Tab/Enter می‌پذیرد و ↑↓ را نمی‌گیرد.
  ⚠️ همان تکملهٔ درون‌خطیِ برنامه است، نه پاپ‌آپ: قاعدهٔ «فهرستِ شناور
  برداشته شد» سرِ جایش ماند.
- آزمون: بندِ ۱۱ در `verify`.

### فاکتور در حسابِ همان قرض‌دار

گزارشِ صاحب ریپو: «فاکتور به اسمِ قرض‌داری که هست نمی‌رود داخلِ حسابِ همان
قرض‌دار و تو صفش نیست.»

- فاکتورِ **تاییدشده** از قبل درست کار می‌کرد و حالا قفل شده: بندِ ۱۲ در
  `verify` یک فاکتور به نامِ یک قرض‌دارِ موجود می‌سازد، تایید می‌کند و
  می‌سنجد که قرض‌دارِ تکراری ساخته نشود و ردیفِ رسیدش در همان حساب بنشیند.
- فاکتورِ **در صف** هیچ‌جای صفحهٔ شخص دیده نمی‌شد — همان «تو صفش نیست».
  حالا دکمهٔ «🧾 فاکتورها: ۲ · 🟡 ۱ در صف» در نوارِ حساب (بغلِ آرشیو) فهرستِ
  خواندنیِ فاکتورهای همان مشتری را باز می‌کند.
  ⚠️ **فقط دیدنی است** — هیچ عددی از آن‌جا وارد بدهی، الباقی یا رسید
  نمی‌شود؛ تایید همان‌جای همیشگی‌اش (بخشِ فاکتورها) است.
  ⚠️ و همهٔ فاکتورها خوانده نمی‌شوند: `InvoiceService.ForAccountAsync` صافیِ
  خودِ SQLite است (`DebtAccountId` یا نامِ دقیق).

## 🖱️ ته اسکرول باید بایستد (از ۱۴۰۵/۰۶/۲۷)

گزارشِ صاحب ریپو: «وقتی اسکرول — چه بالا و پایین، چه راست و چپ — به آخر
می‌رسد متوقف نمی‌شود؛ یک لگِ کوچک می‌زند و پرپر می‌شود، بعد درست می‌شود.»

سنجهٔ تازه: `dotnet run --project PumpYaqobi.UiTests -c Release -- scrollend`
— ته صفحه و ته لغزشِ افقی می‌ایستد و چند فریم فقط **نگاه** می‌کند: بلندیِ
صفحه، جای اسکرول، ظاهر/ناپدید شدنِ نوارِ افقی، و **شمارهٔ نخستین ردیفِ
دیده‌شده** با چرخِ واقعیِ ماوس (`win.MouseWheel`)، هم ته و هم سرِ صفحه.

- ⛔ **در حالتِ چسبان، چرخ هرگز به خودِ `DataGrid` نمی‌رسد** — حتی وقتی صفحه
  دیگر جا ندارد (`ExcelGrid.OnPointerWheelChanged`). جای ردیف‌های قابِ چسبان
  را فقط `SyncSticky` از روی لغزشِ صفحه می‌نویسد؛ اگر جدول هم خودش بلغزاند،
  یک فریم ردیف‌ها می‌پرند و فریمِ بعد برمی‌گردند — همان پرپر.
- ⚠️ با `Offset`ی دستی هیچ‌چیز دیده نمی‌شود؛ باید **چرخِ واقعی** زد، چون
  مسیرِ چرخ از `ExcelGrid` رد می‌شود نه از `ScrollViewer`.

## 📏 نوشتهٔ خانه از کادرش بیرون نزند (از ۱۴۰۵/۰۶/۲۷)

گزارشِ صاحب ریپو با عکس: «بالای ورق، جایی که شروع و ختمِ پایه‌ها نوشته
می‌شوند، از کادرشان خارج شده… و آن قرض و مصرف که داخلِ کادر رفته کمی
کم‌پیدا است.»

اندازه‌گیری (بندِ «نوشتهٔ خانه» در `parcha`، با `FormattedText`ِ بی‌قید — نه
`DesiredSize` که خودش با همان قیدِ تنگ سنجیده می‌شود و دروغ می‌گوید) دو
ریشه داد و هر دو بسته شد:

- ⛔ **فاصلهٔ افقیِ خانه دوبار شمرده می‌شد.** `DataGridTextColumn`ی آوالونیا
  خودش روی نوشتهٔ هر خانه `Margin="12,0,12,0"` می‌گذارد — مقداری **محلی** که
  هیچ سبکی عوضش نمی‌کند — و پهنای طبیعیِ ستون هم از همان درمی‌آید. با
  `Padding="10,4"`ِ خودِ خانه، جای نوشته می‌شد «ستون − ۴۴» و ۲۰ پیکسل کم
  می‌آورد. حالا `Padding="0,4"` است: خانهٔ ۶۱ پیکسلی به‌جای ۱۶، ۳۴ پیکسل به
  نوشته می‌دهد و «۱۰,۹۳۰» دیگر «…,۹۳۰» نمی‌شود.
- ⛔ **پهنای خودکار دیگر به‌جای خواستهٔ کاربر ذخیره نمی‌شود.** هر جدولی در
  نخستین چیدمانش پهنایش را در `settings.json` می‌نوشت — و چون جدولِ ورق با
  ردیف‌های **خالی** باز می‌شود، همان پهنای «سرستونِ خالی» برای همیشه می‌ماند.
  حالا `RememberWidths` فقط وقتی می‌نویسد که پهنا با پهنای خودکارِ سنجاق‌شده
  فرق کرده باشد (یعنی کاربر ستون را کشیده یا دوبار-کلیک کرده).
- **دادهٔ تازه ⇒ پهنای تازه**: با پر شدنِ دوبارهٔ فهرست (ورقِ دیگر، ماهِ دیگر،
  حسابِ دیگر) ستون‌ها به `Auto` برمی‌گردند و از روی محتوای واقعی حساب
  می‌شوند — مگر کاربر خودش پهنا ذخیره کرده باشد.
- کپسولِ «قرض ⇄ مصرف» و «پطرول ⇄ دیزل» سطحِ خودش را گرفت (`Pump.Input`) و
  خطش کمی پررنگ‌تر شد؛ رنگ‌ها همان‌اند، فقط دیده می‌شوند.

⚠️ **یک ریشه مانده و عمداً فقط گزارش می‌شود**: ستونِ `Auto`ی آوالونیا پهنایش
را از خانه‌های ساخته‌شده می‌گیرد و خانهٔ **بازیافتی** با نوشتهٔ تازه دوباره
اندازه گرفته نمی‌شود؛ پس نامی که **پس از** ساخته شدنِ جدول در خانه می‌نشیند
(مثلِ نامِ کارمند که از پارچه می‌آید) ستون را پهن نمی‌کند. راهِ کاربر همان
اکسل است: دوبار-کلیک روی خطِ ستون. سنجه عددش را چاپ می‌کند (⚠️، نه ایراد) تا
فراموش نشود — **و ضعیفش نکنید؛ درستش کنید.**

## 🧩 حساب‌های فرعی هم مقصدِ کامل‌اند (از ۱۴۰۵/۰۶/۲۷)

خواستهٔ صاحب ریپو: «اگر بخواهم برای حسابِ فرعی فاکتور بنویسم این کار هم بشود…
و از ورق وقتی اسمِ حسابِ فرعی را توی جای اسم نوشتم هم همین‌طور… و می‌خواهم
ردیف‌های ورق خیلی خوب و با جزئیات به حساب‌ها بروند.»

- **فاکتور ⇒ حسابِ فرعی** (`InvoiceService.EnsureAccountAsync`). تا امروز فقط
  نامِ **شخص** سنجیده می‌شد، پس نامِ یک حسابِ فرعی به هیچ حسابی نمی‌خورد و
  برنامه یک قرض‌دارِ **تازه** با همان نام می‌ساخت (سنجه بی این اصلاح
  `14 ← 15` می‌داد). ترتیب از خاص به عام:
  ۱) `DebtAccountId`ِ صریح · ۲) نامِ شخص خورد ⇒ اگر همان متن نامِ یکی از
  فرعی‌هایش را هم داشت (`PostingService.SubForText`) همان فرعی، وگرنه اصلی ·
  ۳) نامِ دقیقِ یک حسابِ فرعی · ۴) وگرنه قرض‌دارِ تازه، مثلِ همیشه.
- **ورق ⇒ حسابِ فرعی** از قبل کار می‌کرد (`PostingService.FindAccountForText`
  حساب‌های فرعی را هم امتیاز می‌دهد و `PlaceRow` همان را می‌نشاند) ولی هیچ
  سنجه‌ای نداشت. حالا دارد.
- **جزئیاتِ ورق ⇒ حساب**، هر چهار تا با نوشتنِ واقعی روی صفحهٔ ورق و بعد
  خواندنِ دیتابیس (بندِ «۴ب» در `parcha`):
  - «حواله ۷۴۲ کریم جان» ⇒ `Hawala = "742"`، و واژهٔ «حواله» از نامِ ردیف
    پاک می‌شود (`PostingService.ExtractHawala`).
  - واحد = **تیل** ⇒ دفترِ تیل · واحد = **پول** ⇒ دفترِ پول، و عوض کردنش
    ردیف را **جابه‌جا** می‌کند، نه این‌که در دو دفتر دوتا شود.
  - نوع = **مصرف** ⇒ بخشِ مصارف، و از حسابِ قرض‌دار برداشته می‌شود.
  - نامِ حسابِ فرعی در جای نام ⇒ ردیف در همان فرعی، و حسابِ اصلی دست‌نخورده.
- آزمون: بندِ ۱۳ در `verify` (فاکتور) و بندِ «۴ب» در `parcha` (ورق).

## ⌨️ میانبری که نویسه را می‌خورد — «تو کادرها این‌ها نوشته نمی‌شوند» (از ۱۴۰۵/۰۶/۲۹)

گزارشِ صاحب ریپو: «لاگین مشکل دارد، این کادرِ ایمیل ایمیل نیست و هیچی توش
نوشته نمی‌شود؛ `+ @ # ﷼ ( ) ؟ ؛ : , .` توی هیچ‌کدامشان نوشته نمی‌شوند.»

ریشه **یک خط** بود و ربطی به صفحهٔ ورود نداشت — کلِ برنامه را می‌گرفت:
میانبرِ «`Shift+عدد` ⇒ حذفِ N ردیف» کلید را `Handled` می‌کرد، آن هم در
**تونل**، یعنی پیش از خودِ کادرِ تایپ. و ردیفِ عددها با Shift دقیقاً همان
نویسه‌هاست: انگلیسی `! @ # $ % ^ & * ( )` و فارسی/دری `، ؛ ؟ ﷼ ٪ × ) (`.

- ⚠️ **کلیدی که `Handled` شود، روی ویندوز `WM_CHAR` نمی‌سازد** — پس آن نویسه
  اصلاً تایپ نمی‌شد (و بدتر: همان لحظه چند ردیف هم پاک می‌شد). آوالونیای
  هدلس این را تقلید نمی‌کند و نویسه را جدا می‌فرستد، پس سنجه‌ای که فقط به
  متنِ داخلِ کادر نگاه کند **سبزِ دروغ** می‌دهد؛ سنجهٔ درست خودِ `Handled`
  است.
- ⛔ قاعده از این پس: **هیچ میانبری نویسه را نخورد.** هر کلیدی که نویسه
  می‌سازد، داخلِ کادرِ تایپ دستِ خودِ کادر است — `Shortcuts.TypingInBox`.
  `Ctrl` و `Alt` این مشکل را ندارند (نویسه نمی‌سازند) و سرِ جایشان ماندند؛
  `AltGr` هم از قبل رد می‌شد چون `Ctrl+Alt` را با هم دارد.
- ⚠️ و آن طرفِ سکه هم سنجیده می‌شود: **بیرونِ** کادرِ تایپ، `Shift+عدد`
  همچنان ردیف پاک می‌کند (بندِ آخرِ `inputchars` و بندِ میانبرها در `shots`).
- سنجهٔ تازه: `dotnet run --project PumpYaqobi.UiTests -- inputchars` — با
  کلیدِ واقعی در کادرِ ایمیل، نامِ پمپ و جست‌وجوی قرض‌داران تایپ می‌کند و
  می‌گوید کدام نویسه خورده می‌شود. آزمونِ سورس:
  `AppLinksTests.ShiftAdad_DakheleKadreTypAn_MianborNist`.
- ⚠️ **قلم بی‌گناه بود**: `Assets/Vazirmatn.ttf` برای هر یک از آن نویسه‌ها
  گلیف دارد (سنجیده شد، نه حدس). دنبالِ قلم نگردید.

## 🟢 چراغِ سرور — «سرور روشن است اما برنامه می‌گوید خاموش» (از ۱۴۰۵/۰۶/۲۹)

گزارشِ صاحب ریپو: «ببین سرور روشن است اما پمپ بنزین می‌گوید خاموش است؛
آن‌جوری هست که هر ثانیه چک کند و بگردد که وصل است یا نه؟»

چراغ **درست** می‌گفت که وصل نیست — ولی برنامه اصلاً تلاش نمی‌کرد:

- ⛔ **قفلِ اشتراک روی «عکسِ زنده» است، نه روی خودِ اتصال.** نخستین خطِ
  `PublishOnceAsync` با بی‌اشتراک بودن `return false` می‌کرد، و چون **تنها
  جای وصل شدن به سرورِ خانگی همان‌جا بود**، برنامهٔ بی‌اشتراک هیچ‌وقت وصل
  نمی‌شد و چراغ می‌گفت «سرور جواب نمی‌دهد». حالا اتصال جداست
  (`StationPublisher.KeepLinkAsync`) و همیشه برقرار می‌ماند — که رایگان است و
  هیچ دستورِ دیتابیسی نمی‌زند — و آن‌چه قفل می‌شود همان انتشارِ عکس است.
- **اتصال هر پنج ثانیه سنجیده می‌شود** (`LinkTick`)، انتشار همان هر بیست
  ثانیه (`Interval`) — چون ساختنِ عکس با پنج سال داده یک ثانیه CPU است و
  «هر ثانیه» یعنی کامپیوترِ داغ. خودِ چراغ از قبل هر ثانیه خوانده می‌شد.
- **قطعیِ ناگهانی زود دوباره می‌گردد**: `EnrollRetry` پنج دقیقه است برای
  «هیچ‌وقت وصل نشده»، ولی اگر یک بار وصل بوده‌ایم و قطع شد
  `EnrollRetryLost` (۳۰ ثانیه) — چون آی‌پیِ خانگی با هر روشن شدنِ مودم عوض
  می‌شود و کاربر همان لحظه چراغِ سرخ را می‌بیند.
- **چراغ حالا کلیک‌شدنی است** (`CheckServerCommand`): همان لحظه می‌گردد
  (`force` یعنی ترمزِ پنج‌دقیقه‌ای را هم رد کن) و با توست جواب می‌دهد. و
  `ToolTip` می‌گوید «آخرین وصل: HH:mm» و این‌که خودش هر پنج ثانیه می‌گردد.
  ⛔ همچنان **هیچ نام یا نشانیِ سروری** در چراغ نوشته نمی‌شود (قاعدهٔ
  ۱۴۰۵/۰۶/۲۶ سرِ جایش است، و آزمون قدغنش کرده).
- سنجهٔ تازه: `dotnet run --project PumpYaqobi.UiTests -c Release -- serverdot`
  — یک **سرورِ خانگیِ واقعی** (وب‌سوکت روی `127.0.0.1`) بالا می‌آورد و چهار
  چیز را می‌سنجد: روشن ⇒ سبز (حتی **بی اشتراک**)، خاموش ⇒ سرخ با «آخرین
  وصل»، دوباره روشن ⇒ خودش سبز می‌شود، و کلیکِ چراغ ⇒ همان لحظه وصل.
  آزمونِ سورس: `AppLinksTests.CheraghSarvar_HaghighatRaMigooyad`.
- ⚠️ **نکتهٔ سنجه‌نویسی**: `AppHost.Start(dbPath)` خودش
  `AppSettings.DirOverride` را به پوشهٔ همان دیتابیس می‌برد، پس نوشتنِ
  تنظیمات **پیش از** آن به فایلِ دیگری می‌رود و بی‌اثر است (خودِ همین سنجه
  گرفتش).
- سنجیده شد که هزینه اضافه نکرد: `idle` هر سه ستون صفر (تیکِ اتصال هیچ
  دستورِ دیتابیسی نمی‌زند) و `startup` بی‌کاری ۲٪ یک هسته با صفر چیدمان.

## ⚙️ تنظیمات: سه صفحه و بس — رمزها · بک‌اپ · سطل زباله (از ۱۴۰۵/۰۶/۲۸)

خواستهٔ صریحِ صاحب ریپو: «بخشِ تنظیمات این مدل باشه، سه بخش داشته باشه؛ هر
کدوم که بزنی صفحهٔ جداگانه باز بشه… و تنظیماتِ الان همه‌شونو حذف کن و این
تنظیمات توش باشه و اون تنظیماتِ قدیم هیچ از اون‌ها نباشه داخلش.»

```
تنظیمات ─┬─ 🔑 رمزها و کد   (keys)     رمزِ برنامه · رمزِ «مفاد/ضرر» و «زیانِ افزایش قیمت» · کدِ اپِ کارمندان
         ├─ 💾 بک‌اپ و به‌روزرسانی‌ها (backups) تازه‌ترین بکاپ + کادرِ کشویی · بازگردانی · به‌روزرسانی
         └─ 🗑️ سطل زباله  (trash)     تازه‌ترین پاک‌شده + کادرِ کشویی · بازگرداندن · ۱۵ روز
```

- ⛔ **صفحهٔ «تنظیمات» هیچ کادرِ تنظیمی ندارد و نباید بگیرد** — فقط سه کارت.
  `SettingsHasNoOldSettingCardsAnyMore` هر کدام از تنظیماتِ قدیمی را جدا قدغن
  کرده. آنچه برداشته شد و کجا رفت:
  - نرخِ اتحادیه ⇒ از قبل در خودِ بخشِ «مفاد / ضرر / اتحادیه» بود (کارتِ
    «🏛️ نرخ اتحادیه»)، پس تنظیمات فقط رونوشتِ دومش بود.
  - آستانهٔ هشدارِ مخزن ⇒ بخشِ «مخزن»، کنارِ «ظرفیت مخزن».
  - تم ⇒ کادرِ تمِ سربرگ · کدِ پمپ و کیو‌آرِ کارمندان ⇒ «پروفایل».
  - آوردنِ دادهٔ نسخهٔ وب ⇒ صفحهٔ «بک‌اپ و به‌روزرسانی‌ها» (کارش همان است:
    فایلِ بیرونی جای حساب‌ها را می‌گیرد، با عکسِ ایمنیِ پیش از نوشتن).
  - ظاهرِ جدول‌ها (رنگ/ضخامتِ خط) **فقط کادرهایش** رفت؛ مقدارِ ذخیره‌شده
    همچنان روی همهٔ جدول‌ها می‌نشیند (`TableStyle.Apply`).
- **کشویی**: تازه‌ترین قلم یک کارتِ بالاست و بقیه پشتِ یک دکمه (`ShowOlder`).
  ⚠️ جدولِ داخلِ کادرِ کشویی عمداً `ExcelGrid` است: کادرِ بسته یعنی جدولِ
  نامرئی و جدولِ نامرئی ردیف‌هایش را پارک می‌کند. با `ItemsControl` ساده،
  چهارده بکاپ و پانصد قلمِ سطل تا ابد زنده می‌ماندند — همان چیزی که `idle`
  قدغن کرده (سنجیده شد: هر سه ستونِ `idle` صفر ماند).
- **رمزِ بخش** (`Services/Security/SectionLockService.cs`): فقط دو شناسه قفل
  می‌پذیرند — `profit` و `priceloss` (`SectionLockService.Ids`).
  - ⚠️ رمز فقط به شکلِ `pbkdf2$sha256$…` در `Settings` می‌نشیند
    (`lock.<id>`)، هیچ‌وقت خام. همان `PasswordHasher`ِ رمزِ خودِ برنامه.
  - ⚠️ «باز بودن» در حافظه است، نه روی دیسک: یک‌بار رمز زدن برای همان اجرا
    بس است و اجرای بعدی دوباره می‌پرسد.
  - قفل **پیش از** نشان دادن صفحه پرسیده می‌شود: `MainViewModel.UnlockAsync`
    در `GoAsync`، و برای زیربخش `UnlockThenShowAsync` (اول `OpenSub = null`،
    بعد با رمزِ درست دوباره باز). رمزی که پس از دیده شدنِ داده پرسیده شود
    هیچ چیزی را نگه نداشته است.
  - ⚠️ `OpenStartSectionAsync` بخشِ قفل‌دار را باز نمی‌کند و به صفحهٔ اول
    برمی‌گردد: سرِ بالا آمدنِ برنامه رمز پرسیدن یعنی کاربری که انصراف بدهد
    صفحهٔ خالی ببیند.
- **رمزِ برنامه** از همین صفحه عوض می‌شود (`AuthService.ChangePassword`) و
  چون اپِ کارمندان همان هش را می‌گیرد، رمزِ گوشی‌ها هم همان لحظه عوض می‌شود —
  و همین‌جا نوشته شده تا کاربر غافلگیر نشود.
- سطلِ زباله همان `TrashService` است و دست نخورده: پانزده روز
  (`RetentionDays`)، پاک شدنِ خودکار (`PruneAsync` با هر باز شدنِ صفحه) و
  بازگرداندن (`RestoreAsync`، که «حذفِ نرم» را برمی‌دارد، رکورد را دوباره
  نمی‌سازد). فقط شکلِ صفحه تازه است.
- آزمون: `SettingsPagesTests` (سه در و ترتیبشان، عوض کردنِ رمزِ برنامه،
  رمزِ غلط، چرخهٔ قفل، خام‌نبودنِ رمز، و باز نشدنِ بخشِ قفل‌دار با
  `Dialogs.PromptHook`) و `SubSectionTests` که جای هر سه را زیرِ «تنظیمات»
  قفل کرده.

⚠️ **عکسِ زیربخش‌های آخر در `shots` یک قدم عقب می‌افتد** — `CaptureRenderedFrame`
در اجرای بی‌پنجره صفحهٔ **بخشِ میزبان** را می‌دهد، در حالی که خودِ صفحه در درخت
هست و دیده می‌شود. با گشتنِ درخت ثابت شد (`IsShown`، `IsEffectivelyVisible` و
اندازهٔ ۱۴۴۰×۹۹۰ برای صفحهٔ باز) و روی `main` هم همین بود (`ratehist`). پس اگر
عکسِ یک زیربخش صفحهٔ پدرش را نشان داد، **اول درخت را بسنجید، بعد باور کنید**.

## 🪪 ثبت‌نام با ایمیل — سه پله، و صفحهٔ ورودی که کلِ پنجره را می‌گیرد (از ۱۴۰۵/۰۶/۲۹)

گزارشِ صریحِ صاحب ریپو: «می‌خواهم با ایمیلِ خودم حسابی بسازم، نمی‌شود و
می‌گوید بعداً فعلاً بی حساب ادامه دهید — این را درست کن، و با تست نه حدس.» و
بعدش: «این لاگین تمام صفحه باشد و کلِ صفحه را بگیرد و یک برگشت هم داشته باشد…
تمام صفحه منظورم فقط همین را نشان بده در صفحه، نه بخش‌ها باشند نه غیره.»

سرور را محلی با دیتابیسِ واقعی بالا آوردیم و هر مسیر را زدیم (حدس نزدیم):

```
POST /api/auth/register          ⇒ 403 verification_required   ← عمداً بسته است
POST /api/auth/register/start    ⇒ کدِ شش‌رقمی به ایمیل
POST /api/auth/register/verify   ⇒ { ticket, terms }
POST /api/auth/register/complete ⇒ 201 { user, accessToken, refreshToken }
POST /api/auth/login             ⇒ { accessToken, … }
```

- ⛔ **`/api/auth/register` یک‌مرحله‌ای را نزنید** — سرور ایمیلِ تاییدنشده را
  حساب نمی‌کند و همان ۴۰۳ بود که کاربر می‌دید. راه، همان سه پله است.
- ⛔ **نامِ فیلدِ توکن `accessToken` است، نه `token`** (`CloudLink.SeatAsync`
  اول آن را می‌خواند و فقط اگر نبود به `token` برمی‌گردد). با خواندنِ `token`
  ورودِ موفق هم بی‌توکن می‌ماند.
- ⚠️ رمز دستِ‌کم **هشت نویسه** و نه همه‌رقم (قاعدهٔ خودِ سرور)، و **تیکِ شرایط
  و ضوابط اجباری** است (`terms_required`)؛ متنش از `GET /api/auth/terms`.
- ⚠️ `ticket` و نسخهٔ شرایط فقط در حافظه‌اند؛ رمز همچنان هیچ‌جا روی دیسک نه.
- **گام‌ها چهار تا شدند**: ۱ حساب · ۲ کدِ ایمیل (فقط در ثبت‌نام) · ۳ پمپ ·
  ۴ تمام. پس `ShowLoginPage => LoginStep < 4`.
  ⚠️ دو «کدِ شش‌رقمی» هست و یکی نیستند: کدِ **ایمیل** (گامِ ۲) و کدِ
  **اشتراک** (گامِ ۳، `device/activate`).
- ⛔ **صفحهٔ ورود بیرونِ `SectionPage` است** و پوستهٔ پنجره هم پنهان می‌شود:
  `IsPageOpen = ShowLoginPage` ⇒ `MainViewModel.IsChromeVisible`. قاعدهٔ
  تازه‌ای ساخته نشد؛ همان راهِ صفحهٔ حسابِ قرض‌دار.
  ⚠️ در **دو جا** گذاشته می‌شود (با عوض شدنِ گام، و صریح در `ShowLogin`) — گامِ
  یک مقدارِ پیش‌فرض است و عوض نشدنش هیچ خبری نمی‌دهد.
- ⚠️ **بلندی از `TopLevel.ClientSize` می‌آید**، نه از XAML: صفحه‌ها داخلِ
  `ScrollViewer`/`StackPanel` بلندیِ خودکار دارند. `AccountSectionView.axaml.cs`
  `MinHeight` می‌گذارد — بی هیچ شنوندهٔ چیدمانی.
- **«‹ برگشت به برنامه»** (`CloseLoginCommand` + `AppHost.GoHome`): کسی که
  نمی‌خواهد حساب بسازد نباید گیر کند.

### 🧑‍🤝‍🧑 عکسِ آدمک‌ها — نقشهٔ حرفه‌ایِ برداری، با رنگِ تمِ برنامه

سه خواستهٔ پشتِ سرِ هم در ۱۴۰۵/۰۶/۲۹، و جوابِ نهایی همان که ماند:

> «فوری کن و برنامه رو ببین سنگین نکنه با یک عکس.»
> «اون آدمک‌ها رو باسازی کن و بک‌گراندشو درست کن و با رنگ و تمِ خودِ برنامه
> باشه و همه‌چی با کیفیتِ خیلی بالا درست کن.»
> «نه، این چیه؛ یک عکس پیدا کن مثلِ همون که بود ولی با کیفیت و جزئیات.»

⛔ **طرحِ دست‌سازِ خودمان رد شد** (یک بار در XAML کشیدیمش؛ صاحب ریپو نپسندید).
پس یک نقشهٔ **آمادهٔ حرفه‌ای** گذاشته شد:

```
Assets/login-art.svg     نقشهٔ «کارهای انجام‌شده»ی unDraw (پروانهٔ MIT)
Assets/ART-LICENCE.md    از کجا آمده و با چه پروانه‌ای — کنارِ خودش
Controls/LoginArt.axaml.cs  رنگ‌هایش را با تم عوض می‌کند و نشان می‌دهد
```

- **همان چیدمانِ عکسِ مرجع**: دو آدم کنارِ یک فهرستِ تیک‌دارِ بزرگ. ۵۰ مسیرِ
  برداری با خم و سایه — «جزئیات» از خودِ نقشه می‌آید، نه از ما.
- ⚠️ **SVG است، نه عکسِ پیکسلی** (`Avalonia.Svg.Skia`): در هر اندازه و روی
  هر صفحه‌ای (۴K هم) تیز است. فایل ۱۶ کیلوبایت.
- ⚠️ **رنگ‌ها با تم جا عوض می‌کنند و خودِ فایل دست‌نخورده می‌ماند**: پالتِ
  unDraw (`#ccc` · `#e6e6e6` · `#3f3d56` · `#2f2e41` · `#fff`) پیش از ساختنِ
  تصویر روی **متنِ حافظه** با رنگ‌های تم جا عوض می‌شود (`LoginArt.Palette`) —
  آبی در تمِ روشن، طلایی در تمِ تیره. رنگِ پوست دست نمی‌خورد، و در تمِ تیره
  لباس‌ها روشن می‌شوند وگرنه آدم‌ها روی بومِ مشکی گم می‌شدند.
- ⚠️ **نتیجهٔ هر تم یک بار ساخته و کَش می‌شود** (`Dictionary<bool, SvgImage>`)
  و متنِ فایل هم یک بار خوانده می‌شود، پس رفتن و برگشتن به صفحه هیچ کاری
  نمی‌کند.
- ⛔ **پس‌زمینهٔ عکس دیگر آن کادرِ سفیدِ داخلِ کادرِ بنفش نیست**: بومِ نقشه
  شفاف است و پشتش `Pump.Section`ِ خودِ تم با دو هالهٔ نرم.
- ⛔ **هیچ چیزی از عکسِ قدیمی در ویومدل نمانده**: نه `LoginArt`، نه
  `DecodeToWidth`، نه `CroppedBitmap`، نه `AssetLoader` — و
  `Assets/login-art.jpg` پاک شد.
- **سنجه**: `dotnet run --project PumpYaqobi.UiTests -c Release -- loginart` —
  پنج بند (نبودِ عکسِ قدیمی · نشاندنِ نقشه در درخت · فایل و پروانه و
  پرجزئیاتی‌اش · عوض شدنِ نقشه با تم و برگشتِ کَش · برداری بودن) و دو عکسِ
  چشمی (`%TEMP%/pump-loginart.png` و `pump-loginart-dark.png`).
  آزمونِ سورس: `AppLinksTests.Adamakha_Bordari_Ast_Va_HichAksi_DarKarNist`.
- ⚠️ **نکتهٔ وابستگی**: `Avalonia.Svg.Skia` تنها بستهٔ تازه‌ای است که برای این
  کار اضافه شد. اگر روزی برداشته شد، نقشه را باید به PNGِ پرتراکم (دستِ‌کم
  ۳ برابرِ نمایش) تبدیل کنید — وگرنه «کیفیتِ خیلی بالا» از دست می‌رود.

## 🔒 قفلِ اشتراک — سه کارِ ابری، و پشتیبانی که هرگز قفل نمی‌شود (از ۱۴۰۵/۰۶/۲۸)

خواستهٔ صریحِ صاحب ریپو: «این‌ها را کاری کن که دیگر کار نکنند و قابلِ دسترس
نباشند مگر اشتراک داشته باشند: کیو‌آر، اپِ کارمندان و ربات، بک‌اپِ ابری.
پشتیبانی باشد چون یکی از واجبات است. و نمی‌خواهم کسی که اشتراک خریده با یک
باگ خراب شود.»

جدولِ پلن‌ها با **قیمت‌های خالی** (خودِ صاحب ریپو بعداً پرشان می‌کند):
`native/docs/PLANS-fa.md`. ⛔ قیمت را از خودت پر نکن.

```
Services/Entitlements.cs  ⇒  Allows(kar | qrlive | cloudbackup)  ⇒  سه جای قفل
                             Allows(support) و هر چیزِ دیگر ⇒ همیشه true
```

- **کجا قفل شد** (و جای دیگری نباید اضافه شود بی نوشتنِ همان‌جا):
  - `kar` ⇒ `StationPublisher.PublishOnceAsync` (عکسِ زنده ساخته و فرستاده
    نمی‌شود) و کیو‌آرِ کدِ پمپ در `AccountSectionViewModel`.
  - `qrlive` ⇒ هر سه جای ساختِ کیو‌آرِ حساب (`PersonViewModel.ShowQr`،
    `DebtSectionViewModel.ShowCardQrAsync`، `CompanySectionViewModel.ShowQrAsync`)
    و `PublishAccountsAsync`.
  - `cloudbackup` ⇒ `BackupPusher.RunOnceAsync`.
- ⛔ **این‌ها هرگز قفل نمی‌شوند**: خودِ دفتر (هیچ بخشی)، چاپ، سطلِ زباله،
  بک‌اپ و بازگردانیِ **محلی**، و **چتِ پشتیبانی**. `Entitlements.Allows` برای
  هر شناسهٔ ناشناسی **باز** برمی‌گرداند — قفلِ ناخواسته بدتر از بازِ ناخواسته
  است.
- ⚠️ **قفل بی‌صدا نباشد**: `Entitlements.Gate(host, feature)` تنها راهِ قفلِ یک
  دکمه است و خودش توست می‌دهد. دکمه‌ای که زده شود و هیچ اتفاقی نیفتد، در چشمِ
  کاربر باگ است نه قفل.
- ⚠️ **«با یک باگ خراب نشود» — تصمیم سه‌لایه است و هر لایه می‌تواند «باز»
  بگوید**: ۱) `CloudLink.Subscription.Active`ی همین حالا · ۲) مجوزِ امضاشدهٔ
  سالم · ۳) ارفاقِ ۱۴ روزه از آخرین «باز»ی که دیده‌ایم
  (`AppSettings.EntitledUntil` که `Entitlements.Remember` فقط **جلو** می‌بردش).
  پس نبودِ اینترنت، سرورِ خاموش، مجوزی که صادر نشده و خرابیِ خودمان دو هفته
  هیچ چیزی را نمی‌بندد. `Grace` را کم نکنید.
- ⚠️ **«فهرستِ `feat` نیامده» با «فهرستِ خالی» یکی نیست** —
  `LicenseCheck.HasFeatureList`. نیامده ⇒ مجوزِ نسلِ اول ⇒ پلنِ کامل (وگرنه با
  آمدنِ همین قفل، همهٔ مشتری‌های امروز یک‌شبه خاموش می‌شدند)؛ خالی (`[]`) ⇒
  پلنِ پایه ⇒ هیچ‌کدام. پس سرور برای «پایه» باید `feat: []` را **صریح**
  بفرستد.
- پروفایل کارتِ «دسترسی‌ها» دارد: هر سه کارِ ابری با ✅/🔒، پشتیبانی همیشه
  «✅ همیشه باز»، و نوارِ «⏳ ۹ روز ارفاق» وقتی اشتراک تمام شده ولی ارفاق
  هست. کاربری که نداند چه خریده، فردا شکایت می‌کند.
- آزمون: `EntitlementsTests` (پشتیبانی هرگز، بی‌اشتراک هر سه بسته، پایه با
  فهرستِ خالی، VIP با فهرستِ نام‌دار، مجوزِ نسلِ اول = کامل، ارفاق، پس از
  ارفاق، «سرور فعال گفته ولی مجوز نرسیده»، و جلو-رفتنِ مُهر).
  ⚠️ در آزمونِ انقضا، تاریخ را دستِ‌کم چند دقیقه عقب بگذارید: `LicenseGuard`
  یک دقیقه ارفاقِ ساعت دارد و «۱ میلی‌ثانیه پیش» هنوز معتبر است (همین آزمون
  گرفتش).

**مانده و کارِ سرور است** (`vil3ntec-it/shop`): پلن‌ها با همان `feat`ها —
`پایه: []`، `VIP/مالکیت: ["kar","qrlive","cloudbackup"]` — و `sub_ends`ِ دور
برای مالکیت. تا آن نشود، هر اشتراکِ فعالی پلنِ کامل دیده می‌شود.

## 📲 لینکِ اپِ گوشی · کارتِ ورودِ پروفایل · بخشِ وی‌آی‌پی (از ۱۴۰۵/۰۶/۲۸)

خواستهٔ صریحِ صاحب ریپو: «لینکِ دانلودِ اپِ اندروید و لینکِ برنامهٔ آیفون را
توی یک بخشِ جدید توی تنظیمات بزار که کپی کنم یا به کارفرما تو واتساپ بفرستم…
و کدِ برنامه که حساب‌های همان پمپ را نشان می‌دهد توی همان بخشِ جدید هم دیده
شود و توی پروفایل هم دیده شود… و بخشِ وی‌آی‌پی را هم اعمال کن که من ببینم و
تست کنم… و پروفایل یک بخش بزار، بخشِ لاگین با ایمیل با اضافه کردنِ کدِ
شش‌رقمی و زدنِ اسم و اسمِ پمپش… و ببین آن عکس باعثِ کند شدنِ اپ یک درصدِ
ثانیه هم نشود.»

### ۱) درِ چهارمِ تنظیمات — `apps`

```
تنظیمات ─┬─ 🔑 رمزها و کد (keys) ─┬─ 💾 بک‌اپ (backups) ─┬─ 🗑️ سطل زباله (trash)
         └─ 📲 اپِ گوشی — لینک و کد (apps)   دو لینک · کدِ پمپ · پیامِ آمادهٔ واتساپ
```

- `KarLink.ApkUrl(viewer)` = `…/downloads/PumpYaqobiKar.apk` و
  `KarLink.IphoneUrl(viewer)` = همان `…/kar/`. هر دو **روی دامنهٔ خودِ پمپ**،
  نه روی نشانیِ مخزن (همان قاعدهٔ «نامِ مخزن هیچ‌جا نیاید»).
  ⚠️ برای این‌که آن لینک ۴۰۴ نشود، `deploy-pages.yml` حالا فایلِ نصبِ
  `kar-latest` را هم کنارِ سایت می‌گذارد (`grab('kar-latest', '.apk', …)`).
- ⛔ **فایلِ نصبِ سایتِ قدیم (`android-latest` ⇒ `PumpYaqobi.apk`) هیچ‌وقت
  داده نمی‌شود** — قاعدهٔ ۱۴۰۵/۰۶/۲۴ سرِ جایش است.
- `KarLink.ShareText(code, pumpName, viewer)` همان پیامی است که در واتساپ
  چسبانده می‌شود: دو لینک + کدِ پمپ. ⛔ هیچ رمزی در هیچ‌کدام نیست، نه رمزِ
  برنامه نه رمزِ خواندنِ سرور.
- کد این‌جا فقط **دیده** می‌شود؛ گرفتن و عوض کردنش همان‌جای همیشگی‌اش
  («پروفایل») است، چون کد از ابر می‌آید و کادرِ تایپ ندارد (`ProfilePillTests`).

### ۲) ثبت‌نام و ورود — **دو گام** (اصلاحِ دومِ همان روز)

گزارشِ صریحِ صاحب ریپو: «نه می‌گوید حساب داری یا نه، نه ثبت یا ساختنِ حساب
دارد و نه رمز دارد و می‌خواهد — خیلی اشتباه درست شده. اول اسم، ایمیل، رمز،
تکرارِ رمز؛ بعد برود بخشِ بعدی: کدِ شش‌رقمی و تاییدِ آن از سرور و اسمِ پمپ و
لوکیشنِ پمپ. همین و بعد هم تمام.»

```
گامِ ۱  حساب   ‹حساب می‌سازم | حساب دارم›   نام · ایمیل · رمز · تکرارِ رمز
               └── یا «ورود با گوگل»   └── یا «بعداً» (بی‌اینترنت)
گامِ ۲  پمپ    کدِ شش‌رقمی → تاییدِ سرور · نامِ پمپ · لوکیشنِ پمپ
گامِ ۳  تمام   خلاصه + «تغییرِ حساب / تغییرِ پمپ»
```

شرحِ کامل و **کارِ ماندهٔ سرور**: `native/docs/ACCOUNT-fa.md`. سرِ خط:

- ⚠️ **رمز هیچ‌جا روی این کامپیوتر نمی‌نشیند** — نه خام، نه هش؛ فقط به ابر
  می‌رود (`CloudLink.RegisterAsync` / `SignInWithPasswordAsync`) و بعد از هر
  گام از حافظهٔ صفحه هم پاک می‌شود (`RamzeAbr_RoyeDisk_ZakhireNemishavad`).
- ⚠️ **دو مسیرِ ابر هنوز روی `shop` نیست**: `POST /api/auth/register` و
  `POST /api/auth/login` با همان پاسخِ راهِ گوگل. تا آن روز برنامه **دروغ
  نمی‌گوید**: ۴۰۴ را «رمز غلط» ترجمه نمی‌کند و می‌گوید «سرورِ حساب هنوز ورود
  با ایمیل و رمز را ندارد — فعلاً ورود با گوگل» (کدِ `no_route`).
- ⚠️ **«بعداً» را برندارید** — برنامه بی‌اینترنت هم باید کار کند؛ نام و ایمیل
  محلی می‌نشینند و گامِ دو باز می‌شود.
- ⚠️ گامِ جاری از حالِ واقعی حساب می‌شود (`ShowLogin`)، نه از حافظهٔ صفحه.
- نامِ پمپ و لوکیشن **پیش از** فرستادنِ کد ذخیره و **همان لحظه** نشان داده
  می‌شوند (`RefreshAll` پیش از فراخوانِ ابر) و همراهِ فعال‌سازی به ابر هم
  می‌روند (`station.location` تازه است؛ سرورِ قدیمی نادیده می‌گیرد).

### ۲ب) صفحهٔ ورودِ تمام‌قد — به مدلِ عکسِ مرجع، با همان آدمک‌ها

اصلاحِ دومِ همان روز، با یک عکسِ مرجعِ لاگین و یک قالبِ ایمیلِ رنگی. دو
جملهٔ خودش، هر دو قاعده‌اند:

> «وقتی پروفایل را کلیک می‌کنم این صفحهٔ لاگین اولویت باشد و تمامِ صفحه همین
> را نشان بدهد برای کسانی که حساب ندارند، و برای کسانی که دارند پروفایل
> همان مشخصات را نشان بدهد… هیچ پکنه‌ای نباشد، نه از گوگل و نه غیره.»
> «همین مدل باشد و این آدمک‌ها هم باشند؛ اصلاً همین عکس باید باشد. و من
> رنگ‌های همان سایت را گفتم بگیر، نه که شبیه آن بسازی. و رنگ را گفتم فقط
> توی صفحهٔ لاگینِ حساب باشد، نه جای دیگر.»

```
ShowLoginPage  (LoginStep < 4)  ⇒ تمامِ پنجره همین صفحهٔ ورود است
ShowProfilePage                 ⇒ خودِ پروفایل، با تمِ خودِ برنامه
```

- دو صفحه داخلِ **یک `Panel`** روی هم‌اند و با `IsVisible` جا عوض می‌کنند، پس
  هیچ‌کدام کارِ دیگری را انجام نمی‌دهد. راهِ برگشت از پروفایل به صفحهٔ ورود
  دکمهٔ «حساب و ورود» است (`OpenAccountPageCommand`).
- **طرح همان عکسِ مرجع است**: آدمک‌ها یک طرفِ کارتِ سفید و فرم طرفِ دیگر،
  «سلام، / خوش آمدید» درشت، کادرهای **خط‌دار** (فقط یک خطِ زیرین، بی قاب، با
  نوشتهٔ راهنما داخلِ خودِ کادر)، دکمهٔ کپسولیِ تیره، و لینکِ «بعداً». قابِ
  کارت از ۱۴۰۵/۰۶/۲۹ **کشیده** است (اندازهٔ صریحِ ۹۸۰×۵۳۶ برداشته شد)؛ فقط
  ستونِ فرم پهنای صریح دارد (`440`)، وگرنه قاب به اندازهٔ پهن‌ترین پنل جمع
  می‌شود و فرم تمامِ عکس را می‌پوشاند (یک بار شد).
- ⛔ **از عکس فقط آدمک‌ها بریده می‌شوند** (`ArtCrop = new(52, 86, 368, 396)`):
  نه نوشته‌های انگلیسی‌اش و نه آن دو نشانِ «App Store / Google Play» — همان
  «هیچ پکنه‌ای نباشد». فرم، فرمِ خودِ برنامه است، نه فرمِ داخلِ عکس.
- ⛔ **هیچ دکمهٔ گوگل یا هر سرویسِ بیرونیِ دیگری در این صفحه نیست** و
  `SignInAsync` از ویومدل هم برداشته شد. ⚠️ ولی `Services/GoogleSignIn.cs` و
  `CloudLink.SignInAsync` پاک **نشدند**: اپِ کارمندان (`kar/cloud.js`) همان
  راه را دارد و سرور همان مسیر را می‌شناسد — فقط این صفحه نشانش نمی‌دهد.
- ⚠️ **پالتِ این بند در ۱۴۰۵/۰۷/۰۴ کاملاً عوض شد** (بخشِ «ریمیکِ صفحهٔ ورود»
  در انتهای همین فایل مرجع است، نه این خط). آن‌چه **عوض نشد** خودِ قاعده
  است: رنگ‌ها فقط زیرِ `Border.loginpage` می‌مانند.
  ⛔ **صفحهٔ پروفایل دست نمی‌خورد** و همچنان از تمِ برنامه رنگ می‌گیرد؛ آزمونِ
  `RangBandi_FaghatDarSafheyeVorud_Ast` هر یک از این رنگ‌ها را در بخشِ
  پروفایل **قدغن** کرده. دو تمِ برنامه (آبی و طلایی) هم دست نخورده‌اند.
  ⚠️ رنگِ پس‌زمینهٔ خودِ **قالبِ** `TextBox` باید عوض شود
  (`/template/ Border#PART_BorderElement`)، وگرنه در تمِ تیره کادر سیاه
  می‌ماند روی کارتِ سفید.
  ⚠️ ترتیبِ سبک‌ها مهم است: در آوالونیا سبکِ **بعدی** برنده است، نه خاص‌تر.
- ⚠️ **عکس یک درصدِ ثانیه هم به برنامه اضافه نمی‌کند** و سه قاعده‌اش با آزمون
  قفل شده (`Akse_Adamakha_Hast_Va_HichHazineyeShoruAppNadarad`): ۱) فقط از
  `OnActivatedAsync` خوانده می‌شود — پردهٔ لودینگ فقط `EnsureLoadedAsync` را
  می‌زند؛ ۲) روی نخِ دیگر با `Bitmap.DecodeToWidth(s, ArtWidth)` (۷۳۶، اندازهٔ
  خودِ فایل) و بعد بریدن؛ ۳) یک بار برای همیشه (`static _art`). خودِ فایل ۳۳
  کیلوبایت است و آزمون سقفِ ۱۵۰ کیلوبایت را نگه می‌دارد.
  ⚠️ بریدن **باید روی نخِ رابط** باشد: `CroppedBitmap` یک `AvaloniaObject`
  است و روی نخِ دیگر «Call from invalid thread» می‌دهد — یک بار همین باگ عکس
  را کاملاً ناپدید کرد و `catch { }` هم صدایش را خورد.
- ⚠️ «بعداً» در **هر دو گام** هست (`SkipAccountCommand` و `SkipPumpCommand` با
  `AppSettings.LoginSkipped`): صفحهٔ ورود نباید دیواری جلوی دفترِ خودِ کاربر
  شود. با ساختنِ حسابِ واقعی همان نشان پاک می‌شود.
- ⚠️ نوشتهٔ کارت **وسط‌چین** است، نه راست‌چین: `NoViewPinsTextToTheRight`
  هر `TextAlignment="Right"`ی را روی `TextBlock` رد می‌کند (همین آزمون گرفتش).
- عکس‌ها: `20-account.png` (صفحهٔ ورود — همان چیزی که کاربرِ تازه می‌بیند) و
  `20b-account-profile.png` (خودِ پروفایل). شرحِ کامل: `native/docs/ACCOUNT-fa.md`.

### ۳) «💎 اشتراک و پلن‌ها» — زیربخشِ خودِ پروفایل (`vip`)

- حالِ همین حالا (پلن، مانده، وضعیت، ارفاق) + هر سه کارِ ابری با ✅/🔒 و
  پشتیبانی «✅ همیشه باز»، و چهار پلن با **قیمتِ خالی** (همان
  `native/docs/PLANS-fa.md`). ⛔ قیمت را از خودت پر نکن.
- ⚠️ **کلیدِ «🧪 آزمایشِ حالتِ بی‌اشتراک»** (`Entitlements.TestDeny`) تنها
  راهی است که صاحبِ پمپ خودش قفل‌ها را می‌بیند. **فقط می‌بندد و هیچ‌وقت باز
  نمی‌کند**، و فقط در حافظه است (روی دیسک نمی‌نشیند) — پس راهِ دور زدنِ
  اشتراک نیست. آزمونش
  `KelideAzmayesh_FaghatMibandad_HichVaghtBazNemikonad`.
- هیچ تصمیمی این‌جا گرفته نمی‌شود؛ تنها جای تصمیم `Entitlements` است.
- آزمون: `AppLinksTests` (لینک‌ها، پیام، ورک‌فلو، صفحهٔ تازه، صفحهٔ ورود،
  عکس و پنجرهٔ بریدنش، بی‌سرویسِ بیرونی بودن، اولویتِ صفحهٔ ورود، رنگ‌بندیِ
  فقط-در-ورود، بخشِ وی‌آی‌پی) و `SubSectionTests` که `apps` را زیرِ «تنظیمات» و `vip`
  را زیرِ «پروفایل» قفل کرده. عکس: `35-sub-apps.png`، `36-sub-vip.png` و
  `20-account.png`.

### ۴) «تست بزن ببین لاگین می‌شود یا نه» — بندهای ۱۴ تا ۱۶ در `verify`

خواستهٔ صاحب ریپو (۱۴۰۵/۰۶/۲۸). سه بندِ تازه در
`dotnet run --project PumpYaqobi.UiTests -c Release -- verify` که **خودِ فرم را
پر می‌کنند و دکمه را می‌زنند**، بعد تنظیمات و دیتابیس را می‌خوانند — نه
متغیرهای روی صفحه:

- **۱۴) صفحهٔ ورود**: باز می‌شود و **اولویت دارد** (خودِ پروفایل دیده نمی‌شود)،
  آدمک‌های عکس واقعاً می‌آیند و **بریده**‌اند (۳۶۸×۳۹۶ — همین عدد ثابت می‌کند
  `CroppedBitmap` کار کرده و نوشته‌ها و نشان‌های فروشگاهِ عکس بیرون مانده‌اند)،
  کادرِ نامِ پمپ از تنظیمات پر می‌شود، ایمیلِ بی «@» رد می‌شود و
  **هیچ چیزی ذخیره نمی‌کند**، بی کد نام و ایمیل و نامِ پمپ می‌نشینند و
  پروفایل همان لحظه نامِ تازه را نشان می‌دهد، و کدِ سه‌رقمی رد می‌شود بی
  ساختنِ هیچ توکنی؛ و «بعداً»ی گامِ دو صفحه را به خودِ پروفایل می‌برد و
  «حساب و ورود» برمی‌گرداند.
- **۱۵) اشتراک و پلن‌ها**: چهار پلن با قیمتِ خالی، پشتیبانی «✅ همیشه باز»، و
  کلیدِ آزمایش قفل‌ها را می‌بندد و با خاموش شدن **همان حالِ واقعی** برمی‌گردد.
- **۱۶) اپِ گوشی**: دو لینک و پیامِ آماده، و هیچ رمزی در پیام نیست.

### ۵) 🧪 `cloudlogin` — «لاگین می‌شود؟ کد بزنی چه می‌شود؟ حساب ساخته می‌شود؟»

خواستهٔ صریحِ صاحب ریپو (۱۴۰۵/۰۶/۲۸): «با تست ببین برنامه لاگین می‌شود، کار
می‌کند، کد بزنی چه می‌شود و حسابی ساخته می‌شود از اپ یا که نه.»

```
dotnet run --project PumpYaqobi.UiTests -c Release -- cloudlogin
```

هشت بند: اولویتِ صفحهٔ ورود · ثبت‌نامِ سه‌پله‌ای (`register/start` ⇒ `verify`
⇒ `complete`) و نشستنِ توکنِ
حساب روی **دیسک** · ورود با رمزِ غلط (پیامِ خودِ سرور) و بعد درست · کدِ غلط
(هیچ توکنی) · کدِ درست ⇒ توکنِ دستگاه، قفلِ کلیدِ عمومی، مجوزِ امضاشده،
«VIP · ۴۱ روز»، هر سه کارِ ابری باز، و **فعال ماندن پس از بسته و باز شدنِ
برنامه** · کدِ اپِ کارمندان · نشانیِ خانگی از `/api/pump/me` · و سرورِ جعلی
با کلیدِ دیگر ⇒ `key_mismatch`.

- ⚠️ **ابرِ واقعی در کار نیست و نباید باشد**: یک ابرِ ساختگی در همین فرآیند
  جواب می‌دهد (`CloudLink.TestTransport`) با همان شکلِ پاسخِ سرورِ واقعی، و
  مجوزش واقعاً با ES256 امضا می‌شود تا `LicenseGuard` هم واقعاً سنجیده شود.
  ⛔ این قفلِ نشانی را باز نمی‌کند: فقط با یک خطِ **کد** مقدار می‌گیرد، نه از
  تنظیمات و نه از محیط — و خودِ سنجه می‌سنجد که درخواست به همان
  `CloudConfig.BaseUrl` رفته باشد.
- ⛔ **باگی که همین سنجه گرفت**: `AccountSectionViewModel.Cloud` پاسخِ ذخیره‌اش
  `AppSettings.Load().Save()` بود — یعنی یک شیءِ **تازه** از دیسک روی شیئی که
  `CloudLink` عوضش کرده بود نوشته می‌شد و توکنِ حساب، توکنِ دستگاه، کلیدِ
  عمومی و مجوز **هیچ‌وقت روی دیسک نمی‌نشستند**. تا بسته شدنِ برنامه کار
  می‌کرد و دفعهٔ بعد کاربر باید دوباره کدِ شش‌رقمی می‌زد. حالا همان شیء ذخیره
  می‌شود و `AppLinksTests.TanzimateAbr_HamanShey_Zakhire_Mishavad` قدغنش کرده.
  شرحِ کامل: `native/docs/ACCOUNT-fa.md`.

⚠️ **فعال‌سازیِ واقعیِ کدِ شش‌رقمی عمداً در CI نمی‌دود** — پشتِ
`PUMP_VERIFY_CLOUD=1` است. دو دلیل: اینترنت و سرورِ ابر لازم دارد، و نباید هر
اجرا یک کدِ الکی به سرورِ واقعیِ اشتراک بفرستد. پس «کد را زدم و اشتراک فعال
شد» را فقط روی ماشینی می‌شود دید که به `api.vill3n.top` دسترسی دارد (سندباکسِ
سیزن‌های Claude این دسترسی را ندارد: `CONNECT ... 403`). همین‌طور «ورود با
گوگل» که مرورگرِ سیستم را باز می‌کند و در اجرای بی‌پنجره سنجیدنی نیست.

## 🔐 نشستِ ابر: انقضا، تازه‌سازی، خروج و رمزِ فراموش‌شده (از ۱۴۰۵/۰۶/۳۰)

خواستهٔ صریحِ صاحب ریپو: «سیستمِ ورودِ فعلیِ برنامهٔ نیتیو را کامل بررسی،
تست، عیب‌یابی و اصلاح کن… Token چگونه Refresh می‌شود، بعد از Logout چه
اتفاقی می‌افتد، Token قدیمی قابلِ سوءاستفاده نباشد… و آن را داخلِ فایلِ
متنیِ ساده ذخیره نکن.»

قرارِ سرور این بار **خوانده شد، نه حدس زده** (`vil3ntec-it/shop` →
`server/src/routes/auth.js` و `src/config.js`):

```
توکنِ دسترسی   یک ساعت      توکنِ تازه‌سازی   نود روز
POST /api/auth/refresh          { refreshToken } ⇒ { accessToken, accessExpiresAt }
POST /api/auth/logout           { refreshToken } + Bearer ⇒ هر دو باطل
POST /api/auth/password/forgot  { email }        ⇒ کد به ایمیل
POST /api/auth/password/reset   { email, code, password } ⇒ رمزِ تازه + نشست
```

- ⛔ **باگی که برنامه را یک‌ساعته خاموش می‌کرد**: `CloudRefreshToken` ذخیره
  می‌شد و **هیچ‌جا خوانده نمی‌شد**، و هیچ ۴۰۱ی فهمیده نمی‌شد. یعنی یک ساعت
  پس از ورود، `/api/pump/me` و کدِ اپِ کارمندان بی‌صدا می‌مردند و **دیگر
  هیچ‌وقت درست نمی‌شدند** — در حالی که پروفایل «وارد شده‌اید» می‌گفت.
  `kar/cloud.js` این را از اول داشت (`authed()`)؛ فقط نیتیو نداشت.
- ⚠️ **`CloudLink.AccountAsync` تنها راهِ درخواست با توکنِ حساب است**: پیش از
  انقضا خودش تازه می‌کند، و روی ۴۰۱ **یک بار** تازه و **یک بار** دوباره
  می‌زند. ⛔ حلقه نزنید — ۴۰۱ِ بعد از تازه‌سازی یعنی نشست واقعاً مرده است.
- ⛔ **بی‌اینترنت هیچ‌وقت کسی را از حسابش بیرون نمی‌اندازد.** فقط ۴۰۱/۴۰۳ِ
  خودِ سرور نشست را پاک می‌کند؛ تایم‌اوت و قطعیِ مودم و ۵۰۰ هیچ چیزی را
  پاک نمی‌کنند. و پاک شدنِ نشست **اشتراکِ دستگاه را نمی‌برد**.
- ⛔ **توکن‌ها دیگر خام روی دیسک نیستند** (`Services/SecretStore.cs`): DPAPI
  روی ویندوز (P/Invoke، بی بستهٔ تازه — مثلِ `winmm`ِ `WaveRecorder`)،
  AES-GCM با کلیدِ ۶۰۰ روی بقیه. چهار راز: توکنِ حساب، تازه‌سازی، دستگاه، و
  رمزِ نوشتنِ سرورِ خانگی.
  ⚠️ **کلیدِ خامِ کهنه هنوز خوانده می‌شود ولی دیگر نوشته نمی‌شود**، وگرنه
  همهٔ مشتری‌های امروز یک‌شبه از حساب بیرون می‌افتادند و باید دوباره کدِ
  شش‌رقمی می‌زدند. هیچ‌کدام از دو راه چیزی را خالی نمی‌کند، فقط پر می‌کند.
- ⛔ **خروج روی سرور هم باطل می‌کند.** پیش از این فقط محلی بود و توکنِ
  نودروزه زنده می‌ماند. ⚠️ ولی نشدنش جلوی خروجِ محلی را نمی‌گیرد.
- **رمزِ فراموش‌شده گامِ صفر است، نه پنج**: `ShowLoginPage => LoginStep < 4`
  قفل است و باید همان بماند. ⚠️ پیامش نمی‌گوید این ایمیل حساب دارد یا نه —
  خودِ سرور هم برای موجود و ناموجود یک جواب می‌دهد.
- `Services/LoginRules.cs` — رونوشتِ قاعده‌های سرور پیش از رفتن (`@x.com` و
  `a@b` و رمزِ «فقط عدد» رد می‌شوند). ⚠️ **جای** سنجشِ سرور را نمی‌گیرد.
- دکمهٔ «👁 نشان دادنِ رمز»، **Enter روی خودِ کادرها**، نوارِ Loading، و پیامِ
  آدمیزاد برای ۴۲۹. ⚠️ **پیامِ خامِ استثنا به کاربر نمی‌رسد** (ممکن است
  نشانی یا نامِ میزبان داشته باشد).
- آزمون: `CloudSessionTests` و `SettingsSecretTests` در `PumpYaqobi.Tests`،
  و بندهای **۹ تا ۱۴** در
  `dotnet run --project PumpYaqobi.UiTests -c Release -- cloudlogin`.
  شرحِ کامل: `native/docs/ACCOUNT-fa.md`.

## 🧱 فایلِ تنظیمات هیچ‌وقت نیمه نمی‌ماند · ورود نخِ رابط را نمی‌خشکاند (از ۱۴۰۵/۰۶/۳۰)

دو باگی که با گشتنِ دوبارهٔ همان پرامپت پیدا شدند — هر دو سنجیده، نه حدس.

- ⛔ **`AppSettings.Save` دیگر `File.WriteAllText` نیست.** آن فایل را اول
  **خالی** می‌کرد؛ مردنِ برنامه یا رفتنِ برق وسطِ همان لحظه یعنی
  `settings.json`ِ نصفه و یک `Load`ِ **خالی** — و با آن می‌رفت: توکنِ
  دستگاه (کدِ شش‌رقمی دوباره)، `CloudPublicKey` (**قفلِ ضدِ کرک باز
  می‌شد**)، و `StationCode` که به `pump1` برمی‌گشت (**نوشتن روی پوشهٔ
  اشتباهِ سرور**). حالا: قفل، فایلِ موقت، `Flush(true)`، و جایگزینیِ اتمی
  با `.bak`؛ `Load` از `.bak` هم می‌خواند.
  ⚠️ **سه نخ** این فایل را می‌نویسند (رابط، `StationPublisher`ِ
  بیست‌ثانیه‌ای، `BackupPusher`). پیش از این از ۴۰ ذخیرهٔ هم‌زمان **۱۸ تا**
  خراب خوانده می‌شد. (`SettingsDurabilityTests`)
- ⛔ **`LockViewModel.Submit` دیگر هم‌زمان نیست.** PBKDF2ِ ۲۱۰٬۰۰۰ دوری روی
  **نخِ رابط** می‌دوید — سنجیده شد ۱۴۰ms (نخستین اجرا ۲۸۳ms) — پس پنجره در
  هر ورود می‌خشکید، و `Busy` در همان بلوک روشن و خاموش می‌شد یعنی هیچ فریمی
  دیده نمی‌شد و `IsEnabled="{Binding !Busy}"` مرده بود.
  ⚠️ **سنجه‌ها باید منتظر بمانند**: `SubmitCommand.Execute(null)` حالا همان
  لحظه برمی‌گردد. هر ۳۵ جا به `LockIn.Wait(vm.Lock)` رفت؛ بی آن، سنجه
  برنامه را روی صفحهٔ قفل می‌بیند و **سبزِ دروغ** می‌دهد.
- ⛔ **هر کلاسِ آزمونی که `AppSettings.DirOverride` را عوض می‌کند باید
  `[Collection(AppHostCollection.Name)]` داشته باشد.** آن خاصیت **استاتیک**
  است و xUnit کلاس‌ها را موازی می‌دواند؛ پنج کلاس همان را عوض می‌کنند و سه
  تای تازه بی آن نشان اضافه شده بودند، پس روی هم می‌نوشتند و آزمون‌ها
  **گاهی** سرخ می‌شدند — روی برنچ سبز بود و روی `main` سرخ. CI هم همان را
  گرفت (اجرای #125 شکست).
  ⚠️ **یک اجرای سبز، ایرادِ مسابقه‌ای را ثابت نمی‌کند** — چند بار بدوانید.
  ⚠️ و سرِ پاک‌سازی مقدارِ **قبلی** را برگردانید، نه `null`: `null` یعنی
  آزمونِ بعدی به تنظیماتِ **واقعیِ خودِ کاربر** می‌نویسد.
- ⛔ **«بعداً — فعلاً بی حساب ادامه می‌دهم» برداشته شد** (۱۴۰۵/۰۶/۳۰، با
  عکسِ صاحب ریپو: «این نباشه و کار نمی‌کنه»). **دروغ می‌گفت**: نوشته‌اش «بی
  حساب ادامه می‌دهم» بود ولی `LoginSkipped` نمی‌گذاشت و فقط `LoginStep = 3`
  می‌کرد — یعنی کاربر همچنان **داخلِ همان دیوارِ ورود** می‌ماند، این بار روی
  گامِ کدِ پمپ.
  ⚠️ **قاعدهٔ «صفحهٔ ورود نباید دیوار شود» پس گرفته نشد**: راهِ واقعی
  «‹ برگشت به برنامه» است (`CloseLoginAsync`) که واقعاً `LoginSkipped`
  می‌گذارد، و «بعداً»ی گامِ پمپ هم سرِ جایش است. حالا «برگشت به برنامه»
  نام و ایمیل و نامِ پمپِ تایپ‌شده را هم نگه می‌دارد (رمز نه) — همان کارِ
  مفیدِ دکمهٔ برداشته‌شده، روی دکمه‌ای که واقعاً کارش را می‌کند.
- ⛔ **کدِ خطای فنی به کاربر نشان داده نمی‌شود.** «متنِ شرایط از سرور نیامد —
  سرور جواب نداد (۴۰۴)» زیرِ تیکِ شرایط فقط کاربر را می‌ترساند؛ حالا یک
  جملهٔ آرام است. و **۴۲۹ همیشه** «تلاشِ زیاد — چند دقیقه صبر کنید» می‌شود،
  حتی اگر سرور خودش متنی داده باشد.
  ⚠️ سنجه‌های «این در XAML نیست» باید **کامنت‌ها را اول بردارند** — نامِ
  دکمهٔ برداشته‌شده در کامنتِ «این برداشته شد» هست و باید هم باشد.
- ✅ و این‌ها گشته شدند و سالم بودند: هیچ `TODO`/`FIXME`/Mock/رمزِ
  سفت‌وسخت در کد؛ مجوزِ دسترسیِ سرور (`req.stationId` **همیشه از خودِ ردیفِ
  توکن**، نه از درخواست — پس عوض کردنِ شناسه دفترِ پمپِ دیگری را باز
  نمی‌کند)؛ و هیچ `.Result`/`.Wait()`ی در کلِ `PumpYaqobi.App`.

## 🏛️ سرورِ مرکزیِ احراز هویت — یک نشانی، یک شناسه، یک جای ساختن (از ۱۴۰۵/۰۶/۳۰)

خواستهٔ صاحب ریپو: «Login موجود را به سرورِ مرکزیِ احراز هویت وصل کن… تمام
آدرس‌های Server/API باید از Configuration دریافت شوند و داخل کد به صورت
پراکنده Hard-code نشوند (`AUTH_SERVER_URL` · `APPLICATION_ID` ·
`APPLICATION_VERSION`)… این پروژه نباید Authentication Server جداگانه برای
خودش بسازد… اگر APIهای سرور فعلی مطابقت ندارند، فعلاً API ساختگی ایجاد نکن.»

بررسیِ کامل پیش از هر تغییر انجام شد و نتیجه‌اش این است:

- ✅ **سرورِ مرکزی از قبل همان `vil3ntec-it/shop` است** و برنامه هیچ سیستمِ
  احراز هویتِ خودش ندارد. هر ده مسیرِ `‎/api/auth/*‎` که برنامه می‌زند روی
  همان سرور **هست** (`server/src/routes/auth.js`)، و مسیرهای اشتراک هم
  (`routes/pump-device.js`، mount در `app.js:199`). ⛔ **هیچ Endpointی کم
  نبود، پس هیچ API ساختگی‌ای ساخته نشد.**
- ✅ **کدِ شش‌رقمی روی سرور ساخته می‌شود، نه در برنامه** (`lib/otp.js:265`) و
  در پاسخ برنمی‌گردد مگر `env !== 'production'` **و** هیچ سرویسِ ارسالی
  تنظیم نباشد (`otp.js:317`). برنامه هیچ‌جا `devCode` را نمی‌خواند — تنها
  جای این واژه ابرِ **ساختگیِ** `cloudlogin` است.
- ✅ **رمز هیچ‌وقت محلی سنجیده نمی‌شود**: `CloudLink` فقط می‌فرستد و جوابِ
  سرور مرجع است. (⚠️ رمزِ **قفلِ خودِ برنامه** چیزِ دیگری است و عمداً محلی
  می‌ماند — برنامه باید بی‌اینترنت باز شود و اپِ کارمندان همان هش را
  می‌گیرد. `pbkdf2$sha256$210000$…` با `FixedTimeEquals`.)

آنچه **عوض شد**، فقط «پراکندگی»:

- ⛔ **`CloudConfig.Url(path)` تنها جای ساختنِ نشانیِ ابر است.** پیش از این
  پنج جا خودشان `BaseUrl + "…"` می‌چسباندند. آزمونِ
  `HichFayle_Digari_NeshaniRa_Namichasbanad` روی خودِ سورس می‌گردد و هر
  فایلِ دیگری که `api.vill3n.top` یا `CloudConfig.BaseUrl` را در خطِ **کد**
  (نه توضیح) بیاورد قرمز می‌کند.
- سه مقدارِ نام‌دارِ خواسته‌شده یک‌جا نشستند: `AuthServerUrl` (= `BaseUrl`)،
  `ApplicationId` (= `Audience` = `tohid-pump-app`) و `ApplicationVersion`
  (= شمارهٔ اسمبلی). ⚠️ نامِ تازه ساخته نشد و هیچ‌کدام مقدارِ دومی ندارند —
  `aud`ی که `LicenseGuard` می‌سنجد باید همان باشد که به سرور می‌گوییم.
- `X-App-Id` به `Stamp` اضافه شد: سرورِ مرکزی مالِ چند برنامه است و بی این،
  لاگِ خطا نمی‌گوید کدام زده. ⛔ همچنان هیچ چیزِ شناسایی‌کنندهٔ **کاربر** در
  هدر نیست (آزمونِ `HarDarkhast_ShomareyeNoskhe_RaMibarad`).

⛔ **و نشانی قفل ماند — به انتخابِ خودِ صاحب ریپو، پس از این‌که تضاد را
نشانش دادم.** «Configuration» در این ریپو یعنی **یک جای واحد در کد**، نه یک
کادرِ متنی: اگر `AUTH_SERVER_URL` از فایل یا محیط خوانده شود، هر کسی برنامه
را به سرورِ خودش می‌برد، مجوزِ خودش را امضا می‌کند و قفلِ اشتراک با یک خط متن
دور می‌خورد. سه آزمونِ `CloudAddressLockTests` دست‌نخورده‌اند.

⚠️ **و یک محدودیتِ سنجش که پنهانش نمی‌کنیم**: سندباکسِ سیزن‌های Claude به
`api.vill3n.top` نمی‌رسد (`CONNECT tunnel failed, 403`). پس هر ادعایی دربارهٔ
سرورِ **زنده** باید روی کامپیوترِ خودِ صاحب ریپو سنجیده شود؛ `cloudlogin`
عمداً با ابرِ ساختگیِ درون‌فرآیندی می‌دود.

## ⛔ یک رمزگشاییِ ناموفق، رازِ سالمِ دیسک را پاک نکند (از ۱۴۰۵/۰۶/۳۰)

ساختِ ویندوزِ CI (اجرای #۱۲۸) روی `main` سرخ شد در حالی که همان کد روی
لینوکس سبز بود. سه باگِ واقعی زیرش بود و هر سه بسته شد:

- ⛔ **راز پاک می‌شد.** اگر `SecretStore.Unprotect` یک بار خالی برمی‌گرداند،
  آن `AppSettings` با توکنِ **خالی** بالا می‌آمد و `Save`ِ بعدی همان خالی را
  روی بلوکِ سالمِ دیسک می‌نوشت. یعنی **یک** لغزشِ گذرا توکنِ دستگاه را برای
  همیشه می‌برد و کاربر باید دوباره کدِ شش‌رقمی می‌زد. حالا بلوکِ باز-نشده
  دست‌نخورده نگه داشته و همان دوباره نوشته می‌شود؛ با آمدنِ رازِ واقعی، بلوکِ
  کهنه دور انداخته می‌شود. (`BolokeBazNashode_PakNemishavad`)
  ⚠️ نشانه‌اش «۳۸ خرابی از ۴۰» بود — شکلِ **مسمومیت**، نه شکلِ مسابقه: یکی
  خراب می‌شود و بقیه همه پشتِ سرش.
- ⛔ **فایلِ قفل‌شده، پیش‌فرض‌ها را روی دفتر می‌نوشت.** روی ویندوز ضدِ ویروس و
  نمایه‌سازِ سیستم یک لحظه دستهٔ فایل را نگه می‌دارند؛ همان یک لحظه یعنی
  `Load`ی که تنظیماتِ **خالی** می‌داد و `Save`ِ بعدی کدِ پمپ را به `pump1`
  برمی‌گرداند (نوشتن روی پوشهٔ **اشتباهِ** سرور) و قفلِ ضدِ کرک را می‌برد.
  حالا `Read` چند تلاشِ کوتاه می‌کند و اگر باز هم نشد، آن نمونه **کور** است
  و `Save` هیچ‌وقت نمی‌نویسد. ⚠️ خرابیِ خودِ JSON کور نمی‌کند — آن فایل
  واقعاً خراب است و باید روی‌نویسی شود. (`FayleGhoflShode_PishFarzRa_Nemineviseh`)
- ⛔ **کلیدِ `SecretStore` حالا به پوشه‌اش بسته است.** پیش از این یک کلیدِ
  بی‌نام کَش می‌شد و عوض شدنِ `AppSettings.DirOverride` آن را **سراسری** دور
  می‌ریخت — پس هر کلاسِ آزمونی که پوشه را عوض می‌کرد، کلیدِ پوشهٔ کلاسِ دیگری
  را هم باطل می‌کرد و آزمون‌ها گاهی سرخ می‌شدند. `ForgetKey` دیگر برای
  درستی لازم نیست.

⚠️ **و قاعدهٔ «کلاسِ آزمونی که پوشه را عوض می‌کند باید سریال باشد» همان روز
دوباره شکست**: `AccountSwitchTests` بی `[Collection(AppHostCollection.Name)]`
اضافه شده بود. یادآوری در یک فایلِ متنی کافی نیست، پس از امروز
`SettingsCollectionRuleTests` خودِ سورس را می‌گردد و هر کلاسی را که
`DirOverride` یا `AppHost.Start` را لمس کند و نشان نداشته باشد قرمز می‌کند.

⚠️ **و یک تلهٔ آزمون‌نویسی**: `System.Text.Json` نویسهٔ `+`ِ base64 را
`+` می‌نویسد. پس «بلوک را در متنِ فایل با `Replace` خراب کن» بی‌صدا هیچ
کاری نمی‌کند و آزمون **سبزِ دروغ** می‌دهد. خودِ JSON را باز کنید و همان‌جا
عوضش کنید.

## 💳 پلن‌ها با قیمتِ واقعی، و اشتراکی که از حساب می‌آید (از ۱۴۰۵/۰۶/۳۰)

قیمت‌ها و مرزِ پلن‌ها از خودِ صاحب ریپو است — جدولِ کامل و **نقشهٔ کارِ
سرور** در `native/docs/PLANS-fa.md`. سرِ خط:

```
رایگان (پنهان)  ·  استاندارد ۱۲۰$  ·  وی‌آی‌پی ۱۵۰$  ·  دائمی ۶۰۰$
```

- ⛔ **«رایگان» جایی نشان داده نمی‌شود** — «رایگان لازم به دیده شدن نیست».
- ⛔ **کدِ اشتراک در کار نیست.** جملهٔ صریحِ صاحب ریپو: «من یادم نمیاد که
  برای اشتراک کدی گفته باشم — اون رو من به حسابِ یارو از سرور می‌دم و تو
  برنامه تو حسابِ همون ثبت می‌شن.» ⇒ `‎/api/pump/me`ِ سرور **از قبل**
  `entitlement` را می‌فرستاد و برنامه فقط `home` را برمی‌داشت و بقیه‌اش را
  دور می‌ریخت. حالا `HomeFromAccountAsync` همان را می‌نشاند.
  ⚠️ دو کدِ شش‌رقمی هست و یکی نیستند: **کدِ ایمیل** (ساختنِ حساب) و
  **کدِ پمپ** (راه انداختنِ اپِ گوشی). هیچ‌کدام «کدِ اشتراک» نیست.
- ⛔ **سه دروازهٔ تازه**: `profit` (مفاد/ضرر/اتحادیه) · `history`
  (تاریخچه‌ها، و دکمهٔ تاریخچهٔ هر بخش) · `dashboard`. با `kar` و `qrlive` و
  `cloudbackup` می‌شوند شش تا (`Entitlements.Paid`).
  - ⚠️ **داده دست نمی‌خورد و هیچ محاسبه‌ای خاموش نمی‌شود** — فقط درِ صفحه
    بسته است و با ارتقا همان لحظه باز می‌شود («اطلاعاتشون باشن ولی دیده
    نتونن»).
  - ⚠️ **نرخِ اتحادیه آسیب نمی‌بیند**: قفل فقط دیدنِ صفحه است؛ خواندنِ نرخ
    برای زیانِ افزایشِ قیمت و فاکتورها دست‌نخورده.
  - ⛔ **و صفحهٔ اول خالی نمی‌ماند**: صفحهٔ اولِ برنامه داشبورد است و
    داشبورد حالا قفلِ پلن دارد. `OpenStartSectionAsync` اولین بخشی را باز
    می‌کند که پلن اجازه می‌دهد، **بی هیچ پیامی**. بی این خط، نصبِ
    بی‌اشتراک بالا می‌آمد و صفحهٔ خالی می‌دید.
- ⛔ **نامِ قابلیت در برنامه و روی سرور یکی نیست** و این یک بار نزدیک بود
  همهٔ مشتری‌های پولی را قفل کند: کاتالوگِ سرور `kar_app`/`bot`/`cloud`
  دارد، نه `kar`/`qrlive`/`cloudbackup`. `EntitlementState.Has` هم‌معنی‌ها
  را می‌پذیرد و نگاشتش از خودِ سرور درآمده (مسیرِ فایلِ ابری روی `cloud`
  قفل است). ⚠️ `profit` و `history` هنوز روی سرور **نیستند** و `dashboard`
  آن‌جا `core` است — سه کارِ ماندهٔ سرور، در `PLANS-fa.md`.

## 🔬 اسکنِ کاملِ ۱۴۰۵/۰۶/۳۰ (دومی) — دو سرخ و حقیقتشان

`fullscan`: **۱۷ سبز، ۲ سرخ**. هر دو ریشه‌یابی شدند و هیچ‌کدام پس‌رفتِ ما
نبود:

| سنجه | در اسکن | تنها و تمیز | داوری |
|---|---|---|---|
| `bigtable` | ✖ | **✅ سبز** (ردیفِ زنده ۵۰ در همهٔ اندازه‌ها) | آزمونی که هم‌زمان دویده بود عددش را به‌هم زد |
| `years` | ✖ ۵٬۶۹۵ms | ۵٬۶۹۵ms | روی `9f5f5a7` (پیش از کلِ این سیزن) **۵٬۳۵۷ms** — همان‌قدر سرخ |

⚠️ **درسِ دوباره‌ای که گران تمام شد**: وسطِ `fullscan` هیچ کارِ سنگینِ
دیگری ندوانید. یک `dotnet test`ِ کوتاه کافی بود تا `bigtable` سرخ شود و
نیم‌ساعت دنبالِ باگی بگردیم که وجود نداشت.

⚠️ و **`years` را با یک نسخهٔ قدیمی بسنجید، نه با انتظار**: `git worktree`
روی یک کامیتِ پیش از کار، همان‌جا همان‌قدر سرخ بود.

### کارت‌های قرض‌داران — چه سنجیده شد و چه **کار نکرد**

`scrollperf` شکلِ مسئله را روشن کرد: هزینه **ساختنِ کارتِ تازه** است، نه
اسکرول و نه سایه.

| پاس | بیشینهٔ گام | میانه |
|---|---|---|
| بارِ اول | ۱۵۸ms | ۲۳ms |
| بارِ دوم | ۶۴ms | ۱۹ms |
| بی سایه | ۵۳ms | ۲۰ms |

⛔ **یک نظریه آزموده شد و رد شد**: سه بایندِ `$parent[ItemsRepeater]`ِ
دکمه‌های کارت (که در هر کارت درختِ دیداری را بالا می‌روند) با یک ارجاعِ
مستقیمِ `Owner` جایگزین شدند. نتیجه: ۱۵۸ ⇒ ۱۴۹ در پاسِ اول و **۶۴ ⇒ ۷۹ در
پاسِ دوم** — یعنی در حدِ نوسان و اگر چیزی، کمی بدتر. پس **برگردانده شد**.
⚠️ تغییری که سودش با عدد ثابت نشود نمی‌ماند، حتی اگر «تمیزتر» به نظر برسد.

راهِ ریشه‌ای همچنان نوشته نشده و باید با نمونه‌بردار شروع شود
(`dotnet-trace collect … -- scrollperf trace`)، نه با حدسِ بعدی.

## 💾 پشتیبان روی ابر هم می‌رود — از ۱۴۰۵/۰۶/۳۱

تا امروز `BackupPusher` فقط به **سرورِ خانگی** می‌فرستاد. مودم که بسوزد
یا کامپیوترِ سرور که خراب شود، هر دو با هم می‌روند — و
`data/stations/<کد>/backups/` روی همان یک دیسک است. نامِ قابلیت هم
`cloudbackup` بود، که گمراه‌کننده بود: هیچ ابری در کار نبود.

```
هر ۶ ساعت ──▶ VACUUM INTO ──┬─▶ POST /api/stations/<کد>/backup   (خانگی)
                            └─▶ POST /api/pump/device/backups    (ابر)
```

- ⛔ **دو مقصد، نه جانشین.** سرورِ خانگی سریع است و در همان شبکه؛ ابر
  جایی است که آتش‌سوزیِ پمپ به آن نمی‌رسد. هر کدام جدا سنجیده می‌شود و
  نرفتنِ یکی دیگری را نمی‌شکند.
- ⛔ **«رفت» یعنی دستِ‌کم یکی رفت.** پمپی که هنوز سرورِ خانگی‌اش را راه
  نینداخته نباید هر شش ساعت هشدارِ «پشتیبان نرفته» ببیند در حالی که
  نسخه‌اش روی ابر سالم نشسته.
- ⛔ **`SendToCloudAsync` هیچ‌وقت استثنا بیرون نمی‌دهد** و فایلِ
  بزرگ‌تر از `CloudMaxBytes` را اصلاً نمی‌خواند: سرور هم ردش می‌کند و
  خواندنش در حافظه فقط خرج است.
- ⚠️ سهم را **سرور** تصمیم می‌گیرد، نه برنامه: پمپِ بی‌اشتراک جای
  کمتری دارد (`CloudBackupStats.Paid`).

## 💬 پشتیبانیِ صاحبِ پمپ ↔ مدیرِ سامانه — از ۱۴۰۵/۰۶/۳۱

⛔ **پمپ‌داری که گیر می‌کرد هیچ دری نداشت.** `/api/support` مالِ بخشِ
دکان است و این برنامه حساب ندارد. و `/api/pump/device/chat/…` گفت‌وگوی
**مشتریِ کیوآر با صاحبِ پمپ** است، نه با کسی که برنامه را ساخته —
این دو را قاطی نکنید.

```
CloudLink.SupportThreadAsync / SupportSendAsync / SupportSeenAsync
  ⇒ /api/pump/device/support/{thread,messages,read}
```

- ⛔ **کلیدِ رشته `station_id` است**، نه کاربر و نه دستگاه: یک پمپ
  می‌تواند چند کامپیوتر داشته باشد و صاحبش هم از گوشی می‌نویسد. یک
  پمپ، یک گفت‌وگو.
- ⛔ **هیچ‌وقت پشتِ اشتراک نمی‌رود** — «پشتیبانی یکی از واجبات است».
  کسی که اشتراکش تمام شده بیشتر از همه لازم دارد بپرسد چرا.
- ⚠️ **کارِ مانده**: صفحه‌اش در برنامه. لایهٔ ارتباط آماده است ولی
  `ChatSectionViewModel` گفت‌وگوها را با `Acct is not null` از هم جدا
  می‌کند؛ نوعِ سوم یعنی تغییر در مسیرِ فرستادن، صندوق و عنوان.


## 🔑 اشتراک از **حساب** می‌آید، نه از کد — و سرور دیگر کامل است (از ۱۴۰۵/۰۶/۳۱)

خواستهٔ صریحِ صاحب ریپو: «اشتراک رو من به حسابِ یارو از سرور می‌دم
اینترنتی و تو برنامه تو حسابِ همون ثبت می‌شن… من یادم نمیاد که برای
اشتراک کدی گفته باشم.» سه کارِ سروری که `native/docs/PLANS-fa.md` به
سیزنِ بعد سپرده بود، روی ریپوی `shop` انجام شد؛ این‌جا فقط چیزهایی که
**نباید برگردند**:

```
ورود با ایمیل/گوگل ⇒ توکنِ حساب ⇒ POST /api/pump/device/bind
                   ⇒ توکنِ دستگاه + همان مجوزِ امضاشدهٔ `activate`
```

- ⛔ **`CloudLink.BindAsync` تنها راهِ بند شدن با حساب است**، و
  `HomeFromAccountAsync` خودش صدایش می‌زند — پس کاربرِ تازه هیچ کدی
  نمی‌زند. ⚠️ و **پس از** سنجشِ «این حساب مالِ پمپِ دیگری است» صدا زده
  می‌شود، وگرنه ورود با حسابِ پمپِ دیگر همین دستگاه را به آن پمپ
  می‌بست. سنجه‌اش `CloudSessionTests.Hesabe_PompeDigar_…`.
- ⚠️ **و چرا این بیش از یک راحتی بود**: `/api/pump/me` از قبل
  `entitlement` را می‌داد، ولی آن فقط «فعال است یا نه» را می‌گوید —
  **نه فهرستِ قابلیت‌های پلن**. فهرست تنها داخلِ مجوزِ امضاشده (`feat`)
  است و مجوز فقط با توکنِ **دستگاه** صادر می‌شود. پس برنامه‌ای که فقط
  از راهِ حساب می‌آمد `LicenseCheck.Listed`ش دروغ می‌گفت و
  `Entitlements.Allows` همه‌چیز را باز می‌کرد — مشتریِ **استاندارد** هر
  پنج قفل را هم می‌گرفت. مستقیم روی پول.
- ⛔ **قفلِ کلیدِ عمومی (TOFU) در `BindAsync` هم هست** و باید بماند:
  کلیدِ متفاوت `key_mismatch` می‌گیرد و هیچ توکنی نمی‌نشیند. بی این،
  سرورِ ساختگی مجوزِ خودش را امضا می‌کرد.
- ⚠️ **`bind` اشتراک نمی‌سازد.** حسابِ بی‌اشتراک بند می‌شود و
  `license: null` می‌گیرد — که درست است، نه نیمه‌کاره. باز شدنِ قفل‌ها
  کارِ پنلِ سرور است. مجوزِ **خالی** هم روی تنظیمات می‌نشیند، چون نگه
  داشتنِ مجوزِ کهنه یعنی قفلی که تا ده روز باز مانده.
- ⚠️ **دستگاهِ بند‌شده دوباره بند نمی‌شود** (`if (!Activated …)`): توکنِ
  تازه توکنِ کهنه را باطل می‌کند و دو نصب از یک پوشه بی‌صدا هم را بیرون
  می‌انداختند.
- ⚠️ سنجهٔ «پمپی هست؟» (`home`) در `HomeFromAccountAsync` از بالا به
  **پایینِ** بند شدن رفت: پمپی که هنوز سرورِ خانگی‌اش را معرفی نکرده
  `home` ندارد و با ترتیبِ قبلی همان‌جا برمی‌گشتیم — پس نصبِ تازه
  هیچ‌وقت بند نمی‌شد. نشانیِ خانگی رفاه است، اشتراک اصل.

**سمتِ سرور** (`vil3ntec-it/shop` ⇒ `docs/PUMP-fa.md`): `profit` و
`history` به کاتالوگ آمدند، `dashboard` از هستهٔ همیشه‌باز بیرون آمد،
`cloudbackup` کلیدِ **جدا** از `cloud` گرفت (استاندارد پشتیبان دارد و
کیو‌آر نه)، کلِ دفتر رایگان شد، و سه پلن با قیمتِ دالریِ خودِ صاحب ریپو
ساخته شد. دو باگِ پولی هم همان‌جا بسته شد: فهرستِ پلنی که با `days`
برداشته نمی‌شد، و کادرِ پلنِ پمپ که از فهرستِ **دکان** پر می‌شد.

آزمون: `CloudSessionTests` (هفت سنجهٔ تازه برای `bind`) این‌جا، و
`test/pump-plans.test.js` (۲۵ سنجه) روی `shop`.

## 🔔 خبرِ پمپ روی ابر هم می‌رود — «برنامه بسته هم باشد، پیام برسد»

خواستهٔ صاحبِ ریپوی `shop` (همان سامانه): «ببین برنامه‌ها جوری استن که
بسته هم باشن هر اتفاقی که تو برنامه بوفته کم‌بودی یا هر چی به سرور ارسال
میشه و سرور وقتی که برنامه‌ها بسته هم باشن براشون میده پیام‌ها رو.»

⛔ **تا امروز فهرستِ هشدار فقط به سرورِ خانگی می‌رفت** (`live.alerts`) و
گوشیِ کارمند هر پانزده دقیقه از **همان شبکه** می‌پرسید. یعنی صاحبِ پمپی
که بیرون بود — یا مودمش خاموش بود — هیچ‌وقت خبر نمی‌گرفت.

```
StationSnapshot.Alerts ──▶ CloudEvents ──▶ POST /api/pump/device/events
                                        ──▶ ابر گوشی را بیدار می‌کند
```

- ⛔ **فهرست جای دیگری ساخته نمی‌شود**: ورودیِ `CloudEvents` همان
  `StationSnapshot.Alerts` است که رنگِ کارتِ قرض‌دار هم از آن می‌آید.
  قاعدهٔ جدا ننویسید، وگرنه روزی کارت سرخ است و گوشی ساکت.
- ⛔ **کلیدِ هر خبر زمانِ شروعِ همان دوره را هم دارد** (`k:<since>`). سرور
  با `clientId` ردیفِ تکراری نمی‌سازد — که برای «هر بیست ثانیه دوباره
  نفرست» درست است ولی برای «حسابِ خراب‌شدهٔ دوباره» غلط می‌شد: کلیدِ ثابت
  یعنی بارِ دوم برای همیشه ساکت. همان قاعدهٔ گیرندهٔ `kar/`.
- ⛔ **خبر پشتِ اشتراک نیست** (`newsOk = CloudActivated`) — همان قاعدهٔ
  خودِ سرور: خبر پیام است، نه دادهٔ فروشی. ⚠️ ولی قفلِ **عکسِ زنده** سرِ
  جایش ماند؛ آن یکی واقعاً اشتراک می‌خواهد.
- ⚠️ نرفتنِ خبر کلیدش را پس می‌گیرد تا دورِ بعد دوباره برود، و هیچ‌وقت
  استثنا بیرون نمی‌دهد.
- آزمون: `CloudEventsTests` (۱۲ سنجه) و `test/pump-events.test.js` روی
  `shop` (۲۴ سنجه).

## 🪪 اپِ کارمندان می‌گوید کدام برنامه است (از ۱۴۰۵/۰۷/۰۱)

⛔ **باگی که «صاحبِ پمپ هستم» را کاملاً مرده کرده بود.** سرورِ مرکزی
نشست را به بخشِ برنامه مهر می‌زند (`tokens.app`) و توکنِ یک بخش در بخشِ
دیگر **پیدا نمی‌شود**. `kar/cloud.js` هیچ‌وقت نمی‌گفت کیست، پس نشستش
«دکان» می‌شد و همان لحظه `GET /api/pump/me` می‌گفت «چنین نشستی نیست» —
کارمند گوگل را رد می‌کرد و اپ بی هیچ توضیحی برمی‌گشت سرِ خانهٔ اول.

- حالا `APP_ID = 'tohid-pump-app'` روی **هر** درخواست به‌شکلِ هدرِ
  `X-App-Id` می‌رود، و `app: 'pump'` در بدنهٔ ورود هم هست (سرورِ قدیمی
  هدر را نمی‌خواند).
- سمتِ سرور یک جای تصمیم دارد: `lib/tenancy.appOfRequest`.
- آزمون: بخشِ «ابر و ورود» در `tools/check-kar-bot.mjs` با `fetch`ِ
  ساختگی — بدنه، هدر، و «هر درخواستِ دیگری هم شناسه را می‌برد».

## 🧪 سنجهٔ `verify` از امروز **پلن دارد** — و حالتِ بی‌پلن هم می‌سنجد (از ۱۴۰۵/۰۶/۳۱)

⛔ **`scroll-audit` روی `main` سرخ شد و حق داشت.** با آمدنِ شش دروازهٔ پلن
(۳.۱.۱۲۹)، سنجهٔ `verify` — که در یک پوشهٔ موقتِ **خالی** بالا می‌آید،
یعنی «بی‌اشتراک» — دو رفتارِ **سالم** را «✖» دید:

| بند | چه دید | حقیقت |
|---|---|---|
| ۵) تاریخچهٔ گاوصندوق | باز نشد (`safe` ماند) | `history` پلن‌دار است |
| ۱۶) پیامِ آمادهٔ واتساپ | خالی | `kar` پلن‌دار است |

⚠️ سبز بودن روی `908ea2b` و سرخ شدن روی `7e50175` همان‌جا نشانش داد — پس
حدس نبود.

- ⛔ **راهِ درست ضعیف کردنِ سنجه نبود.** `UiTests/FakeLicense.cs` یک
  جفت‌کلیدِ ES256 در همین فرآیند می‌سازد، مجوز را **واقعاً** با آن امضا
  می‌کند و همان کلید را در تنظیمات قفل می‌کند — دقیقاً کارِ نخستین
  فعال‌سازیِ واقعی. هیچ قفلی باز نشد: کلیدِ خصوصی از حافظهٔ سنجه بیرون
  نمی‌رود و روی برنامهٔ واقعی `key_mismatch` می‌گیرد.
- ⚠️ **و باید پیش از ساختنِ `MainViewModel` صدا زده شود.** آن ویومدل
  نسخهٔ خودش از `AppSettings` را نگه می‌دارد و با ذخیرهٔ «آخرین بخش» همان
  را روی دیسک می‌نویسد، پس هر نوشتنی بعد از ساختنش همان لحظه پاک می‌شود.
- **بندِ ۱۷ تازه است و چهار قاعدهٔ `PLANS-fa.md` را با رفتار می‌سنجد**:
  قفل واقعاً می‌بندد · دلیل دارد (بی‌صدا نیست) · داده و کدِ پمپ روی دیسک
  پاک نمی‌شوند · و با برگشتنِ پلن همان لحظه باز می‌شود. ابزارش
  `Entitlements.TestDeny` است که **فقط می‌بندد**، پس راهِ دور زدن نمی‌شود.
- ⚠️ **صفحه‌ای که از قبل باز است دوباره فعال نمی‌شود**، پس `Show()` نمی‌دود
  و سنجه یک قدم عقب می‌ماند (سبزِ دروغ). بندِ ۱۷ صریح
  `apps.OnActivatedAsync()` را می‌زند، نه `ShowSubCommand` را.
- ⛔ **و ابرِ ساختگیِ `cloudlogin` هم فهرستِ شش‌تایی می‌دهد** (`FakeLicense.VipFeatures`).
  با فهرستِ سه‌تایی، یک مشتریِ **وی‌آی‌پی** «مفاد/ضرر» و «تاریخچه‌ها» و
  «داشبورد» را قفل می‌دید و هیچ سنجه‌ای قرمز نمی‌شد.

⛔ **و همان قفل، سنجهٔ `parcha` را هم گرفته بود** — پشتِ سرخیِ `verify`
پنهان مانده بود (کارِ بعدیِ همان job اصلاً نمی‌دوید). سنجهٔ «پهنای هر
بخش» سه بخشِ `dashboard` · `profit` · `history` را «باید پهن می‌بود، ۱۰۰۰
است» می‌دید، چون `GoAsync` قفلشان را می‌خورد و صفحه‌شان هیچ‌وقت چیده
نمی‌شد.

پس **هر سنجهٔ رابطی که پنجره می‌سازد، با نصبِ پلن‌دار می‌دود** — یک خطِ
`FakeLicense.Grant()` درست بعد از `AppHost.Start`. سه استثنا عمدی‌اند و
باید بمانند:

| سنجه | چرا بی پلن |
|---|---|
| `cloudlogin` | خودش فعال‌سازی را از صفر می‌سنجد؛ پلنِ آماده کارش را بی‌معنا می‌کرد |
| `serverdot` | صریحاً می‌سنجد که چراغِ سرور **بی اشتراک هم** سبز می‌شود |
| `startup` | فقط زمانِ باز شدن را می‌سنجد و به هیچ بخشِ پلن‌داری نمی‌رود |

⚠️ **و قفل‌ها بی‌سنجه نماندند**: بندِ ۱۷ی `verify` با `TestDeny` هر چهار
قاعده را می‌سنجد و `EntitlementsTests` مرزِ پلن‌ها را. یعنی «باز» و
«بسته» هر دو سنجیده می‌شوند، نه این‌که یکی از قلم بیفتد.

⚠️ **و `grid-perf` که سرخ مانده، مالِ این کار نیست**: «باز کردنِ حسابی با
۵۰٬۰۰۰ ردیف» روی هر برنچی که امروز دوید از سقفِ ۳٬۰۰۰ms گذشت — روی
برنچِ سیزنِ دیگر **۱۲٬۳۳۵ms** و این‌جا ۷٬۲۳۸ms. همان کارِ نیمه‌تمامی است
که از ۱۴۰۵/۰۶/۲۶ در همین فایل نوشته شده («مجازی‌سازیِ واقعی با اسکرولِ
صفحه هنوز نوشته نشده»). ⛔ سقفش را بالا نبرید و بی عدد حدس نزنید — قاعدهٔ
«اول بساز که ببینی» سرِ جایش است و این سندباکس `dotnet` ندارد.

## ⛔ «سرور جواب نداد (۴۰۴)» — پیامی که هیچ کاری دستِ کاربر نمی‌داد (از ۱۴۰۵/۰۷/۰۱)

گزارشِ صاحب ریپو با عکس: زیرِ دکمهٔ «ساختنِ حساب و ادامه» نوشته می‌شد
«❌ سرور جواب نداد (404)» و ثبت‌نام انجام نمی‌شد.

سنجیده شد، حدس زده نشد — سرورِ `shop` **محلی** با PostgreSQLِ واقعی بالا
آمد و همان درخواستِ برنامه (با همان بدنه و همان هدرِ `X-App-Id`) زده شد:

| سرور | `POST /api/auth/register/start` |
|---|---|
| نسخهٔ امروز (۲.۵.۵) | **۲۰۱** — کد به ایمیل می‌رود |
| نسخهٔ کهنهٔ ۲.۰.۰ (پیش از ثبت‌نامِ سه‌پله‌ای) | **۴۰۱** با JSONِ فارسی «احراز هویت لازم است» |

⚠️ **پس هیچ نسخه‌ای از سرورِ نودِ ما ۴۰۴ِ بی‌بدنه نمی‌دهد**: مسیرِ نبوده
همیشه JSON می‌گیرد (`notFoundHandler` ⇒ «این مسیر وجود ندارد»). یعنی آن
۴۰۴ از **خودِ سرور نیامده** — چیزی جلوی آن نشسته (پروکسی / تونل / دامنه‌ای
که به سرورِ حساب نمی‌رسد). این را فقط `/api/health` می‌تواند روشن کند.

- ⛔ **پیامِ «سرور جواب نداد» برای ۴۰۴ برداشته شد.** سرور **جواب داده بود**؛
  گفته بود «این مسیر را ندارم». `CloudLink.NoRouteAsync` حالا از
  `/api/health` می‌پرسد و دو حالِ واقعی را جدا می‌گوید:
  - سرور بالا است ⇒ «سرورِ حساب (نسخهٔ X) این مسیر را ندارد — سرورِ حساب را
    به‌روز کنید» (نسخهٔ واقعیِ خودِ سرور در پیام است، پس دیگر حدس لازم نیست)
  - سرور جواب نمی‌دهد ⇒ «به سرورِ حساب نرسیدیم — سرور بالا نیست یا درخواست
    به آن نمی‌رسد»
- ⛔ **«برنامه را به‌روز کنید» هم غلط بود و رفت**: برنامه تازه است و سرور
  کهنه؛ آن جمله کاربر را دنبالِ نخودِ سیاه می‌فرستاد.
- ⛔ **هر شش مسیرِ حساب همین را می‌گیرند**، نه فقط ثبت‌نام:
  `register/start` · `register/verify` · `register/complete` · `login` (و
  گوگل) · `password/forgot` · `password/reset`. پیش از این تنها `AuthAsync`
  (ورود) ۴۰۴ را می‌فهمید و پنج مسیرِ دیگر پیامِ خام می‌دادند — و دقیقاً
  یکی از همان پنج تا بود که کاربر دید.
- ⛔ **کدِ بدِ دستگاه دست‌نخورده ماند**: `/api/pump/device/*` برای کدِ غلط
  ۴۰۴ِ **بادار** می‌دهد («این کد پیدا نشد») و آن پیام باید همان بماند —
  پس `NoRouteAsync` فقط روی مسیرهای `‎/api/auth/*‎` است.
- ⚠️ **نامِ میزبان در هیچ‌کدام نمی‌آید** (آزمونِ
  `Payame_404_NameMizban_RaNemibarad`) — همان قاعدهٔ همیشه.
- آزمون: پنج سنجهٔ تازه در `CloudSessionTests` و `/api/health` در ابرِ
  ساختگیِ `cloudlogin`.

### 🔧 و کارِ خودِ سرور — این بخش کدی نیست

⚠️ **این اصلاح پیام را درست می‌کند، ثبت‌نام را نه.** تا وقتی
`api.vill3n.top` مسیرِ `register/start` را جواب ندهد، حساب ساخته نمی‌شود.
دو نشانی که در مرورگر جواب را می‌دهند:

```
https://api.vill3n.top/api/health
  ⇒ JSONی با version   ⇒ سرور بالا است. version را با server/package.json بسنجید
  ⇒ صفحهٔ HTMLِ ۴۰۴    ⇒ درخواست به سرورِ حساب نمی‌رسد (دامنه/پروکسی)

https://api.vill3n.top/api/auth/register/start
  ⇒ JSONِ فارسی («احراز هویت لازم است») ⇒ مسیر به سرور می‌رسد
  ⇒ هر چیزِ دیگری                       ⇒ نمی‌رسد
```

⛔ **و سندباکسِ سیزن‌های Claude به `api.vill3n.top` نمی‌رسد**
(`CONNECT tunnel failed, 403`)، پس این دو را فقط خودِ صاحب سامانه می‌تواند
ببیند. به‌روزرسانیِ سرور: `cd shop/server && ./update.sh` (ریپوی `shop`،
`server/README.md`).

## 🔌 «چرا به سرور وصل نمی‌شوند» — و چراغی که دروغ نمی‌گوید (از ۱۴۰۵/۰۷/۰۱)

خواستهٔ صریحِ صاحب ریپو: «ببین برنامه‌ها چرا به سرور وصل نمی‌شوند، مشکلشان
را حل کن و در جا خودکار وصل شوند — و هیچ کلکِ دروغی نباشد که بگوید وصل است.»

گشتنِ خودِ کد دو چیزِ واقعی داد:

- ⛔ **برنامه هیچ‌وقت خودش به ابر وصل نمی‌شد.** هر شش جایی که `CloudLink`
  ساخته می‌شود پشتِ `CloudDeviceToken` بود، و آن توکن **فقط** وقتی می‌آمد
  که کاربر صفحهٔ **پروفایل** را باز کند. یعنی نصبی که حساب داشت ولی پروفایل
  را ندیده بود تا ابد بند نمی‌شد: نه اشتراکش می‌آمد، نه مجوزش، نه خبری از
  ابر — و برنامه هم هیچ نمی‌گفت.
- ⛔ **«وارد شده‌اید» از روی فایلِ روی دیسک گفته می‌شد.**
  `SignedIn = CloudAccountToken != ""` یعنی «توکنی این‌جا هست»، نه «ابر
  پذیرفتش». پس صفحه با اطمینان می‌گفت وارد شده‌اید حتی وقتی برنامه **هرگز**
  با ابر حرف نزده بود. همان «کلکِ دروغ».

### چه شد

```
حلقهٔ پس‌زمینه ─┬─ هر ۵ث  KeepLinkAsync      سرورِ خانگی  (چراغِ اول)
               └─ هر ۶۰ث CloudKeepAsync     ابر         (چراغِ دوم)
                          ├─ توکنِ حساب هست ⇒ HomeFromAccountAsync
                          │   (نشست تازه · bind خودکار · اشتراک · مجوز · نشانیِ خانه)
                          └─ نیست ⇒ CloudHealthAsync   (بی توکن، فقط «بالا است؟»)
```

- ⛔ **`CloudLink.Reach` تنها جای حقیقت است** و فقط از `SendFull` پر می‌شود،
  یعنی از جوابِ **واقعیِ** سرور. سه حال دارد و «هنوز نپرسیده‌ایم»
  (`Unknown`) عمداً **سبز نیست**.
- ⛔ **«سرورِ ما جواب داد» با «چیزی جواب داد» یکی نیست**: ۲xx یا خطایی به
  شکلِ خودمان (`error.code`) ⇒ وصل؛ پاسخِ بی‌شکل (همان ۴۰۴ِ اچ‌تی‌ام‌الِ
  پروکسی) ⇒ **نرسیدیم**. سبز کردنِ چراغ با جوابِ پروکسی دقیقاً همان دروغی
  است که قدغن شد.
- ⛔ **قطعی، آخرین تاییدِ واقعی را پاک نمی‌کند** (`CloudOkAt`): «الان
  نمی‌رسیم» با «هیچ‌وقت نرسیدیم» دو چیزند و کاربر باید فرقشان را ببیند.
- ⛔ **بی‌اینترنت کسی را از حسابش بیرون نمی‌اندازد** — قاعدهٔ ۱۴۰۵/۰۶/۳۰ سرِ
  جایش است. چراغ سرخ می‌شود، توکن نمی‌رود.
- **چراغِ دوم در سربرگ** کنارِ چراغِ سرورِ خانگی (`CloudDotBrushKey`)، با
  همان قاعده‌ها: دلیل فقط در `ToolTip`، **هیچ نام و نشانیِ سروری** نوشته
  نمی‌شود، و کلیک = «همین حالا بپرس».
  ⚠️ دو چراغ یعنی دو سرورِ جدا (خانگی ≠ ابر) و این عمدی است: تا دیروز یک
  سبز دیده می‌شد و کاربر گمان می‌کرد هر دو وصل‌اند.
- پروفایل هم همان را می‌گوید (`CloudNote()`): «✅ سرورِ حساب تایید کرد ·
  آخرین تماس: HH:mm» یا «⚠️ نشست هست ولی نمی‌رسیم» یا «⏳ هنوز نپرسیده‌ایم».
- ⚠️ **شصت ثانیه، نه پنج**: این تیک واقعاً اینترنت می‌زند؛ تیکِ پنج‌ثانیه‌ایِ
  سرورِ خانگی رایگان است و آن یکی باید همان بماند.
- آزمون: `CloudReachTests` — چهار حالِ واقعیِ پاسخ، نگه داشتنِ آخرین تایید،
  و سه سنجهٔ سورس (حلقه واقعاً سراغِ ابر می‌رود · چراغ از `Reach` می‌خواند و
  پیش‌فرضش خاکستری است · پروفایل از روی دیسک ادعا نمی‌کند).

⚠️ **و این هنوز سرورِ شما را به‌روز نمی‌کند**: اگر `api.vill3n.top` مسیرها را
نداشته باشد، حالا برنامه **راستش را می‌گوید** («به سرورِ حساب نمی‌رسیم»)
به‌جای این‌که ساکت بماند. به‌روزرسانیِ سرور همان `cd shop/server && ./update.sh`
است.

## 🚪 «لاگین‌ها کار نمی‌کنند» — و دو حلقهٔ گم‌شده (از ۱۴۰۵/۰۷/۰۲)

گزارشِ صاحب ریپو: «لاگین‌ها روی اپ‌ها وصل نمی‌شوند و کار نمی‌کنند هیچ‌کدام‌شان…
لاگینِ هر دو برنامه را حرفه‌ای درست کن و باید با تست و عملی ببینی کار می‌کند یا
نه، تا آخرِ مرحله‌ها.»

پس این بار سرورِ `shop` **واقعاً** بالا آمد (PostgreSQLِ واقعی، نسخهٔ ۲.۵.۵) و
همان درخواست‌هایی که `CloudLink.cs` می‌زند — با همان بدنه و همان هدرها — روی آن
بازپخش شد. نتیجه، بی تعارف:

| مرحله | حال |
|---|---|
| `register/start` · `verify` · `complete` | ✅ ۲۰۱، نشست می‌آید |
| `login` (رمزِ غلط و درست) · `refresh` · `logout` | ✅ |
| `POST /api/pump/device/bind` | ❌ **۴۰۴ِ `no_station`** |
| `device/license` · `device/access-code` | ❌ ۴۰۱، چون توکنِ دستگاه نیامد |

⛔ **پس خودِ ورود سالم بود و «مرحلهٔ بعد» نبود.** حسابِ تازه **هیچ پمپی ندارد**
(`/api/pump/me` می‌گوید `station: null`)، و تنها راهِ ساختنِ پمپ در برنامه کدِ
شش‌رقمی بود — همان کدی که صاحب ریپو صریح گفت در کار نیست («اشتراک رو من به
حسابِ یارو از سرور می‌دم»). یعنی کاربرِ تازه روی گامِ دوم گیر می‌کرد.

- ⛔ **`CloudLink.EnsureStationAsync(name)` تنها راهِ ساختنِ پمپ است**:
  `/api/pump/me` ⇒ اگر پمپ نداشت `POST /api/pump {name}` ⇒ بعد `BindAsync`.
  `already_member` خطا نیست (دو کلیک، یا دستگاهِ دیگری که زودتر ساختش).
- ⛔ **خودکار نیست و نباید بشود** — فقط از دکمهٔ خودِ کاربر صدا زده می‌شود، نه
  از حلقهٔ شصت‌ثانیه‌ایِ `StationPublisher`. «هر حساب یک پمپ» قاعدهٔ سرور است و
  ساختنِ بی‌خبر یعنی حسابی که دیگر نمی‌تواند به پمپِ واقعی‌اش بپیوندد.
- ⛔ **و از ۱۴۰۵/۰۷/۰۴ کد کاملاً از گامِ پمپ رفت** — بخشِ «کدِ دوم» در
  انتهای همین فایل مرجع است، نه این خط. (متنِ قدیمی:) کدِ شش‌رقمیِ تمدید همان راهِ همیشگی‌اش
  را دارد (`RedeemAsync`). کدِ **نصفه** همچنان خطاست، کدِ **خالی** نه.
- ⛔ **قفلِ کلیدِ عمومی (TOFU) این‌جا هم زنده است** — ساختنِ پمپ آن را دور
  نمی‌زند (`EnsureStation_KelideDigar_Ra_RadMikonad`).

### اپِ کارمندان: «صاحبِ پمپ هستم» فقط گوگل بود

⛔ **و همان هم روی سرورِ واقعی خاموش است**: `/api/config` را زدیم و
`googleClientId` **خالی** بود (پیش‌فرضش همین است)، پس صفحه فقط می‌گفت «ورود با
گوگل روی این سرور تنظیم نشده است». و مهم‌تر: حسابی که در برنامهٔ کامپیوتر با
**ایمیل و رمز** ساخته می‌شود اصلاً حسابِ گوگلی نیست — همان آدم روی گوشی‌اش وارد
نمی‌شد، در حالی که قاعدهٔ «یک سرور، یک حساب» می‌گوید باید بتواند.

- `kar/cloud.js`: `signInWithPassword` · `forgotPassword` · `resetPassword` —
  همان `‎/api/auth/login‎`ِ برنامهٔ کامپیوتر، با `app: 'pump'` در بدنه **و**
  هدرِ `X-App-Id`. ⛔ راهِ گوگل و راهِ کیو‌آر هیچ‌کدام برداشته نشدند.
- ⚠️ **شناسهٔ `inPass` مالِ صفحهٔ قفل است** — کادرِ رمزِ ورود `inPw` است.
  شناسهٔ تکراری یعنی یکی از دو کادر بی‌صدا از کار می‌افتد؛ همین یک بار پیش آمد
  و سنجه‌اش گرفتش.
- ⚠️ رمز نه در نشست می‌نشیند و نه در کادر می‌ماند.

### و آن‌چه کارِ سرور است، نه کارِ کد

⚠️ اگر سرویسِ ایمیلِ سرور تنظیم نشده باشد، `register/start` همان‌جا
`delivery_failed` می‌دهد و برنامه پیامِ خودِ سرور را نشان می‌دهد («سرویس ایمیل
سرور تنظیم نیست»). این باگِ برنامه نیست و با کد درست نمی‌شود — SMTP در پنلِ
مدیریت تنظیم می‌شود.

⚠️ و سندباکسِ سیزن‌های Claude به `api.vill3n.top` نمی‌رسد
(`CONNECT tunnel failed, 403`)، پس همهٔ این‌ها روی سرورِ **محلی** سنجیده شدند.
به‌روزرسانیِ سرورِ زنده: `cd shop/server && ./update.sh`.

**آزمون‌ها**: پنج سنجهٔ تازه در `CloudSessionTests` · بندِ ۱۸ در
`dotnet run --project PumpYaqobi.UiTests -c Release -- cloudlogin` · بخشِ
«ورود با ایمیل و رمز» در `node tools/check-kar-bot.mjs` (۱۱ بند) · و بندِ ورود
در `node tools/check-kar-ui.mjs` (کرومیومِ واقعی).

## 🐌 حسابِ ۵۰٬۰۰۰ ردیفی: ۱۴ ثانیه برای نوشتنِ یک `bool` (از ۱۴۰۵/۰۷/۰۱)

سنجهٔ `perf` ماه‌ها یک خطِ سرخ داشت و هیچ‌کس ریشه‌اش را نمی‌دانست:
«باز کردنِ حسابی با ۵۰٬۰۰۰ ردیف — ۱۴٬۲۶۰ms» (سقف ۳٬۰۰۰). در همین فایل
نوشته شده بود که این همان «مجازی‌سازیِ واقعیِ کارت‌ها»ی نیمه‌تمام است. **نبود.**
حدس بود، و غلط.

### سنجهٔ تازه: `personperf`

```
dotnet run --project PumpYaqobi.UiTests -c Release -- personperf
dotnet run --project PumpYaqobi.UiTests -c Release -- personperf trace   ← فقط حسابِ بزرگ، برای نمونه‌بردار
```

یک عددِ سربسته «کجا» را نمی‌گوید، پس این یکی همان کار را با **شش اندازه**
(۵۰۰ تا ۵۰٬۰۰۰ ردیف در یک حساب) می‌سنجد و هر بار سه تکه‌اش را جدا می‌کند:
خواندنِ دیتابیس · ساختنِ ویومدل · چیدمان. ستونِ آخرش تصمیم‌گیر است —
**میکروثانیه به ازای هر ردیف**: ثابت بماند یعنی خطی، بالا برود یعنی جایی
کارِ مربعی هست.

| ردیف | خواندن | ویومدل | چیدمان | همه | ردیفِ زنده |
|---|---|---|---|---|---|
| ۲۰٬۰۰۰ | ۳۱۵ms | ۲٬۱۴۹ms | ۵۳ms | ۲٬۵۱۷ms | ۲۵ |
| ۵۰٬۰۰۰ | ۴۹۰ms | ۱۲٬۱۹۵ms | ۵۵ms | ۱۲٬۷۴۰ms | ۲۵ |

⚠️ **ردیفِ زنده در هر شش اندازه ۲۵ بود.** یعنی پنجرهٔ چسبان سالم است و
**چیدمان بی‌گناه**. هزینه در ساختِ ویومدل بود — و آن‌جا هم نه در ساختنِ
ردیف‌ها: ساختنِ هر ۵۰٬۰۰۰ `DebtRowViewModel` روی هم **۲۱ میلی‌ثانیه** است.

### ریشه: `Attach` گرافِ حساب را می‌پیماید

⛔ **`DbSet.Attach(a)` را روی حسابی که ردیف‌هایش در حافظه‌اند نزنید.**
`Attach` گرافِ موجودیت را می‌پیماید، پس هر `DebtRow`ی که در `a.FuelRows` و
`a.MoneyRows` نشسته ردیابی می‌شود و بعد `SaveChanges` رویشان `DetectChanges`
می‌دود. یعنی مهرِ یک‌باره‌ی «مهاجرت شد» — که فقط **یک ستونِ `bool`** را عوض
می‌کند — روی حسابی که تازه از `LoadFullAsync` آمده **۱۲٬۴۵۶ میلی‌ثانیه** طول
می‌کشید. و خطی هم نبود: ۲۰٬۰۰۰ ردیف ۲٬۲۰۲ms، ۵۰٬۰۰۰ ردیف ۱۲٬۴۵۶ms — دو و
نیم برابر ردیف، پنج برابر وقت.

- `DebtorService.MarkOnly(db, entity)` تنها راهِ ذخیرهٔ خودِ حساب یا خودِ شخص
  است (`UpdateAccountAsync` · `UpdateDebtorAsync` · `CommitReceiptMigrationAsync`).
- ⚠️ **`AutoDetectChangesEnabled = false` لازم است، نه تجمل**: بی آن،
  `Entry(...)` و `SaveChanges` هر دو `DetectChanges` می‌زنند و همان پیمایشِ
  گراف از درِ دیگر برمی‌گردد.
- ⚠️ و چرا بی‌خطر است: `State = Modified` همهٔ ستون‌های خودِ موجودیت را
  «عوض شده» می‌کند، پس `UPDATE` همان است که بود. ردیف‌ها از این در ذخیره
  نمی‌شدند (`SaveRowAsync` کارِ خودش را دارد) — فقط شمرده و پیموده می‌شدند.
- آزمون: `AccountUpdateTests` — سه بند که **رفتار** را قفل می‌کنند نه سرعت
  را: عددهای حساب می‌نشینند، و ردیفی که در حافظه دست‌کاری شده به دیسک
  **نمی‌رود**.

نتیجه، با همان سنجه: حسابِ ۵۰٬۰۰۰ ردیفی **۱۳٬۴۸۵ ⇒ ۱٬۰۴۸ms**، و شمارِ
دستورِ دیتابیسش ۶۰ ⇒ ۱۶ (همان عددِ همهٔ اندازه‌های دیگر). خطِ سرخِ `perf`
هم سبز شد: ۱۴٬۲۶۰ ⇒ ۱٬۴۱۷ms.

### ⚠️ دو درسِ روش، که از خودِ اصلاح مهم‌ترند

- ⛔ **یک نظریهٔ دیگر آزموده و رد شد**: «`Totals` با هر خبرِ `RefreshTotals`
  کلِ ردیف‌ها را از نو می‌سازد و می‌پیماید» — درست بود ولی **هزینه نبود**.
  کَش کردنش عدد را تکان نداد (۱۲٬۷۴۰ ⇒ ۱۳٬۵۹۶، یعنی در حدِ نوسان)، پس
  **برگردانده شد**. قاعدهٔ «تغییری که سودش با عدد ثابت نشود نمی‌ماند» سرِ
  جایش است، حتی وقتی تغییر «درست» به نظر می‌رسد.
- ⚠️ **نمونه‌بردار همیشه جواب نمی‌دهد**: `dotnet-trace` روی این سنجه در
  سندباکس ماند و هیچ خروجی نداد. راهی که جواب داد، شمارشِ **تکه‌به‌تکه** با
  `Stopwatch` داخلِ خودِ مسیر بود. مهم این نیست که کدام ابزار — مهم این است
  که عدد از خودِ کد بیاید، نه از حدس.

## ⚠️ `idle` از ۳.۱.۱۳۳ سرخِ دروغ می‌داد — و چرا (از ۱۴۰۵/۰۷/۰۱)

با آمدنِ `FakeLicense.Grant()` در `IdleAudit` (۳.۱.۱۳۳)، قفلِ اشتراک دیگر
جلوی `StationPublisher` را نمی‌گرفت، پس تیکِ بیست‌ثانیه‌ایِ آن شروع کرد به
خواندنِ دیتابیس — و سنجه آن را به گردنِ **هر بخشی** می‌انداخت که همان لحظه
باز بود. یک اجرا «invoices» و «history» سرخ می‌شدند، اجرای بعد «settings».

⛔ **این سرخی جای اشتباه را نشان می‌داد و ربطی به بخش‌ها نداشت.** حالا
`idle` پیش از سنجش حلقهٔ عکسِ ایستگاه را صریح خاموش می‌کند — درست به همان
دلیلی که `VACUUM INTO`ی `BackupPusher` از شمارش بیرون است: پرسشِ این سنجه
«بخشی که تویش نیستم چه مصرفی دارد» است، نه «کلِ برنامه در سه ثانیه چه
می‌کند». هزینهٔ خودِ آن حلقه جای دیگری سنجیده می‌شود (`years` و `startup`).

⚠️ و همین یکی دوباره نشان داد که سنجهٔ سرخ را باید **اول روی `main`** دواند:
روی `main` هم سرخ بود، با بخش‌های دیگر.

⛔ **و خاموش کردنش نباید نخِ رابط را ببندد.** بارِ اول همان‌جا
`pub.DisposeAsync().AsTask().GetAwaiter().GetResult()` نوشتم و سنجه **برای
همیشه ماسید**: ۵۸۰ ثانیه دوید، صفر بایت چاپ کرد و هیچ‌وقت بیرون نیامد. چون
حلقهٔ خودِ `StationPublisher` ‎await‎هایش را روی همان نخِ رابط ادامه می‌دهد،
بستنِ هم‌زمانش یعنی نخِ رابط منتظرِ کاری می‌ماند که فقط خودش می‌تواند انجام
دهد. حالا به `Task.Run` سپرده می‌شود و این‌جا فقط چند فریم پمپ می‌شود.

⚠️ و نشانه‌اش گمراه‌کننده است: خروجیِ ‎.NET‎ وقتی به فایل می‌رود بافر می‌شود،
پس «صفر بایت» یعنی «هنوز بیرون نیامده»، نه «هیچ کاری نکرده». برای دیدنِ
پیشرفتِ یک سنجهٔ ماسیده، ‎stdbuf -oL‎ بزنید یا به ترمینال بدهیدش.

## 🏠 «ابر» همان سرورِ خانگی است — و چرا هیچ لاگینی کار نمی‌کرد (از ۱۴۰۵/۰۷/۰۲)

گزارشِ صاحب ریپو: «چرا توی برنامه‌ها سیستمِ ابری است؟ مالِ من که سرورِ خانگی
است. هیچ‌کدام از برنامه‌ها لاگینشان کار نمی‌کند.»

ریشه در ریپوی `server` (سرورِ خانگی) پیدا و بسته شد، با سرورهای واقعی: تونلِ
`api.vill3n.top` به **پورتِ عمومیِ پنلِ خانگی** می‌رسد، ولی قراردادِ حساب و
اشتراکی که این برنامه با آن نوشته شده (`/api/auth/*` سه‌پله‌ای، `/api/pump/*`،
`device/bind`، مجوزِ ES256) مالِ `shop/server` است و **هیچ راهی از تونل به آن
نبود**. پس «سرور جواب نداد (۴۰۴)»ِ عکس، همان پنلِ خانگی بود که این مسیرها را
نداشت. حالا پنلِ خانگی یک **درگاه** دارد (`homelab-panel/server/src/api/account-proxy.js`)
که هرچه مالِ سرورِ حساب است را به `shop/server`ِ روی همان کامپیوتر می‌برد.

- ⛔ **نشانی همان `api.vill3n.top` می‌ماند و قفل است** — هیچ برنامه‌ای عوض نشد
  و نباید بشود. «ابر» فقط یک نام بود: همان سرورِ حساب است، روی همان کامپیوترِ
  خانگی، پشتِ همان تونل. در نوشته‌های دیدنیِ برنامه (چراغ، پروفایل، پیام‌های
  اتصال) «ابر» شد **«سرورِ حساب»**؛ نامِ کلاس‌ها و کلیدها (`CloudLink`,
  `CloudReach`, `Cloud*` در `AppSettings`) عمداً دست نخورد — تغییرِ اسم روی
  فایلِ تنظیماتِ مشتری‌ها یعنی حساب‌هایی که یک‌شبه گم می‌شوند. «کارهای ابری» و
  «بک‌اپِ ابری» هم نامِ قابلیت‌اند و همان ماندند.
- ⚠️ **دو سرور، دو چراغ**: چراغِ اول سرورِ خانگی (دفترِ زنده، شبکهٔ پمپ) و
  چراغِ دوم سرورِ حساب (همان تونل). هر دو روی یک کامپیوتر می‌نشینند ولی دو
  سرویس‌اند و دو در؛ یکی‌شان کردن غلط است.
- ⚠️ سنجشِ کاملِ همان زنجیره: `gw-e2e` — پنلِ خانگیِ واقعی ⇒ درگاه ⇒ `shop/server`ِ
  واقعی روی پستگرس؛ هر ۱۸ مرحلهٔ این برنامه و ۱۲ مرحلهٔ اپِ کارمندان در
  کرومیوم، از پورتِ عمومیِ پنل. ریپوی `server`: `npm run test:gateway`.

## 🔴 چراغِ سرورِ حساب با «سرورِ حساب روشن نیست» سبز نمی‌ماند (از ۱۴۰۵/۰۷/۰۲)

با عکس دیده شد: درگاهِ پنلِ خانگی وقتی خودِ shop/server خاموش است ۵۰۳ی با
شکلِ خودمان می‌دهد (`account_server_down`)، و چون شکلش «خطای سرورِ ما» بود
چراغِ دوم **سبز** می‌ماند در حالی که هر ورودی همین خطا را می‌گرفت.
`CloudLink.IsDownCode` آن دو کد (`account_server_down` ·
`account_server_unreachable`) را «نرسیدیم» می‌شمارد و `CloudWhy` همان پیامِ
درگاه را می‌گیرد. ⛔ جوابِ درگاه جوابِ سرورِ حساب نیست. سنجه:
`CloudReachTests.Dargah_Miguyad_Sarvare_Hesab_Khamush_Ast_Yani_Ghat`.


## ⏳ قفل‌های اشتراکِ برنامهٔ پمپ فعلاً **باز**اند — موقت (از ۱۴۰۵/۰۷/۰۲)

خواستهٔ صریحِ صاحب ریپو: «برنامهٔ پمپ بنزین رو فعلاً اون‌هایی که گفته بودم
قفل کن رو باز کن، بعداً می‌گم دوباره قفل کنی؛ همین‌ها رو الان لازم دارم قفل
نباشه.»

- ⛔ **یک خط، یک جا**: `Entitlements.UnlockedForNow = true` در
  `Services/Entitlements.cs`. تا این راست است هر شش دروازهٔ `Entitlements.Paid`
  (اپِ کارمندان · کیو‌آر · بک‌اپِ ابری · مفاد/ضرر · تاریخچه‌ها · داشبورد) برای
  همه باز است — با اشتراک، بی اشتراک، فعال‌نشده. هیچ چیزِ دیگری برداشته نشده.
- ⛔ **برای قفل کردنِ دوباره فقط همان یک مقدار را `false` کنید** — نه چیزِ
  دیگری. مجوز، ارفاق، فهرستِ پلن، پیام‌ها و همهٔ سنجه‌ها سرِ جایشان‌اند.
- ⚠️ آزمون‌های مرزِ پلن (`EntitlementsTests`، بندِ کلیدِ آزمایش در
  `AppLinksTests`) با `Entitlements.Unlocked = false` می‌دوند، پس مرزِ واقعی
  همچنان زیرِ آزمون است و روزِ قفل کردن غافلگیر نمی‌شوید. **برنمی‌گردانندش**
  (xUnit کلاس‌ها را موازی می‌دواند؛ برگرداندن وسطِ آزمونِ کلاسِ دیگر می‌افتاد)
  و هیچ آزمونی به «باز» بودنش تکیه ندارد.
- ⚠️ `Entitlements.TestDeny` (کلیدِ «آزمایشِ حالتِ بی‌اشتراک» در پروفایل) از
  این هم جلوتر است و همچنان می‌بندد — پس بندِ ۱۷ی `verify` و `serverdot` که
  با آن قفل را می‌سنجند، دست‌نخورده سبزند.

## 🔄 همگام‌سازیِ VILL3N Sync v1 — نیمهٔ برنامه (از ۱۴۰۵/۰۷/۰۴)

بندِ ۲۰ی پرامپت و بندهای ۱ تا ۱۴ی بخشِ ۲۲. سرور از قبل آماده بود و دست
نخورد؛ آن‌چه نبود، **نیمهٔ برنامه** بود. شرحِ کامل و هر انحراف با دلیلش:
`native/docs/SYNC-fa.md`.

```
کاربر می‌نویسد ─▶ PumpDbContext.SaveChanges ─▶ داده + یک ردیفِ SyncOps
                                                (همان تراکنش)
                 ─▶ Saved ⇒ Nudge (۵۰۰ms) ─▶ POST /api/sync/v1/push
هر ۳۰ث یا پیامِ «changed» از wss ─▶ GET /pull?since=cursor ─▶ SQLِ خام
```

- ⛔ **`PumpDbContext.Stamp()` تنها جای ساختنِ op است.** سی‌وچند سرویسِ
  داده همه سرِ آخر به همان `SaveChanges` می‌رسند، پس «Repositoryِ واحد»ِ
  پرامپت همان‌جاست، نه یک لایهٔ تازهٔ موازی. سرویسِ سی‌وسوم بی این‌که کسی
  چیزی یادش بماند opهایش را می‌نویسد.
- ⛔ **op و داده در یک تراکنش‌اند** — یا هر دو، یا هیچ‌کدام. برق که وسطِ
  کار برود، دفتر و داده از هم جلو نمی‌افتند.
- ⛔ **نوشتنِ خام op نمی‌سازد، و این عمدی است**: تنها جای نوشتنِ خام،
  اعمالِ opهای **رسیده** است (`SyncStore.ApplyIncoming`). اگر آن‌ها op
  می‌ساختند، هر تغییرِ دستگاهِ دیگر دوباره به سرور برمی‌گشت — پژواکِ
  بی‌پایان. در عوض `PumpDbContext.Bump()` دستی زده می‌شود.
- ⛔ **چهار جدول هیچ‌وقت نمی‌روند**: `AppUser` · `Setting` · `TrashItem` ·
  `AuditEntry` — همان چهارتایی که فهرستِ سفیدِ سرور هم ندارد. فرستادنشان
  یعنی رمزِ یک پمپ روی کامپیوترِ دیگری بنشیند.

### ⚠️ شناسه‌ها — انحرافِ عمدی از بندِ ۲، و چرا

بندِ ۲ می‌خواست همهٔ **کلیدهای اصلی** ULID شوند. **نشد و نباید بشود**:
بازنویسیِ کلیدِ سی‌وهفت جدولِ یک دفترِ پنج‌سالهٔ زنده یعنی بازسازیِ همهٔ
کلیدهای خارجی در یک مهاجرتِ یک‌طرفه — همان جایی که داده گم می‌شود.

به‌جایش `EntityBase.SyncUid` (رشته، اختیاری) کنارِ `Id` نشست:

- ردیفِ **تازه** ⇒ ULIDِ واقعی (`PumpYaqobi.Domain/Ulid.cs`، بی هیچ بستهٔ
  تازه‌ای، و در یک میلی‌ثانیه هم تکراری نمی‌سازد).
- ردیفِ **قدیمی** ⇒ **یک** `UPDATE` برای هر جدول: `'<ریشه>-' || Id`.
  ⛔ ریشه یک بار ساخته می‌شود (`SyncState.UidSeed`) و **هیچ‌وقت عوض
  نمی‌شود** — عوض شدنش یعنی همان ردیف‌ها بارِ دوم با شناسهٔ دیگری بروند،
  یعنی دفترِ سرور دو برابر.
- ⚠️ `string?` است نه `string`: ستون با `ALTER TABLE … ADD COLUMN` روی
  دفترِ مشتری می‌نشیند و ردیفِ کهنه تا لحظهٔ پر شدن `NULL` دارد؛ با نوعِ
  غیرقابل‌تهی، EF سرِ اولین خواندن می‌شکست.

### ⚠️ ترمزِ سرعت — قاعدهٔ همیشگی، این‌جا هم

- حلقه با `PumpDbContext.Version` ترمز می‌گیرد: داده عوض نشده، صف خالی و
  وقتِ pull نرسیده ⇒ **حتی یک `SELECT`** هم نمی‌زند.
- ⚠️ `_lastVersion` در **پایانِ** هر دور دوباره خوانده می‌شود، وگرنه
  ذخیرهٔ خودِ حالِ همگام‌سازی دورِ بعد را «داده عوض شد» می‌دید و حلقه تا
  ابد می‌چرخید.
- ⛔ **`SyncEngine.Disabled` در همهٔ آزمون‌ها و سنجه‌ها روشن است**
  (`UiTests/Program.cs` و `Tests/Parallelism.cs`). بی آن، هر سنجه‌ای که
  پنجره می‌سازد از راهِ `Lock.SignedIn` موتور را راه می‌انداخت و — چون
  `FakeLicense.Grant()` توکنِ دستگاه می‌نشاند — واقعاً به سرورِ **زندهٔ**
  اشتراک درخواست می‌زد، از رانرِ CI. برای `idle` هم یعنی خواندنِ دیتابیس
  به گردنِ بخشی که همان لحظه باز بود.
- ⚠️ **شمارِ دستورِ دیتابیس عوض می‌شود**: هر ردیفِ واقعاً تغییرکرده حالا یک
  `INSERT` در `SyncOps` هم دارد. سنجه‌هایی که عدد می‌شمارند (`enterperf`)
  ممکن است عددِ تازه ببینند — این پس‌رفت نیست، ولی اگر سرخ شد **اول روی
  `main` بدوانید**، بعد قضاوت.

### ⛔ ۴۲۶ یعنی «نگه دار»، نه «دور بریز»

نسخهٔ برنامه از سرور جلوتر ⇒ سرور **هیچ چیزی** ننشانده. `SyncState.Holding`
روشن می‌شود، چراغ زرد می‌ماند و دلیلش گفته می‌شود؛ هر دور یک opِ تکی محک
می‌زند که سرور به‌روز شده یا نه. ⛔ هر جدول یا فیلدِ تازه = یک پله بالاتر
در `OpLog.SchemaVersion` **و** در `sync-v1-migrations.js`، با همان شماره.

### ⚠️ `hash` عمداً فرستاده نمی‌شود

سرور آن را اختیاری گرفته و ناجورش **کلِ op را رد می‌کند**. اثرِ انگشتِ
`JSON.stringify`ِ جاوااسکریپت و `JsonSerializer`ِ دات‌نت روی عددِ اعشاری
مو‌به‌مو یکی نیست، پس فرستادنش فقط ریسکِ دادهٔ گم‌شده بود. هش محلی محاسبه
و نگه داشته می‌شود، برای سلامتِ خودِ دفتر.

### ورود با کدِ ایمیلی — درِ **سوم**، نه جانشین

`POST /api/auth/pump/request-code` ⇒ `GET …/request-status` ⇒
`POST …/verify`. راهِ ایمیل‌ورمز و ثبت‌نامِ سه‌پله‌ای **دست‌نخورده ماندند**؛
مشتری‌های امروز با همان وارد می‌شوند.

- شش خانه، پرشِ خودکار، Paste (از هر خانه‌ای، همیشه از اول پخش می‌شود)،
  **ارقامِ فارسی و عربی**، ارسالِ خودکار پس از رقمِ ششم، شمارشِ معکوس با
  عددِ `resend_after`ِ **خودِ سرور**.
- ⚠️ همهٔ تصمیم‌ها در `CodeBoxesViewModel` است و آزمون دارد؛ صفحه فقط
  فوکوس را جابه‌جا می‌کند. `Text` روی خانه‌ها `OneWay` است، وگرنه متنِ خام
  (رقمِ فارسی، شش رقمِ چسبانده) نرمال‌سازی را دور می‌زد.
- ⛔ پاسخِ `request-code` **همیشه** ۲۰۰ است مگر سقفِ نرخ، پس برنامه
  هیچ‌وقت نمی‌گوید «چنین حسابی نیست».

### ⚠️ تپش از `/api/pump/heartbeat` است، نه `/api/me/heartbeat`

آن یکی `requireUser`ِ **دکان** دارد و به توکنِ پمپ «چنین نشستی نیست»
می‌دهد. همان تابعِ سرور است (`portal.heartbeatHandler`)، فقط با هویتِ بخشِ
پمپ. ⛔ و هیچ عددِ قیمتی از آن نمی‌آید و در کلاینت نوشته نشده.

### چراغِ نوارِ پایین و قفلِ نرم

- سبز = همگام · زرد = در صف · خاکستری = بند نشده · سرخ = سرور خطا داد.
  ⛔ **هیچ نام و نشانیِ سروری در آن نوشته نمی‌شود** — همان قاعدهٔ دو چراغِ
  سربرگ؛ دلیل فقط در `ToolTip` و کلیک یعنی «الان همگام کن».
- قفلِ نرم (`SoftLock`) فقط **نوشتن** را می‌بندد؛ دیدن، چاپ، PDF، بکاپ،
  پشتیبانی و خودِ همگام‌سازی همیشه بازند. قلابش
  `PermissionService.ReadOnlyHook` است — یک نقطه، همان‌جا که هر سرویسی
  اجازه می‌گیرد.
- ⚠️ **امروز اصلاً روشن نمی‌شود**، چون `Entitlements.Unlocked` به خواستهٔ
  صریحِ صاحب ریپو `true` است. این کار آن یک خط را **دست نزد**؛ همان که
  `false` شود، قفلِ نرم هم به کار می‌افتد.

### ⛔ کادرِ نشانیِ سرور ساخته نشد

بندِ ۱۱ آن را می‌خواهد، ولی قاعدهٔ خودِ صاحب ریپو جلوتر است و
`CloudAddressLockTests` قدغنش کرده: اگر نشانی از تنظیمات خوانده می‌شد، هر
کسی برنامه را به سرورِ خودش می‌برد، مجوزِ خودش را امضا می‌کرد و قفلِ
اشتراک با یک کادرِ متنی دور می‌خورد.

### پشتیبان و بازیابی

- ⛔ **پیش از مهاجرت، خودکار**: `PatchSyncUid` نخست یک نسخهٔ `pre-sync`
  رمزشده می‌گیرد — و فقط وقتی واقعاً مهاجرتی در پیش باشد.
- ⚠️ دفترِ بزرگ‌تر از ۲۵۶ مگابایت رمز نمی‌شود و همان نسخهٔ سالمِ
  `VACUUM INTO` می‌ماند: AES-GCMِ دات‌نت کلِ بایت‌ها را یک‌جا می‌خواهد و
  نیم‌گیگ دفتر یعنی نزدیک یک‌و‌نیم گیگ رم، درست سرِ بالا آمدنِ برنامه.
- ⛔ **«بازیابی از سرور» هیچ ردیفی را پاک نمی‌کند**: آن‌چه روی سرور هست
  می‌نشیند، و ردیفی که فقط این‌جاست دست نمی‌خورد. پاک کردن بر اساسِ «روی
  سرور نبود» یعنی یک خطای شبکه = دفترِ خالی.

### آزمون

```
dotnet test PumpYaqobi.Tests -c Release      SyncOpLogTests · SyncStoreTests · CodeBoxesTests
dotnet run --project PumpYaqobi.UiTests -c Release -- syncui
```

## 🪪 ریمیکِ صفحهٔ ورودِ برنامهٔ پمپ (از ۱۴۰۵/۰۷/۰۴)

گزارشِ صاحب ریپو با عکسِ مرجع: «بخشِ لاگینِ برنامهٔ پمپ خیلی داغون و خراب
است، اصلاً ریمیک کن ۱۰۰ و همهٔ کادرها و بخش‌هایش را بهتر و حرفه‌ای‌تر کن.
منطق‌ها و کاربردی‌ها یک دانه حتی دست نخورد؛ ظاهری خیلی داغون است. و شرایط و
ضوابط هم ندارد. شبیه این عکس باشد همه‌چی.»

```
کارتِ سفیدِ تمام‌صفحه ─┬─ پنلِ برند (گرادیانِ آبی، لبهٔ موجی، نشان، آدمک‌ها)
                      └─ فرم (برچسب بالای هر کادر، شرایط، دکمهٔ اصلی)
```

- ⛔ **هیچ منطقی عوض نشد و نباید بشود.** `AccountSectionViewModel.cs` در این
  کار **یک کاراکتر** هم دست نخورد؛ فهرستِ `{Binding …}`های فایل پیش و پس از
  کار یکی است، جز `!IsSignUp` که فقط نفیِ همان خاصیتِ موجود است.
  ⛔ برای عنوان و جملهٔ «حساب دارید؟» **خاصیتِ تازه‌ای در ویومدل ساخته نشد**
  (`AccountTitle` · `SwitchHint` · `SwitchAction` · `SwitchTarget` هیچ‌کدام
  وجود ندارند و آزمون قدغنشان کرده) — چند `TextBlock` با `IsVisible` روی
  همان حالت‌های موجود کارِ همان را می‌کند.
- ⛔ **هیچ دکمهٔ سرویسِ بیرونی‌ای ساخته نشد.** عکسِ مرجع ردیفِ
  «Sign Up with» با گوگل و فیسبوک و ایکس و اپل دارد؛ این برنامه **هیچ‌کدام
  را ندارد** و دکمه‌ای که زده شود و هیچ کاری نکند از نبودنش بدتر است. جایش
  همان راهِ سومِ واقعیِ خودمان است: «کدِ ایمیلی». قاعدهٔ ۱۴۰۵/۰۶/۲۸ («هیچ
  پکنه‌ای نباشد، نه از گوگل و نه غیره») هم همین را می‌گفت.
- ⛔ **پالتِ تازه، و باز هم فقط زیرِ `Border.loginpage`**: بومِ `#eaf4fe` ·
  گرادیانِ `#1746c9 → #2a6ae8 → #4b93f7` · نوشتهٔ تیره `#16233d` · متن
  `#3f4d68` · کم‌رنگ `#7b879e` · کادرِ تایپ `#eef4fc` با خطِ `#dce6f5` ·
  کارتِ سفید با گوشهٔ ۲۸ و سایهٔ `0 24 60 0 #1F1746C9`.
  ⛔ هیچ‌کدام در صفحهٔ پروفایل و هیچ‌کدام در `PumpTheme.cs` — و پالتِ نارنجیِ
  قبلی هم **هیچ ردی** نگذاشته. هر سه را `RangBandi_FaghatDarSafheyeVorud_Ast`
  می‌سنجد. ⚠️ عمداً رنگ‌هایی برگزیده شد که با آبیِ خودِ تم یکی **نباشند**،
  وگرنه آن آزمون بی‌معنا می‌شد.
- ⛔ **برچسبِ بالای هر کادر** — همان چیزی که عکس دارد و این صفحه نداشت. تنها
  راهنمای کادرها نوشتهٔ داخلشان بود و با پر شدنِ کادر می‌رفت، پس کاربر دیگر
  نمی‌دانست کدام خانه چیست. همین بخشِ بزرگی از آن «داغون» بودن بود.
- ⚠️ **«شرایط و ضوابط ندارد» درست دیده شده بود، ولی نبودنش نبود**: تیک از
  ۱۴۰۵/۰۶/۲۹ **بود** — ته یک ستونِ شلوغِ وسط‌چین و فقط در حالتِ «حساب
  می‌سازم». حالا یک ردیفِ روشن درست بالای دکمهٔ اصلی است و متنش روی همان
  لینک باز می‌شود. ⛔ خودِ پذیرش اجباریِ **سرور** است (`terms_required`)،
  پس تشریفاتی نیست.
- ⚠️ **آدمک‌های unDraw سرِ جایشان ماندند** و داخلِ پنلِ برند نشستند: خواستهٔ
  ۱۴۰۵/۰۶/۲۹ بود و سنجهٔ `loginart` هم بودنشان در درخت را قفل کرده.
- ⚠️ **لبهٔ موجی داخلِ یک `Panel`ِ `LeftToRight` است.** در چیدمانِ
  راست‌به‌چپ ستونِ صفر سمتِ راست کشیده می‌شود و «چپ» معنایش عوض می‌شود؛ با
  این قید، موج همیشه روی مرزِ فرم می‌نشیند. به همین دلیل حاشیهٔ نامتقارن هم
  در این صفحه به کار نرفت.
- آزمون: `RangBandi_FaghatDarSafheyeVorud_Ast` و
  `SafheyeVorud_Remake_Shod_Va_HichDokmeyeSakhtegi_Nadarad` در
  `AppLinksTests`؛ رفتارِ خودِ ورود دست‌نخورده است، پس بندهای ۱۴ تا ۱۷ی
  `verify` و هجده بندِ `cloudlogin` همان‌اند که بودند.

⚠️ **و یک محدودیتِ سنجش که پنهان نمی‌شود**: این سندباکس `dotnet` ندارد، پس
کامپایل و آزمون‌ها این‌جا **دویده نشدند**. XAML فقط از نظرِ ساختارِ XML و
یک‌به‌یکِ بایندها و کلاس‌ها سنجیده شد، و سنجه‌های تازه روی خودِ فایل‌ها
شبیه‌سازی شدند. «کامپایل می‌شود» را تا سبز شدنِ CI نباید گفت.

## ⛔ کدِ دومِ شش‌رقمی از گامِ پمپ رفت (از ۱۴۰۵/۰۷/۰۴)

گزارشِ صاحب ریپو: «یک منطقِ مسخره هم توی سرور و برنامهٔ پمپ بنزین است، با
این لاگین مشکل دارند: بعد از کد شش‌رقمی یک کد شش‌رقمیِ دیگر می‌خواهد. آن
چیست؟ آن را حذف کن، لازم نیست.»

حق داشت. جریانِ ورود این بود:

```
حساب ⇒ کدِ شش‌رقمیِ **ایمیل** ⇒ پمپ (+ کادرِ شش‌رقمیِ **اشتراک**) ⇒ تمام
                                        ↑ همین
```

دو کادرِ شش‌رقمی، پشتِ سرِ هم، با همان شکل و همان اندازه و هیچ ربطی به هم.
⚠️ «اختیاری» بودنش هم دردی دوا نمی‌کرد: کاربر نمی‌دانست کدام است و دنبالِ
کدی می‌گشت که اصلاً ندارد.

- ⛔ **گامِ پمپ دیگر هیچ کدی نمی‌خواهد.** `LoginCode` و شاخهٔ
  `RedeemAsync`ش کاملاً رفتند و `FinishPumpAsync` همیشه
  `EnsureStationAsync` را می‌زند. (نامِ فرمان هم از `VerifyCodeCommand` به
  `FinishPumpCommand` عوض شد، چون دیگر هیچ کدی را تایید نمی‌کند.)
- ⛔ **ولی هیچ قابلیتی از بین نرفت** و نباید برود: خرج کردنِ کدِ اشتراک
  همان‌جایی ماند که باید باشد — کارتِ «اشتراک»ِ خودِ پروفایل (`SubCode` +
  `RedeemSubCommand` ⇒ `CloudLink.RedeemAsync`). کسی که کد دارد بعد از
  ورود آن‌جا می‌زندش؛ کسی که ندارد اصلاً کادری نمی‌بیند. دو خطِ آزمون
  (`Binding SubCode` و `RedeemSubCommand`) همین را نگه می‌دارند، وگرنه
  «حذفش کردم» می‌توانست یعنی «قابلیت را هم بردم».
- ⚠️ **سنجه‌ها جابه‌جا شدند، ضعیف نشدند**: بندهای ۴ و ۵ی `cloudlogin` و
  بندِ «د»ی `verify` همان «کدِ غلط ⇒ هیچ توکنی» و «کدِ نصفه رد می‌شود» را
  می‌سنجند — فقط از درِ واقعیِ خودش، یعنی `RedeemSubCommand`. ⛔ پاک
  کردنشان به بهانهٔ «آن کادر دیگر نیست» غلط بود: خودِ خرج کردنِ کد هنوز
  هست.
- ⚠️ سمتِ **سرور** هیچ چیزی عوض نشد: `POST /api/pump/device/activate` سرِ
  جایش است و پنلِ سرورِ خانگی هم از همین‌جا کد می‌سازد.

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا
دویده نشدند — CI روی PR می‌دواندشان.

## 🌌 ریمیکِ دومِ صفحهٔ ورود — کارتِ شیشه‌ای وسطِ آسمانِ شب (از ۱۴۰۵/۰۷/۰۵)

خواستهٔ صریحِ صاحب ریپو: «لاگین هنوز مشکل داره. **اون سه مرحله نباید دیده
بشن** و **شرایط و ضوابط هم دیده نمی‌شه و باید با زدنش بره صفحهٔ جداگانه**، و
**لاگین رو هم وسط صفحه‌اش بزار**. تمام دیزاینِ لاگین متفاوت و جذاب باشه —
اینی که الان است، **این مدل و ظاهر یک درصد هم دیگه نباشه**. یک چیزِ متفاوت و
الترا یو‌ای‌یو‌ایکس باشه، و **منطق دست نخوره**.»

⛔ **ویومدل یک کاراکتر هم دست نخورد.** فهرستِ `{Binding …}`ها و `Command`های
صفحه پیش و پس از این کار یکی است، جز `StepDone` که تنها مصرفش همان نوارِ
گام‌ها بود. خودش سرِ جایش است و فقط دیگر شمرده نمی‌شود —
`Assert.Contains("public bool StepDone => LoginStep == 4;")` قفلش کرده تا
«پنهانش کردم» فردا نشود «منطقش را بردم».

### ۱) نوارِ گام‌ها رفت

«۱ حساب · ۲ کدِ ایمیل · ۳ پمپ · ۴ تمام» و سبکِ `stepchip` هر دو برداشته
شدند. ⚠️ خودِ گام‌ها (`StepAccount` · `StepEmailCode` · `StepPump` ·
`StepForgot`) دست‌نخورده‌اند و هر بخش با همان `IsVisible` پیدا و پنهان
می‌شود.

### ۲) شرایط و ضوابط یک **صفحهٔ جدا** شد

⛔ تا دیروز `ShowTerms` یک کادرِ کشوییِ `MaxHeight="160"` را ته فرم باز
می‌کرد — همان چیزی که صاحب ریپو «دیده نمی‌شه» خواندش. حالا همان `ShowTerms`
یک **قابِ تمام‌صفحه** روی آسمان باز می‌کند، با متنِ لغزان و تیکِ پذیرش و
دکمهٔ «بستن و برگشت».

⛔ **و فرمانِ تازه‌ای ساخته نشد**: `LoadTermsAsync` خودش **دوحالته** است
(`ShowTerms = !ShowTerms`)، پس همان یک فرمان هم باز می‌کند هم می‌بندد.
⚠️ تیکِ داخلِ صفحه همان `AcceptTerms`ِ روی فرم است — کسی که تا ته متن آمده
نباید برای زدنِ تیک برگردد.

### ۳) فرم وسطِ صفحه

⛔ دو ستونِ `ColumnDefinitions="*,470"` رفت. حالا یک ستونِ `MaxWidth="520"`
با `HorizontalAlignment="Center"` و `VerticalAlignment="Center"` داخلِ
`ScrollViewer` است. ⚠️ ترازِ وسط روی **محتوا**ی `ScrollViewer` است نه روی
خودش: تا وقتی محتوا از قاب کوتاه‌تر است وسط می‌ماند و بلندتر که شد می‌لغزد.

### ۴) پالتِ تازه — و باز هم فقط زیرِ `Border.loginpage`

```
آسمان  #070b1e → #14225c → #2d1b69      تاکید  #3f8cff → #6c5ce7 → #a855f7
کارت   #F7FBFFFF شیشه‌ای، گوشهٔ ۳۰       سایه   0 40 90 0 #66050a24
نوشته  #101935 · متن #5a6a8c · کم‌رنگ #8d9ab5
کادر   #f0f5ff با خطِ #dbe5f7
```

⛔ پالتِ آبیِ ۱۴۰۵/۰۷/۰۴ (`#eaf4fe` · `#1746c9` · `#2a6ae8` · `#4b93f7` ·
`#16233d` · `#3f4d68` · `#7b879e` · `#eef4fc` · `#dce6f5`) **هیچ ردی
نگذاشت** — همان قاعدهٔ همیشگی، وگرنه صفحه دو رنگ‌بندیِ نیمه‌کاره می‌شد.
⛔ و هیچ‌کدام در صفحهٔ پروفایل و در `PumpTheme.cs` نیست.

⚠️ **`Classes="brand"` فقط دورِ نشان و نام است، نه دورِ کلِ ستون.** سبکِ
`StackPanel.brand TextBlock` نوشته را روشن می‌کند؛ اگر کارتِ روشن هم زیرش
می‌بود، متنِ روی کارت روشنِ روی روشن می‌شد — یعنی نامرئی.

⚠️ **آدمک‌های unDraw سرِ جایشان ماندند** (خواستهٔ ۱۴۰۵/۰۶/۲۹ و سنجهٔ
`loginart`) ولی دیگر کنارِ فرم نیستند: تزئینِ خودِ آسمان‌اند، `Opacity="0.14"`
و گوشهٔ پایین. ⚠️ `Opacity` روی `IsEffectivelyVisible` اثری ندارد، پس
سنجه‌های رفتاری همچنان پیدایشان می‌کنند.

⛔ **و هیچ دکمهٔ سرویسِ بیرونی‌ای اضافه نشد** — قاعدهٔ ۱۴۰۵/۰۶/۲۸ سرِ جایش
است.

آزمون: `SafheyeVorud_Remake_Shod_Va_HichDokmeyeSakhtegi_Nadarad` و
`RangBandi_FaghatDarSafheyeVorud_Ast` (سورس) و بندِ «ب‌ب» در
`dotnet run --project PumpYaqobi.UiTests -c Release -- verify` (رفتار:
صفحهٔ شرایط واقعاً باز و بسته می‌شود و متنش می‌آید).

## 🔌 «چرا به سرور وصل نمی‌شود» — قرارداد سالم است، سرورِ خانگی نه (از ۱۴۰۵/۰۷/۰۵)

گزارشِ صاحب ریپو: «ببین چرا برنامهٔ پمپ بنزین وصل نمی‌شه به سرور.»

سنجیده شد، حدس زده نشد: سرورِ حسابِ **واقعی** (ریپوی `shop`، نسخهٔ ۲.۸.۱) روی
PostgreSQLِ واقعی بالا آمد و **همان درخواست‌هایی که `CloudLink.cs` می‌زند** —
با همان بدنه، همان فعل و همان هدرها — بازپخش شد:

```
/api/health · /api/config                                    ✅
register/start ⇒ verify ⇒ complete  (terms + device)         ✅ نشست آمد
/api/auth/login · /api/auth/refresh                          ✅
/api/pump/me  ⇒  POST /api/pump  ⇒  device/bind              ✅ توکنِ دستگاه + کلیدِ عمومی
POST device/license · GET device/access-code · POST device/home  ✅
                                                       ۱۳ از ۱۳ سبز
```

⛔ **پس هیچ ایرادی در قراردادِ برنامه نیست** و دنبالِ باگ در `CloudLink`
نگردید. آن‌چه می‌ماند سمتِ **سرور** است، و ترتیبِ عیب‌یابی همین است:

```
https://api.vill3n.top/api/health
  ⇒ JSONی با version    ⇒ سرورِ حساب بالاست
  ⇒ JSONِ control-center ⇒ درگاهِ پنلِ خانگی هست ولی کهنه/خاموش است
  ⇒ ۵۰۳ account_server_down ⇒ خودِ shop/server روی آن کامپیوتر بالا نیست
  ⇒ HTMLِ ۴۰۴          ⇒ تونل اصلاً به سرورِ حساب نمی‌رسد
```

⚠️ و همان‌طور که از ۱۴۰۵/۰۶/۳۰ نوشته شده: **سندباکسِ سیزن‌های Claude به
`api.vill3n.top` نمی‌رسد** (`CONNECT tunnel failed, 403`)، پس این چهار حالت را
فقط خودِ صاحب سامانه می‌تواند ببیند. برنامه از ۱۴۰۵/۰۷/۰۱ دروغ نمی‌گوید:
چراغِ دومِ سربرگ سرخ می‌شود و `CloudNote()` می‌گوید «به سرورِ حساب نمی‌رسیم».

## 🐢 «جدول‌ها یک ثانیه بعد میان» — دو ریشه، و یک گمانِ رد‌شده (از ۱۴۰۵/۰۷/۰۵)

گزارشِ صاحب ریپو: «برنامه کند باز می‌شه… و الان هر بخش رو باز می‌کنم جدول‌ها
یک ثانیه بعد میان که خیلی روی مخ من است و این مشکل از بیخ باید درست بشه.»

ترتیبی که کاربر واقعاً می‌دید:

```
SyncContent()  ⇒ صفحه عوض شد و همین حالا دیده می‌شود
               ⇒ _settings.Save()   ← یک fsync، روی نخِ رابط
               ⇒ NotifyPagesChanged() فقط یک Post می‌کرد
چیدمانِ ۱      ⇒ جدول دیده می‌شود ولی ItemsSourceش هنوز **پارک** است
               ⇒ یک جدولِ **خالی** به کاربر نشان داده می‌شود
نوبتِ Loaded   ⇒ تازه حالا فهرست برمی‌گردد
چیدمانِ ۲      ⇒ و تازه حالا ردیف‌ها ساخته می‌شوند
```

### ۱) ⛔ «آخرین بخش» روی نخِ رابط روی دیسک نمی‌نشیند

`AppSettings.Save` عمداً **بادوام** است: فایلِ موقت، `Flush(true)` (یک
`FlushFileBuffers`ِ واقعی) و `File.Replace`ِ سه‌فایلی. آن دوام برای توکنِ
دستگاه و `CloudPublicKey` و `StationCode` **لازم است و برنمی‌گردد** (قاعدهٔ
۱۴۰۵/۰۶/۳۰).

ولی `GoAsync` همان را برای یک مقدارِ **راحتی** صدا می‌زد، درست **بینِ** نشان
دادنِ صفحه و خواندنِ داده‌اش. روی ویندوز، با ضدِ ویروسی که همان لحظه فایل را
باز می‌کند، همان می‌شود «یک ثانیه بعد».

⛔ حالا `AppSettings.SaveSoon()` — همان الگوی ۶۰۰ میلی‌ثانیه‌ایِ اندازهٔ
ماشین‌حساب (از ۱۴۰۵/۰۶/۲۵)، ولی یک‌جا. ⚠️ **فقط برای مقدارهای راحتی** (تم،
آخرین بخش، اندازهٔ ماشین‌حساب)؛ هر چیزی که از دست رفتنش کاربر را از حسابش
بیرون می‌اندازد همچنان `Save()`ِ بادوام را صدا می‌زند. ⚠️ و هر `Save()`ِ
واقعی نوبتِ در صف را هم می‌خورد — آن یکی کلِ شیء را می‌نویسد.

### ۲) ⛔ جدولِ پارک‌شده در **همان پاس** برمی‌گردد

`NotifyPagesChanged` فقط `Post` می‌کرد، چون `IsEffectivelyVisible` همان لحظه
هنوز مقدارِ قبلی را می‌دهد. ولی آن فقط برای جهتِ **پنهان کردن** لازم است.

⛔ حالا دو رویداد است: `PagesShown` (هم‌زمان، فقط برمی‌گرداند) و
`PagesChanged` (با `Post`، فقط پارک می‌کند). جهتِ «برگرد» از
`SectionShown()` می‌پرسد — یعنی از `SectionViewModel.IsShown` که
`SyncContent` **پیش از** صدا زدنِ این متد نوشته است.

⚠️ **هیچ قاعده‌ای پس نرفت**: «بخشِ پنهان صفر ردیفِ زنده دارد» همان است و
`idle` همان را می‌سنجد، چون این جهت فقط جدولی را برمی‌گرداند که بخشش
**همین حالا** دیده می‌شود.

### ۳) ⛔ شنوندهٔ چیدمانِ بی‌نگهبان — بارِ سوم

`DebtArchiveView` یک `LayoutUpdated` بی `IsEffectivelyVisible` داشت که هر
بار یک `GetVisualDescendants()`ِ کاملِ صفحه را می‌دواند — یعنی با هر فریمِ
اسکرولِ **هر بخشِ دیگری**. همان قاعدهٔ ۱۴۰۵/۰۶/۲۶، و حالا آزمون هر سه
شنوندهٔ درختِ دائمی را می‌سنجد (`ExcelGrid` · `TotalsBar` ·
`DebtArchiveView`).

### ⚠️ و یک گمان که **سنجیده شد و رد شد** — ننویسیدش دوباره

گمان: «`PatchSyncUid` با هر نسخهٔ تازه ۳۷ تا `UPDATE ... WHERE SyncUid IS
NULL OR SyncUid=''` می‌زند و روی دفترِ پنج‌ساله هر بار اسکنِ کامل است.»

با SQLiteِ واقعی سنجیده شد (۶۴٬۰۰۰ ردیف، همان ایندکسِ `IX_*_SyncUid`):

```
EXPLAIN QUERY PLAN ⇒ MULTI-INDEX OR      یعنی از ایندکس می‌رود، نه اسکنِ کامل
UPDATEِ بی‌کار (همه پُر):  ۰٫۰۰۷ ms
دو SELECTِ «نگهبان»:      ۰٫۰۱۵ ms      ← یعنی نگهبان **کندتر** بود
```

⛔ پس دست نخورد. قاعدهٔ «تغییری که سودش با عدد ثابت نشود نمی‌ماند» سرِ جایش
است — این بار هم جلوی یک «اصلاحِ» بی‌فایده را گرفت.

### سنجه

```
dotnet run --project PumpYaqobi.UiTests -c Release -- sectionopen
```

⚠️ **ملاکش میلی‌ثانیه نیست**، عددِ ساختاری است: «پس از اولین چیدمانِ بخشِ
تازه، چند ردیف روی صفحه است؟» صفر یعنی کاربر یک جدولِ خالی دید. و
**بازدیدِ دوم** سنجیده می‌شود نه اول — شکایت «هر بخش رو باز می‌کنم» بود،
یعنی رفت‌وآمد بینِ بخش‌هایی که داده‌شان از قبل خوانده شده.

در `fullscan` هم هست. آزمونِ سورس: `SectionOpenSpeedTests` (سه بند).

## ⛔ فعال‌سازیِ بخش هم ترمزِ `Version` می‌خواهد (از ۱۴۰۵/۰۷/۰۵، بارِ دوم)

گزارشِ دومِ صاحب ریپو، با نامِ دقیقِ بخش‌ها: «رسید قرض‌داران · صرافی · مصارف ·
رسید پارچه‌ها · گاوصندوق — جدول‌هاشون دیر باز میشه.»

⚠️ **و بخشی از گزارش مالِ نسخه‌ای بود که هنوز نصب نشده بود**: ۳.۱.۱۴۲ همان
لحظه در حالِ ساخته شدن بود و دستِ کاربر ۳.۱.۱۴۱ بود. ولی زیرش یک ریشهٔ
**سوم** هم بود که آن نسخه نمی‌گرفت:

⛔ **هجده بخش در `OnActivatedAsync` خودشان را از نو می‌خواندند، بی هیچ
ترمزی.** و این «کارِ اضافه» نبود — واقعاً لازم است، چون دادهٔ یک بخش را
بخشِ دیگری عوض می‌کند (مصرفِ ورق در «مصارف» می‌نشیند، رسیدِ فاکتور در
حسابِ قرض‌دار). ولی با **هر** رفت‌وآمد این زنجیره می‌دوید:

```
پرس‌وجوی SQLite ⇒ ساختِ دوبارهٔ همهٔ ردیف‌های ویومدل
                ⇒ Reset روی فهرست
                ⇒ ExcelGrid پهنای ستون‌ها را دور می‌ریزد (_spread=false، همه Auto)
                ⇒ یک پاسِ اندازه‌گیریِ تنگ
                ⇒ و تازه بعد، ردیف‌ها
```

…حتی وقتی **هیچ چیزی نوشته نشده بود**.

- ⛔ حالا `SectionViewModel.ActivationOnlyReadsDb` (پیش‌فرض `false`) +
  `ActivationCanBeSkipped` + `MarkActivationSeen()`. مسیرِ ناوبری
  (`GoAsync` و `OpenSubAsync`) با `PumpDbContext.Version`ِ دست‌نخورده
  اصلاً `OnActivatedAsync` را صدا نمی‌زند.
- ⚠️ **ترمز تازه نیست** — همان `Version`ی که نوار و داشبورد و
  `StationPublisher` و بخشِ ورق از ۱۴۰۵/۰۶/۲۶ دارند. فقط به مسیرِ
  **فعال‌سازی** هم آمد.
- ⛔ **و عمداً همگانی نیست.** بخشی که در فعال‌سازی چیزی **بیرونِ** دفتر
  می‌خواند — فایل‌های پشتیبان، حالِ سرورِ حساب، صندوقِ چت، حالِ همگام‌سازی،
  رمزها — با شمارهٔ دفتر سنجیده نمی‌شود و باید `false` بماند. آزمون هر دو
  سمت را می‌سنجد: یازده بخش باید داشته باشند و پنج بخش **نباید**.
- ⚠️ `MarkActivationSeen()` در **پایانِ** کار خوانده می‌شود، نه آغازش: خودِ
  خواندن هم می‌تواند چیزی بنویسد، و با شمارهٔ آغاز دورِ بعد بی‌جهت «عوض شد»
  دیده می‌شد.

### سنجه‌ها به همان بخش‌هایی رفتند که نام برده شدند

- `sectionopen` حالا دقیقاً همان پنج بخش است: `debtrasid` · `sarrafi` ·
  `expenses` · `rasid` · `safe`.
- `scrollperf` تا امروز فقط **جدول‌ها** را می‌گشت. داشبورد و تنظیمات جدولِ
  بلند ندارند، پس اگر آن‌جا لگی بود هیچ عددی از آن نداشتیم. حالا هر دو در
  سنجه‌اند، به‌علاوهٔ `sarrafi` و `debtrasid` و `rasid`.

⛔ **و دربارهٔ لگِ اسکرولِ داشبورد و تنظیمات هیچ «اصلاحی» کورکورانه نشد.**
قاعدهٔ «اول بساز که ببینی» سرِ جایش است: اول عدد، بعد تغییر. سنجه‌اش حالا
هست و عددش در CI دیده می‌شود.

### ⛔ نوشتنِ در صف باید بداند مالِ کدام پوشه است (همان روز، از CI)

سنجهٔ `test` روی همین PR سرخ شد — ولی فقط در **یکی** از دو اجرا:

```
SettingsDurabilityTests.FayleGhoflShode_PishFarzRa_Nemineviseh
Expected: dev-token
Actual:   pd-test
```

و `pd-test` تنها یک جا نوشته می‌شود: `SettingsPagesTests`. یعنی نوشتنِ یک
کلاسِ آزمون در دفترِ کلاسِ **دیگری** نشسته بود — در حالی که هر دو
`[Collection(AppHostCollection.Name)]` دارند و سریال می‌دوند.

⛔ **ریشه، پس‌رفتِ خودِ ۳.۱.۱۴۲ بود**: `SaveSoon()` ششصد میلی‌ثانیه بعد
می‌نویسد، ولی `Save()` مسیرش را از `AppSettings.Dir`ِ **همان لحظه**
می‌گیرد. پس زنجیره این بود:

```
SettingsPagesTests: new MainViewModel() (تنظیماتِ pd-test را برمی‌دارد)
                  ⇒ GoAsync ⇒ SaveSoon()        ← نوبت گرفت، ننوشت
کلاس تمام شد ⇒ SettingsDurabilityTests: DirOverride = پوشهٔ تازه
۶۰۰ms بعد ⇒ FlushSoon ⇒ Save() ⇒ در پوشهٔ **تازه** نوشت
```

- ⛔ **`_soonDir` کنارِ `_soonWho`**: نوبتِ در صف پوشه‌اش را هم نگه می‌دارد و
  `FlushSoon` تا هم‌پوشه نباشد **نمی‌نویسد**. مقدارِ راحتی (تم، آخرین بخش)
  گم شدنش اشکالی ندارد؛ نشستنش در دفترِ اشتباه دارد.
- ⛔ **`DirOverride` نوبتِ در صف را دور می‌ریزد**، و این کار و خودِ انتساب
  زیرِ **یک** قفل‌اند — وگرنه `FlushSoon` می‌توانست پوشه را پیش از عوض شدن
  بخواند و درست بعدش بنویسد.
- ⚠️ **و درسِ روش، دوباره**: یک اجرای سبز چیزی را ثابت نمی‌کند. همان کامیت
  در یک ورک‌فلو سبز بود و در آن یکی سرخ — شکلِ **مسابقه**، نه شکلِ باگِ
  همیشگی. دو اجرای هم‌زمانِ CI این را نشان داد، نه خواندنِ کد.
- آزمون: `SettingsDurabilityTests.NevashtaneDarSaf_DarPushehyeDigari_Nemineshinad`
  — یک `SaveSoon` می‌گیرد، پوشه را عوض می‌کند، و می‌سنجد که در پوشهٔ تازه
  **هیچ فایلی** ننشسته باشد.

⛔ **و حلقهٔ عکسِ ایستگاه در آزمون‌های واحد خاموش است** (`StationPublisher.Disabled`،
از ۱۴۰۵/۰۷/۱۳، در `Parallelism.cs`). هر آزمونی که `MainViewModel` می‌سازد آن را
راه می‌انداخت و کسی نمی‌بستش؛ هر دقیقه `LicenseClock.Tick` ⇒ `SaveSoon()` در پوشهٔ
**همان لحظه** می‌نوشت — یعنی گاهی در پوشهٔ آزمونِ بعدی، و همین سنجهٔ
`NevashtaneDarSaf_…` را در یکی از دو اجرای CI سرخ کرد. نگهبانِ `_soonDir` این
را نمی‌گرفت، چون نوبت **بعد** از عوض شدنِ پوشه گرفته می‌شد. ⚠️ سنجه‌های رابط
(`serverdot` و …) خودِ حلقه را می‌خواهند و این را نمی‌زنند.

⛔ **و نمونه‌ای که از پوشهٔ دیگری خوانده شده، این‌جا نمی‌نویسد** (`AppSettings._home`،
از ۱۴۰۵/۰۷/۱۳). بیلدِ ۳.۱.۱۷۰ پس از خاموش شدنِ حلقهٔ ناشر **باز** همان سنجه را سرخ
کرد: `_soonDir` پوشهٔ **لحظهٔ** `SaveSoon` را برمی‌داشت، نه پوشهٔ خودِ شیء — پس
ویومدلِ آزمونِ قبلی که دیرتر «آخرین بخش» را ذخیره می‌کرد، در پوشهٔ آزمونِ بعدی
می‌نوشت. حالا `Load()` پوشه را روی نمونه مهر می‌زند و `SaveSoon`/`Save`ِ نمونهٔ
پوشهٔ دیگر هیچ کاری نمی‌کنند. در برنامهٔ واقعی پوشه عوض نمی‌شود؛ نمونهٔ `new`
خانه ندارد و رفتارش همان است. آزمون: `NemooneyePushehyeDigar_InjaNemineviseh`.

⚠️ **و `sectionopen` تا امروز در CI نبود** — ساخته شده بود و فقط دستی
می‌دوید، یعنی همان سنجه‌ای که برای این باگ نوشته شد جلوی هیچ‌کس را
نمی‌گرفت. حالا در `grid-perf.yml` است، کنارِ `scrollperf`.

### عددهای واقعیِ CI (اجرای ۳.۱.۱۴۳)

`idle`: هر بیست بخش **صفر** دستورِ بی‌کاری، **صفر** ردیفِ زندهٔ بخشِ پنهان.

`scrollperf`، با پوششِ تازه — سقف ۱۲۰ms و همه زیرش:

| صفحه | گام‌ها | بیشینه | p95 | لگ |
|---|---|---|---|---|
| داشبورد | ۳ | ۵۰ms (بارِ دوم ۳۲) | ۵۰ | ۰ |
| تنظیمات | — | «صفحه کوتاه است، اسکرولی نیست» | — | — |
| گاوصندوق | ۳۷ | ۵۸ms | ۵۱ | ۲ |
| مصارف | ۲۹ | ۲۰ms | ۱۴ | ۰ |
| صرافی | ۱۴ | ۹۱ms (بارِ دوم ۱۴) | ۹۱ | ۱ |
| رسید قرض‌داران | ۷ | ۱۶ms | ۱۶ | ۰ |
| رسید پارچه | ۴۰۰ | ۴۹ms | ۱۹ | ۰ |

⚠️ **و یک محدودیتِ سنجش که پنهان نمی‌شود**: «تنظیمات» در قابِ ۱۴۴۰×۹۹۰ی
اجرای بی‌پنجره از صفحه بلندتر نمی‌شود، پس سنجه **هیچ گامی** ندارد و دربارهٔ
لگِ آن **هیچ نمی‌گوید**. این «سبز» نیست، «نسنجیده» است. هر کس این را تمام
می‌کند: قاب را کوچک کند تا صفحه واقعاً بلغزد، بعد عدد بگیرد. ⛔ و تا آن روز
ننویسید «تنظیمات لگ ندارد».

### ⚠️ و `sectionopen` بارِ اولِ CI سرخ شد — ایرادِ خودِ سنجه بود

عددِ ساختاری در هر پنج بخش **سالم** بود (ردیف در فریمِ اول: ۸۱ · ۷۱ · ۴۴ ·
۴۴ · ۵۴، و صفر دستورِ دیتابیس در بازدیدِ دوم — یعنی ترمزِ `Version` کار
می‌کند). آن «۳ ایراد» فقط سقفِ ۴۰۰ میلی‌ثانیه بود (۴۸۴ · ۵۶۰ · ۴۳۱) — و
بدتر: پیامِ سنجه می‌گفت «جدول باید در همان فریمِ اول باشد»، که **دلیلِ
غلط** بود. سنجه‌ای که دلیلِ اشتباه چاپ کند، فردا کسی را ساعت‌ها دنبالِ
باگی می‌فرستد که وجود ندارد.

- ⛔ حالا دو شمارنده جداست: فریمِ **خالی** سنجه را سرخ می‌کند، و کُندی فقط
  `⚠️` گزارش می‌شود.
- ⛔ **و این ضعیف کردنِ سنجه نیست**: ملاکِ این سنجه از روزِ اول همان عددِ
  ساختاری بود و دست‌نخورده سخت‌گیر مانده. آن‌چه دروازه‌بودنش برداشته شد یک
  عددِ **بی‌مبنا** است — با `main` سنجیده نشده، چون این سندباکس `dotnet`
  ندارد. همان تله‌ای که `themeflip` را از CI بیرون کرد.
- ⚠️ **کارِ مانده**: هر کس مبنای `main` را گرفت، سقف را دوباره دروازه کند —
  با عددِ خودِ `main`، نه با انتظار.

## 🧾 ردیفِ خودکار در نخستین کادرِ خالی می‌نشیند — هر پنج مسیر (از ۱۴۰۵/۰۷/۰۵)

گزارشِ صاحب ریپو: «از ورق به حسابِ طرف که اتومات می‌ره، می‌ره تهِ جدولِ همان
حساب در حالی که کادرِ جدولِ اولی هم خالی است… اولی اگه نبود دومی و اگه جدولی
نبود خودش ساخته شود. توی گاوصندوق هم که اتومات می‌ره همین‌طور و برای مصارف هم
همین‌طور… حتی یک عدد هم اشتباه به حساب نره که اعتبار من بره هوا.»

- ⛔ **قاعدهٔ «ردیفِ خالی» یک جاست**: `PostingService.IsBlankRow` و
  `FirstBlank`. `PersonViewModel.IsBlank` هم به همان رسید — پیش از این دو
  نسخه بود و مو‌به‌مو یکی نبودند.
  - ⛔ **`RasidFuel` شمرده می‌شود.** رسیدِ تیلِ سربرگ ردیفی می‌سازد که جز
    همان همه‌چیزش صفر است؛ با قاعدهٔ سمتِ ثبت، اولین ردیفِ خودکارِ ورق روی
    آن می‌نشست و **رسیدِ تیلِ مشتری بی‌صدا پاک می‌شد**. خرابیِ داده بود، نه
    کندی.
  - ⛔ **ردیفی که `SrcKey` دارد خالی نیست**، حتی با همهٔ خانه‌های صفر: مالِ
    منبعِ دیگری است.
  - ⚠️ **تاریخ شمرده نمی‌شود** (ردیفِ خالیِ تازه تاریخِ امروز دارد) و ترتیب
    از `SortIndex` است، نه ترتیبِ فهرستِ حافظه.
- ⛔ **ورق ⇒ حساب: دو پاس، نه یک خواندنِ بزرگ.** این‌جا عمداً ته جدول می‌رفت
  چون پیدا کردنِ ردیفِ خالی یعنی خواندنِ کلِ دفترِ همهٔ قرض‌داران. راهِ سوم:
  اول `WaraqPostingService.TargetAccountIds` فقط **نام** را در حافظه تطبیق
  می‌دهد (رایگان)، بعد ردیف‌های خالیِ **فقط همان چند حساب** خوانده می‌شوند.
  ⛔ قاعدهٔ تطبیق آن‌جا دوباره نوشته نشد — همان چهار تابعِ `PostingService` که
  `One()` می‌زند. دو نسخه یعنی روزی ردیف در حسابی بنشیند که خالی‌هایش خوانده
  نشده بود.
- ⛔ **مصارف و گاوصندوق فقط از ردیف‌های خالیِ همان ماه برمی‌دارند.** این دفترها
  ماه‌به‌ماه دیده می‌شوند و ردیفِ خالیِ ماهِ دیگر با نوشتنِ ماهِ ورق از جدولِ
  خودش **ناپدید** می‌شد.
  - ⛔ `SalaryStaffId`دار خالی نیست (مهرِ معاشِ آن کارمند گم می‌شود).
  - ⛔ **ارزِ ردیفِ «فروشِ ورق» صریح نوشته می‌شود** (`Currency.Afn`): ردیفِ
    خالیِ برداشته‌شده می‌توانست روی دالر باشد و فروشِ افغانیِ یک شیفت در
    ستونِ دالر می‌نشست.
- ⛔ **مخزن ⇒ شرکت: ویرایش باید برسد.** `PutReceiptAsync` حالا
  منبع‌شناس است (ردیفِ همان خرید سرِ جایش به‌روز می‌شود و نسخه‌های جامانده در
  شرکت یا دفترِ تیلِ دیگر برداشته می‌شوند)، و فروشنده‌ای که خالی شود
  ردیفش برداشته می‌شود (`UnlinkPurchaseAsync`)، نه این‌که با نامِ خالی بماند.
  - ⚠️ خریدی که کاربر خودش ردیفش را از حسابِ شرکت برداشته **برنمی‌گردد**
    (`UnlinkedPurchasesAsync`)؛ همان قاعدهٔ قدیمی و دست‌نخورده.
  - ⛔ **و حذفِ خرید از مخزن ردیفِ حسابِ شرکت را نمی‌برد** — یک بار در همین
    کار برعکسش نوشته شد و `PurchaseCompanyParityTests.DeletingAPurchaseLeavesTheCompanyRowAlone`
    همان روز گرفتش. نسخهٔ وب هم در پرسشِ پیش از حذف صریح همین را
    می‌گوید: «ردیفِ همین خرید در حساب شرکت دست‌نخورده می‌ماند». دفترِ شرکت
    حسابِ دادوستد است، نه آینهٔ فهرستِ مخزن.
- ⚠️ **صرافی ⇒ شرکت از قبل درست بود**؛ فقط `PayRate`ِ ردیفِ برداشته‌شده پاک
  می‌شود (در `IsEmpty` شمرده نمی‌شود و می‌توانست به رسیدِ تازه بچسبد).
- آزمون: `AutoRowPlacementTests` (۱۲ بند) — قاعدهٔ خالی، ورق ⇒ حساب، مصارف،
  گاوصندوق (ماه و ارز)، و خرید ⇒ شرکت در ویرایش، تغییرِ فروشنده، تغییرِ تیل و
  حذف.

## 🧮 نوارِ «جمله»: خانهٔ بی‌ستون روی خودِ «جمله» می‌افتاد (از ۱۴۰۵/۰۷/۰۵)

گزارشِ صاحب ریپو: «بخشِ گاوصندوق پایینِ جدول جمله‌اش خیلی داغون است و مربوط به
همان کادرِ خودشان نمی‌شوند.»

ریشه: هر شش جمعِ گاوصندوق `TotalCell.NoColumn`اند (جدول یک ستونِ «مبلغ» دارد و
شش جمع) و `TotalsStrip` خانه‌های بی‌ستون را از `x = 0` می‌چید — **دقیقاً روی
نوشتهٔ «🧮 جمله»**.

- ⛔ `TitleGutter()` — خانه‌های بی‌ستون از بعدِ «جمله» شروع می‌شوند.
  ⚠️ **کفِ ثابت دارد**: در نخستین پاسِ چیدمان نوشته هنوز پهنا ندارد و هیچ
  چیزی نوار را دوباره بی‌اعتبار نمی‌کند، پس همان یک فریم برای همیشه می‌مانْد.
- ⛔ وقتی **هیچ** خانه‌ای ستون ندارد، جا به تساوی پخش می‌شود — نوار یک ردیفِ
  کاملِ هم‌قدِ جدول، نه یک کنج.
- ⛔ هر خانه خطِ عمودیِ خودش را دارد، همان چیزی که یک `<td>` دارد.
- ⚠️ خانه‌ای که ستون دارد هیچ تغییری نکرد؛ فقط دیگر از لبهٔ نوار بیرون نمی‌زند.

## 💬 چتِ پشتیبانی و کیو‌آر (از ۱۴۰۵/۰۷/۰۵)

- ⛔ **عنوانِ گفت‌وگو نامِ همان حسابِ کیو‌آر است**، نه `d12`ی خام و نه نامی که
  مشتری تایپ کرده — آن به زیرنویس رفت. `DebtorService.AccountLabelAsync`:
  `d12` ⇒ نامِ حساب، `c3` ⇒ نامِ شرکت.
  - ⚠️ حسابِ فرعی **«شخص › فرعی»** می‌شود: دو نفر می‌توانند فرعیِ هم‌نام
    داشته باشند («دکان»، «موتر») و بی نامِ شخص معلوم نیست پیام از کدام است.
  - ⚠️ همه‌اش پشتِ `try` است و نام یک بار پرسیده و نگه داشته می‌شود؛ نشد ⇒
    نشان پس گرفته می‌شود تا دورِ بعد دوباره بپرسد. **نامِ حساب رفاه است،
    خودِ پیام اصل.**
- ⛔ **پنجرهٔ کیو‌آر «ارسال به جاهای دیگر» دارد**: کپیِ لینک · کپیِ پیامِ آماده ·
  واتساپ · تلگرام. روی ویندوز اشتراک‌گذاریِ سیستمی نیست، پس راهِ واقعی همین
  است — نشانی‌ای که مرورگرِ خودِ سیستم باز می‌کند، و باز نشدنش پنجره را
  نمی‌شکند.
  - ⛔ **هیچ رمزی در پیام نیست**: نشانیِ کیو‌آر فقط رمزِ همان **یک** حساب را
    دارد (`k`)، نه رمزِ برنامه و نه رمزِ خواندنِ سرور.

## 🪪 ریمیکِ پروفایل و اشتراک‌ها — و قفلِ «منطق دست نخورد» (از ۱۴۰۵/۰۷/۰۵)

خواستهٔ صاحب ریپو: «بخشِ پروفایل را ۱۰۰ فیصد ریمیک کن… و برای اشتراک‌ها هم
بهترین‌ها را… منطق را اصلاً دست نمی‌زنی. این‌ها فقط برای پروفایل و اشتراک‌ها
بودند و دیگر جا را دست‌کاری نمی‌کنی.»

- ⛔ **سه ستونِ هم‌وزنِ قدیمیِ پروفایل رفت** («آواتار | مشخصات | اشتراک»): هر سه
  از بالای صفحه شروع می‌شدند، پس هیچ‌کدام سرصفحه نبود. حالا سرصفحهٔ پهن +
  چهار عددِ سرِ دست + دو ستون + ردیفِ تب‌ها.
- ⛔ **پلن‌ها کنارِ هم‌اند، نه زیرِ هم**: کارِ آن صفحه مقایسه است و چهار کارتِ
  صفحه‌قد یعنی چهار بار اسکرول برای یک تصمیم.
- ⛔ **فهرستِ ریشهٔ `{Binding …}`های هر دو صفحه پیش و پس از کار مو‌به‌مو یکی
  است** — نه یکی کم، نه یکی زیاد؛ و هیچ ویومدلی عوض نشد.
  `ProfileRemakeTests` همین را قفل می‌کند و با هر بازچینیِ بعدی هم می‌مانَد:
  هر ریشهٔ اتصال باید از قبل در سورسِ ویومدل باشد، و دوازده فرمانِ پروفایل
  یک‌به‌یک سنجیده می‌شوند.
  ⚠️ `[RelayCommand]` نامِ `XxxCommand` را خودش می‌سازد و در سورس نیست، پس
  آزمون پسوندِ `Command` را برمی‌دارد — وگرنه هر فرمانی را «تازه» می‌دید.
- ⛔ **پالتِ صفحهٔ ورود به پروفایل نیامد** و `RangBandi_FaghatDarSafheyeVorud_Ast`
  سرِ جایش است: پروفایل فقط از `Pump.*`ِ خودِ تم رنگ می‌گیرد.
- ⚠️ **قیمتِ پلن‌ها در ویومدل هنوز `—` است** در حالی که `PLANS-fa.md` قیمتِ
  دالری دارد. آن یک **مقدار** است نه ظاهر، و این کار فقط ظاهر بود.

## ⌨️ میانبرهای صفحه‌کلید — جابه‌جاییِ Alt، و کلیپ‌بوردِ اکسلی (از ۱۴۰۵/۰۷/۰۵)

خواستهٔ صاحب ریپو، با فهرست: «Ctrl+S ذخیره · Ctrl+C/V/X · Ctrl+Z/Y · Ctrl+A ·
Enter و Esc و Delete و Tab و کلیدهای جهت‌دار · اف‌۱ تا اف‌۱۲ هم قابلِ
استفاده‌اند — برای اف‌ها نمی‌دانم چی‌ها لازم است یا نه… **alt+عدد برود
بخش‌هایی که موجود است** · **Control+عدد اضافه کردنِ جدول** · **Shift+عدد هم
پاک کردنِ جدول** · همه‌شان با دقت و ظرافت کار کنند و هیچ بخشی منطقش خراب یا
دست‌کاری نشود.»

⚠️ **بیشترِ این فهرست از قبل بود، ولی با کلیدِ دیگر** — و یکی‌شان با خواسته
تضاد داشت. پس کارِ این بخش بیشتر «جابه‌جایی و کامل کردن» بود تا ساختن.

### ۱) ⛔ `Alt+عدد` بخش است و `Ctrl+Shift+عدد` کارت — نه برعکس

تا ۳.۱.۱۴۴ وارونه بود. جابه‌جا شد، و **هیچ‌کدام برداشته نشد**:

```
Alt + عدد          → بخشِ شمارهٔ N در نوار (۰ = دهم)
Ctrl + Shift + عدد → کارتِ شمارهٔ N در قرض‌داران و شرکت‌ها
Ctrl + عدد         → افزودنِ N ردیف
Shift + عدد        → برداشتنِ N ردیفِ آخر
```

- ⚠️ **«جدول» در خواستهٔ صاحب ریپو همان «ردیف» است** — همان کاری که
  `Ctrl+عدد`/`Shift+عدد` از روزِ اول می‌کردند (`IRowBatchHost`) و دکمه‌اش در
  خودِ برنامه «➕ ردیف» نام دارد. اگر روزی منظورش «آرشیو کردنِ کلِ جدول» بود،
  آن کارِ دیگری است و این دو کلید جایش نیستند.
- ⚠️ `Shift+عدد` همچنان **داخلِ کادرِ تایپ گرفته نمی‌شود** — قاعدهٔ ۱۴۰۵/۰۶/۲۹
  دست‌نخورده است، وگرنه دوباره `@ # ﷼ ( )` تایپ نمی‌شوند.
- ⚠️ ترتیبِ رها کردن: `Alt` که بالا آمد بخش باز می‌شود؛ برای کارت باید **هر
  دو** کلید بالا بیایند، وگرنه عددِ چندرقمی نصفه می‌پرد.
- آزمون: `ShortcutHelpTests.Alt_Baraye_Bakhsh_Ast_Va_CtrlShift_Baraye_Kart`
  روی خودِ سورس می‌گردد و برگشتن به حالتِ قبل را قرمز می‌کند.

### ۲) ⛔ کلیپ‌بورد و برگشت مالِ **جدول**‌اند، نه شنوندهٔ تونلیِ پنجره

`Ctrl+C/V/X/A/Z/Y` در `ExcelGrid.OnKeyDown` گرفته می‌شوند و **فقط بیرونِ حالتِ
ویرایش**. گرفتنشان در `ShortcutService` (که تونلی است) یعنی کادرِ تایپ دیگر
نمی‌تواند متنِ خودش را کپی یا برگردان کند — همان اشتباهی که یک بار با
`Shift+عدد` شد.

- ⛔ **`ExcelGrid.Write` تنها جای نوشتنِ کپی/برش/پیست/`Delete` است** و رشته را
  در همان خاصیتِ `…Text`ی ستون می‌نشاند — مو‌به‌مو همان کاری که تایپِ کاربر
  می‌کند، پس همان `Touch()` و `Apply()` و ذخیرهٔ تأخیری. هیچ محاسبه‌ای دور زده
  نمی‌شود و هیچ سرویسی از کنار صدا زده نمی‌شود.
- ⛔ **ردیفِ قفل‌شده (`ILockedRow`) و ستونِ خواندنی نوشته نمی‌شوند.** ⚠️ این
  یک **اصلاح** هم بود: تا دیروز `Delete` این قید را نداشت و می‌شد ردیفِ 📦
  خریدِ مخزن را در حسابِ شرکت خالی کرد، در حالی که تایپ رویش بسته بود.
- ⛔ **پیست ردیف نمی‌سازد** — آن‌چه از قابِ جدول بیرون بزند دور ریخته می‌شود.
  یک `Ctrl+V`ی ناخواسته نباید صدها ردیفِ واقعی در دفتر بسازد.
- ⚠️ یک خانه روی کادرِ چندتایی ⇒ همان مقدار در همهٔ خانه‌ها (مثلِ اکسل).
- ⚠️ `SelectedItems` ترتیبِ **انتخاب** را می‌دهد نه ترتیبِ جدول، پس `Selection()`
  ردیف‌ها را از روی `ItemsSource` مرتب می‌کند — وگرنه انتخاب از پایین به بالا
  متنِ وارونه کپی می‌کرد.
- ⚠️ نامش `Board` است نه `Clip`: `Visual.Clip` از پیش هست و هم‌نامی پنهانش
  می‌کرد.

### ۳) ⚠️ برگشت (`Ctrl+Z`) فقط «ویرایشِ خانه» است — و این عمدی است

⛔ **پس گرفته شد (۱۴۰۵/۰۷/۱۲)**: حالا حذفِ ردیف و حساب هم برمی‌گردد، از راهِ
سطلِ زباله، و برگشت مالِ کلِ برنامه است نه هر جدول — بخشِ «🔁 شیفت‌+عدد، برگشت
و دوباره» در انتهای همین فایل. متنِ پایین تاریخ است.

تایپ، خالی کردن، برش و پیست. **ساختن و حذفِ ردیف، حذفِ حساب و هر چیزی که در
دیتابیس ردیف می‌سازد یا می‌برد در آن نیست**: برگرداندنشان یک «تاریخچهٔ
عکس‌فوریِ کلِ دیتابیس» می‌خواهد که ساخته نشده، و وانمود کردن به داشتنش از
نداشتنش بدتر است. برای حذف، سطلِ زباله سرِ جایش است.

- ⛔ **تاریخچه مالِ همان جدول است، نه سراسری.** با `Reset` شدنِ فهرست (ماهِ
  دیگر، حسابِ دیگر) پاک می‌شود، و پیش از هر برگشت `Live()` می‌سنجد که آن ردیف
  هنوز در همان جدول هست — وگرنه نوشتن روی ردیفِ حذف‌شده یک ذخیرهٔ ناخواسته
  می‌ساخت.
- ⚠️ **حذفِ یک ردیف کلِ تاریخچه را پاک نمی‌کند** (فقط `Reset` می‌کند): ردیفِ
  رفته را خودِ `Live()` رد می‌کند، و بردنِ برگشتِ پنج خانهٔ تایپ‌شده به‌خاطرِ
  یک حذفِ بی‌ربط آزاردهنده است.
- ⚠️ عکسِ «پیش از ویرایش» در `BeginningEdit` گرفته می‌شود نه `CellEditEnding`،
  و مقایسه یک تیک **بعد** از پایانِ ویرایش انجام می‌شود — نشستنِ مقدار کارِ
  خودِ اتصال است و همان لحظه نیست.

### ۴) `Ctrl+S` — و چرا لازم بود

برنامه از روزِ اول خودکار ذخیره می‌کند (هر خانه ۳۵۰ میلی‌ثانیه بعد از آخرین
تایپ). پس `Ctrl+S` چیزِ تازه‌ای نمی‌سازد — **همین حالا** می‌نویسد و می‌گوید که
نوشت. برای کسی که سال‌ها با اکسل کار کرده، نبودنِ این کلید یعنی «پس ذخیره
نشد؟».

- ⛔ **اول خانهٔ بازِ جدول بسته می‌شود** (`ExcelGrid.CommitNow`)، وگرنه دقیقاً
  همان چیزی که کاربر همین لحظه تایپ کرده جا می‌ماند.
- ⚠️ همان الگوی بازتابیِ `Ctrl+P`: `FlushAsync`ی **عمومی** پیدا می‌شود و
  `private` نه — پس `FlushAsync`ی پیام‌رسان (که کارش فرستادنِ پیام است) صدا
  زده نمی‌شود.

### ۵) کلیدهای اکسلی از قبل بودند — دست نخوردند

`Enter` · `Esc` · `Tab`/`Shift+Tab` · فلش‌ها · `Home`/`End` · `PageUp`/`PageDown` ·
`Delete`/`Backspace` · `Ctrl+Delete` · `F2` · دوبار-کلیک روی خطِ ستون — همه از
پیش در `ExcelGrid` بودند و **یک خطشان هم عوض نشد**.

### ۶) `F1` فهرستِ خودِ میانبرهاست — و بقیهٔ اف‌ها عمداً آزادند

صاحب ریپو: «برای اف ۱ تا ۱۲ نمی‌دانم چی‌ها بزنم لازم است یا نه.» پس فقط `F1`
ساخته شد (`Views/ShortcutsWindow`) و `F2` که از قبل مالِ خودِ جدول بود دست
نخورد. ⛔ **برای `F3`…`F12` کارِ حدسی نبندید**: کلیدی که کاری نکند از کلیدی که
کارِ نامنتظره بکند بهتر است.

- ⛔ **`ShortcutsWindow.All` تنها جای نوشته شدنِ فهرست است** و
  `ShortcutHelpTests` هر کلیدِ گرفته‌شده را با آن می‌سنجد. فهرستی که با کد جور
  نباشد از نبودنِ فهرست بدتر است — آدم را دنبالِ کلیدی می‌فرستد که کاری
  نمی‌کند.
- ⚠️ رنگ‌های آن پنجره با `DynamicResource` بسته‌اند، نه مقدارِ ثابت: وگرنه با
  تعویضِ تم رنگِ تمِ قبلی را نگه می‌داشت.

⚠️ **و باز هم محدودیتِ سنجش**: این سندباکس `dotnet` ندارد، پس کامپایل و
آزمون‌ها این‌جا **دویده نشدند**. آن‌چه این‌جا شد: تراز شدنِ آکولادها، سنجشِ
هر رشتهٔ ادعاشده در آزمون‌ها روی خودِ فایل‌ها، و گرفتنِ سورسِ خودِ آوالونیا
۱۱.۲.۳ برای تاییدِ `DataGrid.SelectAll()` و سازندهٔ `DataGridBeginningEditEventArgs`
و `IBinding` بودنِ `DynamicResourceExtension`. «کامپایل می‌شود» را تا سبز شدنِ
CI نباید گفت.

## ⛔ «برنامه به‌روز است» فقط وقتی گفته می‌شود که واقعاً پرسیده باشیم (از ۱۴۰۵/۰۷/۰۶)

گزارشِ صاحب ریپو با عکسِ صفحهٔ «بک‌اپ و به‌روزرسانی‌ها»: «از داخل برنامه چرا
اپدیت نمیشه؟» — نسخهٔ فعلی ۳.۱.۱۴۴، و همان لحظه ۳.۱.۱۴۶ منتشر شده بود. و
بعدش: «برای برنامه سرور و فروشگاه هم همین طور، از گیت‌هاب اپدیت نمیگیرن.»

سنجیده شد، حدس زده نشد — هر سه انتشار با API خوانده شد:

| برنامه | تازه‌ترین ساخت | حال |
|---|---|---|
| پمپ (کامپیوتر) | `v3.1.146` با `PumpYaqobi-app-94629a9b.zip` (۶٫۶MB) | هست |
| مرکز فرمان | `windows-preview` با `ControlCenter-Setup-1.47.2.exe` | هست |
| دکان (اندروید) | `tohid-native/version.txt` ⇒ `3.2.272` (کدِ ۲۰۰) | هست |

⚠️ **و شناسهٔ پایه در ۱۴۳ تا ۱۴۶ یکی است** (`94629a9b`)، پس نصبِ ۳.۱.۱۴۴ باید
همان بستهٔ شش‌مگابایتی را بگیرد، نه ۱۱۰ مگابایت. یعنی بسته سرِ جایش بود و
مشکل در **خواندن** بود، نه در ساختن.

### ۱) ⛔ هر شکستی «به‌روز است» ترجمه می‌شد

`CheckAsync` روی هر خطا `None(current)` می‌داد و صفحه رویش می‌نوشت «برنامه
به‌روز است». پس قطعیِ اینترنت، بسته بودنِ مسیر، ۴۰۳ِ سقفِ نرخ (ساعتی ۶۰
درخواست برای آی‌پیِ بی‌توکن، و آن سهم پشتِ یک اینترنتِ مشترک زود تمام
می‌شود) و هر خطای TLS — همه یک جمله می‌دادند. همان «کلکِ دروغ»ی که برای
چراغِ سرور قدغن شد، این بار در به‌روزرسانی.

- ⛔ **سه حال، سه جمله، و هر سه در یک جا ساخته می‌شوند**:
  `UpdateInfo.StatusText` و `UpdateInfo.StatusBrushKey`. «نرسیدیم» سرخ است
  (`Pump.Danger`) و «به‌روز است» نه. ⛔ ویومدل جملهٔ خودش را نمی‌سازد —
  آزمونِ `TheScreenShowsTheHonestSentence` رشتهٔ «برنامه به‌روز است» را در
  ویومدل **قدغن** کرده.
- ⛔ **پیامِ خامِ استثنا به کاربر نمی‌رسد** (`Why(Exception)`): ممکن است نام
  یا نشانیِ میزبان داشته باشد. فقط جنسِ خرابی گفته می‌شود، و در نوشته
  «سرورِ به‌روزرسانی» است، نه نامِ هیچ سرویسی.

### ۲) ⛔ درِ دوم — بی هیچ پرس‌وجویی

ریپوی خواهر (`shop`، `update/Updater.kt`) از روزِ اول دو در داشت و دلیلش را
هم نوشته بود؛ برنامهٔ پمپ فقط یک در داشت.

```
۱) فهرستِ انتشار    یادداشت و اندازهٔ دقیق — ولی سقفِ نرخ دارد
۲) desktop-latest/version.txt + base.txt    بی سقف، بی احراز هویت
```

- ⛔ **نشانیِ درِ دوم از خودِ `FeedUrl` ساخته می‌شود** (`FileUrl`)، نه از یک
  رشتهٔ دوم: نامِ مخزن باید در کلِ برنامه یک جا باشد و
  `TheUpdateServiceItselfKeepsTheAddressPrivate` هر رشتهٔ دومی را رد می‌کند.
- ⛔ **`base.txt` لازم است**: درِ دوم هیچ فهرستی نمی‌خواند، پس فقط از روی آن
  می‌فهمد نامِ بستهٔ کوچک چیست و آیا با پایهٔ این نصب یکی است یا نه. ناجور
  بود ⇒ نصاب.
- ⛔ **برچسبِ بی‌شماره دیگر «به‌روز است» نمی‌شود**: اگر `tag_name` شماره
  نداشت (انتشارِ اپِ گوشی، یا هر برچسبِ چرخشی)، آن جواب مالِ برنامهٔ
  کامپیوتر نیست و درِ دوم زده می‌شود. همان تله‌ای که ریپوی سرور یک بار با
  انتشارِ اندروید خورد (`panelReleaseTag`).
- ⛔ **`desktop-latest` باید `prerelease: true` و `make_latest: false` باشد.**
  اگر «تازه‌ترین انتشار» شود، برنامه از آن هیچ شماره‌ای درنمی‌آورد و برای
  همیشه می‌گوید به‌روز است.
- ⚠️ **فایلِ نیمه‌کاره در هر دو در رد می‌شود**: درِ دوم اندازه را از قبل
  نمی‌داند، پس `DownloadAsync` آن را از خودِ پاسخ برمی‌دارد (`expected`).
  بی این خط، نگهبانِ «نیمه‌کاره» روی آن مسیر بی‌اثر بود.

### ۳) ⛔ و همان برچسبِ نبوده، دانلودِ ویندوزِ سایت را هم کشته بود

`deploy-pages.yml` سه بار `grab('desktop-latest', …)` می‌زد و چنین برچسبی
**هیچ‌وقت ساخته نمی‌شد** (سنجیده شد: ۴۰۴). پس فقط یک `::warning::` می‌گرفت،
`downloads/PumpYaqobi-Setup.exe` کنارِ سایت نبود و `version.json` ستونِ
`windows` را `null` می‌داد — ماه‌ها، بی‌صدا. حالا `build-native.yml` همان
برچسب را منتشر می‌کند و `InstallerTests` هر دو سمت را قفل کرده.

- ⛔ **زیپِ آن برچسب کنارِ سایت گذاشته نمی‌شود**: آن بستهٔ **کوچکِ
  به‌روزرسانی** است، نه برنامهٔ کامل. کسی که شش مگابایت بگیرد برنامه‌ای
  می‌گیرد که بالا نمی‌آید. سایت فقط نصاب را می‌دهد.

### ۴) و آن‌چه کارِ کد نیست

⚠️ **اگر هر دو در بسته باشند، مشکل شبکه است نه برنامه** — و از امروز خودِ
برنامه همین را می‌گوید. نشانه‌اش این است که اپِ دکان هم به‌روز نمی‌شود، چون
درِ دومش یک فایلِ ساده روی مسیرِ دانلود است (بی API و بی سقفِ نرخ): اگر آن
هم نمی‌آید، `github.com` از آن شبکه در دسترس نیست.

⚠️ **مرکز فرمان از اول راستش را می‌گفت**: `checkForUpdate` خطا را در
`out.error` می‌گذارد و `Updates.tsx` همان را نشان می‌دهد، و «به‌روز است» را
جدا. پس اگر پنل به‌روز نمی‌شود، همان سطرِ سرخِ خودش دلیل را می‌گوید. ⛔ آن
هم درِ دوم ندارد — کارِ مانده، در ریپوی `server`.

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا دویده
نشدند — CI روی PR می‌دواندشان.

### ⛔ و دکمه‌ای که دیده نمی‌شد — سفید روی سفید (همان روز)

گزارشِ دومِ صاحب ریپو: «دکمهٔ دیدنِ این‌که به‌روزرسانی است را اصلاً نمی‌بینم،
آن دکمه‌اش کجا شده؟»

دکمه **سرِ جایش بود**. ریشه در سبکِ **پایهٔ** دکمه بود:
`Selector="Button"` زمینه‌اش `Pump.Card` را می‌گذاشت، و در تمِ آبی هم
`Pump.Card` سفیدِ خالص است هم `Pump.Panel` — پس دکمهٔ **بی‌کلاس** می‌شد
سفید روی سفید، با یک خطِ ۱ پیکسلیِ `#b9d3f5`. دیده نمی‌شد چون هیچ
اختلافی نداشت، نه چون نبود.

- ⛔ **سبکِ پایهٔ دکمه از امروز `Pump.Input` است، نه `Pump.Card`.** همان
  قاعدهٔ کلیدهای ماشین‌حساب از ۱۴۰۵/۰۶/۲۵ («کلیدها `Pump.Input` تا در تمِ
  تیره از کارت جدا باشند») — این بار برای هر دکمهٔ بی‌کلاسِ کلِ برنامه، که
  **۴۵** تاست. در آبی `#e4ecf8` روی کارتِ سفید، در طلایی `#131317` زیرِ
  کارتِ `#212127`: در هر دو تم یک پله جدا.
  ⚠️ `accent` · `ghost` · `seg` · `fuelchip` · `nav` · `pill-btn` و بقیه
  زمینهٔ خودشان را دارند و دست نخوردند.
- ⛔ **و خودِ «🔄 بررسیِ به‌روزرسانی» تاکیدی شد** (`Classes="accent"`): کارِ
  اصلیِ آن کارت است و کاربر باید بی گشتن پیدایش کند.
- آزمون: `APlainButtonHasItsOwnSurface_NotTheCardsOwn` (سبکِ پایه + وجودِ
  `Input` در هر دو تم) و `TheCheckButtonIsThePrimaryActionOfItsCard`.

⚠️ **درسِ روش**: «نمی‌بینمش» با «نیست» یکی نیست. اول در XAML گشتم و دکمه
**بود**؛ جواب در رنگِ سطح‌ها بود، نه در چیدمان. اگر دنبالِ باگِ چیدمان
می‌رفتم، ساعت‌ها جای اشتباه را می‌گشتم.

## ⛔ دکمهٔ به‌روزرسانی خودش را غیرفعال می‌کرد (از ۱۴۰۵/۰۷/۰۶، بارِ سوم)

گزارشِ صاحب ریپو: «هنوز اون دکمه کار نمیکنه و برسی نمیکنه و دانلود هم نیست…
ببین فرقش با ده نسخه قبل چیه که این مشکل به وجود اومده، درستش می‌کنی با تست
نه حدس.»

**فرقی نبود.** `git log` روی همان دو فایل: `UpdateService.cs` از **۳.۱.۴۹**
دست نخورده بود و `BackupSectionViewModel.cs` از **۳.۱.۱۰۴**. یعنی این باگ
پس‌رفتِ ده نسخهٔ آخر نبود؛ از اول بود و هیچ‌وقت سنجیده نشده بود. چهار ریشه:

### ۱) ⛔ `AsyncRelayCommand`ِ پیش‌فرض دکمهٔ خودش را می‌کشد

تا پایانِ یک اجرا `CanExecute` را `false` می‌کند — و آن اجرا **یک درخواستِ
اینترنتی** است، تا ۷۵ ثانیه با دو در. پس کلیکِ اول دکمه را خاکستری می‌کرد و
هر کلیکِ بعدی بی‌صدا بلعیده می‌شد. و `IsEnabled="{Binding !Checking}"` همان
را دوبار می‌کرد، چون بررسیِ **خودکارِ** سرِ باز شدنِ صفحه هم `Checking` را
روشن می‌کرد.

- ⛔ `[RelayCommand(AllowConcurrentExecutions = true)]` — همان قاعدهٔ
  `DebtSectionViewModel.OpenAsync` («کلیکِ اول هیچ کاری نمی‌کند»)، این بار
  برای کارتِ به‌روزرسانی. و `IsEnabled` از آن دکمه برداشته شد.
- ⛔ کلیک، بررسیِ در جریان را **لغو** می‌کند و از نو می‌پرسد — نه این‌که
  بی‌صدا رد شود. دکمه‌ای که زده شود و هیچ اتفاقی نیفتد باگ است، نه قفل.

### ۲) ⛔ هیچ `catch`ی نبود

استثنا از فرمان بیرون می‌زد و صفحه برای همیشه روی «در حال بررسی…» می‌ماند.
حالا هر شکستی یک جملهٔ **سرخ** است، و `DownloadAsync` هم هیچ‌وقت استثنا
بیرون نمی‌دهد (یک قطعیِ وسطِ دانلود، ضدِ ویروسی که `.part` را قفل کند، یا
دیسکِ پر) — همان «دانلود هم نیست».

### ۳) ⛔ نصاب در پوشهٔ **دیگری** می‌نشست

`UsePreviousAppDir=yes` پوشه را از **ثبتِ نصبِ پیشین** برمی‌دارد، و نصبی که
کاربر خودش از زیپ باز کرده هیچ ثبتی ندارد. پس نصابِ بی‌صدا می‌رفت در
`%LocalAppData%\Programs\PumpYaqobi` می‌نشست — یک پوشهٔ دیگر — و نسخهٔ در
حالِ اجرا (`D:\…\PumpYaqobi`) دست‌نخورده می‌ماند. حالا `/DIR="<پوشهٔ نصب>"`.

### ۴) ⛔ راهی برای بیرون نبود

«باز کردنِ صفحهٔ دانلود» صفحهٔ انتشار را در مرورگرِ سیستم باز می‌کند.
⚠️ نشانی همچنان در رابط **نوشته نمی‌شود** و ساختنش داخلِ `UpdateService`
است، همان یک جا (`TheUpdateServiceItselfKeepsTheAddressPrivate`).

### ⛔ و ریشهٔ ریشه‌ها: هیچ آزمونِ **رفتاری**ای نبود

سی‌ویک آزمون دربارهٔ به‌روزرسانی داشتیم و **هیچ‌کدام** نه `CheckAsync` را
می‌دواند و نه `DownloadAsync` را — همه رشته‌های سورس و خودِ رکورد را
می‌سنجیدند. پس هر خرابیِ رفتاریِ این مسیر بی‌صدا از CI رد می‌شد.

- ⛔ `UpdateService.TestTransport` (همان الگوی `CloudLink.TestTransport`) و
  `UpdateBehaviourTests`: درِ اول، بستهٔ کوچکِ هم‌پایه، پایهٔ ناجور ⇒ نصاب،
  سقفِ نرخ ⇒ درِ دوم، **هر دو در بسته ⇒ «به‌روز است» نمی‌گوید**، دانلودِ
  کامل، دانلودِ بریده، و استثنا. ⛔ این درگاه را برندارید.
- ⚠️ `AppBase.LocalIdOverride` هم فقط برای همین است — مثلِ
  `AppSettings.DirOverride`، و فقط از خودِ آزمون مقدار می‌گیرد.

## 🧵 پارچه‌ها: کادرِ بلند، هشدارِ بی‌جابه‌جایی، زنجیرهٔ پایه (از ۱۴۰۵/۰۷/۰۶)

### ۱) کادرِ نامِ کارمند **بلندتر**، نه پهن‌تر

«طولش بزرگ باشه نه عرضش.» پس ستونِ ۲۱۰ همان ماند و فقط `MinHeight="48"`
آمد. ⛔ پهن‌ترش نکنید — همان اشتباهِ بارِ اول بود.

### ۲) ⛔ ریشهٔ «نمی‌گذارد بروم کادرِ پایین» یک جابه‌جاییِ چیدمان بود

کادرِ هشدارِ «پایه کمتر است» با دکمهٔ «✔ دیدم» داخلِ همان ستون **باز
می‌شد** و «ختم پایه» را ~۷۰ پیکسل پایین می‌پراند: کاربر تایپ می‌کرد و کادرِ
بعدی از زیرِ نشانگرش فرار می‌کرد.

- ⛔ حالا یک `Grid Height="22"`ِ **همیشه‌حاضر** است (خالی، وقتی هشداری
  نیست)، پس **هیچ چیزی جابه‌جا نمی‌شود**. کادرِ بازشو را برنگردانید.
- ⛔ **خودِ عدد داخلِ کادرش سرخ می‌شود** (`ShiftFormViewModel.StartBrushKey`)
  — «توی خود همون کادر هم عدد سرخ بشه». پس با رفتن به کادرِ دیگر نشانه از
  چشم نمی‌افتد و پیام هم دوباره «نمی‌پرد».
- ⛔ و مانع نیست، مثلِ همیشه: ذخیره سرِ جایش است (`BaseHistoryTests`).
- ⚠️ `Enter` از قبل به کادرِ بعدی می‌رفت (`FieldNavigationService`) و دست
  نخورد؛ `IsField` دکمه را هدف نمی‌گیرد، پس «دیدم» راهِ کلید را نمی‌بندد.

### ۳) ⛓️ زنجیرهٔ پایه — «ختمِ این، شروعِ بعدی» (اختیاری)

«یکی رو رسوندم ۱۰۰۰۰۰ و ختمِ این باشه؛ شروعِ اون پارچهٔ جدید باید ۱۰۰۰۰۰
باشه، و اگه مثلاً ۱۰۰۰۰۱ بود بگه این مقدار بیشتر زده شده و بررسی باید بشه…
و من چندین پایه دارم و می‌خوام با پایه‌ها در ارتباط باشن.»

سه حال، و هر سه **به ازای همان شمارهٔ پایه** (`PostingService`مانندِ
`Apply` در `ParchaSectionViewModel`):

| شروع در برابر ختمِ قبلیِ همان پایه | چه می‌شود |
|---|---|
| کمتر | «کمتر است» — همان هشدارِ قبلی، دست‌نخورده |
| بیشتر | «N لیتر بیشتر زده شده — بررسی شود» — **تازه و اختیاری** |
| برابر | زنجیره سالم است، هیچ نشانه‌ای |

- ⚠️ **روز و شبِ یک پایه یک زنجیره‌اند**، نه دو تا: شمارندهٔ پایه یکی است و
  شیفتِ شب از همان‌جایی شروع می‌شود که روز تمام کرده. پایهٔ دوم زنجیرهٔ
  خودش را دارد و هیچ‌وقت با پایهٔ اول سنجیده نمی‌شود — همان کلیدِ `PumpNum`
  که `LastBaseAsync` و `BaseHistoryAsync` از قبل داشتند.
- ⛔ **«بیشتر» فقط با شمارهٔ پایهٔ نوشته‌شده سنجیده می‌شود** (`num > 0`): بی
  شماره، `LastBaseAsync` بزرگ‌ترین ختمِ **همهٔ** پایه‌ها را می‌دهد و برابری
  با آن بی‌معناست — هر پارچهٔ سالمی هشدار می‌گرفت.
- ⚠️ کلیدش `AppSettings.ParchaChainCheck` است (پیش‌فرض روشن) و با
  `SaveSoon()` می‌نشیند؛ پس باید در فهرستِ `SaveComfortOnly` هم باشد —
  آن دو فهرست باید یکی بمانند.
- ⚠️ و مقدارش در `LoadAsync` خوانده می‌شود، **نه در سازنده**: سازندهٔ بخش‌ها
  روی مسیرِ باز شدنِ برنامه است.

### ۴) ⛔ دکمه‌های انتقالِ پایه هیچ فلشی ندارند

«دکمه‌های چپ و راست برعکس کار می‌کنن.» **رفتارشان درست بود** (`PullBase` ختمِ
کارتِ دیگر را در شروعِ این می‌نشاند و `PushBase` برعکس). آن‌چه برعکس بود
نوشتهٔ رویشان: کارتِ روز ستونِ صفر است و در راست‌به‌چپ **سمتِ راست** رندر
می‌شود، پس «از شب» یعنی از چپ به راست ولی فلشِ `←` رویش بود. و بدتر: `←` و
`→` هر دو `Bidi_Mirrored=Yes` هستند، پس در متنِ راست‌به‌چپ خودِ شکل‌دهنده
آینه‌شان می‌کند — یعنی هر کدام را می‌گذاشتیم، «درست» بودنش به رفتارِ رندرِ
متن بند بود.

⛔ پس فلش رفت و جایش **واژه** نشست: «گرفتن از شب» · «فرستادن به شب».
فلش را برنگردانید.

## 🕘 تاریخچه‌ها: برگشت به همان بخشی که از آن آمدیم (از ۱۴۰۵/۰۷/۰۶)

«دکمهٔ برگشت به همون بخش که ازش رفتم نداره و می‌ره توی بخشِ تاریخچه‌ها.»

راهِ رفت (`AppHost.OpenHistory`) بود و راهِ برگشت نبود: «برگشت به تاریخچه‌ها»
فقط از صفحهٔ یک بخش به فهرستِ کارت‌ها می‌رفت، و کاربری که از «گاوصندوق» آمده
بود باید از نوار دنبالش می‌گشت.

- `AppHost.GoSection` (شناسهٔ بخش) + `HistorySectionViewModel.SetOrigin`.
  ⛔ **از همان `GoAsync`ی همیشگی می‌رود**، نه یک مسیرِ دوم — پس قفلِ پلن و
  رمزِ بخش سرِ جایشان‌اند.
- ⚠️ مبدأ **پیش از** `GoAsync` ثبت می‌شود، و اگر خودِ تاریخچه‌ها باز بود دست
  نمی‌خورد (زدنِ کارتِ دیگر راهِ برگشت را به «تاریخچه‌ها» عوض نمی‌کند).
- ⚠️ و رفتنِ **مستقیم** به تاریخچه‌ها (از نوار یا `Alt+عدد`) مبدأ را پاک
  می‌کند (`_historyFromSection`) — وگرنه دکمهٔ «برگشت به گاوصندوق» یک هفتهٔ
  بعد هم آن‌جا می‌مانْد.
- دکمه روی **هر دو** صفحه است: «برگشت به تاریخچه‌ها» آدم را در فهرستِ
  کارت‌ها می‌گذارد و راهِ برگشت باید آن‌جا هم باشد.

## ⛽ ورق‌ها: ستونِ «نوع تیل» رفت، و نام خودش سوخت و واحد را می‌گوید (از ۱۴۰۵/۰۷/۰۶)

«از داخلِ بخشِ ورق‌ها از تراکنش‌ها نوعِ تیل رو حذف کن… ولی اگر توی نام پطرول
یا دیزل نوشتم توی حسابِ یارو همان انتخاب بشه اتومات… و اگه «پ» خالی نوشتم
پطرول ذخیره بشه و اگه «د» خالی نوشتم دیزل — هر جای جمله، وسط یا اول یا آخر…
و توی نام‌ها و توضیحاتِ همون حساب دیده نشه… و اسمِ قرض‌دار رو که نوشتم
سیستم اتومات تشخیص بده واحدِ این حساب تیل است یا پول… این‌ها برای حساب‌های
فرعی هم صدق بشه.»

### ۱) ⛔ ستونِ «نوع تیل» از **تراکنش‌ها** برداشته شد و برنمی‌گردد

⚠️ فقط **ستونش** — خودِ `Fuel`ی ردیف نرفت، و `FuelChipText` /
`ToggleFuelCommand` سرِ جایشان‌اند چون جدولِ **پایه‌ها**ی همان صفحه هم از
همان کپسول می‌خواند. ستونِ «واحد» هم ماند: آن تصمیمِ **دفتر** است.

### ۲) ⛔ نشانهٔ تک‌حرفی فقط وقتی **واژهٔ کامل** باشد

`PostingService`: `DetectFuelType` · `MentionsFuel` · `StripFuelWords` حالا
«پ» و «د»ی خالی را هم می‌شناسند — ولی **واژه‌به‌واژه**، نه زیررشته‌ای.

⛔ این مهم‌ترین بندِ این بخش است: با سنجشِ زیررشته‌ای، «د»ی داخلِ «داوود» و
«پ»ی داخلِ «پرویز» می‌گرفت — یعنی نامِ نیمِ قرض‌دارها «دیزل» خوانده می‌شد و
یک حرف از نامِ نمایشیِ حسابشان هم پاک می‌شد. `FuelMarkAndUnitTests` هر پنج
نام را جدا قدغن کرده.

⚠️ و **واژهٔ کامل جلوتر از نشانهٔ تک‌حرفی** است: «دیزل پ هارون» دیزل می‌ماند.

### ۳) واحدِ حساب: تیل یا پول — و «هر دو» یعنی دست نزن

`PostingService.UnitForAccount(hasFuelRows, hasMoneyRows, mode)`:

| حساب | نتیجه |
|---|---|
| فقط دفترِ تیل ردیف دارد | تیل |
| فقط دفترِ پول ردیف دارد | پول |
| **هر دو** | `null` ⇒ ⛔ **دست نزن** — «میرزا خودش تغییر بده» |
| هیچ‌کدام (حسابِ خالی) | واحدِ اعلامیِ خودِ حساب (`DebtAccount.Mode`) |

⛔ `null` بودنِ حالتِ «هر دو» جانِ این قاعده است: حدس زدنش یعنی یک قرضِ تیل
در دفترِ پول — همان «حتی یک عدد هم اشتباه به حساب نره».

### ۴) ⚠️ و هیچ ردیفی برای این کار خوانده نمی‌شود

`DebtorService.AccountUnitsAsync()` — «این دفتر ردیف دارد یا نه» با دو
`Distinct` روی همان ایندکس‌های `FuelAccountId` و `MoneyAccountId` درمی‌آید.
نتیجه در `WaraqSectionViewModel` با ترمزِ `PumpDbContext.Version` کَش
می‌شود، پس تا شمارهٔ دفتر عوض نشود **صفر** دستورِ دیتابیس — این تابع با
**هر حرفِ تایپِ کاربر** صدا زده می‌شود.

⛔ و تطبیقِ نام دوباره نوشته نشد: همان `PostingService.FindAccountForText`ِ
خودِ پست، که حساب‌های **فرعی** را هم امتیاز می‌دهد. دو نسخه یعنی روزی ردیف
در دفتری بنشیند که صفحه نشانش داده بود.

⚠️ این یک **راحتی** است: خودِ پست (`WaraqPostingService`) مستقل و دوباره
همین دو قاعده را می‌زند، پس نشدنش هیچ عددی را غلط نمی‌کند.

### آزمون

`FuelMarkAndUnitTests` (۲۲ سنجهٔ خالص) · `ParchaHistoryWaraqUxTests` (نُه
سنجهٔ سورس) · و دو سنجهٔ **پایان‌به‌پایان** در `WaraqPostingDbTests`:
«نامِ حسابِ فرعی در ورق در حسابِ خودِ همان فرعی می‌نشیند و قرض‌دارِ تکراری
ساخته نمی‌شود» و «‹د›ی خالی سوخت را دیزل می‌کند و در نامِ حساب دیده
نمی‌شود».

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا دویده
نشدند. آن‌چه شد: تراز شدنِ آکولادها، سنجشِ XMLِ هر سه نما، شبیه‌سازیِ
پایتونیِ قاعدهٔ تازهٔ «پ/د» روی هر ۲۲ ورودیِ آزمون، و سنجشِ هر رشتهٔ
ادعاشده در آزمون‌ها روی خودِ فایل‌ها. «کامپایل می‌شود» را تا سبز شدنِ CI
نباید گفت.

## 🔓 برنامه بی رمز باز می‌شود — و رمز را خودِ کاربر می‌گذارد (از ۱۴۰۵/۰۷/۰۷)

خواستهٔ صریحِ صاحب ریپو: «برای هر کامپیوتر یک حسابِ جدید باشه و بدون رمز، مگر
حسابِ کاربری‌شو بزنه که حساب‌های همون شخص بیاد… کسایی که تازه به برنامه
می‌رسن نباید رمز داشته باشن و خودِ طرف برای خودش رمزِ خودشو می‌زنه… و هر کی
روی کامپیوترِ جدید نصب کنه اطلاعات نباشه داخلش، مگر این که فولدرها رو یکی
برده باشه به کامپیوترِ جدید.»

```
نصبِ تازه ⇒ AuthService.OpenWithoutPassword()  ⇒ هیچ صفحهٔ قفلی، همان لحظه باز
رمز گذاشتن ⇒ تنظیمات ← 🔑 رمزها و کد           ⇒ از آن پس صفحهٔ قفل هست
برداشتنش  ⇒ همان‌جا، با رمزِ فعلی              ⇒ دوباره بی‌رمز
```

- ⛔ **صفحهٔ قفل دیگر رمز نمی‌سازد.** «نخستین اجرا ⇒ رمز بساز» (کادرِ «تکرارِ
  رمز» و `IsFirstRun`/`Confirm`) کاملاً برداشته شد. تنها جای **ساختنِ** رمز
  «تنظیمات ← رمزها و کد» است.
- ⛔ **هشِ خالی یعنی «رمزی نیست»، نه «رمزی که خالی است».**
  `PasswordHasher.Verify` با هشِ خالی هیچ‌وقت درست نمی‌گوید (حتی با رشتهٔ
  خالی)، پس تصمیم فقط از `AuthService.HasPassword()` می‌آید — نه از مقایسهٔ
  رشته. `NoPasswordTests` هر دو را جدا می‌سنجد.
- ⛔ **`MainViewModel.LockOrOpenAsync` تنها جای تصمیم است** و هر سه راهِ
  خروجِ `WarmUpAsync` از آن رد می‌شوند. پیش از این سه جا `Phase = Locked`
  می‌نوشتند و یکی‌شان جا می‌ماند یعنی گاهی صفحهٔ قفلِ بی‌رمز.
- ⛔ **بی رمز، «خروج» یک بن‌بست است** و انجام نمی‌شود: صفحهٔ قفلی که رمزی
  برای زدن ندارد کاربر را از دفترِ خودش بیرون می‌گذاشت و تنها راهِ برگشت
  بستن و باز کردنِ برنامه بود. `SignOut` می‌گوید چرا (توست)، بی‌صدا رد
  نمی‌شود.
- ⚠️ **بی رمز، اپِ کارمندان هم باز نمی‌شود** و این عمدی است: آن‌چه منتشر
  می‌شود هشِ **خالی** است و گوشی خودش می‌گوید صاحبِ پمپ هنوز رمزی نگذاشته.
  کیو‌آر و کدِ پمپ روی کاغذ می‌گردند؛ دفترِ پمپ نباید بی رمز از شبکه خوانده
  شود. کارتِ «رمزها و کد» همین را می‌گوید (`AppLockHint`).
- ⚠️ **«هر کامپیوتر، دفترِ خودش» از اول درست بود** و حالا آزمون دارد: دفتر
  در `%AppData%\PumpYaqobi\pump.db` است، نه کنارِ فایل‌های نصب. پس نصبِ تازه
  روی کامپیوترِ تازه **خالی** بالا می‌آید، و بردنِ همان پوشه یعنی بردنِ همان
  دفتر (`Daftar_DarPusheyeKarbar_Ast_NaKenareNasab`).
- ⚠️ **سنجه‌ها رمزشان را خودشان می‌گذارند**: هر سنجهٔ رابطی که پیش از این به
  «نخستین اجرا» تکیه می‌کرد، حالا درست بعد از `AppHost.Start` یک
  `CreateFirstAdmin("1234")` دارد — پس همان مسیرِ رمزدار سنجیده می‌شود و
  عددهای کارایی جابه‌جا نمی‌شوند. `LockIn.Wait` هم نصبِ بی‌رمز را می‌فهمد و
  در آن حالت **هیچ دکمه‌ای نمی‌زند** (زدنِ «ورود» روی حسابِ بی‌رمز فقط یک
  «رمز درست نیست»ِ بی‌معنا می‌ساخت).
- آزمون: `NoPasswordTests` + `NoPasswordSourceTests` (۹ بند).

## 🧮 نوارِ «جمله» به مرزِ ستون‌ها چسبید · گاوصندوق سه خانه شد (از ۱۴۰۵/۰۷/۰۷)

گزارشِ صاحب ریپو: «پایینِ جدول‌ها توی جمله‌ها خطِ جدول روی جمله‌ها وصل نیست و
به‌خوبی دیده نمی‌شود که این جمله برای کدام است… بخشِ گاوصندوق پایینِ جدول توی
جمله‌ها چی‌ها نوشته درستش کن، خیلی خراب است؛ با کادرهای خودِ همان کادر وصل
نیست، هم‌اندازهٔ همان‌ها نمی‌شود، و آدم نمی‌فهمد چی به چیه.»

دو ریشهٔ جدا، و هر دو بسته شد:

- ⛔ **خانهٔ بی‌ستون حالا به مرزِ ستون‌های واقعی می‌چسبد** (`TotalsStrip.Snap`).
  خانهٔ **ستون‌دار** از اول درست بود (لبه‌هایش همان لبه‌های سرستون)، ولی
  بی‌ستون‌ها جا را به تساوی می‌گرفتند، پس خطِ کنارشان هیچ‌جا روی خطِ جدول
  نمی‌افتاد. حالا هر خانه یک یا چند **ستونِ کامل** را می‌گیرد.
  - ⛔ **و اگر جا نبود، دست نمی‌زند**: خانه‌های بیشتر از ستون‌های آزاد یعنی
    چسباندنی در کار نیست، و نصفه‌چسباندن از نچسباندن بدتر است — همان چیدنِ
    همیشگی سرِ جایش می‌ماند.
- ⛔ **گاوصندوق از شش خانه به سه خانه رسید.** شش خانهٔ بی‌ستون در نواری با شش
  ستون هیچ‌وقت جا نمی‌شد، و بدتر: واحد فقط در برچسبِ ریز نوشته می‌شد. حالا
  هر خانه یک موضوع است («بردگی» · «ماندگی» · «خالص») و واحد **کنارِ خودِ
  عدد** («۱۲٬۰۰۰ افغانی · ۳۰۰ $»).
  - ⚠️ هیچ عددی عوض نشد و هیچ جمعِ تازه‌ای ساخته نشد؛ افغانی و دالر همچنان
    **هرگز** با هم جمع نمی‌شوند.
  - ⚠️ رنگِ «خالص» با منفی بودنِ **هر کدام** از دو عدد سرخ می‌شود، وگرنه یک
    خالصِ منفیِ دالری بی‌نشان می‌مانْد.
- ⚠️ سنجهٔ `chrome` دست‌نخورده سبز می‌ماند: خانهٔ بی‌ستون را عمداً نمی‌سنجد و
  فقط «هر جمله ستونی به نامِ خودش دارد؟» را می‌گیرد.

## 🧹 نوارها جا اضافه نگیرند — قرض‌داران · حسابِ شخص · شرکت‌ها · صرافی (از ۱۴۰۵/۰۷/۰۷)

چهار گزارشِ جدا، یک درد: نوارِ ابزار می‌شکست و کادرها به خطِ بعد می‌افتادند.

| کجا | چه شد |
|---|---|
| **قرض‌داران** | «مدت عضویت همه» و «اسکنِ کیو‌آر» از نوار رفتند؛ جست‌وجو هم‌ترازِ دکمه‌ها شد |
| **حسابِ شخص** | نوشتهٔ «فاکتورها» و «آرشیو» کوتاه شدند تا کنارِ هم در یک خط بمانند |
| **شرکت‌ها** | «جدول‌های آرشیو» و «جستجوی خرید» از کشویی **بیرون** آمدند و کنارش نشستند |
| **صرافی** | سربرگ از دو طبقه به **یک ردیفِ سه‌کارته** رسید، مثلِ «مصارف» |

- ⛔ **«مدت عضویت» گم نشد**: از روزِ اول **زیربخشِ** همین بخش است
  (`MembershipSectionViewModel`، کارتِ «⏳ مدت عضویت قرض‌داران») و آن دکمه فقط
  درِ دومِ همان بود. ⚠️ و هیچ منطقی برداشته نشد: `ShowMembershipCommand` و
  `ScanQrCommand` سرِ جایشان در ویومدل‌اند.
- ⛔ **جست‌وجوی قرض‌داران بلندی و قلمِ صریحِ خودش را پس داد** (۴۴ و ۱۴٫۵): هر
  دو از دکمهٔ بی‌کلاسِ کنارش بلندتر و درشت‌تر بودند. حالا هر سه از سبکِ پایه
  بلندی می‌گیرند (۳۶) — همان «اندازه‌های فونتشون رو برابر و دقیق بزار،
  هم‌تراز باشن».
- ⛔ **«🧾 فاکتورها: ۲ · 🟡 ۱ در صف» شد «🧾 فاکتورها ۲ 🟡۱ ▼»** و «🗂️ آرشیو —
  جدول ۳» شد «🗂️ آرشیو ۳». ⚠️ هیچ عددی عوض نشد، فقط کوتاه‌تر نوشته می‌شود؛
  و «· همه تایید شده» که هیچ کاری نمی‌کرد کلاً رفت.
- ⛔ **«خریدهای مخزن» و «جدول جدید» عمداً داخلِ کشویی ماندند** — خواستهٔ
  ۱۴۰۵/۰۷/۰۶ («چرا دوتا از هر کدام است؟») سرِ جایش است. فقط آن دو تایی که
  صاحب ریپو نام برد بیرون آمدند.
- ⛔ **نوارِ پهنِ «الباقی»ِ صرافی برداشته شد** و الباقی کارتِ سومِ همان ردیف
  شد. ⚠️ `BaqiSentence` در ویومدل دست‌نخورده است و `BaqiSentenceNote` زیرِ
  عدد می‌ماند تا معلوم باشد مثبت یعنی طلب و منفی یعنی بدهی.
- ⛔ **نامِ شرکت روی کارتش وسط‌چین و کامل شد** (`TextBlock.cname`): نامِ
  دوکلمه‌ای با `NoWrap` بریده می‌شد، یعنی «اسمِ دقیق» دیده نمی‌شد. ⚠️
  `.pname` دست‌نخورده ماند — کارتِ قرض‌دار همان است که بود.

## 📅 ماه و سال: از تاریخِ خودِ ردیف، و نیمه‌شبِ آخرِ ماه خودش جلو می‌رود (از ۱۴۰۵/۰۷/۰۷)

پرسشِ صاحب ریپو: «بخش‌هایی که ماه و سال دارند با دقت کار می‌کنند؟ عوض کردنِ
تاریخ از کامپیوتر یا جای دیگر به‌هم که نمی‌خورد؟ و اتومات ماه که عوض بشه، ماه
هم جدید می‌شود با حساب‌های ماهِ جدید می‌آید یا که نه؟»

سه جواب، و هر سه حالا آزمون دارند (`MonthRolloverTests`):

- ✅ **ماهِ هر ردیف از تاریخِ خودِ همان ردیف درمی‌آید** (`MonthKey` روی دیسک
  می‌نشیند، از `DateShamsi`ِ همان ردیف). پس عوض شدنِ ساعتِ کامپیوتر **هیچ
  ردیفی را از ماهش بیرون نمی‌برد** — دفترِ پارسال پارسال می‌ماند. تاریخِ
  خراب هم هیچ ماهی نمی‌سازد، نه «ماهِ امروز».
- ⛔ **و نیمه‌شبِ آخرِ ماه، تا امروز جلو نمی‌رفت.** `_month` یک بار در سازنده
  از `Shamsi.ThisMonth()` پر می‌شد (یعنی لحظهٔ باز شدنِ برنامه) و
  `DayChanged` فقط تاریخِ سربرگ و نوار را از نو می‌ساخت. برنامه‌ای که شبِ
  آخرِ ماه باز مانده بود، فردا هنوز جدولِ ماهِ گذشته را نشان می‌داد. حالا
  `SectionViewModel.OnDayChanged()` هست و بخش‌های دفتری به ماهِ تازه می‌روند.
- ⛔ **ماهِ انتخابیِ خودِ کاربر هیچ‌وقت زیرِ دستش عوض نمی‌شود.** تصمیم خالص و
  آزمون‌دار است: `MonthRoll.Decide(shown, auto, now)` — فقط وقتی ماه واقعاً
  عوض شده **و** کاربر همان ماهِ خودکار را می‌دیده. «۱۴۰۴/۰۳» یا «همهٔ
  ماه‌ها» دست نمی‌خورد.
- ⚠️ **پیش‌فرضِ `OnDayChanged` هیچ کاری نمی‌کند** — همان قاعدهٔ «بخشی که
  تویش نیستی هیچ مصرفی ندارد». فقط بخش‌های دفتری آن را می‌گیرند.
- ⚠️ و ردیفی که تاریخش در نمای جاری نمی‌گنجد از قبل هم گم نمی‌شد:
  `AddRowAsync` خودش نما را به ماهِ همان ردیف می‌برد.

## 🔌 «سرور با برنامهٔ پمپ» — جوابِ سنجیده (از ۱۴۰۵/۰۷/۰۷)

خواستهٔ صاحب ریپو: «درست کردنِ سرور با برنامهٔ پمپ بنزین… ببین مشکل از برنامه
است که وصل نمی‌شود یا سرور.»

این بار **کلِ زنجیره** یک‌جا دوید، نه دو نیمهٔ جدا: پنلِ خانگیِ واقعی بالا آمد،
خودش `shop/server`ِ واقعی (۲.۸.۱، PGlite) را روشن کرد، و **هر** مسیری که
`CloudLink.cs` می‌زند از **پورتِ عمومی** — همان چیزی که تونل می‌بیند — با همان
بدنه و همان سرآیندها زده شد: **۳۸ سبز، ۰ سرخ**
(ریپوی `server`: `npm run test:pump-e2e`).

⛔ **پس قراردادِ این برنامه سالم است** و دنبالِ باگ در `CloudLink` نگردید.
آن‌چه واقعاً ورود را می‌شکند **رباتِ ایمیلِ تنظیم‌نشدهٔ سرورِ حساب** است: کدِ
شش‌رقمی ساخته می‌شود و به دستِ هیچ‌کس نمی‌رسد (نه ایمیل، نه در پاسخ، نه در
لاگ)، در حالی که `register/start` عمداً همیشه ۲۰۰ است. شرحِ چهار قاعده‌ای که
با هم این تله را می‌سازند و ردیفِ عیب‌یابیِ تازهٔ پنل، در `CLAUDE.md`ِ ریپوی
`server` (بخشِ «چرا برنامهٔ پمپ به سرور وصل نمی‌شود»).

⚠️ و باز هم: این سندباکس `dotnet` ندارد و به `api.vill3n.top` هم نمی‌رسد، پس
کامپایل و آزمون‌های این ریپو این‌جا **دویده نشدند** — CI روی PR می‌دواندشان.

## 🔑 افتتاحِ حساب هیچ چیزی را از سر نمی‌کند — و دادهٔ بی‌حساب با کاربر می‌آید (از ۱۴۰۵/۰۷/۰۷)

خواستهٔ صریحِ صاحب ریپو: «یارو اگه حساب نداره و اطلاعاتِ خیلی زیاد روی
برنامه داشته و می‌خواد حسابِ جدید برای خودش افتتاح کنه، جوری نشه که اطلاعات
همه از سر بشن و همون اطلاعاتی که اول توی برنامه بی حساب داشت توی حسابِ
جدیدش بیاد. و اگه از قبل حساب داشت قضیه فرق می‌کنه و همون حساب میاد روی این
حسابی که بدون حساب داشت. ولی برای افتتاحِ حساب نباید اطلاعات همه حذف یا از
سر یا ریست بشن.»

سنجیده شد، حدس زده نشد — هر سه مسیر روی خودِ کد دنبال شد:

| حالت | چه می‌شد | حکم |
|---|---|---|
| بی‌حساب ⇒ **حسابِ تازه** | «بارِ اول» (`SyncStore.SeedStep`) همهٔ ردیف‌ها را به همان حساب می‌برد | ✅ از قبل درست بود |
| بی‌حساب ⇒ **حسابی که از قبل داشت** | دادهٔ این کامپیوتر بالا می‌رود و دفترِ حساب هم پایین می‌آید — **ادغام** | ✅ از قبل درست بود |
| حسابِ A ⇒ **حسابِ B** روی همین نصب | ❌ **سه چیزِ غلط** | ⛔ باگ، و بسته شد |

⛔ **آن سه چیز، و چرا هر کدام خرابیِ داده بود:**

- `SeededAt` بزرگ‌تر از صفر می‌ماند ⇒ «بارِ اول» دوباره نمی‌دوید ⇒ دفترِ
  همین کامپیوتر **هیچ‌وقت** به حسابِ تازه نمی‌رسید. کاربر حسابِ تازه‌اش را
  خالی می‌دید و فقط تغییرهای فردا می‌رفتند.
- opهای **نرفتهٔ** حسابِ قبلی در دفترِ حسابِ **تازه** می‌نشستند — نشتِ داده
  از یک حساب به حسابِ دیگر.
- `Cursor`ِ حسابِ قبلی سرِ جا می‌ماند ⇒ هر چه پیش از آن شماره روی دفترِ
  حسابِ تازه بود **پریده** می‌شد.

### ⛔ `SyncStore.BindTo(accountId)` تنها جای تصمیم است

`SyncStateRow.AccountId` می‌گوید این دفترِ همگام‌سازی مالِ کدام حساب است، و
`SyncEngine.StepAsync` **پیش از** «بارِ اول» می‌سنجدش.

- ⛔ **یک بیت از دفتر لمس نمی‌شود.** فقط `SyncOps` و ردیفِ حال — هیچ جدولِ
  داده‌ای، هیچ ردیفِ حساب، هیچ ردیفِ دفتر. آزمونِ سورس همین را روی خودِ
  بدنه می‌سنجد (نامِ هیچ جدولِ داده‌ای در آن نباشد).
- ⛔ **`UidSeed` عوض نمی‌شود** — شناسهٔ هر ردیفِ قدیمی از همان ساخته شده و
  روی دیسک نشسته؛ عوض کردنش یعنی دفترِ سرور دو برابر (قاعدهٔ ۱۴۰۵/۰۷/۰۴).
- ⛔ **opهای حسابِ قبلی برداشته می‌شوند، ولی داده‌شان گم نمی‌شود**: خودِ
  ردیف‌ها سرِ جایشان‌اند و «بارِ اول»ی که همان لحظه از نو روشن می‌شود
  **همه** را دوباره می‌فرستد — این بار به حسابِ تازه.
- ⛔ **`Cursor = 0` یعنی ادغام، نه جایگزینی**: گرفتن هیچ‌وقت چیزی را پاک
  نمی‌کند (همان قاعدهٔ «بازیابی از سرور هیچ ردیفی را پاک نمی‌کند»)، پس دادهٔ
  این کامپیوتر می‌ماند و دادهٔ حساب هم رویش می‌آید.
- ⚠️ **حسابِ ناشناس چیزی را از نو نمی‌کند**: فقط وقتی شناسهٔ کهنه را
  **داریم** و با تازه **یکی نیست**. پس نصبِ پیش از این نسخه (شناسه‌اش خالی
  است) و ورودِ دوبارهٔ همان حساب هیچ هزینه‌ای ندارند — همان قاعدهٔ
  `ReleaseIfOtherAccountAsync`، با همان ملاک (`user.id`، نه ایمیل).
- ⚠️ **بی‌صدا نیست**: چراغ زرد می‌شود و می‌گوید «حسابِ تازه — کلِ دفترِ این
  کامپیوتر برای حسابِ شما فرستاده می‌شود». ⛔ و هیچ نام و نشانیِ سروری در
  آن نیست.
- ⚠️ **حالِ همگام‌سازی یک بار خوانده می‌شود** و همان پایین به کار می‌رود،
  وگرنه هر دورِ حلقه یک `SELECT`ِ اضافه می‌شد — قاعدهٔ همیشگیِ `idle`.
- ⚠️ **ستونِ تازه باید در `PatchColumns` هم نوشته شود** (فهرستش **دستی**
  است): ستونی که آن‌جا نباشد روی دفترِ مشتری ساخته نمی‌شود و همگام‌سازی
  همان لحظه با خطای SQLite می‌ایستد. آزمون همین را قفل کرده.

آزمون: `AccountDataCarryTests` (۶ بند — چهارتایش **رفتاری**، با دفترِ واقعیِ
SQLite: دادهٔ بی‌حساب کامل می‌رود، عوض شدنِ حساب یک ردیف را هم کم نمی‌کند،
opهای حسابِ قبلی به حسابِ تازه نمی‌روند، و ورودِ دوبارهٔ همان حساب هیچ
هزینه‌ای ندارد).

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا
**دویده نشدند** — CI روی PR می‌دواندشان.

## 🔒 قفل‌ها برگشتند — و اشتراکی که مدیر می‌دهد واقعاً می‌رسد (از ۱۴۰۵/۰۷/۰۸)

خواستهٔ صریحِ صاحب ریپو، دو تکه در یک پیام: «اشتراک که می‌دم توی حسابِ همون
نفر توی برنامه برای همون حسابِ مد نظر قفل بشه… برنامه اونو دریافت کنه و
قفل‌ها باز بشه و درست کار کنه… و بتونم اشتراکشو بردارم یا روش اضافه کنم… و
تاریخِ موعود که رسید اشتراک تموم بشه و دسترسی‌ها و غیره» — و «و برنامه رو هم
قفل کن، همون‌هایی که قفل بودن رو بزار قفل‌هاشو.»

### ۱) ⛔ `UnlockedForNow` حالا `false` است

همان یک مقدار در `Services/Entitlements.cs`، و **فقط همان**. بازِ موقتِ
۱۴۰۵/۰۷/۰۲ («بعداً می‌گم دوباره قفل کنی») تمام شد؛ هر شش دروازهٔ
`Entitlements.Paid` دوباره اشتراک می‌خواهند.

- ⚠️ **و این یک خط `SoftLock` را هم زنده کرد** — تا دیروز اصلاً روشن
  نمی‌شد. یعنی از امروز اشتراکِ تمام‌شده برنامه را **فقط‌خواندنی** می‌کند.
  ⛔ ولی نصبِ **بند‌نشده** و مشتریِ درونِ **ارفاقِ چهارده‌روزه** هیچ‌وقت قفل
  نمی‌شوند، و دیدن و چاپ و بکاپِ محلی و پشتیبانی همیشه بازند.
- ⚠️ سه سنجهٔ رابطی که عمداً بی پلن می‌دوند (`cloudlogin` · `serverdot` ·
  `startup`) دست‌نخورده سبزند — هیچ‌کدام به بخشِ پلن‌داری نمی‌روند و
  `NotActivated` بودنشان قفل را باز نگه می‌دارد. بقیه `FakeLicense.Grant()`
  دارند (قاعدهٔ ۱۴۰۵/۰۶/۳۱، سنجیده شد: ۳۱ فایل از ۳۴).

### ۲) ⛔ باگِ پولی: اشتراکی که می‌دادی هیچ‌وقت نمی‌رسید

`CloudLink.RefreshAsync` **تنها** جای گرفتنِ مجوزِ امضاشده است، و تنها
صداکننده‌اش صفحهٔ **پروفایل** بود. حلقهٔ شصت‌ثانیه‌ایِ پس‌زمینه
(`StationPublisher.CloudKeepAsync`) فقط `HomeFromAccountAsync` را می‌زد — و
آن برای دستگاهی که **از قبل بند شده** هیچ مجوزی نمی‌گیرد (`BindAsync` فقط
با `!Activated` صدا زده می‌شود).

⛔ **و زیانش دو طرفه بود، هر دو مستقیم روی پول:**

| کارِ مدیر روی سرور | آن‌چه برنامه می‌دید |
|---|---|
| اشتراک **داد** | `/api/pump/me` می‌گفت «فعال» ⇒ `Entitlements.Remember` مُهرِ `EntitledUntil` را جلو می‌برد ⇒ `InGrace` **هر شش قفل** را باز می‌کرد. مشتریِ **استاندارد** پلنِ کامل می‌گرفت |
| اشتراک **برداشت** | مجوزِ کهنه روی دیسک می‌ماند و تا انقضای خودش باز بود، و بعدش هم چهارده روز ارفاق |

⚠️ **و چرا از راهِ `me` درست نمی‌شد**: پاسخِ `me` فهرستِ قابلیت‌ها را
**دارد** (`entitlement.features`)، ولی آن فقط در حافظهٔ همان `CloudLink`ِ
یک‌بارمصرفِ حلقه می‌نشیند. تصمیمِ واقعیِ قفل‌ها `Entitlements.Allows` ⇒
`State()`ِ **بی‌پارامتر** است، که `live` ندارد و فقط از **دیسک** می‌خواند.
و تنها چیزی که آن‌جا می‌نشست مُهرِ ارفاق بود. یعنی «فهرست آمد» یک توهم بود.

### ⛔ `CloudLink.KeepLicenseFreshAsync` تنها جای تصمیم است

```
مدیر اشتراک داد   ⇒ me: فعال · مجوز: بسته  ⇒ ناجور ⇒ همین حالا مجوز بگیر
مدیر اشتراک گرفت  ⇒ me: خاموش · مجوز: باز  ⇒ ناجور ⇒ همین حالا مجوز بگیر
هیچ چیزی عوض نشد  ⇒ جور  ⇒ صفر درخواست، تا تیکِ ده‌دقیقه‌ای
```

- ⛔ **حلقه بعد از `HomeFromAccountAsync` صدایش می‌زند، نه پیش از آن**: بی
  حالِ تازهٔ اشتراک، «ناجوری» معنا ندارد. آزمون ترتیبش را قفل کرده.
- ⛔ **«مجوز چه می‌گوید» از خودِ `LicenseGuard` پرسیده می‌شود، نه از مُهرِ
  ارفاق.** ارفاق عمداً دیر می‌بندد؛ اگر ملاک آن بود، برداشتنِ اشتراک دو
  هفته دیده نمی‌شد.
- ⚠️ **تیکِ ده‌دقیقه‌ای لازم است، نه تجمل**: عوض شدنِ **پلن** (استاندارد ⇒
  وی‌آی‌پی) هر دو طرف را «فعال» نگه می‌دارد، پس از راهِ ناجوری هرگز دیده
  نمی‌شود. آن سقف تنها چیزی است که فهرستِ تازه را می‌آورد.
- ⚠️ **ارزان است**: تا چیزی عوض نشود و تیک نرسد، **صفر** درخواست — همان
  قاعدهٔ همیشگیِ این ریپو. و نصبِ بند‌نشده هیچ‌وقت نمی‌زند (مجوز فقط با
  توکنِ دستگاه صادر می‌شود).
- ⛔ **هیچ‌وقت استثنا بیرون نمی‌دهد و هیچ‌وقت چیزی را نمی‌بندد**: بی‌اینترنت
  یعنی همان مجوزِ دیروز. «نمی‌خوام کسی که اشتراک خریده با یک باگ خراب بشه.»

### ✅ و «با تاریخ نتونه دور بزنه» — سنجیده شد، نه فرض

سه قید از قبل سرِ جایشان بودند و هیچ‌کدام دست نخورد:

- `LicenseGuard` **اول امضا** را می‌سنجد (ES256)، بعد `nbf`/`exp` را با
  ارفاقِ یک‌دقیقه‌ای. عقب بردنِ ساعتِ ویندوز مجوزِ منقضی را زنده نمی‌کند،
  چون `exp` داخلِ خودِ امضا است و مجوز کوتاه‌عمر است.
- جلو بردنِ ساعت هم کاری نمی‌کند: `Entitlements.Remember` مُهر را **فقط
  جلو** می‌برد، پس عقب بردنِ بعدی‌اش ارفاق را دوباره نمی‌سازد.
- `duid` و `stn` مجوز را به همان کامپیوتر و همان پمپ می‌بندند — «توی حسابِ
  همون نفر برای همون حسابِ مد نظر قفل بشه».

آزمون: `LicenseDeliveryTests` (۷ بند — شش‌تایش **رفتاری**، با سرورِ ساختگی
و مجوزی که واقعاً با ES256 امضا می‌شود).

- ⛔ **این کلاس هیچ‌کدام از کلیدهای سراسریِ `Entitlements` را دست نمی‌زند**
  (`Unlocked` · `TestDeny` · `Now`): `AppLinksTests` همان‌ها را عوض می‌کند و
  `[Collection]` ندارد، پس موازی می‌دود. به‌جایش ادعاها روی خودِ **رکوردِ**
  `EntitlementState` است (`Open` · `Listed` · `Features`) که خالص است. مرزِ
  خودِ `Allows` جای خودش سنجیده می‌شود (`EntitlementsTests`).
- ⚠️ و یک فلکِ **خفته** همان‌جا بسته شد: `CloudLink.Reach` استاتیک است و هر
  درخواستِ ساختگی پرش می‌کند، ولی بندِ ۱ی `CloudReachTests` («تا
  نپرسیده‌ایم، هیچ ادعایی نیست») روی `Unknown` بودنش حساب می‌کند و
  هم‌کالکشنِ `CloudSessionTests` است — که پرش می‌کرد و `ResetReach()`
  نمی‌زد. یعنی به **ترتیبِ اجرا** بند بود. حالا هر دو کلاس در `Dispose`
  برش می‌گردانند.
- ⚠️ **شکلِ پاسخِ سرورِ ساختگی خوانده شد، نه حدس زده**
  (`shop/server/src/lib/entitlement.js`): کلیدِ «فعال بودن» `source` است
  (`subscription` · `trial` · `free` · `none`)، نه یک `active`ِ بولی. بارِ
  اول با `active:true` نوشتمش و سنجه **سبزِ دروغ** می‌داد، چون
  `Subscription.Active` هیچ‌وقت راست نمی‌شد.
- ⚠️ و JSONِ سرورِ ساختگی با `JsonSerializer` ساخته می‌شود، نه رشتهٔ
  `$$"""`: JSONِ تودرتو دنباله‌ای از `}}` می‌سازد و آن در رشتهٔ درج‌دارِ
  خام نشانهٔ بستنِ درج است.

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا
**دویده نشدند**. آن‌چه شد: یک لِکسرِ واقعیِ C# (کامنت، رشتهٔ خام با N
گیومه، verbatim، درج‌دار، نویسه) روی هر فایلِ عوض‌شده — و **اول روی
فایل‌های کامیت‌شده کالیبره شد**، چون شمارندهٔ خام‌ترِ قبلی روی خودِ
`CloudSessionTests` هم `+7` می‌داد. «کامپایل می‌شود» را تا سبز شدنِ CI
نباید گفت.

## 🎯 «تراز» — یک چراغ، یک قالب، و دو کشویی (از ۱۴۰۵/۰۷/۱۰)

چهار گزارشِ صاحب ریپو با عکس، در یک نشست. هیچ‌کدام سلیقه نبود.

### ۱) ⛔ سربرگ یک چراغ دارد، نه دو

«چرا دو نوع سرور رو برای من نشون میده؟ یکی باشه اصلی که واقعاً نشون بده
که وصل است یا که نه؛ الان دوتا استن، یکی میگه وصل یکی میگه قط.»

⚠️ **و یادداشتِ ۱۴۰۵/۰۷/۰۱ی همین فایل («دو چراغ یعنی دو سرورِ جدا و این
عمدی است») پس گرفته شد.** آن وقتی درست بود که مسئله «یک سبز دیده می‌شد و
کاربر گمان می‌کرد هر دو وصل‌اند» بود؛ ولی دو نقطهٔ بی‌برچسب کنارِ هم همان
سردرگمی را از درِ دیگر می‌سازند — کاربر نمی‌داند کدام کدام است.

- ⛔ **راهِ درست یکی کردنِ دو چراغ بود، نه یکی کردنِ دو حقیقت.**
  `TickServerDot` و `TickCloudDot` مو‌به‌مو سرِ جایشان‌اند و `TickLinkDot`
  فقط **خروجیِ** آن دو را می‌خواند. هیچ تصمیمِ سومی ساخته نشد.
- ⛔ **و سبز دروغ نمی‌گوید**: «یکی وصل، یکی نه» **زرد** است و `ToolTip`
  می‌گوید کدام کدام. سبز کردنش همان «کلکِ دروغ»ی است که این‌جا قدغن است،
  و سرخ کردنش می‌گوید هیچ چیزی کار نمی‌کند در حالی که نیمی سرِ جایش است.
- ⛔ **متنِ هر سرور دوباره نوشته نشد** — همان دو `…DotReason` کنارِ هم
  می‌نشینند. دو جای نوشتن یعنی روزی چراغ یک چیز می‌گوید و `ToolTip` چیزِ
  دیگر.
- ⚠️ و کلیکِ همان یک چراغ **هر دو** را می‌پرسد (`CheckLinksAsync` همان دو
  تابعِ موجود را پشتِ سرِ هم می‌زند؛ منطقِ پرسیدن دوباره نوشته نشد).
- ⚠️ **سنجهٔ کهنه پاک نشد، از درِ تازه گرفته شد**: `AppLinksTests` و
  `CloudReachTests` قبلاً `CheckServerCommand`/`CheckCloudCommand` را در
  XAML می‌خواستند؛ حالا `CheckLinksCommand` را می‌خواهند **و** صریح
  می‌گویند آن دوِ قبلی نباشند (یک چراغ، نه دو).

### ۲) ⛔ کارتِ ورق نشانِ ☀️🌙 ندارد

«اون ایکونِ ماه و آفتاب رو حذف کن نباشن و کادرشون کمی تو رفته.»

آن دو نشان هیچ چیزی نمی‌گفتند که کارت از قبل نگوید (هر ورق **هر دو** شیفت
را دارد) و در چیدمانِ راست‌به‌چپ سرِ خط می‌نشستند، پس تاریخ را از لبهٔ کارت
تو می‌بردند — همان «کادرشون کمی تو رفته». ⚠️ و ☀️/🌙ی **انتخابِ** روز/شب
در صفحهٔ ورق و کارت‌های پارچه دست‌نخورده‌اند: آن‌جا نشان یک **تصمیم** است،
نه تزئین.

### ۳) ⛔ فرمِ پارچه یک قالب دارد — `ColumnDefinitions="*,8,106"`

«هیچ کادرش برابر نیست و نامِ کارمندها از همه متفاوت‌تر است و فاصله‌هاشو
چرا رعایت نکردی؟ همه را با دقت درست کن و تراز.»

حق داشت و **سه پهنا و سه قد** روی یک کارت بود:

| ردیف | پیش از این |
|---|---|
| نامِ کارمند | پهنای **ثابتِ** ۲۱۰ و بلندیِ دستیِ ۴۸ |
| شروع/ختمِ پایه | `*` منهای دکمهٔ کنارش |
| بقیه | تمام‌عرض |
| خانه‌های «خودکار» | `Border.calc` بی `MinHeight` — از همه کوتاه‌تر |

- ⛔ **ستونِ کناری روی هر ردیف رزرو می‌شود**، حتی ردیفی که دکمه ندارد.
  این تنها چیزی است که لبهٔ همهٔ کادرها را یکی می‌کند — و ردیفِ تازه‌ای که
  فردا اضافه شود خودبه‌خود تراز می‌ماند.
- ⛔ **و قد یک جا نوشته شده** (`.wfield` / `.wside` در `Controls.axaml`)،
  نه در تک‌تکِ نماها. سنجه می‌شمارد که **هیچ** کادری بیرونِ آن قالب نمانده.
- ⚠️ **خواستهٔ ۱۴۰۵/۰۷/۰۶ پس گرفته نشد** («کادرِ نام طولش بزرگ باشه نه
  عرضش»): ۴۴ از پیش‌فرضِ ۳۶ بلندتر است و حالا **همه** آن را دارند — هم
  بزرگ، هم برابر. و سنجه‌اش عددِ ثابت نمی‌خواهد، می‌خواهد از **پیش‌فرضِ
  خودِ `TextBox` بلندتر** باشد؛ پس اگر روزی پیش‌فرض بالا رفت، «بلند»
  بی‌صدا معنایش را از دست نمی‌دهد.
- ⚠️ **و یک داد‌وستد که پنهان نشد**: پهنای کادرِ نام از ۲۱۰ی ثابت به
  ستونِ مشترک رفت، یعنی **پهن‌تر شد**. این با «عرضش بزرگ نباشه»ی
  ۱۴۰۵/۰۷/۰۶ می‌ساید — ولی آن خواسته وقتی بود که این کادر در چیدمانِ
  دو‌ستونه از بقیه پهن‌تر می‌شد، و خواستهٔ تازه صریح است: «هیچ کادرش
  برابر نیست… همه را تراز کن». پس «پهن‌تر نشدن» حالا یعنی **هیچ پهنای
  صریحی روی خودش ندارد** و از همان ستونِ همسایه‌ها پهنا می‌گیرد — نه
  بیشتر. ⛔ پهنای ثابت برنگردد؛ همان بود که کارت را ناهموار کرد.

### ۴) ⛔ جهتِ کلیدِ چپ/راست **سنجیده** می‌شود، نه فرض

«کلید چپ به راست میره و برعکسش.»

⚠️ و این **دومین** بار است که این گزارش می‌آید — بارِ اول («راست می‌زنم چپ
می‌رود») با یک جابه‌جاییِ **ثابت** در چیدمانِ راست‌به‌چپ بسته شد. همان
جابه‌جاییِ ثابت است که حالا وارونه عمل می‌کند. یعنی پرسشِ اصلی هیچ‌وقت
جواب داده نشده بود: آیا `TranslatePoint` آینهٔ راست‌به‌چپ را **در خودش
دارد** یا نه؟

- ⛔ **حالا از خودِ کنترل پرسیده می‌شود** (`Mirrored`): لبهٔ چپ و لبهٔ راستِ
  همان کنترل به فضای ریشه برده می‌شوند و مقایسه می‌شوند. هر جوابی که
  آوالونیا بدهد، جهت درست درمی‌آید — و نسخهٔ بعدیِ آوالونیا هم این را
  نمی‌شکند.
- ⛔ **حدس، دوبار غلط جواب داد؛ اندازه‌گیری یک بار درست.** همان قاعدهٔ
  «با تست بگو، نه با حدس».

### ۵) ⛔ واحدِ رسید دو **کشویی** است، نه یک دکمهٔ چرخشی

«چرا اون واحدِ رسید دکمه‌ای است؟ اون و اپشن‌هاش رو کشویی کن که هر کدوم رو
خواستم انتخاب کنم… الان شاید واحد تیل باشه و رسیدش دیزل… یا رسیدِ پول
هیچ‌کدوم لازم نیست، نه پطرول نه دیزل.»

- ⛔ **دکمهٔ چرخشی گزینه‌ها را نشان نمی‌داد** و رسیدنِ به «تیلِ دیزل» از
  «پول» دو کلیک لازم داشت — کاربر باید حدس می‌زد چه چیزهایی هست.
- ⛔ **و دو تصمیم‌اند، نه یکی**: در دفتر هم `Unit` و `Fuel` دو ستونِ جدا
  هستند. یکی کردنشان در یک فهرستِ سه‌تایی همان چیزی بود که این گزارش را
  ساخت.
- ⛔ **رسیدِ پول ⇒ کشوییِ تیل بسته می‌شود** — و **پنهان نه**: پنهان شدنش
  ردیف را جابه‌جا می‌کند و کادرِ بعدی از زیرِ دستِ کاربر فرار می‌کند، همان
  درسی که کادرِ هشدارِ پارچه داد. و برچسبِ زیرش می‌گوید **چرا** بسته است؛
  قفلِ بی‌توضیح باگ است.
- ⛔ **هیچ منطقی عوض نشد**: `Unit` و `Fuel` همان دو خاصیت‌اند و `AddAsync`
  همان‌ها را می‌گیرد. دو نما به همان دو نشستند، و سنجه همین را می‌خواهد.
- ⚠️ ترتیبِ کادرها هم عوض شد: **تاریخ ← نام ← واحد ← نوعِ تیل ← مقدار ←
  توضیحات** («کادرِ تاریخ رو بیار اولِ اول»). در `WrapPanel`ِ راست‌به‌چپ،
  نخستین فرزند همان سمتِ راست است.

### سنجه‌ها

`native/PumpYaqobi.Tests/FormAlignTests.cs` (۴ بند) به‌علاوهٔ بندهای
به‌روزشدهٔ `AppLinksTests` و `CloudReachTests`.

⚠️ **و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا
دویده نشدند** — CI روی PR می‌دواندشان. آن‌چه این‌جا شد: تراز شدنِ آکولادها
با یک لِکسرِ واقعیِ C#، سنجشِ XMLِ هر سه نما، و **شبیه‌سازیِ پایتونیِ
تک‌تکِ ادعاهای آزمون روی خودِ فایل‌ها** — که یک بار هم به کار آمد: بندِ
رسیدات اول روی «WrapPanel.Styles» می‌بُرید و چون آن رشته دو بار می‌آید،
تکهٔ اشتباه را می‌سنجید و سبزِ دروغ می‌داد.

## ⛔ گامِ پمپ دیوار بود — و «خطای داخلی سرور» هیچ سرنخی نمی‌داد (از ۱۴۰۵/۰۷/۱۰)

گزارشِ صاحب ریپو با عکس: «این بخش برای چیه که مانعِ ساختِ حساب می‌شه؟ این
رو درست کن که دیگه این مشکل نباشه… اون لوکیشن رو حذف کن لازم نیست، یارو
همین که اسمِ پمپشو بزنه بسه… منو دیوانه نکنی.» روی صفحه: **«❌ خطای داخلی
سرور»**.

### آن‌چه سنجیده شد، نه حدس زده

سرورِ حسابِ **واقعی** (PGlite) بالا آمد و همان ترتیبی که
`FinishPumpAsync` می‌زند بازپخش شد:

```
register/start ⇒ verify ⇒ complete        ۲۰۱
GET  /api/pump/me                         ۲۰۰  (station: null)
POST /api/pump {name}                     ۲۰۱
POST /api/pump/device/bind {device}        ۲۰۱
و بارِ دوم: /api/pump ⇒ ۴۰۹ already_member · bind ⇒ ۲۰۱
```

⛔ **پس روی کدِ امروزِ سرورِ حساب هیچ ۵۰۰ی در این مسیر نیست.** و آن پیام
هم متنِ خودِ برنامه نبود — `res.Why` است، یعنی جملهٔ خودِ سرور.

⚠️ **و یک نشانهٔ خواندنی در همان عکس بود**: سرورِ حساب از ۲.۸.۵ روی هر
۵۰۰ یک **کدِ پیگیری** می‌گذارد. عکس کد نداشت ⇒ سرورِ حسابِ او از آن
قدیمی‌تر است. سرورِ حسابی که پنل بالا می‌آورد از داخلِ همان **فایلِ نصب**
می‌آید، پس این با کد درست نمی‌شود — با انتشارِ فایلِ نصبِ تازه درست
می‌شود.

### ولی گیر کردنِ کاربر این‌طرف بود، و همان درست شد

- ⛔ **گامِ پمپ دیگر دیوار نیست.** کارِ این گام یک چیز است: «این حساب پمپ
  داشته باشد». پس پیش از نوشتنِ «نشد»، از خودِ سرور پرسیده می‌شود
  (`CloudLink.HasStationAsync`) و اگر **دارد**، گام تمام است — هر چه آن
  یک درخواستِ شکست‌خورده گفته باشد.
  ⚠️ **و این پنهان کردنِ خطا نیست**: بند شدنِ دستگاه گم نمی‌شود، حلقهٔ
  شصت‌ثانیه‌ایِ پس‌زمینه خودش دوباره می‌زندش. و آن تابع **هیچ چیزی
  نمی‌سازد** — یک `GET` است و بس؛ سنجه نبودنِ `HttpMethod.Post` را در
  بدنه‌اش می‌خواهد.
- ⛔ **کادرِ «لوکیشنِ پمپ» برداشته شد و برنمی‌گردد.** و نکتهٔ مهمش این
  است که **هیچ‌وقت به سرور نمی‌رفت**: `EnsureStationAsync` از روزِ اول
  فقط `{ name }` می‌فرستد. یعنی یک خانهٔ دیگر روی تنها دیوارِ بینِ کاربر و
  برنامه بود، بی هیچ کاری.
- ⛔ **و «خطای داخلی سرور» دیگر بی‌سرنخ نمی‌ماند** (`PumpStepWhy`): کد
  پیگیری آمده ⇒ «این کد را در سرورِ حساب ← لاگ بگردید»؛ نیامده ⇒ «سرورِ
  حساب کهنه است، فایلِ نصبِ تازه را بگیرید». ⚠️ نامِ هیچ میزبانی در آن
  نیست — همان قاعدهٔ همیشگی.

⚠️ **و سه سنجهٔ کهنه پاک نشدند، از درِ تازه گرفته شدند**: `AppLinksTests`
حالا **نبودنِ** کادرِ لوکیشن را می‌خواهد، و `CloudLoginProbe`/`VerifyProbe`
همان ادعای «پیش از رفتن به سرور ذخیره می‌شود» را روی چیزی که واقعاً مانده
(نامِ پمپ) نگه داشتند.

- آزمون: بندِ `GameParcheyePomp_Divar_Nist_Va_Location_Nemikhahad` در
  `FormAlignTests` (۵ بند شد).

## ⛔ «مگه از سرور اپدیت می‌گرفت؟» — واژه‌ای که کاربر را به بیراهه برد (از ۱۴۰۵/۰۷/۱۰)

گزارشِ صاحب سامانه با عکس: «برنامه پمپ بنزین اشتباه جا رو انتخاب کرده برای
اپدیت شدن… اون دیگه سرور اپدیت نداد، مگه از سرور اپدیت می‌گرفت؟ اینو درست
کن و دوباره به همین شاخه گیت هاب وصل کن.»

### آن‌چه سنجیده شد — و سالم بود

| سنجیده شد | نتیجه |
|---|---|
| نشانیِ آپدیت (`FeedUrl`) | همین مخزن ✅ |
| درِ اول (`releases/latest`) | `v3.1.152` با بستهٔ کوچکِ هم‌پایه ✅ |
| درِ دوم (`desktop-latest`) | `version.txt` + `base.txt` تازه، `prerelease: true` ✅ |
| هر دو در پشتِ سرِ هم زده می‌شوند؟ | بله — `catch`ِ درِ اول `null` می‌دهد و `FromFileAsync` می‌دود ✅ |

⛔ **و برنامهٔ پمپ هیچ‌وقت از سرورِ خانگی آپدیت نمی‌گرفته** — گشته شد، هیچ
مسیری نیست. «وصل کردنِ دوباره» لازم نبود؛ وصل بود.

عکس نسخهٔ **۳.۱.۱۵۱** را نشان می‌داد (پس باگِ پوشهٔ نصبِ ۳.۱.۱۴۸ هم ربطی
نداشت) و پیامش این بود: «سرورِ به‌روزرسانی جواب نداد — وقت تمام شد». یعنی
هر دو در مهلتشان (۲۵ث) تمام شد ⇒ **آن کامپیوتر به بیرون نرسید**.

### ⛔ ولی یک چیزِ واقعی خراب بود: خودِ جمله

این برنامه **سه سرور** دارد — خانگی · حساب · به‌روزرسانی — و جملهٔ خطا
گفته بود «سرور». صاحب سامانه آن را **سرورِ خانگیِ پمپ** خواند و رفت دنبالِ
درست کردنِ چیزی که اصلاً خراب نبود. یعنی پیام فنی‌اش درست بود و **کارش را
نکرد**.

- ⛔ **واژهٔ «سرور» از جمله‌های `Why()` رفت و جایش «گیت‌هاب» نشست** —
  خواستهٔ صریحِ بعدیِ خودش: «اپدیت از گیت‌هاب بگیره نه سرور». جمله حالا
  **نامِ جا را می‌گوید**، نه یک واژهٔ سربسته.
- ⛔ **و `Broken()` یک خطِ دوم گرفت**: «به‌روزرسانی مستقیم از گیت‌هاب
  می‌آید و به سرورِ خانگیِ پمپ ربطی ندارد». این تزئین نیست — تا نوشته
  نشود، کاربر سراغِ چراغِ سربرگ و سرورِ خانگی می‌رود؛ دو بار واقعاً رفت.
- ⚠️ **و این قاعدهٔ «نشانی در رابط نوشته نمی‌شود» را نشکست**: آن قاعده
  مالِ **نامِ مخزن و نشانی** است (`NoUserFacingFileMentionsTheRepository`
  دنبالِ `vil3ntec` و `pump-staion-yaqobi` و `api.github.com` می‌گردد).
  نامِ خودِ گیت‌هاب نه نشانی است و نه راهی برای بردنِ برنامه به جای دیگر.
- ⛔ **و همان سنجه نزدیک بود با خودِ این کار شکسته شود**: بندِ تازه اول
  `Assert.DoesNotContain("vil3ntec", …)` داشت — یعنی نامِ مخزن را در یک
  فایلِ `.cs`ِ دیگر می‌نوشت و همان اسکنر را قرمز می‌کرد. حالا ادعا
  «نشانی نیست» است (`http` · `.com` · `/`)، نه یک نامِ خاص. **پیش از
  نوشتنِ ادعا، خودِ اسکنر را بخوانید.**
- ⚠️ **مهلت بالا نرفت**: ۲۵ ثانیه برای هر در می‌ماند. وقتی راه بسته است،
  طولانی‌تر کردنِ انتظار فقط کاربر را بیشتر معطل می‌کند — همان قاعدهٔ
  «ساعتِ دیوار را از سنجه بیرون کنید».
- آزمون: `ATimeoutBlamesTheInternet_NotThePumpsOwnServer` در
  `UpdateBehaviourTests` — **رفتاری**، با `TestTransport`ی که
  `TaskCanceledException` می‌اندازد. و ادعاهای کهنه پاک نشدند، از درِ تازه
  گرفته شدند: «نامِ میزبان نیست» و «‹به‌روز است› نمی‌گوید» هر دو همان‌جا
  خواسته می‌شوند.

⚠️ **و این ثبت‌نام و خودِ آپدیت را درست نمی‌کند**: تا وقتی `github.com` از
آن شبکه در دسترس نباشد، هیچ آپدیتی نمی‌آید. کارِ این اصلاح فقط این است که
**راست را بگوید و کاربر را جای درست بفرستد** — دکمهٔ «باز کردنِ صفحهٔ
دانلود» هم برای همین سرِ جایش است.

⚠️ سمتِ ریپوی `server` همان روز یک ایرادِ **واقعی** پیدا شد و بسته شد:
برچسبِ `windows-preview` (کانالِ به‌روزرسانیِ مرکز فرمان) از شاخه‌های کاری
هم پر می‌شد. شرحش در `CLAUDE.md`ِ همان ریپو.

## ⏳ ورود به حساب، و لودینگ تا دفترش برسد (از ۱۴۰۵/۰۷/۱۱)

خواستهٔ صریحِ صاحب ریپو: «برنامه آفلاین و آنلاین است و یک چالش وسط است:
(۱) برنامه علناً بدون رمز نیست و برای هر کاربر جدید خالی از اطلاعات دیده
نمی‌شود — رمز را خودِ طرف از تنظیمات بگذارد، نه این‌که اولِ برنامه رمز
باشد؛ و کسی که تازه نصب می‌کند باید برنامه را بی اطلاعات ببیند. (۲) یارو
اینترنت دارد و می‌رود توی حساب، و لودینگ روی صفحه نمی‌آید تا اطلاعاتی که
توی حساب و سرور است بیاید روی همان حساب.»

### ۱) «بی رمز و خالی» — کد از ۳.۱.۱۴۹ درست بود، ولی **سنجه‌اش نبود**

گشته شد، حدس زده نشد. هر سه راهِ بالا آمدن از `MainViewModel.LockOrOpenAsync`
رد می‌شوند، `AuthService.OpenWithoutPassword` نصبِ تازه را بی هیچ صفحهٔ قفلی
باز می‌کند، و تنها جای **ساختنِ** رمز «تنظیمات ← رمزها و کد» است. نصاب هم
هیچ `pump.db`ی با خودش نمی‌برد و دفتر در `%AppData%\PumpYaqobi\` است.

⚠️ **ولی «نصبِ تازه خالی است» هیچ‌وقت با شمردنِ ردیف‌ها سنجیده نشده بود** —
`NoPasswordTests` فقط می‌گفت دفتر **کجاست**، نه این‌که **چه دارد**.

- ⛔ `FreshInstallEmptyTests` فهرستِ جدول‌ها را از خودِ `sqlite_master`
  می‌خواند و ردیف‌های هر کدام را می‌شمارد. سه جدول استثنا هستند و
  استثنای چهارم اضافه نمی‌شود: `Settings` (مهرِ اسکیما)، `SyncState`
  (ریشهٔ شناسه‌ها) و `Audit`. هر چیزِ دیگری حتی یک ردیف داشته باشد، سرخ.
- ⛔ **فهرست دستی نوشته نمی‌شود**: جدولِ تازه‌ای که فردا ساخته شود و کسی
  بی‌خبر در آن ردیفِ «نمونه» بگذارد همین‌جا گرفته می‌شود — کاری که یک
  فهرستِ دستی هیچ‌وقت نمی‌کرد.
- ⚠️ و «اسکیما ساخته نشد» از «خالی بود» جدا می‌شود (سقفِ ۲۰ جدول)، وگرنه
  یک `EnsureReady`ِ شکست‌خورده **سبزِ دروغ** می‌داد.
- ⚠️ **برای صاحبِ نصبِ قدیمی**: اگر رمزی از نسخه‌های پیش از ۳.۱.۱۴۹ روی
  دستگاهش مانده، برنامه همچنان آن را می‌پرسد و این درست است — برداشتنش
  «تنظیمات ← 🔑 رمزها و کد ← برداشتنِ رمز» است (با رمزِ فعلی).

### ۲) ⏳ پردهٔ «آوردنِ اطلاعاتِ حساب» — و مکثی که شکستنی شد

تا دیروز کاربری که وارد حسابش می‌شد **هیچ چیزی نمی‌دید**: صفحهٔ خالی، و
`SyncEngine` پانزده ثانیه بعد بی‌صدا شروع به گرفتن می‌کرد.

```
ورود تمام شد (LoginStep = 4) ─▶ SyncEngine.PrimeNow()   ← مکثِ ۱۵ث را رد می‌کند
                              ─▶ Priming = true         ← پردهٔ تمام‌صفحه
   بارِ اول ⇒ فرستادن ⇒ گرفتن … تا HasMore == false
                              ─▶ PrimedAt مهر می‌خورد ⇒ پرده می‌رود
                              ─▶ PrimeFinished ⇒ بخشِ جلوی چشم از نو خوانده می‌شود
```

- ⛔ **`SyncEngine.PrimeWanted` تنها جای تصمیم است** و **هر دو** شرط را
  می‌خواهد: `PrimedAt == 0` **و** `Cursor == 0`.
  - `PrimedAt` یک **مهرِ صریح** است، نه «مکان‌نما بزرگ‌تر از صفر»: حسابی
    که روی سرور هیچ چیزی ندارد با یک گرفتنِ کاملاً موفق هم `Cursor`ش صفر
    می‌ماند، پس با ملاکِ مکان‌نما پرده **هر سی ثانیه** برمی‌گشت.
  - `Cursor == 0` نصب‌های امروزی را — که از قبل همگام‌اند و این ستون را
    تازه گرفته‌اند — از یک پردهٔ بی‌دلیل نگه می‌دارد.
- ⛔ **پوسته تصمیمِ دومی نمی‌سازد**: `MainViewModel` فقط `Priming` و
  `PrimeText`ِ موتور را می‌خواند و آزمون `PrimedAt`/`PrimeWanted` را در آن
  فایل **قدغن** کرده. دو جای تصمیم یعنی روزی پرده هست و همگام‌سازی نیست.
- ⛔ **پرده هیچ‌وقت دیوار نیست** — همان قاعدهٔ صفحهٔ ورود: «ادامه در
  پس‌زمینه» همیشه رویش هست (`DismissPrime`)، و **هر** شکستی (گرفتن،
  فرستادن، ۴۲۶) خودش پرده را می‌برد و کار را به چراغِ نوارِ پایین می‌سپارد.
  برنامه آفلاین هم باید کار کند.
- ⛔ **یک بار در هر اجرا** (`_primeOver`): بی این قفل، یک شبکهٔ لرزان هر
  چند دقیقه پرده را جلوی چشمِ کاربر روشن و خاموش می‌کرد.
- ⛔ **مکثِ آغاز دیگر `Task.Delay`ِ شکست‌ناپذیر نیست** —
  `_startNow.WaitAsync(FirstDelay, ct)`. ⚠️ و آن مکث را **فقط یک در** رد
  می‌کند (`PrimeNow`، از `OnLoginStepChanged` وقتی گام به «تمام» می‌رسد)،
  نه هر ذخیرهٔ دیتابیس: وگرنه بالا آمدنِ برنامه خودش با همگام‌سازی رقیب
  می‌شد و قاعدهٔ «صفحهٔ اول بی رقیب» می‌شکست.
- ⛔ **وقتی دفتر رسید، بخشِ جلوی چشم از نو خوانده می‌شود** (`ReloadAsync`).
  ردیف‌های رسیده با SQLِ خام می‌نشینند و `Bump()` می‌خورند، پس هر بخشِ
  **دیگری** سرِ نخستین دیدارش خودش تازه می‌شود (ترمزِ `Version`) — ولی
  بخشی که همین حالا باز است تا کاربر جایی نرود عقب می‌ماند.
- ⛔ **حسابِ تازه مهر را پاک می‌کند** (`SyncStore.BindTo` ⇒ `PrimedAt = 0`)،
  و ورودِ دوبارهٔ همان حساب هیچ هزینه‌ای ندارد.
- ⚠️ **ستونِ `PrimedAt` در `PatchColumns` هم نوشته شد** — فهرستش دستی است
  و ستونی که آن‌جا نباشد روی دفترِ مشتری ساخته نمی‌شود و همگام‌سازی همان
  لحظه با خطای SQLite می‌ایستد. آزمون با `pragma_table_info` روی یک دفترِ
  واقعی می‌سنجدش.
- ⚠️ **هزینه‌ای اضافه نشد**: نصبِ بند‌نشده همان‌جا (`!CanSync`) پیش از هر
  دستورِ دیتابیسی برمی‌گردد و `SyncEngine.Disabled` در همهٔ سنجه‌ها روشن
  است، پس `idle` دست‌نخورده صفر می‌ماند.
- ⛔ **هیچ نام و نشانیِ سروری روی پرده نوشته نمی‌شود** و انیمیشنش با کلاسِ
  `pulse` به `IsSyncPriming` بسته است (قاعدهٔ «هیچ انیمیشنِ بی‌پایانی بی
  کلاسی که خاموشش کند»).

آزمون: `FreshInstallEmptyTests` (۲ بند) و `AccountPrimeTests` (۸ بند —
سه‌تایش رفتاری، با دفترِ واقعیِ SQLite).

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا
**دویده نشدند**. آن‌چه شد: تراز شدنِ آکولادها با لِکسرِ C# که **اول روی
نسخهٔ کامیت‌شدهٔ همان فایل‌ها کالیبره شد**، سنجشِ XMLِ پنجره، و شبیه‌سازیِ
تک‌تکِ ۲۲ ادعای سورسیِ آزمون‌های تازه روی خودِ فایل‌ها. «کامپایل می‌شود»
را تا سبز شدنِ CI نباید گفت.

## 👥 هر حساب، دفترِ خودش — و هیچ داده‌ای پاک نمی‌شود (از ۱۴۰۵/۰۷/۱۱)

خواستهٔ صریحِ صاحب ریپو: «حسابِ اول با حسابِ دوم عوض بشه، اطلاعات دست
نخوره توی حساب‌ها بمونن، حساب‌ها عوض می‌شه و اطلاعاتِ همون حساب نشون داده
بشه — مثلِ برنامه‌های حرفه‌ای.»

```
%AppData%\PumpYaqobi\pump.db                     ← دفترِ ریشه (نخستین حساب)
%AppData%\PumpYaqobi\accounts\<نامِ امن>\pump.db  ← حساب‌های بعدی
```

### ⛔ چیزهایی که نباید برگردند

- ⛔ **هیچ فایلی پاک، کپی یا جابه‌جا نمی‌شود.** نه دفترِ ریشه به پوشهٔ
  حساب می‌رود، نه دفترِ حسابِ قبلی پاک می‌شود. عوض کردنِ حساب فقط یعنی
  **فایلِ دیگری باز می‌شود**؛ دفترِ قبلی سرِ جایش منتظرِ برگشتنِ صاحبش
  است. آزمونِ سورس `File.Delete` · `File.Move` · `File.Copy` را در این
  مسیر قدغن کرده.
- ⛔ **`AppHost.UseLedgerOf` تنها جای تصمیم است** و تنها جایی که
  `Db.SwitchTo` را می‌زند (آزمون می‌شمارد: دقیقاً یک بار). مسیر را
  `AccountLedger.PathFor` می‌گوید و پوسته فقط `LedgerSwitched` را
  می‌شنود. سه جای تصمیم یعنی روزی دفتر عوض می‌شود و صفحه عددِ دفترِ
  قبلی را نشان می‌دهد.
- ⛔ **نخستین حساب دفترِ موجود را برمی‌دارد** (`AppSettings.LedgerAccountId`)
  — و همین است که قاعدهٔ ۱۴۰۵/۰۷/۰۷ را نگه می‌دارد: کسی که بی‌حساب کار
  کرده و حالا حساب می‌سازد، **همان دفتر** را با خود می‌برد و «بارِ اول»ِ
  همگام‌سازی همه‌اش را بالا می‌فرستد. فقط حسابِ **دوم** دفترِ تازه می‌گیرد.
  ⚠️ با `Save()`ی بادوام می‌نشیند، نه `SaveSoon()`: گم شدنش یعنی دفترِ
  ریشه «بی‌صاحب» دیده می‌شود و حسابِ بعدی آن را تصاحب می‌کند.
- ⛔ **خروج از حساب دفتر را عوض نمی‌کند.** شناسهٔ خالی یعنی «نمی‌دانیم
  کیست»، نه «برگرد به دفترِ ریشه» — و دفترِ ریشه مالِ حسابِ **دیگری**
  است. بی آن یک خط، خروجِ حسابِ دوم دفترِ حسابِ اول را جلوی چشمش
  می‌گذاشت.
- ⛔ **شناسهٔ خام هیچ‌وقت نامِ پوشه نمی‌شود** (`AccountLedger.SafeName`):
  نویسه‌های ناامن می‌افتند و یک اثرِ انگشتِ هشت‌حرفی ته نام می‌نشیند. یک
  `..` در شناسه یعنی نوشتن بیرونِ پوشهٔ برنامه، و دو شناسهٔ متفاوت
  هیچ‌وقت نباید به یک پوشه برسند.
- ⛔ **پوشهٔ `accounts/` کنارِ دفتری می‌نشیند که برنامه با آن بالا آمد**
  (`AppHost._rootDb`)، نه کنارِ `PumpDbFactory.DefaultPath`. سنجه‌ها و
  ابزارِ عکس‌گیری دیتابیسِ موقتِ خودشان را می‌دهند؛ با مسیرِ پیش‌فرض،
  نخستین «حساب عوض شد» آن‌ها را به پوشهٔ **واقعیِ** کاربر می‌برد و روی
  دفترِ خودِ صاحبِ پمپ می‌نوشتند.

### ⚠️ سه تله که سنجیده شدند

- ⛔ **حلقهٔ همگام‌سازی روی نخِ دیگری می‌دود** و می‌تواند پیش از پوسته
  بیدار شود. پس `SyncEngine.StepAsync` **پیش از هر خواندنی از دفتر**
  `UseLedgerOf` را می‌زند و اگر جابه‌جا شد همان دور را رها می‌کند. بی آن،
  یک دور opهای دفترِ حسابِ **قبلی** را با توکنِ حسابِ **تازه** می‌فرستاد
  — دادهٔ یک مشتری در دفترِ ابریِ مشتریِ دیگر. این کندی نیست، خرابیِ
  داده است. و `SyncEngine.Hold(bool)` وسطِ خودِ جابه‌جایی هیچ دوری را
  نمی‌گذارد بدود.
- ⛔ **استخرِ اتصال خالی می‌شود** (`SqliteConnection.ClearAllPools`): رشتهٔ
  اتصال `Pooling=True` دارد، پس بی آن، اتصالِ دفترِ قبلی زنده می‌ماند و
  فایلش (با `-wal` و `-shm`) روی ویندوز قفل — یعنی پشتیبان گرفتن از آن
  پوشه شکست می‌خورد.
- ⛔ **صفحهٔ باز با تازه شدنِ فهرست بسته نمی‌شود.** حسابِ قرض‌دار، صفحهٔ
  شرکت، ورق و حسابِ امانت شیءِ زنده‌اند و ردیف‌های دفترِ قبلی را در خود
  دارند. `SectionViewModel.CloseOpenPage()` برای همین است و آن چهار بخش
  رویش می‌نویسند؛ بی آن، کاربر پس از عوض کردنِ حساب هنوز حسابِ قرض‌دارِ
  حسابِ قبلی را جلوی چشمش می‌دید.

### ⚠️ رمزِ برنامه هم مالِ همان دفتر است

`AppUser` هیچ‌وقت همگام نمی‌شود (در `OpLog.Local` است) و در خودِ دفتر
می‌نشیند، پس **هر حساب رمزِ خودش را دارد**. دفترِ تازه‌ای که رمز ندارد
بی رمز باز می‌شود (قاعدهٔ ۱۴۰۵/۰۷/۰۷) و دفتری که رمز دارد رمزش را
می‌پرسد — `MainViewModel.OnLedgerSwitchedAsync` همان `LockOrOpenAsync`ِ
همیشگی را می‌زند و هیچ راهِ دومی ساخته نشد.

⚠️ و چون `SignedIn` از امروز می‌تواند **دوباره** شلیک شود، کارهای
یک‌بارمصرفِ پس از ورود پشتِ `_afterSignIn` رفتند: بی آن، سه شنوندهٔ
موتورِ همگام‌سازی دو برابر می‌شدند و هر توستِ اعلان دو بار می‌آمد.

⚠️ **و `StationPublisher` عمداً گِیت نشد**: عوض شدنِ حساب
`ForgetStationAsync` را می‌زند که کدِ پمپ و هر دو توکن و نشانیِ سرورِ
خانگی را پاک می‌کند، پس ناشر جایی برای فرستادن ندارد. اگر روزی آن
پاک‌سازی برداشته شود، این یکی هم `Hold` می‌خواهد.

### آزمون

```
AccountLedgerTests        رفتارِ واقعی روی دو دفترِ SQLite — و هیچ فایلی پاک نمی‌شود
AccountLedgerSourceTests  هفت قاعدهٔ بالا روی خودِ سورس
```

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا
**دویده نشدند**. آن‌چه شد: تراز شدنِ آکولاد و پرانتز با یک لِکسرِ واقعیِ
C#، شبیه‌سازیِ پایتونیِ `AccountLedger.PathFor`/`SafeName` روی هر ده
ورودیِ آزمون، و سنجشِ تک‌تکِ ادعاهای سورسیِ آزمون‌ها روی خودِ فایل‌ها.
«کامپایل می‌شود» را تا سبز شدنِ CI نباید گفت.

## 🔴 چراغِ همگام‌سازی دربارهٔ **حالا** حرف می‌زند، نه گذشته (از ۱۴۰۵/۰۷/۱۱)

گزارشِ صاحب ریپو با سه عکس: سرورِ حساب سبز و وصل («✅ سرورِ حساب جواب داد —
نسخهٔ ۲.۷.۰») ولی چراغِ نوارِ پایین **سرخ**: «همگام نشد».

### ⛔ ریشه — یک زنجیرهٔ چهارخطی

```
۱) یک نرسیدنِ گذرا (یک لحظه قطعیِ اینترنت) ⇒ LastError پر می‌شود
۲) دورِ بعد صف **خالی** است                ⇒ کلِ بلوکِ فرستادن رد می‌شود
   …و تنها جای `LastError = ""` داخلِ همان بلوک بود
۳) گرفتن موفق است                          ⇒ ولی چیزی را پاک نمی‌کرد
۴) ته حلقه: صف ۰ و خطا پر                  ⇒ «همگام نشد»ِ سرخ، **برای همیشه**
```

یعنی چراغ دربارهٔ **گذشته** حرف می‌زد. ⛔ همان «کلکِ دروغ»ی که در این ریپو
قدغن است، فقط **وارونه**: می‌گفت خراب است در حالی که نبود.

⚠️ و بدترین جایش این بود که پمپی که **هنوز چیزی ننوشته** صفش خالی است، پس
هیچ‌وقت به آن خط نمی‌رسید — یعنی نصبِ تازه، که قرار است به مشتری فروخته
شود، یک چراغِ سرخِ جاویدان می‌گرفت.

- ⛔ **گرفتنِ موفق خطای دورِ قبل را پاک می‌کند** — هم در حافظه و هم در
  حالِ روی دیسک (`x.LastError`). ⚠️ ولی خطای **واقعیِ همین دور** (ردیفی که
  ننشست) پاک نمی‌شود.
- ⛔ **پاک کردن دیگر فقط در بلوکِ فرستادن نیست.** آن بلوک شرطِ
  `pending > 0` دارد و همین یک شرط، کلِ باگ بود.
- آزمون: `SyncLightTruthTests` — و **دندانش سنجیده شد**: با بازسازیِ کدِ
  پیش از اصلاح، هر پنج ادعا سرخ شد.

### ✅ و سرورِ ۲.۷.۰ با سنجهٔ واقعی دویده شد

پرسشِ صاحب ریپو: «با سایت تست زدی؟» — این بار با **همان نسخه‌ای که روی
کامپیوترِ خودش است**: `shop` روی کامیتِ ۲.۷.۰ در یک `git worktree` بالا
آمد و `npm run test:pump-e2e` (ریپوی `server`) رویش دوید.

| بخش | حال روی ۲.۷.۰ |
|---|---|
| ثبت‌نام · ورود · `refresh` · کدِ ایمیلی | ✅ |
| `POST /api/pump` · `device/bind` · مجوزِ ES256 | ✅ |
| **Sync v1 — رفت‌وبرگشتِ واقعی، هر ۹ بند** | ✅ |
| **دادنِ اشتراک از پنل** | ❌ ۵ سرخ |

⛔ **پس «همگام نشد» ربطی به نسخهٔ سرور نداشت** — Sync v1 از خودِ ۲.۷.۰ هست
و کامل کار می‌کند. باگ در خودِ برنامه بود (بالا).

⚠️ **ولی فروشِ اشتراک روی ۲.۷.۰ کار نمی‌کند** و این کارِ کد نیست:
- جست‌وجوی مشتری **با ایمیل** در «اشتراک بده» چیزی پیدا نمی‌کند
  (`items: []`) — روی `shop` در ۱۴۰۵/۰۷/۰۸ درست شد؛
- در نتیجه دادنِ اشتراک `400 bad_id` می‌گیرد؛
- اشتراکِ داده‌شده **پلنِ کامل** می‌دهد، نه فهرستِ محدود — یعنی
  «استاندارد دادم، وی‌آی‌پی گرفت»؛
- مسیرِ `addons` اصلاً نیست (`404`).

⛔ **برای فروختنِ اشتراک، سرورِ حساب باید ۲.۹.۰ یا بالاتر باشد.** آن
فایلِ نصبِ مرکز فرمان است که سرورِ حساب را با خود می‌آورد
(`resources/account-server`)، پس راهش انتشارِ تازهٔ آن است — نه تغییری در
برنامهٔ پمپ.

## 🔁 «هر بار می‌روم پروفایل، دوباره اسمِ پمپ را می‌خواهد» (از ۱۴۰۵/۰۷/۱۱)

گزارشِ صاحب ریپو با دو عکس: «حسابِ تازه ساختم… همه‌شو تموم کردم و هر بار که
روی پروفایل برم می‌گه اسمِ پمپ رو انتخاب کن، انتخاب می‌کنم، می‌رم دوباره
همون بند و بساط. و بعد از ساختِ حساب هم ۳۰ روز رایگان فعال نمی‌شه.»

و در عکسِ دوم، پروفایل **هم‌زمان** می‌گفت «✅ پمپِ شما روی حسابتان هست» و
«سرورِ حساب: فعال نشده» و «ماندهٔ اشتراک: —».

### ⛔ هر سه شکایت یک ریشه داشتند: دستگاه بند نمی‌شد

```
کلیدِ عمومیِ جامانده ⇒ BindAsync ⇒ key_mismatch (بی هیچ پیامی)
                    ⇒ توکنِ دستگاه خالی می‌ماند
                    ⇒ «تمام» که به همان توکن بند بود هیچ‌وقت نمی‌رسید ⇒ حلقه
                    ⇒ مجوز صادر نمی‌شد ⇒ ۳۰ روزِ رایگان هم نمی‌آمد
```

چهار قاعده که نباید برگردند:

- ⛔ **«تمام» به بند شدنِ دستگاه بند نیست.** بند شدن کاری نیست که کاربر در
  آن صفحه بتواند انجام دهد — کادرِ کد از ۱۴۰۵/۰۷/۰۴ برداشته شده و تنها
  دکمه‌اش «ساختنِ پمپ» است. پس شرط کردنش یعنی گامی که **هیچ راهِ خروجی
  ندارد**: یک دیوارِ نامرئی، همان چیزی که بندِ ۱۴۰۵/۰۷/۱۰ برای برداشتنش
  نوشته شد و از درِ دیگر برگشت. ملاک حالا `AppSettings.PumpStepDone` است —
  یک **حقیقتِ سنجیده‌شده** («سرور گفت این حساب پمپ دارد»)، با `Save()`ی
  بادوام، و هر دو مسیر (موفق و «پمپ از قبل هست») مهرش می‌زنند.
- ⛔ **کلیدِ عمومی هم یک «بند» است** (`ReleaseIfOtherAccountAsync`). آن خط
  سه مورد داشت و کلید را نه، پس نصبی که کلید را قفل کرده ولی توکن نگرفته
  بود، با ورودِ حسابِ دیگر کلیدش را نگه می‌داشت و از آن به بعد هر بند شدنی
  `key_mismatch` می‌گرفت. **یک قفلِ ضدِ کرک، به جانِ خودِ مشتری افتاده بود.**
- ⛔ **کلیدی که چیزی را نگه نمی‌دارد، قفل نیست** — ولی **فقط در
  `BindAsync`**. کارِ آن قفل این است که سرورِ *دیگری* نتواند برای دستگاهی
  که *فعال شده* مجوز امضا کند؛ دستگاهی که نه توکن دارد و نه مجوز چیزی برای
  محافظت ندارد. پس آن‌جا کلیدِ تازه می‌نشیند و TOFU همان لحظه از نو بسته
  می‌شود. ⛔ `ActivateAsync` این استثنا را **ندارد و نباید بگیرد**: آن‌جا
  کاربر کدِ شش‌رقمی زده و کلیدِ ناجور یک هشدارِ واقعی است.
  ⚠️ و همین است که کاربرانِ **همین حالا گیرکرده** را بی هیچ کاری آزاد
  می‌کند — خواستهٔ صریحِ صاحب ریپو: «کسایی که از قبل تازه حساب باز کردن به
  اون‌ها هم برسه.»
- ⛔ **شکستِ بند شدن بلعیده نمی‌شود** (`CloudLink.LastBindWhy`). حلقهٔ
  شصت‌ثانیه‌ای هر دور `BindAsync` را می‌زد و **هم استثنا و هم نتیجه** را
  دور می‌ریخت، پس یک شکستِ دائمی برای همیشه نامرئی بود. حالا پروفایل
  می‌گوید «فعال نشده — <دلیل>». ⚠️ نامِ هیچ میزبانی در آن نیست.

⚠️ **و ۳۰ روزِ رایگان خودش می‌آید**: دورهٔ آزمایشی داخلِ همان مجوزی است که
`device/bind` صادر می‌کند (سنجیده شد: روی سرورِ ۲.۷.۰ هم «مجوزِ دورهٔ
آزمایشی می‌آید»). پس با بند شدنِ دستگاه، قفل‌ها همان لحظه باز می‌شوند —
هیچ کدی و هیچ کارِ دستی‌ای لازم نیست.

آزمون: `PumpStepLoopTests` (۵ بند).

## ⏳ قفل‌ها دوباره بازند — موقت، بارِ دوم (از ۱۴۰۵/۰۷/۱۲)

خواستهٔ صریحِ صاحب ریپو: «الان قفلی نزار روی برنامه برای فعلاً؛ هر وقت
خواستم بهت می‌گم که دوباره بذاریش.»

- ⛔ **یک خط، یک جا**: `Entitlements.UnlockedForNow = true` در
  `Services/Entitlements.cs`. هر شش دروازهٔ `Entitlements.Paid` (اپِ
  کارمندان · کیو‌آر · بک‌اپِ ابری · مفاد/ضرر · تاریخچه‌ها · داشبورد) برای
  همه باز است — با اشتراک، بی اشتراک، فعال‌نشده. و `SoftLock` هم از همین
  می‌خواند، پس هیچ اشتراکِ تمام‌شده‌ای برنامه را فقط‌خواندنی نمی‌کند.
- ⛔ **برای قفل کردنِ دوباره فقط همان یک مقدار را `false` کنید** — نه چیزِ
  دیگری. مجوز، ارفاق، فهرستِ پلن، پیام‌ها و همهٔ سنجه‌ها سرِ جایشان‌اند.
- ⚠️ بخشِ «🔒 قفل‌ها برگشتند» (۱۴۰۵/۰۷/۰۸) بالاتر **پاک نشد**: آن‌چه آن‌جا
  دربارهٔ رسیدنِ اشتراک از سرور نوشته شده (`KeepLicenseFreshAsync`) هنوز
  درست است و دست نخورده — فقط همین یک کلید عوض شد.
- ⚠️ `EntitlementsTests` همچنان با `Entitlements.Unlocked = false` می‌دود،
  پس مرزِ واقعیِ پلن‌ها زیرِ آزمون است و روزِ قفل کردن غافلگیر نمی‌شوید.

### ⛔ و قفلِ TOFU برای دستگاهِ **خالی** استثنا دارد (همان روز)

دو سنجهٔ `CloudSessionTests` با اصلاحِ حلقهٔ گامِ پمپ سرخ شده بودند، چون
حالتی را می‌ساختند که خودِ استثنا برای آن نوشته شده بود: کلیدِ قفل‌شده،
**بی** توکنِ دستگاه و **بی** مجوز.

- ⛔ **سنجه‌ها ضعیف نشدند، از درِ درست گرفته شدند**: حالا دستگاهِ
  **فعال‌شده** را می‌سنجند (توکن و مجوز دارد) — همان چیزی که این قفل از آن
  محافظت می‌کند — و صریح می‌خواهند که هیچ‌کدام از آن دو دست نخورند.
- ⛔ و بندِ تازهٔ `Bind_DastgaheKhali_KelidRa_DobareGhofl_Mikonad` طرفِ
  دیگر را قفل کرد: دستگاهی که هیچ چیزی ندارد کلیدِ تازه را می‌پذیرد و TOFU
  همان لحظه از نو بسته می‌شود. یعنی «باز» و «بسته» هر دو سنجیده می‌شوند.
- ⛔ `ActivateAsync` این استثنا را **ندارد و نباید بگیرد**.

## 🧵 فرمِ پارچه: نوارِ خالیِ کنارِ ردیف‌ها رفت، و دکمه دکمه شد (از ۱۴۰۵/۰۷/۱۲)

گزارشِ صاحب ریپو با عکس: «اون بغل‌ها خالی گذاشته شدن و کادرش به تمامِ ورق
نمی‌رسه. و اون دو کادر رو هم رنگ بده — گرفتن از روز و شب — اون‌ها دکمه‌ستن،
باید متفاوت باشن.»

هر دو حق بود، و ریشهٔ هر دو یک قاعدهٔ خودِ ما بود.

### ۱) ⛔ بندِ «ستونِ کناری روی **هر** ردیف رزرو می‌شود» پس گرفته شد

قاعدهٔ ۱۴۰۵/۰۷/۱۰ برای تراز کردنِ کارت نوشته شده بود و کارش را هم کرد، ولی
هزینه‌اش دیده نشده بود: **نُه ردیف از یازده ردیف دکمه ندارند** و یک نوارِ
۱۰۶ پیکسلیِ **کاملاً خالی** کنارشان می‌ماند — فقط برای این‌که دو ردیفِ دیگر
دکمه دارند. یعنی کادرِ «جمله قرض» و «فی لیتر» و «فروش» و بقیه هیچ‌وقت به
لبهٔ کارت نمی‌رسیدند.

- ⛔ حالا آن قالب (`ColumnDefinitions="*,8,106"`) فقط روی ردیفی است که
  **واقعاً** خانهٔ دوم دارد: نامِ کارمند (+ شمارهٔ پایه) و شروع/ختمِ پایه.
  بقیه تمام‌عرض‌اند.
- ⚠️ **و ترازی از دست نرفت**: لبهٔ **شروعِ** همهٔ کادرها (سمتِ راست، در
  چیدمانِ راست‌به‌چپ) همان یکی است و قدشان هم یکی (`.wfield`) — همان چیزی
  که نسخهٔ وب دارد. آن‌چه عوض شد فقط لبهٔ **پایانِ** دو ردیفِ دکمه‌دار است،
  که ذاتاً باید کوتاه‌تر باشد.
- ⛔ **و سنجه ضعیف نشد، از درِ تازه گرفته شد**: به‌جای «دستِ‌کم ۱۱ ردیف آن
  قالب را دارند»، حالا خواسته می‌شود که **هر** `Grid`ی با آن قالب واقعاً
  `Grid.Column="2"` داشته باشد (`rows == seconds`) — وگرنه همان نوارِ خالی
  از درِ دیگر برمی‌گردد.

### ۲) ⛔ دکمهٔ کناری رنگِ شیفتِ خودش را دارد، نه سطحِ کادرِ تایپ

«گرفتن از شب» و «فرستادن به شب» **دکمه** بودند و دقیقاً هم‌رنگِ کادرهای
کنارشان دیده می‌شدند. ریشه‌اش قاعدهٔ ۱۴۰۵/۰۷/۰۶ بود: سبکِ **پایهٔ** دکمه
زمینه‌اش `Pump.Input` است و کادرِ تایپ هم همان — پس دکمهٔ بی‌کلاس روی این
فرم نامرئی می‌شد. همان درسِ «سفید روی سفید»، این بار روی `Input`.

- `Button.wside` حالا `Classes.day`/`Classes.night` می‌گیرد و رنگش از خودِ
  کارت می‌آید (نارنجیِ `#f0952b` روز · بنفشِ `#7c4dbe` شب) — همان دو رنگی
  که سربرگ و دکمهٔ ذخیره دارند. ⛔ **رنگِ تازه‌ای به برنامه اضافه نشد.**
- ⚠️ **`:pointerover` هم نوشته شد**، وگرنه زیرِ ماوس به رنگِ پیش‌فرضِ
  آوالونیا برمی‌گشت و همان لحظه دوباره شبیهِ کادر می‌شد. (`save-btn` این
  را ندارد و همان‌جا هم یک لغزشِ کوچک است.)
- ⛔ **هیچ منطقی عوض نشد**: `PullBaseCommand` و `PushBaseCommand` و
  `PullText`/`PushText` دست‌نخورده‌اند، و واژه‌های روی دکمه هم همان‌اند
  (فلش برنگشت — قاعدهٔ ۱۴۰۵/۰۷/۰۶).

آزمون: `FormAlignTests.FormeParcha_HameyeKadrha_Taraz_Ast` (بازنویسی‌شده) —
و بندِ `ColumnDefinitions="*,8,106"` در `ParchaHistoryWaraqUxTests`
دست‌نخورده سبز می‌ماند، چون آن قالب هنوز هست.

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌ها این‌جا
**دویده نشدند**. آن‌چه شد: سنجشِ XMLِ هر دو فایل، و شبیه‌سازیِ پایتونیِ
تک‌تکِ ادعاهای آزمون روی خودِ فایل‌ها. «کامپایل می‌شود» را تا سبز شدنِ CI
نباید گفت.

## ✅ بی‌حساب نوشت، بعد حساب ساخت — دفترش با او می‌آید (سنجیده، از ۱۴۰۵/۰۷/۱۲)

پرسشِ صاحب ریپو: «کسی بدون حساب اگه حساب‌ها رو تو برنامه می‌زاشت و بعدن
بخاد لاگین کنه، اگه حساب جدید بود همون اطلاعات که بودن باقی بمونه و روی
حساب همون شخص تو سرور رسیده بشه؟ و ببین سرور هم کار می‌کنه یا که نه.»

### ۱) زنجیرهٔ برنامه — هر سه حلقه‌اش از قبل درست بود

```
AppHost.UseLedgerOf("")      ⇒ دفترِ ریشه            (بی‌حساب)
نخستین ورود ⇒ ShouldClaimRoot ⇒ LedgerAccountId = همان حساب
            ⇒ PathFor        ⇒ **همان** دفترِ ریشه   ⛔ هیچ پوشهٔ تازه‌ای
SyncStore.BindTo(id)          ⇒ «نمی‌دانستیم مالِ کیست» ⇒ فقط ثبت، هیچ ریستی
SyncStore.SeedStep            ⇒ **همهٔ** ردیف‌ها به صفِ همان حساب
```

⚠️ **ولی هیچ سنجه‌ای کلِ زنجیره را با هم نمی‌سنجید** و همین شکاف بود:

```
AccountLedgerTests    ⇒ مسیرِ دفتر درست است        (بی همگام‌سازی)
AccountDataCarryTests ⇒ «بارِ اول» همه را می‌فرستد (روی یک فایلِ ثابت)
```

پس اگر روزی نخستین حساب به‌جای دفترِ ریشه یک پوشهٔ **خالیِ** تازه
می‌گرفت، **هر دو** کلاس سبز می‌ماندند و کاربر پس از ورود دفترِ **خالی**
می‌دید — دقیقاً همان چیزی که پرسیده شد.

- ⛔ `AccountFirstLoginCarryTests` همان شکاف را بست: **یک** زنجیره روی
  SQLiteِ واقعیِ روی دیسک. ۵۵ ردیف بی‌حساب (۴۰ امروزی با op + ۱۵ کهنهٔ
  بی‌op) ⇒ نخستین ورود ⇒ **همان فایل** باز می‌ماند، هیچ پوشهٔ `accounts/`
  ساخته نمی‌شود، و هر ۵۵ ردیف در صفِ همان حساب می‌نشیند.
- ⛔ و بندِ دوم طرفِ دیگر را قفل کرد: حسابِ دوم دفترِ نفرِ اول را نه
  می‌بیند و نه می‌برد، و **برگشتنِ** حسابِ اول دفتر و صفِ نرفته‌اش را
  دست‌نخورده برمی‌گرداند (`SeededAt > 0` ⇒ «بارِ اول» دوباره نمی‌دود و
  همان ردیف‌ها بارِ دوم نمی‌روند).
- ⛔ **و تصمیمِ دومی ساخته نشد**: سنجه همان دو تابعِ واقعیِ
  `AccountLedger` را به همان ترتیبی صدا می‌زند که `UseLedgerOf` می‌زند، و
  بندِ تازهٔ `Nokhostin_Vorood_Daftare_BiHesab_Ra_Ba_Khodash_Mibarad` در
  `AccountLedgerSourceTests` همان ترتیب را روی خودِ سورس قفل می‌کند
  (`ShouldClaimRoot` ← `PathFor` ← `Db.SwitchTo`).

### ۲) و سرور — سنجیده شد، حدس زده نشد

`npm run test:pump-e2e` در ریپوی `server` روی سرورِ حسابِ **واقعیِ ۲.۹.۰**
دوید (پنلِ خانگیِ واقعی ⇒ درگاه ⇒ shop/server روی PGlite، از **پورتِ
عمومی**): **۶۰ سبز، ۰ سرخ** — از جمله هر ۹ بندِ رفت‌وبرگشتِ واقعیِ Sync v1
و هر ۲۱ پیشوندِ درگاه.

⛔ **پس قراردادِ برنامه و درگاهِ سرورِ خانگی هر دو سالم‌اند.** اگر روی
کامپیوترِ خودِ صاحب سامانه چیزی کار نکرد، ترتیبِ عیب‌یابی همان است که در
`CLAUDE.md`ِ ریپوی `server` نوشته شده (`/api/health` و ردیفِ «سرورِ حساب»
در عیب‌یابیِ پنل) — و تلهٔ اصلی همچنان **رباتِ ایمیلِ تنظیم‌نشده** است.

⚠️ و باز هم: این سندباکس `dotnet` ندارد، پس کامپایل و آزمون‌های ریپوی پمپ
این‌جا **دویده نشدند** (سنجهٔ سرور واقعاً دوید). آن‌چه شد: تراز شدنِ
آکولاد و پرانتز با لِکسرِ C#، و شبیه‌سازیِ پایتونیِ
`PathFor`/`ShouldClaimRoot` و بندِ تازهٔ سورس روی خودِ فایل‌ها.

## 💾 هیچ نوشته‌ای گم نشود — نگهبانِ ذخیره (از ۱۴۰۵/۰۷/۱۲)

گزارشِ صاحب ریپو: «وقتی از برنامه بیرون شدم همهٔ ورق‌های روزانه ناپدید
شدند… الان چندین ورق را پر کردم اما با اعتماد به تو همه‌شان پاک شدند و
هیچ چیزی ثبت نشده بود… یک ربات الترا اختصاصی توی برنامه بخواه که هر چه
نوشته یا عوض شد **درجا** ثبت شود، حتی اگر وسطِ جمله بیرون بروم یا برق
یک‌باره برود.»

گشته شد، نه حدس زده. سه چیز با هم این را می‌ساختند:

- ⛔ **برنامه هیچ شنوندهٔ `Closing` و `ShutdownRequested` نداشت** (صفر تا،
  گشته شد). یعنی بستنِ پنجره وسطِ همان ۳۵۰ میلی‌ثانیهٔ ترمزِ تایپ، آن
  نوشتن را برای همیشه می‌برد.
- ⛔ **هر ذخیره با `_ = …` رها می‌شد** و استثنایش «مشاهده‌نشده» بود: یک
  قفلِ لحظه‌ایِ فایل از سوی ضدِ ویروس یعنی یک ردیفِ گم‌شده و صفحه‌ای که
  سالم به نظر می‌رسید — همان «کلکِ دروغ»ی که این‌جا قدغن است.
- ⛔ **هیچ تلاشِ دوباره‌ای نبود** و هیچ‌جا هم گفته نمی‌شد.

### قاعده‌ها

```
Touch() ⇒ SaveGuard.Track(this) ⇒ فهرستِ زندهٔ «ردیف‌های نوشته‌نشده»
بستنِ پنجره · Ctrl+S · عوض شدنِ دفتر ⇒ SaveGuard.FlushAllAsync()
```

- ⛔ **`Services/SaveGuard.cs` تنها فهرستِ «هنوز نوشته نشده» است** و
  `IPendingWrite` تنها درش. هر چیزِ تازه‌ای که ذخیرهٔ تأخیری دارد باید
  همان رابط را بگیرد، نه فهرستِ دومی بسازد — دو فهرست یعنی روزی یکی
  خالی است و کاربر فکر می‌کند همه‌چیز ثبت شده.
- ⛔ **مرجع‌ها ضعیف‌اند** (`WeakReference`): این فهرست نباید ردیف‌های
  ماهِ پارسال را تا ابد زنده نگه دارد.
- ⛔ **نوشتنی که شکست بخورد سه بار تلاش می‌کند** (`0 · 400 · 1500ms`) و
  بعد **می‌گوید** (`SaveGuard.Failed` ⇒ توستِ سرخ). ⚠️ و ردیف کثیف
  می‌ماند تا بسته شدنِ برنامه یا `Ctrl+S` دوباره امتحانش کند — پاکش
  نمی‌کنیم.
- ⛔ **`Closing` نخست `e.Cancel = true` می‌کند**، خانهٔ بازِ جدول را
  می‌بندد (`ExcelGrid.CommitFocused`)، همه را می‌نویسد و بعد واقعاً
  می‌بندد. بی آن `Cancel`، پنجره پیش از تمام شدنِ نوشتن رفته است.
  ⚠️ `_closing` لازم است، وگرنه `Close()`ی دوم دوباره همان راه را
  می‌رود.
- ⛔ **دو نوشتنِ هم‌زمانِ یک ردیف ممنوع** (`Interlocked` در
  `RowViewModel._writing`) — دو تراکنشِ رقیب روی یک سطر.
- ⚠️ سقفِ `FlushAllAsync` هشت ثانیه است و `ShutdownRequested` سه ثانیه:
  بسته شدنِ برنامه هیچ‌وقت نباید بماسد.
- ⛔ **نوشتنِ تکی هم به نگهبان سپرده می‌شود** — `SaveGuard.Watch(task, "چه")`.
  گشتنِ دوباره نُه نوشتنِ دیگر را پیدا کرد که هنوز با `_ = …` رها می‌شدند:
  سربرگِ ورق (نامِ کارمند و قرضِ پارچه)، حسابِ قرض‌دار (دو جا)، رسیدِ
  سربرگ، شماره و یادداشتِ قرض‌دار، حسابِ امانت، یادداشتِ بخش و سربرگِ
  جدولِ آرشیو. بستنِ برنامه منتظرشان نمی‌ماند و شکستشان دیده نمی‌شد.
  ⚠️ **بی تلاشِ دوباره، عمداً**: افزودنِ رسید تکرارپذیر نیست و تلاشِ دوباره
  یعنی رسیدِ دوتایی. همان متد، همان آرگومان، همان لحظه — فقط حالا
  `FlushAllAsync` منتظرشان می‌ماند و شکستشان توست می‌شود.
  ⛔ هر `_ = …Async(…)`ی که داده می‌نویسد باگ است؛ سنجه‌اش
  `SaveDurabilityTests.Neveshtanhaye_Taki_Raha_Nemishavand`.

## 🧾 پهنای ستون‌ها، و «همه نوشته‌ها رفتند کنج» (از ۱۴۰۵/۰۷/۱۲)

- ⛔ **`WidthKey` دیگر اجباری نیست.** در کلِ برنامه فقط **سه** جدول کلید
  داشتند (هر سه در ورق) و `RememberWidths` نخستین خطش «کلید نداری؟
  برگرد» بود — پس کشیدنِ ستون در حسابِ قرض‌دار، گاوصندوق، مصارف، صرافی و
  شرکت‌ها **هیچ‌وقت** ذخیره نمی‌شد. حالا کلید از اثرِ انگشتِ سرستون‌ها
  ساخته می‌شود (`EffectiveKey`/`Fingerprint`). ⛔ به «یادت باشد در XAML
  کلید بگذاری» برنگردید؛ همان بود که سی‌ودو جدول را جا انداخت.
- ⛔ **نخستین `SpreadColumns` پس از `Reset` یک پاس صبر می‌کند**
  (`_freshCols`). با عوض شدنِ ماه فهرست `Reset` می‌شود و ستون‌ها به
  `Auto` برمی‌گردند، ولی `ActualWidth` هنوز پهنای **کهنه** است؛ اگر
  `LayoutUpdated` همان پاس برسد، پهنای کهنه برای همیشه سفت می‌شود و
  ستون‌ها در یک گوشه جمع می‌شوند — همان «تمام سربرگ‌ها رفتند کنج». و
  چون `_spread` همان‌جا `true` شده، خودش هیچ‌وقت درست نمی‌شد.
- ⛔ **بارِ خودکار هیچ‌وقت از قاب بیرون نمی‌زند** (`FitToRoom`): پهنای
  طبیعیِ `Auto` به قلم و DPIِ همان کامپیوتر بند است، پس روی نمایشگرِ
  دیگر از قاب می‌زد بیرون. ⚠️ این خواستهٔ «اگر بزرگ شد به چپ و راست
  اسکرول شود» را پس نمی‌گیرد — آن دربارهٔ ستونی است که **کاربر** کشیده
  (`PinOnUserResize`، دست‌نخورده).
- ⛔ **جهتِ کلیدِ چپ/راست همان است که بود و دست نمی‌خورد** —
  `MoveColumn(e.Key == Key.Right ? -1 : +1, …)`. یک بار در همین کار از
  `FlowDirection` خوانده شد و `KeyboardAndZeroTests.ArrowKeysFollowWhatTheEyeSees`
  همان را گرفت (کامنتِ بالای `case Key.Left` هم می‌گوید آن راه یک بار آزموده
  و رد شده). ⚠️ آن‌چه ماند فقط این است: ستونِ جاری **پیش از** `CommitEdit`
  گرفته می‌شود — `CommitEdit` خودش خانهٔ جاری را جابه‌جا می‌کند، و همان بود
  که «بارِ دوم درست می‌شود» را می‌ساخت.

## 🎨 خوانایی عدد دارد، نه سلیقه (از ۱۴۰۵/۰۷/۱۲)

- ⛔ **هر جفتِ نوشته/سطح دستِ‌کم `4.5×` کنتراست دارد** (WCAG AA) و سه
  جفتِ درشت دستِ‌کم `3.0×`. `ThemeReadabilityTests` در `dotnet test`
  قفلش کرده و ستونِ «نوشته» در `-- look` بدترین جفتِ هر تم را چاپ
  می‌کند. ⛔ جفتی را از آن فهرست برندارید تا سبز شود.
- ⚠️ پیش از این **هیچ سنجه‌ای خوانایی را نمی‌سنجید**؛ `LookAudit` فقط
  سطح‌ها را می‌دید. هر دو تم جفتِ زیرِ آستانه داشتند (تیره ۵٫۱۵×، روشن
  ۴٫۰۸×) و همه‌جا سبز بود.
- ⛔ **تمِ سوم اضافه نمی‌شود** و هویتِ «مشکی و زرد» پس گرفته نشده — فقط
  پالتش در ۱۴۰۵/۰۷/۱۲ از نو ساخته شد. طلا فقط جای تاکید خرج می‌شود؛
  خطِ جدول و نوارِ سرش خنثی‌اند (زردِ کم‌نور روی مشکی خط را محو می‌کرد).

## 🖨️ عدد و واحدش یک چیزند (از ۱۴۰۵/۰۷/۱۲)

- ⛔ **`DocStyle.Tight` تنها جای این قاعده است** و از `TdText`/`Tf` رد
  می‌شود، پس هر بیست گزارش را می‌گیرد: خانه‌ای که **رقم دارد و از ۲۴
  نویسه کوتاه‌تر است** فاصله‌اش نشکن می‌شود. «۱۲۳٬۴۵۶ افغانی» که دو خط
  شود، هم کلِ ردیف را دو برابر می‌کند (بلندیِ ردیف از بلندترین خانه
  می‌آید) و هم عددی می‌سازد که واحدش معلوم نیست.
- ⛔ **متنِ آزاد و سربرگ‌ها دست نمی‌خورند** — یادداشتِ نشکن از کادر
  می‌زند بیرون، که بدتر است.
- ⚠️ **ترازِ ستون‌های گزارش‌ها هنوز کارِ مانده است** و پنهان نمی‌شود:
  وزنِ `RelativeColumn` هر ستون با چشم دیده می‌شود، نه با استدلال، و
  سنجه‌اش `printshot` است. ⛔ بی عدد، بیست گزارش را با حدس تنظیم نکنید.

## 🧱 برنامه ۳۲بیتی هم منتشر می‌شود (از ۱۴۰۵/۰۷/۱۲)

خواستهٔ صاحب ریپو: «۳۲ بیت هم باشد، برنامه ۶۴ و ۸۶ هم باشد.»

⚠️ «۸۶ بیت» چیزی نیست: `x86` **همان ۳۲بیتی** است. پس دو هدف، نه سه.

- ⛔ **نامِ فایل‌های ۶۴بیتی یک نویسه هم عوض نشد** (`PumpYaqobi-Setup.exe`
  · `base.txt`): هر نصبِ امروزی از همان نام به‌روزرسانی می‌گیرد و عوض
  کردنش یعنی همهٔ مشتری‌ها یک‌شبه از آپدیت می‌افتند. ۳۲بیتی پسوند
  می‌گیرد (`-x86`).
- ⛔ **`AppArch` تنها جای تصمیمِ معماری است** و `UpdateService` بسته‌ای
  را که مالِ این معماری نیست **رد می‌کند** — وگرنه نصبِ ۳۲بیتی بستهٔ
  ۶۴بیتی می‌گرفت و بالا نمی‌آمد.
- ⛔ **`AppGuid`ِ ۶۴بیتی همان است که بود**؛ ۳۲بیتی GUID و پوشهٔ خودش را
  دارد، پس دو نسخه روی یک کامپیوتر هم را پاک نمی‌کنند.
- ⚠️ نصابِ ۶۴بیتی روی ویندوزِ ۳۲بیتی با پیامِ آدمیزاد رد می‌شود
  (`InitializeSetup` + `IsWin64`) — نه `ArchitecturesAllowed` که املایش
  بینِ Inno 6.2 و 6.3 عوض شد.

## 📅 «ورق‌ها ناپدید شدند» — ماه عوض شده بود، نه دیسک (از ۱۴۰۵/۰۷/۱۲)

گزارشِ صاحب ریپو: «ورق‌ها ساخته می‌شد و درست بودن، برنامه رو که می‌بستم…
دوباره که میومدم هیچ ورقی نبود… ولی توی پارچه‌ها گزارش‌هاش بودن.» دو روز پس
از آغازِ میزان.

سنجیده شد، نه حدس زده — ‎`persist`‎ در **دو فرآیندِ جدا** (نوشتن، بعد باز
کردنِ دوباره) می‌دود و هر چهار حالت را می‌سنجد:

```
dotnet run --project PumpYaqobi.UiTests -c Release -- persist          ⇐ همهٔ حالت‌ها
                                                    -- persist close    ✕ زده شد
                                                    -- persist crash    برق رفت، بی بستن
                                                    -- persist newmonth ماهِ پیش نوشته، ماهِ تازه باز
                                                    -- persist widths   پهنای ستونِ هر حساب
```

- ⛔ **داده سالم بود.** روی نسخهٔ ۳.۱.۱۵۷ هم هر ورق و هر ردیف پس از بستن
  روی دیسک بود. آن‌چه «ناپدید» شد **نما** بود: فهرستِ ورق‌ها فقط ماهِ جاری
  را نشان می‌دهد و صریح می‌نوشت «هیچ ورقی ثبت نشده» — در حالی که ورق‌های
  سنبله همه سرِ جایشان بودند. فهرستِ پارچه ماه‌به‌ماه نیست، برای همین آن‌ها
  «بودند».
- ⛔ **فهرستِ ورق‌ها**: تا کاربر خودش ماهی برنگزیده (`_monthAuto`)، ماهِ جاری
  اگر ورق دارد، وگرنه **تازه‌ترین ماهی که ورق دارد**. ماهی که کاربر
  برگزیده هرگز زیرِ دستش عوض نمی‌شود. و `EmptyText` فقط وقتی «هیچ ورقی
  ثبت نشده» می‌گوید که واقعاً هیچ ماهی ورق ندارد.
- ⛔ **دفترهای ماهانه** (گاوصندوق، مصارف، صرافی، …) عمداً خودکار جابه‌جا
  نمی‌شوند — ردیفِ تازه جایش ماهِ جاری است — ولی ماهِ خالی **می‌گوید** که
  ماهِ دیگر داده دارد و یک دکمه به آن‌جا دارد. یک نوار، در پوستهٔ پنجره
  (`SectionViewModel.MonthHint`)، برای همهٔ بخش‌ها.
  ⛔ **پس گرفته شد (۱۴۰۵/۰۷/۱۳)**: نوار رفت و جایش نقطهٔ سرخ روی کشوی ماه
  نشست — بخشِ «🔴 ماهِ تازه: نقطهٔ سرخ» در انتهای همین فایل.
- ⛔ **«×»ی کارتِ ورق پیش از حذف می‌پرسد.** کلِ ورق را با هر دو شیفت و
  ردیف‌هایی که از آن به حساب‌ها رفته‌اند می‌برد؛ تا امروز بی هیچ پرسشی.
- ⚠️ **«درجا ثبت»**: متنِ در حالِ تایپِ خانهٔ جدول **همان لحظه** به ردیف
  می‌رسد (سنجیده شد)؛ تنها فاصله مکثِ ذخیره بود. `RowViewModel.SaveDelayMs`
  از ۳۵۰ به **۱۵۰** رسید و ‎`persist crash`‎ (برق رفت، پیش از Enter) سبز
  است. ⛔ بالا نبرید. و فرستادن به سرور همان است که بود: هر ذخیره موتورِ
  همگام‌سازی را بیدار می‌کند (`PumpDbContext.Saved ⇒ Nudge`، ۵۰۰ms).
- ⚠️ یک یافتهٔ **نبسته** که پنهان نمی‌شود: کلیدهای خارجی (`WaraqId`،
  `ShiftId`، …) در Sync v1 **عددِ محلی** می‌روند و در دستگاهِ دیگر ترجمه
  نمی‌شوند. روی یک کامپیوتر بی‌اثر است؛ برای دو دستگاه روی یک پمپ باید
  پدر با `SyncUid` فرستاده شود — و آن کارِ سرورِ حساب هم هست (شکلِ فیلدها).

## 📏 پهنای ستون: فقط خواستهٔ کاربر، و هر حساب برای خودش (از ۱۴۰۵/۰۷/۱۲)

«هر جدول باید اندازهٔ خودشو ثبت‌شده داشته باشه و رو حساب‌های افرادِ دیگه
تأثیر نذاره، هر کی برای خودش.» سنجهٔ ‎`persist widths`‎ دو ریشه داد:

- ⛔ **پهنای خودکار روی پهنای کاربر ذخیره می‌شد.** پس از پر شدنِ دوبارهٔ
  فهرست `SpreadColumns` عمداً یک پاس صبر می‌کند (`_freshCols`)، ولی
  `RememberWidths` در همان پاس پهنای `Auto` را روی دیسک می‌نوشت و
  `Saved()`ِ پاسِ بعد همان را برمی‌گرداند. یعنی ستونی که کاربر پهن کرده بود،
  با **نخستین باز شدنِ دوباره** پاک می‌شد. حالا `if (!_spread) return;`.
- ⛔ **کشیدنِ ستون در جدولِ ستاره‌ای هیچ‌وقت ذخیره نمی‌شد**: خطِ پایهٔ
  «خودکار» **پس از** کشیدن گرفته می‌شد و ستونِ کشیده‌شده با خودش برابر
  درمی‌آمد. حالا `_starBase` پیش از کشیدن گرفته می‌شود.
- ⛔ **پر شدنِ دوبارهٔ جدول ستون‌ها را از نو نمی‌سنجد، مگر سنجشِ قبلی روی
  جدولِ خالی بود** (`_spreadRows`). ‎`bigtable`‎ با A/B گرفتش: با بسته شدنِ
  باگِ بالا، هر دفتر با هر باز شدن ~۱۰۰ms کندتر شده بود (۱۵ ⇒ ۱۳۶ms)،
  چون آن باگ بی‌صدا جلوی از-نو-سنجیدن را می‌گرفت. حالا همان عددهای `main`.
- ⚠️ `inputchars` از ریمیکِ صفحهٔ ورود (۳.۱.۱۴۰) روی `main` هم سرخ بود:
  نوشتهٔ راهنمای کادرِ ایمیل «you@example.com» شده بود و سنجه دنبالِ
  «ایمیل» می‌گشت. کادر حالا با «@» پیدا می‌شود؛ برنامه خودش سالم بود.
- ⛔ **`ExcelGrid.WidthScope`**: حسابِ قرض‌دار (`debt-<Id>`) و صفحهٔ شرکت
  (`co-<Id>`) یک جدول‌اند که دادهٔ هر حساب را می‌گیرند؛ دامنه به کلید اضافه
  می‌شود. حسابی که پهنایی ذخیره نکرده پهنای **خودکار** می‌گیرد، نه پهنای
  حسابِ دیگر. ⚠️ ورق‌ها عمداً دامنه ندارند («همهٔ ورق‌ها برابر بشوند»).
- ⛔ **نوارِ «جمله» جای سرستون‌ها را هم در امضایش دارد** (`TotalsStrip.Signature`).
  پس از نشستنِ پهنای ذخیره‌شده، نوار در پاسی چیده می‌شد که ستون‌ها پهنای
  تازه و سرستون‌ها جای کهنه داشتند؛ پاسِ بعد امضا عوض نمی‌شد و هر جمله
  ~۶۰۰ پیکسل دور از ستونش می‌ماند. سنجه‌اش لبه‌به‌لبه می‌سنجد.

## 🖨️ ورقِ چاپی = صفحهٔ برنامه، و سیاه‌وسفیدی که واقعاً دیده می‌شود (از ۱۴۰۵/۰۷/۱۲)

```
dotnet run --project PumpYaqobi.UiTests -c Release -- printpages <پوشه>
```

دکمهٔ PDFِ پنج بخشِ واقعی زده می‌شود (`Documents.CaptureHook` — فقط سنجه‌ها)،
و ورقِ اولِ هر سند رنگی و سیاه‌وسفید تصویر می‌شود تا با **چشم** دیده شود.

- ⛔ **سندِ حسابِ قرض‌دار با صفحهٔ حساب فرق داشت، روی عددِ پول**: ستونِ
  «الباقی»ِ ردیف‌ها خالی چاپ می‌شد، رسیدِ کادرها از فیلدِ کهنهٔ سربرگ بود
  (نه رسیدهای جدول) و علامتِ الباقی قرینه — حسابی که روی صفحه «رسید قبلی
  ۴۵ · الباقی ۱۹۵» بود، روی کاغذ «۰ · −۲۴۰» می‌شد. حالا رسید را خودِ صفحه
  می‌دهد (`HeadRasidOf`) و فرمول همان `Remainder` است. ⛔ دو جای تصمیم
  نسازید.
- ⛔ **تاریخ و عدد دو خط نمی‌شوند**: `DocStyle.TdText` خانهٔ کوتاهِ عددی را
  با سقفِ یک خط (`MaxHeight`) و `ScaleToFit` می‌چیند. ⚠️ `ScaleToFit`
  تنها بی‌اثر است: شکستن به دو خط هم «جا شدن» حساب می‌شود. صرافی از دو
  ورق به یک ورق رسید.
- ⛔ **سیاه‌وسفید سه قاعده دارد**: `Ink` (نوشته، هرگز سفید روی سفید)،
  `Edge` (خط، همیشه سیاه)، `InkOn` (نوشتهٔ روی نوارِ تیره). `Paint`ی تنها
  خطِ خانه‌ها را سفید و برچسبِ کم‌رنگ را ناپدید می‌کرد.
- ⛔ **هیچ رنگِ خامی در `*Report.cs` نیست** — شصت‌ویک جا بود و هیچ‌کدام
  به سیاه‌وسفید و خاکستری گوش نمی‌داد. `HichRangeKhamiDarGozareshha_Nist`
  هر خطِ تازه را می‌گیرد.
- «پطرول» با «پ» است؛ «بطرول» غلطِ تایپیِ سه‌جاییِ سایت بود که رونوشت شده بود.
- آزمون: `SheetsWidthsPrintTests` و دو گامِ تازه در `scroll-audit.yml`.

## 🔎 بازبینیِ فهرستِ یازده‌تایی — چه نصفه مانده بود (از ۱۴۰۵/۰۷/۱۲)

خواستهٔ صاحب ریپو: «دقیق چک کن که کدوم‌ها جا مونده یا کدوم‌ها نصفه یا حتی
اجرا نشده.» سنجهٔ تازه `-- audit11` (در CI) چهار بندی را می‌سنجد که تا امروز
فقط «درست شد» گفته شده بودند، و سه‌تایش نصفه بود:

- ⛔ **نسخهٔ ۳.۱.۱۵۸ هیچ‌وقت منتشر نشد.** در `[Code]`ِ فایلِ نصاب توضیح با
  «;» بود (Pascal «//» می‌خواهد) و `ISCC` با «'BEGIN' expected» ایستاد — و
  چون `build-native.yml` فقط **پس از** مرج می‌دود، کسی نفهمید. حالا
  `installer-check.yml` روی هر PRِ نصاب هر دو نصاب را روی ویندوز می‌سازد و
  `InstallerTests.Code_section_has_no_semicolon_comments` همان یک نویسه را
  می‌گیرد. ⛔ هر فایلی که فقط پس از مرج ساخته می‌شود، سنجهٔ پیش از مرج
  می‌خواهد.
- ⛔ **فرمِ خریدِ مخزن جلوی چشم می‌آمد ولی «قفل» نبود**: یک چرخِ ماوس صفحه را
  می‌لغزاند و فرم ۶۰۰ پیکسل بالاتر از قاب می‌رفت. تا فرم باز است چرخ مالِ
  خودِ فرم است (`BuyOverlay` ⇒ `PointerWheelChanged`).
- ⛔ **جدولِ پایه‌های ورق با کوچک شدنِ پنجره عدد می‌بُرید.** ستونِ ستاره‌ای به
  نسبت کوچک می‌شود، پس روی ۱۱۰۰ پیکسل شروع و ختم «۱,۲۳۴,…» می‌شدند.
  `ExcelGrid.StarFloors` کفِ هر ستون را از `FitToRoom` می‌گیرد و `FitToRoom`
  حالا **اول ستون‌های نوشته‌ای** (نام، یادداشت) را کوتاه می‌کند و ستونِ عددی
  را تا آخرین چاره دست نمی‌زند (`Numeric` از نوشتهٔ خانه‌ها). ⚠️ کف با دست
  گذاشتن روی سرستون برداشته می‌شود (`ReleaseStarFloors`)، وگرنه هیچ ستونی را
  نمی‌شد باریک‌تر از محتوایش کشید. ⚠️ جای خانه‌ها = قاب منهای ستونِ «#».
- ✅ ماه عوض کردن در هر دو تم: سرستون‌ها با خانه‌ها صفر پیکسل فاصله و ۹۷٪ پخش.
- ✅ جهتِ کلید در حالتِ نوشتن (`keys`): از همان فشارِ اول درست.
- **ماهِ تازه**: نوارِ پوستهٔ پنجره صریح می‌گوید «📅 ماهِ «X» شروع شد… هیچ
  چیزی پاک نشده» و یک «✕» دارد که فقط **همان جمله** را پنهان می‌کند (در
  حافظه؛ اجرای بعدی باز یک بار می‌گوید). سرِ نیمه‌شبِ آخرِ ماه یک توستِ
  گذرا (`MainViewModel.DayChanged`) — نه پنجرهٔ پرسش.
  ⛔ **پس گرفته شد (۱۴۰۵/۰۷/۱۳)**: نه نوار، نه توست — نقطهٔ سرخ (انتهای همین فایل).


### ⛔ ردیفِ حذف‌شده برمی‌گشت — و آن را سنجهٔ `person` گرفت (همان روز)

با کوتاه شدنِ مکثِ ذخیره (۳۵۰ ⇒ ۱۵۰ms) سنجهٔ `person` روی CI سرخ شد و حق
داشت. زنجیره: رسیدِ دیزل از سربرگ آمد، همان لحظه حذف شد، ولی ذخیرهٔ
تأخیریِ خودِ ردیف هنوز در صف بود — و بعد همان ردیف را با `IsDeleted = false`
دوباره نوشت. «جدولِ جدید» یک ردیفِ آرشیوشده/حذف‌شده را در جدولِ تازه نشان
داد؛ در کارِ واقعی همین با **بستنِ برنامه** رخ می‌داد
(`SaveGuard.FlushAllAsync` همهٔ ردیف‌های کثیف را می‌نویسد). مکثِ ۳۵۰ فقط
پنهانش می‌کرد.

- ⛔ **`RowViewModel.RetireAsync()` پیش از هر حذفِ ردیف** — صفِ تأخیری لغو،
  ردیف دیگر کثیف نیست، و نوشتنِ در راه تمام می‌شود پیش از آن‌که حذف به دیسک
  برسد. هر هفت مسیرِ حذف و هر دو «جدولِ جدید» (قرض‌دار و شرکت) صدایش
  می‌زنند. مسیرِ حذفِ تازه‌ای که فردا نوشته شود باید همین را بزند.
- ⛔ **دو نوشتنِ یک ردیف پشتِ سرِ هم‌اند، نه «دومی رد شود»** (`SemaphoreSlim`).
  پیش از این `FlushAsync`ی که وسطِ نوشتنِ تأخیری می‌رسید همان لحظه برمی‌گشت
  و «جدولِ جدید» پیش از تمام شدنِ نوشتن آرشیو می‌کرد.
- ⛔ **مکث را به ۳۵۰ برنگردانید** — آن جلوی باگ را نمی‌گرفت، پنهانش می‌کرد.
- آزمون: `RowRetireTests` (چهار بند، سه‌تایش رفتاری) و `person`.

⚠️ و `cells` هم روی همین PR سرخ شد و **مالِ این کار بود**: کفِ «پهنای
طبیعی» (`StarFloors`) نمی‌گذاشت ستونِ پیکسلی‌شده باریک‌تر از محتوایش شود
(۳۰ ⇒ ۱۱۶). حالا هر ستونِ پیکسلی (کشیدن، دوبار-کلیک، پهنای ذخیره‌شده) کف را
برمی‌دارد (`PinOnUserResize` ⇒ `ReleaseStarFloors`).

## 🔁 شیفت‌+عدد، برگشت و دوباره، تکملهٔ کم‌رنگ، «دیدم» داخلِ کادر (از ۱۴۰۵/۰۷/۱۲)

گزارشِ صاحب ریپو: «شیفت با عدد جدولی رو حذف نمیکنه — اگه پر بود تایید بخواد،
اگه خالی بود بی سوال حذف کنه… کنترول زد اصلن کار نمیکنه — هر چیزی از کادر حذف
بشه یا حسابی حذف بشه — و کنترول وای هم جلو نمیره… پیشنهادی‌ها هم‌رنگِ نوشته‌های
من‌اند… زیان ناشی از افزایش قیمت را خالی کن، کادرش بماند… و «دیدم» داخلِ خودِ
شروعِ پایه باشد که فاصله نیندازد.»

سنجهٔ تازه — **با کلید و کلیکِ واقعی**، نه صدا زدنِ فرمان:

```
dotnet run --project PumpYaqobi.UiTests -c Release -- undokeys     (در scroll-audit.yml)
```

### ۱) ⛔ کلیکِ تک فقط «انتخاب» است — ریشهٔ «شیفت با عدد حذف نمی‌کند»

سنجه پیش از اصلاح نشان داد: بارِ اول کار می‌کرد و بارِ دوم نه. `DataGrid`ی
آوالونیا با **کلیکِ دوم روی خانهٔ جاری** ویرایش را باز می‌کند و از آن لحظه
فوکوس در کادرِ تایپ است — پس `Shift+عدد` نویسه می‌شد (عمداً، قاعدهٔ
۱۴۰۵/۰۶/۲۹: `( ) ، ٪`)، `Delete` یک حرف پاک می‌کرد و `Ctrl+Z` مالِ کادر بود.

- ⛔ `ExcelGrid` در `BeginningEdit` هر ویرایشی را که با `PointerPressed` و
  `ClickCount < 2` باز شود لغو می‌کند. راه‌های باز کردن همان سه راهِ اکسل‌اند:
  **تایپ** (جایگزین می‌کند) · **دوبار-کلیک** · **F2**.
- ⚠️ ستونِ تیک (`DataGridCheckBoxColumn`) استثناست — تیک با یک کلیک باید بخورد.
- ⛔ قاعدهٔ «`Shift+عدد` داخلِ کادرِ تایپ نویسه است» **دست نخورد**؛ سنجه هر دو
  سمت را دارد.

### ۲) ⛔ `Shift+عدد` می‌پرسد فقط وقتی چیزی نوشته شده

`IRowBatchHost.LastRows(n)` + `RowData.HasData` (در `KeyboardContracts.cs`).
«پر» یعنی چیزی که **کاربر** نوشته: تاریخ، فی، نرخ، ارز و نوعِ تیلِ پیش‌فرض
شمرده نمی‌شوند؛ ردیفِ قرض‌دار با همان `PostingService.IsBlankRow`. خانهٔ باز
اول بسته می‌شود، بعد تصمیم.

### ۳) ⛔ `Ctrl+Z`/`Ctrl+Y` برای کلِ برنامه — `Services/UndoHub.cs`

- **یک پشته**، به ترتیبِ واقعیِ کار: ویرایشِ خانه (از `ExcelGrid`، در حافظه)
  و **هر حذف** (ردیف، حساب، ورق، شرکت…).
- ⛔ **حذف از راهِ سطلِ زباله برمی‌گردد، نه یک سازوکارِ دوم.** هر حذف در همان
  ذخیره یک قلمِ سطل می‌گذارد، پس `PumpDbContext.TrashAdded` (یک نقطه، پس از
  ذخیره) می‌گوید «همین حالا چه رفت». برگشت = `TrashService.RestoreTracedAsync`
  (همان بازگردانیِ آزموده، با ردِ رکوردهای برگشته)؛ دوباره =
  `DeleteAgainAsync` — **همان** رکوردها با `db.Remove`، در **یک** ذخیره تا
  مُهرِ زمانشان یکی باشد و برگشتِ بعدی باز پدر و فرزندان را با هم بیاورد.
- ⛔ `UndoHub.Group(...)` چند ذخیرهٔ یک کار را یک قدم می‌کند (`Shift+5`).
- ⛔ **داخلِ کادرِ تایپ `Ctrl+Z` مالِ خودِ کادر است** (`!TypingInBox`).
- ⛔ برگرداندن/دوباره در حالِ پخش هیچ قدمِ تازه‌ای نمی‌سازد (`_replaying`)،
  وگرنه «دوباره»ی یک حذف خودش پشته را پاک می‌کرد.
- ⚠️ پس از برگشتِ حذف، صفحهٔ جلوی چشم **و صفحهٔ بازِ درونش** از نو خوانده
  می‌شود (`SectionViewModel.AfterUndoAsync` — قرض‌دار، شرکت، ورق، امانت
  نسخهٔ خودشان را دارند).
- ⚠️ عوض شدنِ دفتر (حسابِ دیگر) پشته را پاک می‌کند (`UseLedgerOf`).
- ⚠️ نوعی که بازگردانی ندارد (`companyarchive`، `debtarchive`) اصلاً در پشته
  نمی‌نشیند (`TrashService.CanRestore`) — قولی که نمی‌شود داد، داده نمی‌شود.

### ۴) تکملهٔ خودکار کم‌رنگ است

تکمله همان تکهٔ **انتخاب‌شده**ی کادر است و انتخاب با رنگِ همان نوشته کشیده
می‌شد. `Suggest` تا تکمله دیده می‌شود کلاسِ `ghost` را می‌گذارد: زمینهٔ انتخاب
شفاف و نوشته‌اش `Pump.Ghost` (نیمه‌راهِ کارت و `Muted`، شبیهِ نوشتهٔ راهنمای
کادر). ⚠️ هر جا تکمله برمی‌خیزد کلاس هم برمی‌خیزد، وگرنه انتخابِ واقعیِ بعدی
نامرئی می‌شد. ⚠️ سبکِ `TextBox.ghost` **پس از** دو سبکِ `SelectionBrush` آمده —
در آوالونیا سبکِ بعدی برنده است.

### ۵) «زیان ناشی از افزایش قیمت» — کادر هست، محتوا نه

- ⛔ زیربخشِ `priceloss` زیرِ «قرض‌داران» **ماند** (کارت، عنوان، رمزِ بخش،
  دروازهٔ پلن) و صفحه‌اش فقط می‌گوید «این بخش فعلاً خالی است».
- ⛔ `PriceLossService`، `PriceLossTests` و سنجهٔ `plprof` **پاک شدند**؛ در
  تاریخچهٔ گیت هستند. **فرمولِ تازه را صاحب ریپو می‌دهد** — از خودت نساز و
  قدیمی را برنگردان.

### ۶) «✔ دیدم» داخلِ خودِ کادرِ شروعِ پایه

ردیفِ همیشه‌حاضرِ ۲۲ پیکسلی (۱۴۰۵/۰۷/۰۶) جابه‌جا نمی‌کرد ولی همیشه فاصله
می‌گذاشت. حالا ⚠️ و «✔ دیدم» در `TextBox.InnerRightContent`ِ همان کادرند و
جملهٔ کامل `ToolTip`ِ آن دو. سنجه فاصلهٔ زیرِ کادر را با فاصلهٔ کادرهای دیگر
می‌سنجد (پیش و پس از هشدار یکی).

⛔ **و عددِ «ختم» هم‌پای عددِ «شروع» می‌رود** (عکسِ صاحب ریپو: «هر دو عدد با هم
بروند یک طرف، و اگر مشکلی نبود هر دو سرِ جایشان»). هشدار جا می‌گیرد و عددِ
وسط‌چینِ شروع را کنار می‌برد؛ پس کادرِ ختم **رونوشتِ نامرئیِ همان تکه** را دارد
(`Opacity=0`، بی‌کلیک، بی‌فوکوس، با همان `IsVisible`) — رونوشت، نه فاصلهٔ عددی،
تا پهنایش با هر قلم و اندازه‌ای همان باشد. سنجه وسطِ هر دو عدد را پیش، حین و
پس از هشدار می‌سنجد (اختلاف زیرِ ۱ پیکسل).

آزمون: `UndoDeleteTests` (رفت‌وبرگشتِ واقعیِ حذف/برگشت/دوباره روی SQLite،
`RowData.HasData`، و قاعده‌های سورس) و بندهای به‌روزشدهٔ `ShortcutHelpTests` و
`ParchaHistoryWaraqUxTests`.


## 🛡️ ممیزیِ امنیتیِ برنامه — ۱۴۰۵/۰۷/۱۲ (نسخه 3.1.161)

شرحِ کامل (یافته‌ها، معماری، جریانِ آنلاین/آفلاین، مدلِ تهدید، جدولِ دیسک و
پیام‌ها): `native/docs/SECURITY-AUDIT-fa.md`. این‌جا فقط چیزهایی که **نباید
برگردند**:

- ⛔ **هیچ کلیدِ خصوصی و هیچ رازی داخلِ برنامه نیست** — فقط کلیدِ عمومی.
- ⛔ **ارفاق فقط از خودِ مجوزِ امضاشده است** (`exp` + ۱۴ روز، با فهرستِ `feat`ِ
  همان مجوز). `EntitledUntil` فقط نمایشی است و هیچ قفلی را باز نمی‌کند؛ پاسخِ
  بی‌امضای `/api/pump/me` هم مُهر نمی‌زند. بی این، یک ویرایشِ JSON هر شش قفل را
  برای همیشه باز می‌کرد.
- ⛔ **`LicenseGuard.CheckStored` تنها سنجشِ مجوزِ روی دیسک است** و «اکنون»ش از
  `LicenseClock` می‌آید (کفِ یک‌طرفه در برابرِ عقب بردنِ ساعت).
- ⛔ **`CloudLink.AdoptLicense` تنها درِ نشستنِ مجوزِ تازه است**: امضا و هویت
  **پیش از** نشستن. مجوزِ خالیِ سرور مجوزِ کهنه را پاک می‌کند.
- ⛔ **`LicenseKeys` (از `vars.LICENSE_PUBLIC_KEYS`) وقتی پر است تنها ریشهٔ اعتماد
  است** و کلیدِ `settings.json` نادیده گرفته می‌شود. ⚠️ پیش از پر کردنش، کلیدِ
  **فعلیِ** سرور را با همان `kid` بگذارید — وگرنه همه `key_mismatch` می‌گیرند.
- ⛔ **فقط `device_revoked` / `device_not_registered` توکنِ دستگاه را پاک می‌کنند**
  (`DevSendAsync`). خطای شبکه، ۵xx و ۴۰۱ِ بی آن دو کد هیچ چیزی را پاک نمی‌کنند.
- ⛔ **تازه‌سازیِ نشست یک قفلِ سراسری دارد** و داخلِ قفل توکن‌ها را از دیسک
  دوباره می‌خواند. قفلِ هر نمونه همان «خروجِ تصادفی» بود.
- ⛔ **یک نمونه در هر پوشهٔ تنظیمات** (`SingleInstance`)؛ فایلِ موقتِ تنظیمات
  نامِ یکتا دارد.
- ⛔ **رمزِ نوشتنِ سرورِ خانگی فقط در تنظیماتِ رمزشده** — هیچ‌وقت در `pump.db`
  (که در هر پشتیبان هست). `SettingsService.Remove` واقعاً `DELETE` می‌کند.
- ⛔ **به‌روزرسانی: فقط https و میزبان‌های گیت‌هاب، `SHA256SUMS.txt` لازم**، و
  هشِ دوباره درست پیش از اجرا. `UPDATE_PUBLIC_KEY` را فقط با رازِ
  `UPDATE_SIGNING_KEY` بگذارید.
- ⛔ **`SafeOpen` تنها راهِ «باز کن»** (http/https/rtsp/rtmp/mailto) و
  **`ErrorText` تنها راهِ متنِ خطا** — متنِ خامِ استثنا فقط در `crash.log`.
- ⛔ **عوض/برداشتنِ رمزِ بخش رمزِ فعلی می‌خواهد**، با مکثِ فزاینده پس از پنج غلط.
- ⚠️ `Entitlements.UnlockedForNow = true` دست نخورد؛ هیچ‌کدامِ این قیدها امروز
  قفلی را نمی‌بندد.

## 🧾 ورق با عکسِ صاحب ریپو — دیوار، قرائتِ پمپ، جملهٔ PDF و چاپِ تیز (از ۱۴۰۵/۰۷/۱۲)

```
dotnet run --project PumpYaqobi.UiTests -c Release -- waraqfit [پوشهٔ عکس]     (در scroll-audit.yml)
```

### ۱) ⛔ ستون‌ها از کادرِ جدول بیرون نمی‌زنند — «دیوار»

- ⛔ **ریشه یک عدد بود**: ‎`SpreadColumns`‎ جای خانه‌ها را ‎`Bounds.Width`‎ می‌گرفت
  و ستونِ «#» (~۴۰ پیکسل) را نمی‌شمرد، پس در مسیرِ «جای اضافه نیست» ستون‌ها
  درست به همان اندازه بیرون می‌زدند — «یادداشت» و کپسول‌های «نوع»/«واحد»
  نیمه‌بریده. حالا **`CellRoom()`** تنها جای این حساب است (‎`StarFloors`‎ از
  روزِ اول همین را کم می‌کرد و این یکی نه).
- ⛔ **`ExcelGrid.KeepInside`** فقط روی سه جدولِ ورق: پهنای **ذخیره‌شده**‌ای که
  در این قاب جا نمی‌شود کوچک نشان داده می‌شود، پنجره که کوچک/بزرگ شد ستون‌ها
  کوچک و **دوباره باز** می‌شوند (`_fullWidths`)، و ستونی که کاربر پهن کشید جا را
  از ستون‌های دیگر می‌گیرد. ⚠️ پهنای ذخیره‌شدهٔ کاربر **روی دیسک دست نمی‌خورد**
  (`_autoWidths` همان عددِ کوچک‌شده است) — روی نمایشگرِ بزرگ‌تر برمی‌گردد.
  ⚠️ خواستهٔ قدیمیِ «ستونِ کشیده‌شده به چپ و راست اسکرول شود» برای بقیهٔ
  جدول‌ها سرِ جایش است.
- ⛔ **نوشتهٔ فارسیِ بلند در خانه فقط «…» می‌شد** — باگِ آوالونیا ۱۱.۲.۳، سنجیده
  با برنامهٔ جدا: هر حالتِ ‎`TextTrimming`‎ روی متنِ راست‌به‌چپ کلاً «…» است و
  بی آن آغازِ جمله از راست بیرون می‌افتد. **`RtlTrim`** (روی سبکِ
  ‎`DataGridCell TextBlock`‎) برای همان نوشته‌ها ‎`Wrap` + `MaxLines=1` + `None`‎
  می‌گذارد — خطِ اول تا آخرین واژهٔ جاشده. عدد و لاتین همان «…» می‌مانند.
  ⚠️ فقط سه خاصیتِ ظاهری عوض می‌شود، نه ‎`Text`‎ — اتصالِ ستون سالم می‌ماند.

### ۲) قرائتِ پمپ از پارچه

- ⛔ ستون‌های **«تاریخ»** و **«هشدار»** برداشته شدند و برنمی‌گردند («بالای
  پارچه نوشته، بسه»). ‎`PumpDate`‎ و ‎`LowBase`‎ در داده می‌مانند.
- ⛔ **تیلِ ردیفی که از پارچه آمده (`SrcKey`) این‌جا عوض نمی‌شود** — کپسولِ
  قفل (`Border.fuelchip.locked`)، و ‎`ToggleFuel`‎ خودش هم رد می‌کند. ردیفِ دستیِ
  «پایهٔ تازه» همچنان عوضش می‌کند.
- ⛔ **خطِ خودِ خانه سرخ می‌شود، نه یک کادرِ دوم داخلش** (عکسِ صاحب ریپو).
  ‎`IssueTextColumn`‎ کلاسِ ‎`issue`‎ را روی خودِ ‎`DataGridCell`‎ می‌گذارد و سبکش
  ‎`CellBorder`‎ی قالب و ‎`PART_RightGridLine`‎ را سرخ می‌کند؛ یک مثلثِ کوچک در
  گوشه. ⚠️ هنوز ‎`DataGridTextColumn`‎ است، پس کپی/پیستِ ‎`ExcelGrid.Write`‎ سالم.
- ⛔ **پیام فقط با ماوس و کوتاه و یک‌خطی** — «۱٬۰۰۰ لیتر کمتر از ختمِ قبلی
  (۱۲۰٬۰۰۰)». انتخابِ خانه هیچ کادری خودکار باز نمی‌کند: کادرِ خودکار جلوی
  نوشته‌های ردیفِ بالا را می‌گرفت. ⚠️ پیام یک ‎`TextBlock`‎ِ صریح است
  (`IssueTextColumn.TipOf`)، نه رشتهٔ خام — رشته زیرِ سبکِ خانه «…» می‌شد.
- «ختمِ قبلی» از ‎`ParchaDataService.PrevEndForWaraqAsync`‎ می‌آید: همان زنجیرهٔ
  ‎`BaseHistoryAsync`‎ (پارچه‌های **پیش از** همین یکی)، و **فقط برای ردیفِ
  نشان‌دار** — ورقِ سالم هیچ پرس‌وجوی تازه‌ای ندارد.
- برداشتنِ نشان (همان «عادی شد»ِ قبلی): راست‌کلیک ⇐ «✔ نشانِ سرخ را بردار»
  (`IFlaggedRow`). درست کردنِ شروع به همان ختمِ قبلی نشان را خودش برمی‌دارد.

### ۳) ⛔ PDFِ ورق «🧮 جمله این شیفت» دارد

ته جدولِ پایه‌ها: یک ردیف برای هر تیل (وقتی هر دو هست) و یک ردیفِ جمع — لیتر ·
فروش · قرض، هر کدام زیرِ ستونِ خودش. ⛔ **عددِ تازه‌ای ساخته نشد**:
‎`WaraqReport.ShiftTotalRows`‎ همان قاعدهٔ جدول (‎`max(0, ختم−شروع)`‎ × فی) است و
سنجه برابری‌اش را با ‎`WaraqService.ShiftTotals`‎ی صفحه می‌خواهد.

### ۴) ⛔ پیش‌نمایشِ چاپ تیز است، با هر زوم

- ریشه: ورق با **۹۶ نقطه** تصویر می‌شد و روی قاب و ویندوزِ ۱۲۵٪/۱۵۰٪ کش داده
  می‌شد؛ هر زوم بدترش می‌کرد.
- ⛔ **پاسِ اول همان ۹۶ می‌ماند** (پنجره همان لحظه باز شود — ‎`printperf`‎)، و
  همین که همه آمدند پاسِ **تیز** با dpiی که پیکسلِ **واقعیِ** نمایشگر می‌خواهد
  (‎`PageWidth × RenderScaling`‎، ۱۴۴ تا ۳۰۰) در پس‌زمینه ساخته می‌شود؛ زوم که
  بالاتر رفت، دوباره (با مکثِ ۲۲۰ms). `Image` با ‎`HighQuality`‎.
- ⚠️ **SVG آزموده و رد شد**: ‎`GenerateSvg`‎ی QuestPDF متنِ فارسی را با
  ‎`<text>`‎ و شمارهٔ گلیف می‌نویسد و خالی درمی‌آید.
- ⚠️ **JPEGِ بهترین کیفیت، نه PNG** — سنجیده: ۱۹۲ نقطه، PNG ۲۲۳ms و JPEG ۱۰۲ms
  برای هر ورق.
- ⚠️ **فقط ورقِ جلوی چشم بازشده در رم است**؛ بقیه فشرده‌اند.
- ⛔ چاپ و ذخیره از این مسیر نمی‌گذرند و همان ‎`Setup.Dpi`‎ی کاربر را دارند.

آزمون: `WaraqSheetFixTests` (۱۳ بند) و `waraqfit` (بیش از پنجاه بند، هر دو
مسیرِ «پهنای ذخیره‌شده» و «پهنای خودکار»؛ سنجیده شد که روی منطقِ پیشین سرخ است).

## 🕘 تاریخچه‌ها با ستون‌های خودِ هر بخش · اسکرولِ بی نوارِ سفید (از ۱۴۰۵/۰۷/۱۲)

```
dotnet run --project PumpYaqobi.UiTests -c Release -- historyfit [پوشهٔ عکس]     (در scroll-audit.yml)
```

- ⛔ **هر بخشی که ستون‌های خودش را دارد، دیگر همه‌چیز را در یک «توضیح» نمی‌ریزد**
  (`HistoryService.ColumnsOf` + `HistoryRow.Cells`):
  - **پارچه‌ها**: تاریخ · پارچه · تیل · شیفت (روز/شب) · کارمند · شمارهٔ پایه · شروع ·
    ختم · لیتر · پول. همان دو ردیفِ هر پارچه.
  - **ورق‌ها**: یک ردیف برای هر **شیفت** (روز و شب هرگز با هم جمع نمی‌شوند) —
    شمار و جملهٔ قرض، شمار و جملهٔ مصرف، لیتر و جملهٔ فروش. جمع‌ها از
    `WaraqService.ShiftTotals` (همان شش کادرِ صفحهٔ ورق). شیفتِ بی پایه و بی ردیفِ
    نام‌دار نمی‌آید و کارت هم همان را می‌شمارد (`TheCardsMatchTheFeed`).
  - **صرافی**: «💵 تحویل به صرافی» (مبلغ · واحد · فی · دالر) یا «📤 بردگیِ پمپ از
    صرافی» (دالر) — همان دو ستونِ صفحهٔ صرافی.
  - **شرکت‌ها**: «بردگیِ پمپ از شرکت ($)» (تن × قیمت، و به افغانی) و «رسیدِ پمپ =
    بردگیِ شرکت» **با ارزِ خودش**. ⚠️ تا امروز ارزِ رسید نوشته نمی‌شد و رسیدِ
    دالری افغانی خوانده می‌شد.
  - بقیه (مصارف، گاوصندوق، …) همان جدولِ کلی‌اند.
- ⛔ **هیچ عددِ تازه‌ای ساخته نشد** — هر خانه از سرویسِ خودِ بخش. `Title`/`Detail`/
  `Amount`/`Tone` هم سرِ جایشان‌اند.
- ⛔ جدولِ هر بخش یک **نمونهٔ تازه** است (`HistorySectionView.axaml.cs`)، نه عوض
  کردنِ ستون‌های جدولِ قبلی — `ExcelGrid` پهنا را یک بار سفت می‌کند. ⚠️ بازسازی
  فقط با خودِ `Columns` (نه `OpenKind` که زودتر می‌نشیند و جدول با ستون‌های بخشِ
  قبلی ساخته می‌شد). ⚠️ و `DataGrid.ItemsSourceProperty`، نه
  `ItemsControl.ItemsSourceProperty` (آن یکی روی `DataGrid` هیچ کاری نمی‌کند).

### ⛔ نوارِ سفیدِ بالای سرستون — در **همهٔ** صفحه‌های باز، نه فقط تاریخچه

قابِ چسبان (`StickyRowLimit`) از زیرِ نوارِ بخش‌ها شروع می‌شود (`BlindTop`). صفحهٔ
باز (تاریخچهٔ یک بخش، حسابِ قرض‌دار، …) آن نوار را **پنهان** می‌کند، ولی
`Bounds`ِ کنترلِ پنهان همان عددِ آخرش است — پس قاب ۵۳ پیکسل پایین‌تر می‌نشست و
بالای سرستون یک نوارِ سفید می‌ماند. حالا `Blind(bar)`: نوارِ پنهان صفر است.

### `ExcelGrid.HeaderFollows` — سرستون همراهِ دید بیاید یا نه

پیش‌فرض همان اکسل است (سرستون بالای دید می‌ماند). تاریخچه‌ها `false` دارند
(«سرِ جدول‌ها توی اسکرول تا ته صفحه با من می‌آید»): قاب به اندازهٔ بلندیِ سرستون
بالاتر می‌نشیند و یک سرستون بلندتر است. ⚠️ **مجازی‌سازی سرِ جایش است** و جای هر
ردیف همان است، چون ردیف‌ها به همان اندازه‌ای می‌لغزند که قاب. سنجه: بالای دید
ردیف است (نه سفید)، پایینِ دید هم، و شمارهٔ ردیف‌ها پشتِ سرِ هم.

## 🧾 فهرستِ نُه‌تاییِ ۱۴۰۵/۰۷/۱۳ — و «چرا به سرور وصل نمی‌شود»

```
dotnet run --project PumpYaqobi.UiTests -c Release -- round13 [پوشهٔ عکس]           (در scroll-audit.yml)
dotnet run --project PumpYaqobi.UiTests -c Release -- livestack <live.json> [پوشه]  (با سرورهای واقعی، دستی)
```

### ۱) مخزن: عددِ خرید هزار برابر بود

کادر «وزن (تن)» بود و مردم **کیلو** می‌زدند؛ `KgOf` آن را × ۱۰۰۰ می‌کرد. حالا
کادر «وزن (کیلو)» است (`BuyKg`) و همان عدد کیلو می‌نشیند؛ کارت کیلو و تن را
کنارِ هم نشان می‌دهد. ⛔ **خریدهای ذخیره‌شده دست نخوردند** — تبدیلِ خودکارِ
دادهٔ مشتری برگشت‌ناپذیر است و انجام نشد. خریدی که وزنش باورنکردنی است
(`Implausible`، ≥ ۳۰۰ تن) یک هشدار روی کارتش دارد؛ درست کردنش حذف و ثبتِ
دوباره به دستِ خودِ کاربر است. ⛔ «کیلو»ی حذف‌شدهٔ ۱۴۰۵/۰۶/۲۶ پس گرفته شد: آن
بند وقتی بود که دو کادر بود؛ حالا یک کادر است و واحدش همان است که مردم می‌زنند.

### ۲) نوارِ «همگام‌سازی» پایینِ پنجره رفت

نوارِ پایین فقط وقتی دیده می‌شود که پیامی هست (`HasNotice`). حالِ همگام‌سازی
همچنان در چراغِ یگانهٔ سربرگ و پروفایل است.

### ۳) فاکتورها — «‹ برگشت» و کلیک روی کارت

«‹ برگشت» روی فهرستِ «در صف»/«تاییدشده» و روی صفحهٔ هر فاکتور؛ برگشت از صفحهٔ
فاکتور به همان جایی است که از آن آمده (`_cameFrom`). کلیک روی خودِ کارت فاکتور
را باز می‌کند، مگر کلیک روی دکمه‌ای داخلِ کارت باشد.

### ۴) پارچه — گزارش‌ها صفحهٔ جدا، سال ← ماه، فقط‌خواندنی

- ⛔ **فهرستِ گزارش‌ها تا صفحه‌اش باز نشده خوانده نمی‌شود** — صفحهٔ اول فقط
  `CountAsync` می‌زند (قاعدهٔ «برای یک عدد، همهٔ ردیف‌ها را نخوان»).
- گروه‌بندیِ سال و ماه با `Expander`؛ کلیک ⇒ همان گزارش با دو شیفتش. حذف
  می‌پرسد.

### ۵) شرکت‌ها

- ⛔ ستونِ 📦 از جدولِ حساب رفت (قفلِ ردیفِ خریدِ مخزن سرِ جایش است).
- آرشیو: صافیِ تیل (همه/پطرول/دیزل)، جملهٔ «خالی» که می‌گوید آرشیو چطور ساخته
  می‌شود، و **PDF/چاپ برای هر آرشیو** (`CompanyReport` با ردیف‌های همان آرشیو).
  ⚠️ خواندنِ `RowsJson` حالا نام‌های بزرگ/کوچک و عددِ رشته‌ای را می‌پذیرد
  (`ArchiveRead`). «آرشیو خالی / دیزل نیست» با دادهٔ سنجه بازسازی **نشد**.
- جست‌وجوی خرید درون‌خطی (`FindText` + Enter): یک نتیجه ⇒ مستقیم همان حساب؛
  چند ⇒ صفحهٔ جست‌وجو با همان پرسش؛ هیچ ⇒ توست.

### ۶) سربرگ: نامِ ماه، و کلیک ⇒ تنظیمِ تاریخ و ساعتِ ویندوز

`MainViewModel.HeaderDate` ⇒ «پنج‌شنبه، ۲ میزان ۱۴۰۵». کلیک روی تاریخ/ساعت
`SystemClockSettings.Open()` (‎ms-settings:dateandtime‎، پشتیبان `timedate.cpl`).
⛔ **برنامه ساعتِ سیستم را خودش عوض نمی‌کند** — دسترسیِ مدیرِ ویندوز می‌خواهد.

### ۷) ⛔ جدول جای خالی ندارد — `ExcelGrid.FillGapStep`

پهنای ذخیره‌شده/پیکسلی که از قاب کوچک‌تر است، جای باقی‌مانده را به ستونِ
**پُرکننده** می‌دهد (ستونِ ستاره‌ایِ اعلام‌شده در XAML، وگرنه آخرین ستون).
پهنای کاربر روی دیسک دست نمی‌خورد. تاریخچهٔ پارچه صافیِ تیل و دکمهٔ هر شمارهٔ
پایه دارد (`HistoryRow.Fuel`/`Pump`).

### ۸) ورق: پیامِ خانهٔ سرخ روی **کلِ** خانه، دو خط، و کارت‌های بلندتر

`IssueTextColumn` کادرِ پیام را روی خودِ `DataGridCell` می‌گذارد، نه روی
نوشته. پیام دو خط است: چه شد + عددها («ختمِ قبلیِ پایهٔ ۲: … · این شروع: …»).

### ۹) ⛔ «به سرورِ خانگی وصل نمی‌شود» — کدِ پمپِ مشترک

با **پنلِ خانگیِ واقعی و سرورِ حسابِ واقعی** سنجیده شد (`livestack`): برنامه سرور
را با UDP پیدا می‌کرد ولی ثبت نمی‌شد، چون **همهٔ نصب‌ها با `pump1`** ثبت
می‌خواستند و سرور درست می‌گفت «مالِ برنامهٔ دیگری است» — و از آن به بعد برای
همیشه `already_taken`.

- ⛔ نصبی که **رمزِ هیچ پوشه‌ای ندارد** با کدِ یکتای حسابِ خودش ثبت می‌شود
  (`StationLink.UniqueCode` از `CloudStationId`).
- ⛔ روی `already_taken` (بی رمز و بی پین) جایگزین‌ها به ترتیب: کدِ حساب ⇒
  کدِ حساب + شناسهٔ این کامپیوتر ⇒ یک پسوندِ تصادفی. خطای دیگری حلقه را
  می‌بندد.
- ⛔ **نصبی که رمز دارد هیچ‌وقت کدش عوض نمی‌شود**، و رمزِ پوشهٔ دیگری گرفته
  نمی‌شود — شریک شدن در همان پوشه همچنان «کدِ جفت‌شدن»ِ پنل است.
- چراغِ یگانه: «یکی وصل، دیگری هنوز وصل نشده» زرد است، نه سبز.
- ⚠️ `livestack` در CI نیست: پنل و سرورِ حساب را بیرون از فرآیند می‌خواهد.
  آزمون: سه بندِ آخرِ `OwnerRound13Tests`.
- سمتِ سرور (ریپوی `server`): بستهٔ سرورِ حساب از SIGPIPEِ ورک‌فلو هیچ‌وقت
  منتشر نمی‌شد، و فهرستِ پمپ‌ها پمپِ وصل را «خبری نیست» می‌گفت — شرح در
  `CLAUDE.md`ِ همان ریپو.
- ⛔ **آن دو سنجهٔ سرخ (`syncui` و `cloudlogin`) ریشه‌یابی شدند — هر دو
  ایرادِ خودِ سنجه بودند، نه برنامه** (بخشِ بعد).
- ⚠️ **سنجه‌ای که پنلِ واقعی بالا می‌آورد، بعدش همهٔ فرزندانش را هم ببندد**:
  پنلِ یتیم روی UDPِ ۴۷۰۲ جواب می‌داد و `serverdot` را سرخِ دروغ کرد («چراغ
  سرخ نشد») — برنامه به همان پنلِ جامانده وصل می‌شد.

## 🧪 دو سنجهٔ سرخِ «مالِ ما نیست» — ریشه در خودِ سنجه بود (از ۱۴۰۵/۰۷/۱۳)

خواستهٔ صاحب ریپو: «اون دو تستِ سرخ رو هم ریشه‌یابی و درست کن.» هر دو روی
`main` هم سرخ بودند و در CI نبودند، پس هیچ‌کس نمی‌دید. **هیچ خطی از برنامه عوض
نشد.**

- ⛔ **`cloudlogin` بندِ ۱۸ («توکنِ دستگاه نشست»)**: ابرِ ساختگی مسیرِ
  `POST /api/pump/device/bind` را **هیچ‌وقت نداشت** (`git log -S` گشته شد) و آن
  درخواست به ۴۰۴ِ «مسیر نیست» می‌افتاد — در حالی که برنامه از ۱۴۰۵/۰۶/۳۱ با
  همان مسیر بند می‌شود. حالا ساختگی مو‌به‌مو همان `pump-device.js`ِ سرورِ
  حساب است: ۲۰۱ با `deviceToken` · `station` · `entitlement` و مجوزِ امضاشده،
  و حسابِ بی‌پمپ ⇒ ۴۰۴ِ `no_station`. ⛔ ساختگی باید همان کاری را بکند که
  واقعی می‌کند.
- ⛔ **`syncui` بندِ Paste و «کدِ غلط ⇒ همان گام»**: سنجه شش رقمِ **درست** را
  می‌چسباند، و پُر شدنِ خانهٔ ششم (به‌درستی، `CodeBoxes.Completed`) خودش
  می‌فرستد — ابرِ ساختگی هم‌زمان جواب می‌دهد، پس همان لحظه وارد می‌شد و
  بندهای بعدی روی گامِ ۳ می‌افتادند. و کدِ غلط با `Fill` + یک `Execute`ِ دستی
  **دو بار** فرستاده می‌شد. حالا Paste با کدِ **نیمه** سنجیده می‌شود (و
  سنجیده می‌شود که چیزی **نفرستاد**)، کدِ غلط با چسباندنِ کامل — همان راهِ
  کاربر — و شمارِ ارسال‌ها **دقیقاً یک** است.
- ⚠️ **سنجه ضعیف نشد**: هر بندِ قبلی سرِ جایش است و دو بندِ تازه گرفت (کدِ
  نیمه نمی‌فرستد · چسباندنِ کامل دقیقاً یک بار می‌فرستد).
- ⛔ **و هر دو به `scroll-audit.yml` رفتند** — ابرشان درون‌فرآیندی است
  (`CloudLink.TestTransport`) و هیچ درخواستی به سرورِ واقعی نمی‌زنند. سنجه‌ای
  که در CI نیست، سرخ ماندنش را هیچ‌کس نمی‌بیند.

## 💳 اشتراکی که از پنل داده/برداشته می‌شود — در یک دقیقه، با نامِ درست (از ۱۴۰۵/۰۷/۱۳)

گزارشِ صاحب ریپو: «نمی‌تونم اشتراکِ حسابی رو حذف کنم… روزِ مونده رو نمی‌بینم…
ببین برنامه با دقت می‌گیره و می‌گه چقدر مونده یا فعال شده یا نه.» این بار با
**سرورهای واقعی** سنجیده شد (سنجهٔ `livestack`: پنلِ خانگی + سرورِ حسابِ واقعی،
دادن ⇒ تمدید ⇒ لغو از خودِ API پنل، و خواندن از خودِ برنامه). چهار ریشه:

- ⛔ **اشتراکی که به حسابِ «آزمایشی» داده می‌شد تا ده دقیقه نمی‌رسید.** دورهٔ
  آزمایشی ⇒ وی‌آی‌پی هر دو طرف را «باز» نگه می‌دارد، پس «ناجوری» نمی‌دیدش و
  فقط تیکِ ده‌دقیقه‌ای می‌آوردش. حالا `KeepLicenseFreshAsync` هر بار که حالِ
  `/api/pump/me` با حالی که مجوزِ **روی دیسک** برایش گرفته شده فرق کند، همان
  دور مجوز می‌گیرد (`_lastSub = (مجوز، کلید)`).
  - ⚠️ «آخرین حال» به **خودِ مجوزِ روی دیسک** بسته است: مجوزی که این نمونه
    نگرفته (برنامهٔ تازه‌بازشده) نخستین دیدنش خطِ پایه است، نه «تازه» — وگرنه
    هر بالا آمدن یک درخواستِ بی‌دلیل.
  - ⚠️ کلید **پیش از** تازه‌سازی ثبت می‌شود: `RefreshAsync` حال را از
    `device/me` (شکلِ دیگر) می‌خواند و ثبتِ آن یعنی یک درخواست هر دقیقه.
    سنجه‌اش `LicenseDeliveryTests.EshterakeTaze_BiEntezar_MajvozMigirad`.
- ⛔ **سربرگ بی باز کردنِ پروفایل تازه نمی‌شد.** `CloudLink.LicenseChanged`
  (فقط وقتی مجوزِ روی دیسک واقعاً عوض شد) ⇒ `Account.RefreshAll()`.
- ⛔ **هر اشتراکِ بازی «VIP» خوانده می‌شد** — دورهٔ آزمایشیِ حسابِ تازه هم،
  استاندارد هم. مجوزِ سرورِ حساب برای اشتراکِ خریده‌شده **کدِ پلن** می‌فرستد
  (`vip`، `std`، `perm`)، نه عنوانش، و آزمایشی «دوره‌ی آزمایشی».
  `AccountSectionViewModel.KindOf` تنها جای ترجمه است ⇒ «آزمایشی · 29 روز»،
  «VIP · 364 روز»، «دائمی». نشانِ طلاییِ سربرگ «فعال» است نه «VIP»، و عنوانِ
  کارت «💎 اشتراک». سنجه: `ProfilePillTests`.
- ⛔ **لغو در ماهِ اول، دورهٔ آزمایشی را برمی‌گرداند** — سمتِ سرورِ حساب بود
  (`shop` ⇒ `lib/entitlement.js`)؛ شرحش در `CLAUDE.md`ِ همان ریپو.

## 🔔 خبر به گوشیِ **بسته** — آیفون با پوش، اندروید از تونل (از ۱۴۰۵/۰۷/۱۳)

«ببین برنامه بسته باشه پیام می‌ره برای کاربر… توی آیفون و اندروید به خوبی تست
کن.» سنجیده شد و **نمی‌رفت**:

| | پیش از این | حالا |
|---|---|---|
| آیفون، اپِ بسته | ⛔ هیچ — `kar/sw.js` رویدادِ `push` نداشت و گوشی هیچ‌جا ثبت نمی‌شد | پوشِ خودِ سرورِ خانگی |
| اندروید بیرون از شبکهٔ پمپ | ⛔ هیچ — کارِ پس‌زمینه فقط `http://192.168.x.x` را می‌زد | همان سرور از راهِ تونل |
| اندروید در شبکهٔ پمپ | ✅ | ✅ |

- ⛔ **خبر را خودِ سرورِ خانگی پوش می‌کند**، با هر خبرِ تازه در همان
  `live.alerts`ی برنامهٔ کامپیوتر (ریپوی `server` ⇒ `stations/alert-push.js`).
  برنامهٔ کامپیوتر هیچ تغییری نکرد و نباید بکند: فهرست یک جا ساخته می‌شود.
- ⛔ **ثبتِ گوشی با رمزِ خواندنِ همان پمپ است** (`POST /api/stations/<کد>/push`)
  و اول نشانیِ خانگی، بعد تونلِ `https://api.vill3n.top` — **در کد قفل** (همان
  `CLOUD`ِ `cloud.js`)، نه از تنظیمات.
- ⛔ **رفتن به پمپِ دیگر اشتراکِ پوش را باطل می‌کند** (`dropPush` ⇒
  `unsubscribe()`) — نشانیِ قبلی برای همیشه می‌میرد، پس خبرِ پمپِ قبلی به این
  گوشی نمی‌رسد. «با پمپ‌های دیگر قاطی نشود.»
- ⚠️ **روی آیفون فقط از «افزودن به صفحهٔ اصلی»** (iOS 16.4+) — سافاریِ ساده پوش
  ندارد و کادرِ خبرها همین را می‌گوید (`pushHint`)، نه امید.
- ⚠️ روی اندروید (فایلِ نصب) پوش نمی‌رود (`!hasBackground()`): WebView پوش ندارد
  و `AlertJob` کارِ خودش را می‌کند.
- ⛔ **`Alerts.TUNNEL` در کد قفل است** و فقط وقتی زده می‌شود که نشانیِ خانگی جواب
  نداد؛ رمز هست ولی نشانیِ خانگی نه ⇒ کار لغو نمی‌شود.
- ⚠️ `CACHE` به `pump-kar-v9` رفت.
- آزمون: بخشِ «خبر به گوشیِ بسته» در `node tools/check-kar-bot.mjs`، و سنجهٔ زنده
  `npm run test:station-push` در ریپوی `server` (سرورِ واقعی، کلیدهای واقعیِ
  P-256، و **باز کردنِ بستهٔ رمزشده** با کلیدِ خصوصیِ گوشی). `Alerts.java` یک بار
  هم با اندرویدِ ساختگی کامپایل و روی سرورِ واقعی اجرا شد: نسخهٔ پیشین بیرون از
  پمپ **صفر** اعلان داد، نسخهٔ تازه هر دو.

## 🆔 «pump1» رفت — هر حساب، کدِ پمپِ خودش (از ۱۴۰۵/۰۷/۱۳)

خواستهٔ صریحِ صاحب ریپو: «این pump1 را حذف کن و جای آن برای هر حساب کاربری
یک ایدی باشد تا قاطی نشود، و برای هر حساب، حسابِ خودشان بیاید.»

سنجیده شد: سه جا سه کدِ جدا داشتند و هیچ‌کدام با دیگری نمی‌خواند —
پوشهٔ سرورِ خانگی (‎pump1‎ برای همه)، کدی که اپِ کارمندان از سرورِ حساب
می‌گرفت (‎station.code‎)، و ‎s‎ی کیو‌آرِ زنده (باز ‎pump1‎ — یعنی کیو‌آرِ هر
نصبِ بی‌سرورِ خانگی سراغِ پمپِ «pump1»ی کسِ دیگر روی سرورِ حساب می‌رفت).

```
کدِ پمپ = AppSettings.CloudStationCode   (همان station.code ی سرورِ حساب، مثلاً p1b5feb57)
        ⇒ پوشهٔ سرورِ خانگی · کدی که گوشیِ کارمند می‌گیرد · s ی کیو‌آرِ زنده
بی حساب ⇒ "d-" + شناسهٔ همین کامپیوتر
```

- ⛔ **`StationLink.CodeFor` تنها جای تصمیم است** و هیچ پیش‌فرضِ مشترکی ندارد.
  `HomeLink.DefaultStationCode` پاک شد و `AppSettings.StationCode` پیش‌فرضش
  خالی است. تنها جایی که واژهٔ «pump1» در کدِ برنامه مانده
  `StationLink.LegacySharedCode` است — فقط برای **شناختنِ** نصب‌هایی که
  هنوز رویش‌اند. آزمونِ `DarKodeBarname_HichPump1ePishfarzi_Nist` هر جای دیگر
  را قرمز می‌کند.
- ⛔ **کدِ حساب از سرورِ حساب می‌آید و با جدا شدن می‌رود**: هر جا
  `CloudStationId` می‌نشیند `CloudStationCode` هم (فعال‌سازی، `bind`،
  `device/me`، و `/api/pump/me` برای نصبی که از قبل بند بوده)، و
  `ForgetStationAsync` پاکش می‌کند.
- ⛔ **نصبِ کهنه خودش جابه‌جا می‌شود** (`StationLink.NeedsMove` در
  `StationPublisher.ReadyAsync`): پوشه‌ای که مالِ این حساب نیست (‎pump1‎،
  کدِ حسابِ دیگر، کدِ بی‌حسابِ همین کامپیوتر) ⇒ ثبت با کدِ حساب **بی رمزِ
  پوشهٔ قبلی**، و فقط وقتی ثبتِ تازه نشست اتصال عوض می‌شود. نرسیدن به
  سرور اتصالِ فعلی را نمی‌بُرد. پوشهٔ قبلی روی سرورِ خانگی پاک **نمی‌شود**.
  - ⚠️ «مالِ این حساب» یعنی خودِ کد یا `<کد>-…` (جایگزینی که با
    `already_taken` ساخته می‌شود) — آن‌ها دوباره جابه‌جا نمی‌شوند.
  - ⚠️ حسابی که سرورِ حساب واقعاً «pump1» برایش ثبت کرده (نخستین پمپ) همان
    را نگه می‌دارد: آن کدِ یک حساب است، نه پیش‌فرض.
  - ⚠️ بی حساب، کدی که کاربر روزی خودش نوشته دست نمی‌خورد؛ فقط «pump1».
- ⛔ **برنامه پوشهٔ واقعی‌اش را به سرورِ حساب می‌گوید**
  (`PublishHomeAsync(…, station)` ⇒ `stations.home_station`ِ ریپوی `shop`،
  مهاجرتِ ۰۲۷) و گوشی همان را می‌گیرد، نه `code` را کورکورانه. بی این، پمپی
  که روی جایگزین نشسته بود هیچ‌وقت درِ شبکهٔ پمپ را روی گوشی باز نمی‌کرد.
- ⛔ **`s`ی کیو‌آرِ زنده از `HomeLink.CloudCode`** است (کدِ حساب)، نه کدِ پوشه.
- ⛔ **اپِ کارمندان هم هیچ «|| 'pump1'»ی ندارد** و صفحهٔ دستی بی کدِ پمپ وصل
  نمی‌شود؛ `Alerts.java` با کدِ خالی هیچ پرسشی نمی‌زند.
- ⚠️ **گوشی‌ای که با کیو‌آرِ دستیِ قدیم (station=pump1) جفت شده بود**، بعد از
  جابه‌جاییِ برنامهٔ کامپیوتر یک بار باید کدِ پمپ را دوباره بزند. گوشی‌هایی
  که با کدِ پمپ پیوسته‌اند خودشان پوشهٔ تازه را از سرورِ حساب می‌گیرند.
- آزمون: `StationCodePerAccountTests` (۱۴ بند) و بندِ «۳ب» در
  `dotnet run --project PumpYaqobi.UiTests -c Release -- livestack <live.json>`
  (سرورهای واقعی): کدِ حساب ⇒ پوشه، نصبِ کهنهٔ «pump1» ⇒ خودش جابه‌جا و سبز،
  عکسِ زنده در پوشهٔ همان حساب، و گوشیِ کارمند همان پوشه را می‌گیرد.

## 🐢 «داشبورد، مفاد/ضرر و مخزن‌ها سنگین اسکرول می‌شوند» — سه ریشه (از ۱۴۰۵/۰۷/۱۳)

گزارشِ صاحب ریپو: «داشبورد، مفاد و ضرر، مخزن‌ها هم لگ داره هم باگِ اسکرول —
تمام برنامه رو هم چک کن، هیچ بخشی نباید کند بشه.»

⚠️ **اول عددِ CI خوانده شد، نه حدس**: آخرین `scrollperf` روی همان محتوای
`main` (اجرای ۳۳۵) داشبورد را با **دو گام** سنجیده بود (در قابِ ۱۴۴۰×۹۰۰
بیشتر جا نمی‌شود) و **مفاد/ضرر و مخزن اصلاً در سنجه نبودند** — یعنی «سبز»ِ
آن جدول دربارهٔ دو بخش از سه بخشِ نام‌برده هیچ نمی‌گفت. پس اول سنجه کامل شد،
بعد سه ریشهٔ ساختاری که از خودِ کد پیدا شد بسته شدند:

- ⛔ **کارتِ بزرگ سایهٔ محو ندارد** — `Border.card.calm`. شش کارتِ ۹۰۰×۴۴۰ی
  داشبورد و شش کارتِ تمام‌عرضِ مفاد/ضرر `CardShadow`ی محو داشتند؛ قاعدهٔ
  ۱۴۰۵/۰۶/۲۶ («سایهٔ محو فقط برای کارتِ کوچک») فقط روی `section` و `panel`
  پیاده شده بود. `scrollperf why` همان روز گفته بود هر شعاعِ محوی ~۳۰ms برای
  یک کادرِ صفحه‌قد است و بی‌محو صفر؛ هزینه با **مساحت** بالا می‌رود. پنج کارتِ
  آماریِ کوچکِ داشبورد محو می‌مانند. زیرِ ماوس هم `calm` همان می‌ماند.
- ⛔ **فهرستِ خریدهای مخزن پنجره دارد** — `StorageSectionViewModel.PurchaseCards`
  (تازه‌ترین `PurchasePage` = ۱۲ کارت) + دکمهٔ «خریدِ قدیمی‌تر» که هر بار یک
  پنجرهٔ دیگر می‌آورد (همان الگوی `ShowOlder`ِ بک‌اپ و سطل). تا این روز
  `ItemsControl` **همهٔ** خریدها را کارت می‌کرد و هر کارت **هشت** کادرِ `.stat`
  با سایهٔ محو دارد: با یک خرید در هفته، پنج سال = ~۱۳۰ کارت برای هر تیل =
  بیش از هزار کادرِ زنده، همه با هر فریمِ اسکرول. همان چاله‌ای که
  `ExcelGrid.StickyRowLimit` برای جدول‌ها بست، این‌جا برای کارت‌ها باز بود.
  ⚠️ `Purchases` (فهرستِ کامل) دست نخورده: موجودی، پول‌ها، PDF و «فی لیتر» از
  همان حساب می‌شوند و یک ردیف هم کم نمی‌شود. فقط **کارت‌ها** پنجره دارند.
  ⛔ و `ItemsControl Border.stat` سایهٔ **بی‌محو** می‌گیرد: «کارتِ کوچک» یعنی
  چند تا در صفحه، نه هشت تا در هر قلمِ یک فهرست (خریدهای شرکت هم همین را
  می‌گیرد). ردیفِ `UniformGrid.stats`ِ بالای صفحه در فهرست نیست و محو می‌ماند.
- ⛔ **«برای یک جمع، همهٔ ردیف‌ها را نخوان» — این بار مفاد/ضرر و مخزن.**
  مفاد/ضرر با هر فعال‌سازی `ReportsAsync` (همهٔ پارچه‌های پنج سال با هر دو
  شیفت، سه موجودیت برای هر روز)، `ExpenseLedger.ListAsync(null)`،
  `ExtraIncomeLedger.ListAsync(null)` و `Invoices.ListAsync()` را می‌زد تا پنج
  عدد جمع بزند؛ مخزن همان `ReportsAsync` را با هر باز شدن **و هر ویرایشِ هر
  خرید** (`RecalcAsync`). ⚠️ و SQLite کارِ «async»ش را روی همان نخِ صداکننده
  انجام می‌دهد، پس آن خواندن روی **نخِ رابط** بود — همان مکثی که کاربر درست
  سرِ اسکرول حس می‌کرد. حالا:
  - `StorageDataService.ShiftSumsAsync(fuel)` — فقط چهار ستونِ `Sale`/`Profit`ِ
    دو شیفت (`LEFT JOIN`ی خودِ EF)، جمع در حافظه با `decimal`. ⛔ نه `SUM` و نه
    `CAST` به SQLite (قاعدهٔ همیشگی).
  - `InvoiceService.ApprovedRatesAsync()` — پنج ستونِ فاکتورهای تاییدشدهٔ تیلی
    (`InvoiceRate`)؛ قاعدهٔ `InvoiceRateDiff` **یک جا** ماند (`RateDiffOf`) و
    هر دو نسخه از آن می‌گذرند.
  - `ProfitInput` چهار جمعِ اختیاری گرفت (`ShiftProfitPetrol/Diesel` ·
    `ExtraIncomeSum` · `ExpenseSum` · `InvoiceRateDiffSum`)؛ خالی ⇒ همان
    فهرست جمع می‌شود، پس آزمون‌های برابری با سایت دست‌نخورده‌اند.
  - `StorageService.Tank(purchases, soldLiters, …)` — نسخهٔ با `reports` به
    همین می‌رسد؛ قاعده یک جاست.
  ⚠️ داشبورد عمداً دست نخورد: خواندنش از قبل ترمزِ `Version` و سنجهٔ
  `dashperf` (سقف ۴۰۰ms) دارد و `DashboardService.Series` واقعاً ردیفِ
  روزانه می‌خواهد. `StationSnapshot` و `MainViewModel` هم `ReportsAsync` را
  برای چیزی جز جمع می‌خوانند و سرِ جایشان‌اند.

### سنجهٔ `scrollperf` از امروز کلِ نوار را می‌گردد

- مفاد/ضرر و مخزن صریح، بعد **همهٔ** بخش‌های `NavSections` که نام برده
  نشده‌اند (جز چت و حساب که شبکه می‌خواهند و دوربین که جریانِ تصویر دارد).
- دو ستونِ **ساختاری** به جدول اضافه شد: «سایهٔ محو» (کادرِ دیدنی با
  `Blur > 0`) و «کنترل» (کنترلِ دیدنی). ⛔ سایهٔ محوِ بالای `BlurBroken` (۲۰۰)
  سرخ است، بالای `BlurWarn` (۶۰) فقط ⚠️. سایهٔ بی‌محو شمرده نمی‌شود.
- مخزن دروازهٔ ساختاریِ خودش را دارد: کارتِ ساخته‌شده ≤ `PurchasePage` در
  حالی که خودِ فهرست بزرگ‌تر است، و «خریدِ قدیمی‌تر» دقیقاً یک پنجره می‌آورد.
- ⚠️ **محدودیتی که پنهان نمی‌شود**: داشبورد در قابِ ۱۴۴۰×۹۰۰ فقط دو گام
  می‌لغزد، پس عددِ زمانی‌اش کم‌مایه است و **عددِ ساختاری** (۱۱ ⇒ ۵ کادرِ محو)
  ملاک است. و این سندباکس `dotnet` ندارد: کامپایل و سنجه‌ها این‌جا **دویده
  نشدند** — CI روی PR می‌دواندشان. آن‌چه شد: سنجشِ XMLِ هر چهار نما، لِکسرِ
  C# روی هر هشت فایلِ عوض‌شده، و سنجشِ تک‌تکِ ادعاهای سورسیِ آزمون روی خودِ
  فایل‌ها.

آزمون: `ScrollWeightTests` (۱۰ بند — سورس، خالص، و دو بند روی SQLiteِ واقعی:
`ShiftSumsAsync` مو‌به‌مو همان جمعِ `ReportsAsync`، و `ApprovedRatesAsync`
همان اختلافِ نرخِ همهٔ فاکتورها).
## 🔌 «برنامه به سرورِ خانگی نمی‌رسد و خانه‌اش سبز نمی‌شود» — سه ریشهٔ سمتِ برنامه (از ۱۴۰۵/۰۷/۱۳)

ریشهٔ اصلی سمتِ سرور بود (مرکز فرمانِ ویندوز پنل را فقط روی `127.0.0.1` بالا
می‌آورد — `CLAUDE.md`ِ ریپوی `server`، نسخهٔ ۱.۵۰.۱۲)، ولی سه ایرادِ برنامه هم
همان را به «سرخ برای همیشه» تبدیل می‌کرد:

- ⛔ **اولین جوابِ کشف برداشته می‌شد، نه نشانیِ رسیدنی.** `ServerFinder.FindReachableAsync`
  هر نشانی را با `GET /health` می‌سنجد؛ نشانیِ شبکه پیش از `127.0.0.1` (همان است که
  به گوشی‌ها هم می‌رسد). ثبت فقط از این در می‌رود.
- ⛔ **نشانیِ مُرده هیچ‌وقت دوباره گشته نمی‌شد.** `EnsureAsync` با «نشانی و رمز داریم»
  برمی‌گشت؛ حالا `StationPublisher.ReadyAsync` وقتی وصل نشد (با ترمزِ ۳۰ ثانیه‌ایِ
  `_lastRepairTry`) با `force` دوباره ثبت می‌کند — همان پوشه، همان رمز، نشانیِ تازه.
- ⛔ **`127.0.0.1` به گوشیِ کارمند داده می‌شد** وقتی سرور روی همان کامپیوتر بود.
  `HomeLink.ShareUrl` (کیو‌آر و ابر) نشانیِ شبکه را از کارتِ خودِ سرور می‌گیرد
  (`FoundServer.LanUrl` ⇒ `AppSettings.ServerLanUrl`).
- ⛔ **کلیکِ چراغِ نصبِ تازه بن‌بست نیست** — تا امروز «از پروفایل وارد شوید»
  می‌گفت و نمی‌گشت؛ یافتنِ سرورِ خانگی هیچ حسابی نمی‌خواهد.
- آزمون: `HomeReachTests` و
  `dotnet run --project PumpYaqobi.UiTests -c Release -- lanreach <server/homelab-panel/server>`
  (سرورِ خانگیِ واقعی: پنلِ کهنهٔ `127.0.0.1` ⇒ خودش ترمیم و سبز؛ پنلِ تازه ⇒ نشانیِ
  شبکه؛ نگهبانِ پنل ⇒ بیرون از شبکهٔ خانه بسته).

## 🤖 باتِ تلگرام — هشدارِ قرض‌دار و مخزن در تلگرام (از ۱۴۰۵/۰۷/۱۳)

خواستهٔ صریحِ صاحب ریپو: «بات تلگرام رو روی سرور بساز و برای پمپ باشد و دیگه
جای نشتی نکنه و از دکان هم اون رو ول کن… استارت رو که زدن بگه حساب
کاربری‌شونو وارد کنن… وقتی تیلِ یارو تمام می‌شد، قرض‌داری که حسابش تموم شده و
مخزن اگه تیل تموم شد پیام هشدار بره درجا… دکمه برای اندروید و آیفون… و بتونه
بات رو به گروه تلگرامش وصل کنه.»

```
برنامهٔ پمپ ──(از قبل) CloudEvents──▶ POST /api/pump/device/events ──▶ سرورِ حساب
                                                              └──▶ باتِ تلگرام ──▶ خصوصی و گروه
کاربر در تلگرام: Start ⇒ ایمیلِ حساب ⇒ کدِ شش‌رقمی به همان ایمیل ⇒ وصل
```

- ⛔ **خودِ بات روی سرورِ حساب است** (ریپوی `shop`، `server/src/lib/telegram.js`،
  آزمونِ ۲۵بندیِ `pump-telegram.test.js`). قاعده‌هایش در `CLAUDE.md`ِ همان ریپو.
- ⛔ **این برنامه برای بات هیچ چیزِ تازه‌ای نمی‌فرستد**: هشدارها همان
  `StationSnapshot.Alerts` ⇒ `CloudEvents` است که از قبل به سرور می‌رفت. قاعدهٔ
  جدا ننویسید، وگرنه روزی کارت سرخ است و تلگرام ساکت.
- ⛔ **رمزِ بات هرگز داخلِ این برنامه نیست** — نه در کد، نه در تنظیمات. هر کسی
  آن را از فایلِ برنامه بیرون می‌کشید و از طرفِ پمپ پیام می‌داد.
  `TelegramBotLinkTests.DarBarname_HichRamzeBat_Va_HichApiTelegram_Nist` کلِ
  سورس را برای شکلِ رمزِ بات و `api.telegram.org` می‌گردد.
- تنها سهمِ برنامه: کارتِ «🤖 باتِ تلگرام» در «تنظیمات ← 📲 اپِ گوشی» که نشانیِ
  بات را از درِ عمومیِ `GET /api/pump/public/telegram` می‌پرسد
  (`CloudLink.TelegramBotUrlAsync`) و فقط `https://t.me/…` را می‌پذیرد. صفحه
  منتظرِ اینترنت نمی‌ماند.
- ⚠️ **راه‌اندازیِ بات کارِ صاحبِ سامانه است، نه کد**: در تلگرام `@BotFather` ⇒
  `/newbot` ⇒ رمز را در پنلِ مدیرِ سرورِ حساب («ایمیل و پوش» ← «باتِ تلگرامِ
  پمپ») بچسبانید. سرویسِ ایمیلِ سرور باید روشن باشد، چون کدِ وصل شدن به
  ایمیل می‌رود — همان تلهٔ همیشگیِ «رباتِ ایمیلِ تنظیم‌نشده».
- ⚠️ هشدار فقط وقتی به سرور می‌رود که برنامه به سرورِ حساب **بند** باشد
  (`newsOk = CloudActivated` در `StationPublisher`). خبر پشتِ اشتراک نیست.

## 🔴 ماهِ تازه: نقطهٔ سرخ، نه نوار — و سه اصلاحِ دیگر (از ۱۴۰۵/۰۷/۱۳)

خواستهٔ صاحب ریپو، چهار بند در یک پیام با عکس:

### ۱) ⛔ نقطهٔ سرخ روی کشوی ماه/سال، به‌جای «ماهِ فلان شروع شد»

«ماه که عوض می‌شود نمی‌خواهم آن مدل باشد که بگوید این ماه فلان شده… روی
همان کادرِ کشوییِ ماه‌ها یا سال‌ها یک نقطهٔ سرخ بیاید، و رویش که زد همان ماهی
که قبلاً بود را با یک نقطهٔ سرخ نشان بدهد… و وقتی رفت رویشان آن نقطه‌ها بروند
و دیگر دیده نشوند، مگر این‌که ماهِ دیگر عوض شود.»

- ⛔ **نوارِ `MonthHint` و توستِ نیمه‌شب هر دو رفتند و برنمی‌گردند.**
- ⛔ **`MonthDot.MarkOf` تنها جای تصمیم است**: ماهِ نقطه‌دار تازه‌ترین ماهی است
  که داده دارد و **پیش از** ماهِ جاری است. ماهِ جاری خودش هیچ‌وقت نقطه
  نمی‌گیرد، و بخشی که پیش از این ماه داده ندارد هم نه.
- ⛔ **«دیدم» یعنی رفتن به همان ماه**، نه باز کردنِ کشویی — `YearMonthPicker.DotSeen`
  فقط با انتخابِ خودِ کاربر شلیک می‌شود (`AfterUserPick`)، نه با پر شدنِ
  کشویی. و ثبتش (`MonthDotStore.Ack`) ماهِ **جاری** را می‌نویسد، پس تا ماهِ
  بعد هیچ نقطه‌ای برنمی‌گردد — با بستن و باز کردنِ برنامه هم نه.
- ⚠️ **`month-dots.json` کنارِ تنظیمات، نه جدولِ `Settings` و نه `settings.json`**:
  آن جدول اجازهٔ `ManageSettings` می‌خواهد (کارمند استثنا می‌گرفت) و هر
  نوشتنش `Version` را بالا می‌برد؛ و `settings.json` از چند نمونه نوشته
  می‌شود. نشدنش هیچ چیز را نمی‌شکند — بدترین حالت یک نقطهٔ دوباره.
- ⚠️ **هیچ منطقِ ماهی عوض نشد**: کدام ماه دیده شود و کِی خودکار جلو برود
  (`MonthRoll.Decide`، `_monthAuto`ِ ورق‌ها) همان است؛ نقطه فقط **نشان** است.
- آزمون: `MonthDotTests` (۱۶ بند) و `persist newmonth` (سه فرآیند: ماهِ پیش
  نوشته ⇒ ماهِ تازه باز ⇒ نقطه هست ⇒ رفتن ⇒ فرآیندِ سوم: نقطه نیست).

### ۲) «۳۰ روزِ آزمایشی را ندادند» — ریشه سرورِ کهنه بود، نه برنامه

سنجیده شد با **همان راهِ کاربر** روی سرورهای واقعی (سنجهٔ `signuptrial`: صفحهٔ
ورود ⇒ کدِ ایمیل ⇒ نامِ پمپ):

| سرورِ حساب | دورهٔ آزمایشی |
|---|---|
| ۲.۱۰.۰ (امروز) | **۳۰ روز** — پنل «۳۰»، برنامه (پیش از اصلاح) «۲۹» |
| ۲.۷.۰ (کهنه) | **۱۴ روز** — پیش‌فرضِ قدیمیِ خودِ سرور، برنامه «۱۳» |
| ۲.۷.۰ ⇒ ۲.۱۰.۰ روی همان داده | همان پمپ خودش ۳۰ روز گرفت |

- ⛔ **پس راهِ درست به‌روز کردنِ سرورِ حساب است** (مرکز فرمان ۱.۵۰.۱۴ خودش
  می‌گیردش — `CLAUDE.md`ِ ریپوی `server`)، نه تغییری در برنامه.
- ⛔ **ولی روزِ مانده رو به بالا شمرده می‌شود** (`Math.Ceiling`)، مثلِ خودِ
  سرور — دورهٔ سی‌روزه همان لحظهٔ ساختنِ پمپ «۲۹» خوانده می‌شد.
- ⛔ **هیچ جمله‌ای کاربر را دنبالِ «کدِ شش‌رقمی» نمی‌فرستد**: این کامپیوتر با
  ورود به حساب و نامِ پمپ خودش بند می‌شود. ⚠️ خرج کردنِ کدِ اشتراک
  (`RedeemSubCommand`) برداشته نشد، فقط دیگر راهِ اصلی خوانده نمی‌شود.
- آزمون: `TrialDaysTests` (۴ بند).

### ۳) مفاد/ضرر: کادرهای برابر، و کشوی ماه و سال

«این سه تا خیلی ضایع دیده می‌شوند… هم برابر و تراز نیستند… منطق‌هایشان را
دست نزن… و کادرِ کشوییِ ماه و سال ندارد.»

- ⛔ **شبکهٔ ستاره‌ایِ وسط‌چین ممنوع** — `HorizontalAlignment="Center"` ستون‌های
  ستاره‌ای را از روی **محتوا** اندازه می‌گیرد، پس سه کادر سه پهنا داشتند.
  کشیده + `MaxWidth` در آوالونیا یعنی وسطِ جا با پهنای کامل. ⚠️ این در هر
  صفحهٔ دیگری هم همین است — شبکهٔ ستاره‌ای را وسط‌چین نکنید.
- ⛔ **ورودی و نتیجهٔ «خرید عمده» یک شبکه‌اند**، با یک قالبِ ستون؛ دو شبکهٔ
  جدا یعنی لبه‌هایی که هیچ‌جا روی هم نمی‌افتند. هر کادرِ تایپِ صفحه
  `TextBox.plbox` است — یک قد، یک قلم.
- ⛔ **دوره فقط ورودی‌ها را کوتاه می‌کند؛ `ProfitLossService.Compute` دست
  نخورد.** `ProfitPeriod` قاعدهٔ تاریخِ هر منبع را همان `_plBreakdownData`ی
  سایت می‌گیرد: پارچه و ردیفِ بی‌فاکتور با `DateKey`ِ خودشان، درآمد و مصرف
  با `MonthKey`، و اختلافِ نرخِ فاکتور با **روزِ تایید** (بی روزِ تایید ⇒
  تاریخِ خودش). «همه» یعنی همه‌چیز، حتی ردیفِ بی‌تاریخ؛ ماه و سال نه.
- ⛔ **«همهٔ زمان‌ها» پیش‌فرض است و مو‌به‌مو همان راهِ پیشین** (`CardAccountsAsync`
  و `ApprovedRatesAsync`) — عددی که کاربر تا امروز می‌دید عوض نمی‌شود.
- ⛔ **برای یک دوره هم همهٔ ردیف‌ها خوانده نمی‌شوند**: `ShiftSumsAsync(fuel, keys)`
  · `NoInvoiceBardagiAsync(keys)` · `SumAsync(…, MonthFilter)` ·
  `ApprovedRatesDatedAsync` — هر کدام همان یکی-دو ستون.
  ⚠️ `NoInvoiceBardagiAsync` همان `BardagiSql`ِ `RollupsAsync` است (یک جا):
  هر ردیف پیش از جمع گرد می‌شود، پس جمعِ شناور دقیق است و «همه»ی آن با
  کارت‌ها مو‌به‌مو یکی (آزمون می‌سنجد).
- ⚠️ جوابِ کهنه روی دورهٔ تازه نمی‌نشیند (`if (period != _period) return;`)، و
  پر کردنِ دوبارهٔ کشویی خودش محاسبه راه نمی‌اندازد (`_loadingPicker`).

### ۴) مخزن: عنوان و عدد وسط، و «جمله ورودی/فروش» با کادر

- ⛔ عنوان، عددِ درشت، «لیتر باقی‌مانده» و نشانِ حال وسط‌اند.
- ⛔ **دو جملهٔ حساب‌شده کادرِ فقط‌خواندنی دارند** (`Border.calc.tankbox`)،
  هم‌قد و هم‌پهنای دو کادرِ تایپِ کنارشان (`TextBox.tankbox`). ⚠️ `TextBox`
  نیستند، پس هیچ تایپی به `TotalIn`/`TotalOut` نمی‌رسد.

### سنجه‌ها

```
dotnet test PumpYaqobi.Tests -c Release       ProfitPeriodTests · MonthDotTests · TrialDaysTests
dotnet run --project PumpYaqobi.UiTests -c Release -- plstore [پوشه]    (در scroll-audit.yml)
```

`plstore` سه ماه داده در دو سال می‌سازد و عددِ هر دوره را **از روی دادهٔ خودِ
سنجه** حساب می‌کند، نه از خودِ برنامه؛ بعد پهنا، قد، لبه و وسط بودنِ هر کادر
را با پنجرهٔ واقعی می‌سنجد. ⚠️ **در راست‌به‌چپ نقطهٔ (۰،۰)ِ محلی لبهٔ راستِ
چشم است** — «مبدأ + اندازه» جای غلط می‌دهد؛ دو گوشه را جدا ببرید
(`ProfitStorageProbe.Screen`). بارِ اول همین سنجه را سرخِ دروغ کرد.


## 🛢️ نقشهٔ مخزن — مخزنِ افقی به‌جای نوارِ عمودی (از ۱۴۰۵/۰۷/۱۳)

خواستهٔ صاحب ریپو با سه عکس: «اون نشانه‌گرِ مخزن رو بردار و این مدلِ جدید رو
براش بذار… دوتا کادرش برن زیرِ اون دو کادرِ دیگه و اون دوتایی که توی کادرِ
آبی کشیدم هم دقیق برن اون‌جا… منطقی خراب نشه، فقط ظاهر رو بهتر می‌کنیم.»

- ⛔ **`Controls/TankGauge.cs` تنها نقشهٔ مخزن است** — مخزنِ افقی با تیلِ
  نارنجی روی آبِ آبی، پروب، شناورِ تیل و آب، پمپِ غوطه‌ور، خطِ نقطه‌چینِ
  آستانه و درصدِ پرشدگی. `PercentToHeightConverter` از این صفحه رفت (داشبورد
  هنوز دارد).
- ⛔ **فقط عددِ واقعی کشیده می‌شود**: سطحِ تیل (`FillPercent`) و خطِ آستانه
  (`ThresholdPercent`). پروب، پمپ و میلهٔ شناور شکلِ دستگاه‌اند.
- ⛔ **آب تا دستگاهِ فیزیکی اندازه نداده دیده نمی‌شود** (همان روز، صاحب ریپو:
  «من نمی‌دونم اصلاً توی مخزن آبی هست یا نه»). `WaterPercent` از نوعِ
  `double?` است و پیش‌فرضش `null` = «اندازه‌ای نداریم»، نه «صفر آب»: نوارِ
  آبی، گویِ شناورِ آب، برچسبش و خطِ راهنمایش همه پشتِ `HasWater` هستند و تیل
  از کف شمرده می‌شود. ⛔ در صفحه به آن **هیچ عددی** نبندید — نه ثابت، نه
  حدس؛ فقط روزی که دستگاه عدد بدهد. (`BiAndazeyeDastgah_HichAbi_Nist`)
- ⛔ **حسابِ سطح‌ها خالص است** (`TankGauge.LevelsOf`): محورِ ظرفیت از سطحِ آب
  تا سقف، بالای صد و زیرِ صفر بریده می‌شوند، `NaN` صفر است، و آستانه روی همان
  محور می‌نشیند. سنجه‌اش `TankGaugeTests`.
- ⛔ **`FlowDirection.LeftToRight`** مثلِ `SparkChart`/`DonutGauge` — وگرنه در
  پنجرهٔ راست‌به‌چپ کلِ نقشه آینه می‌شد.
- ⚠️ نوشتهٔ وسط روی تیل سفید با سایه، روی بدنهٔ خالی رنگِ تم؛ بدنه، لبه و
  نوشته‌ها از `Pump.*`ِ تم، تیل و آب و پمپ رنگِ خودِ نقشه.
- ⛔ **شبکهٔ ۲×۲**: ردیفِ اول ظرفیت و آستانه (کادرِ تایپ)، ردیفِ دوم جملهٔ
  ورودی و فروش (کادرِ خواندنی) — همان کادرها، همان بایندها.
- ⛔ **دکمه‌ها (میله‌زنی · PDF · ثبتِ خرید) در سربرگِ «خلاصه پول‌ها»** نشستند و
  عنوان وسطِ **کلِ** سربرگ می‌ماند (یک `Border`ِ هم‌پهنای دکمه‌ها در ستونِ
  دیگر). ⛔ در کارتِ مخزن دیگر هیچ دکمه‌ای نیست.
- ⛔ **منطق دست نخورد**: در ویومدل فقط `ThresholdPercent` آمد (از همان
  `threshold / cap` که هشدار از آن می‌آید) و `FillText` یک رقمِ اعشار گرفت.
- آزمون: `TankGaugeTests` (۷ بند) و بخشِ مخزن در
  `dotnet run --project PumpYaqobi.UiTests -c Release -- plstore <پوشه>`
  (جای نقشه، کادرها و دکمه‌ها با پنجرهٔ واقعی، و عکسِ ۱۰۰٪ و ۵۹٫۸٪ در هر دو تم).

## 💬 چت‌روم تمام‌صفحه · گروهِ کارکنان · پیام‌ها روی همین کامپیوتر (از ۱۴۰۵/۰۷/۱۳)

خواستهٔ صاحب ریپو با عکسِ مرجع (سه ستون): «چت‌روم تمام‌صفحه… یک طرف من با
کاربرهایی که حساب دارند، یک طرف همان یارو که پیام داده و پایینش اطلاعاتِ
یارو و حساب‌هایش… گروپ چت برای کارمندان و مدیر و میرزا، نه کاربران… عکس و
فیلم و صدا… یک بخشِ کوچک با ساپورت… سرور فضایی نگیرد، این‌ها توی برنامه
باشند… بعد ۱۵ روز بروند سطلِ آشغال، ۱۵ روز هم آن‌جا بمانند، بعد حذفِ کامل.»

```
ChatSectionView  Grid 310,*,320   فهرست · گفت‌وگو · کارتِ طرف (دفترها، حساب‌ها، رسانه)
ChatStore        chat.db + chat-media/ کنارِ pump.db   ۱۵ روز فعال ⇒ ۱۵ روز سطل ⇒ حذف
StationChat      /api/stations/<کد>/chat   گروهِ کارکنانِ همین پمپ (خانگی، بعد تونل)
kar/ «💬 گروه»   همان مسیر با رمزِ خواندن — مدیر، میرزا، کارمند؛ مشتری هرگز
```

- ⛔ **صفحهٔ چت بیرونِ `SectionPage` است و پوستهٔ پنجره را پنهان می‌کند**
  (`IsPageOpen = true`) — همان راهِ صفحهٔ ورود. بلندی از `TopLevel.ClientSize`
  در کدِ پشت می‌آید، **بی هیچ شنوندهٔ چیدمان**. «‹ برگشت» پوسته را برمی‌گرداند.
- ⛔ **`ChatStore.Decide` تنها جای تصمیمِ ماندگاری است و خالص است**: پیام
  ۱۵ روز پس از آخرین حرکت به سطل می‌رود (`DaysLeft` روی هر حباب)، ۱۵ روز
  آن‌جا می‌ماند و بعد با رسانه‌اش کاملاً پاک می‌شود. «↩ برگرداندن» ۱۵ روزِ
  تازه می‌دهد، و `Upsert`ِ دوبارهٔ همان پیام از سرور **سطل را خالی نمی‌کند**.
- ⛔ **پیام‌ها روی همین کامپیوترند، نه روی سرور**: سرورِ خانگی گروه را ۱۵ روز و
  حداکثر ۲۰۰۰ پیام نگه می‌دارد (ریپوی `server`، ۱.۵۰.۲۰) و سرورِ حساب پیام و
  رسانهٔ پشتیبانی و مشتریِ پمپ را ۱۵ روز (ریپوی `shop`، ۲.۱۰.۵، `relayDays`).
  نسخهٔ ماندگار همان `chat.db` است.
- ⛔ **گروه به پوشهٔ همین پمپ بسته است**، نه به موضوعِ سراسریِ `staff`ِ
  پیام‌رسانِ قدیم — آن مالِ همهٔ پمپ‌های یک سرور بود. مشتری هیچ راهی به آن
  ندارد: رمزِ برنامه یا رمزِ خواندن لازم است و کیو‌آرِ مشتری هیچ‌کدام را ندارد.
  هر مشتری فقط گفت‌وگوی خودش را می‌بیند (کلیدِ `k`ِ کیو‌آر).
- ⚠️ **قراردادِ سرورِ خانگی**: `seq` هیچ‌وقت عقب نمی‌رود؛ `last < since` یعنی
  پوشه از نو ساخته شده ⇒ از صفر بپرس. `cid` تلاشِ دوباره را یکی نگه می‌دارد.
  رمزِ غلط و پمپِ ناشناس هر دو ۴۰۴ می‌گیرند (عمداً)، پس یک جملهٔ آدمیزاد.
- ⚠️ **«بخشِ کوچکِ ساپورت»** همان گفت‌وگوی «پشتیبانیِ برنامه» (میرزا ↔ مدیرِ
  سامانه، `CloudLink.Support*`) است — جای خودش در همان فهرست، نه صفحهٔ دوم.
- ⛔ **`AppHost.FindSection` فقط پیدا می‌کند**؛ رفتن همچنان از `GoSection`
  (قفلِ پلن و رمزِ بخش). «📒 باز کردنِ حساب» از همین راه حسابِ قرض‌دار را باز
  می‌کند.
- ⛔ **چت و پروفایل «آخرین بخش» نمی‌شوند** (`GoAsync`) — وگرنه «‹ برگشت» به
  خودِ چت برمی‌گشت و برنامه در چت بالا می‌آمد.
- ⛔ **رنگِ مقدارها با کلاس و `DynamicResource`** (`TextBlock.chatval`)، نه
  `ResourceKeyToBrushConverter` — آن یک بار رنگ می‌گیرد و با عوض شدنِ تم در
  تمِ تیره نامرئی می‌شد. پس‌زمینهٔ صفحه `Pump.AppBg` است؛ ⚠️ `Pump.Canvas`
  کلیدِ تم **نیست** و `ThemeKeyTests` همان روز گرفتش.
- `kar/`: `chatUrl` · `mergeChat` (یکتایی با `seq`، جانشینیِ پیامِ در راه با
  `cid`، سقفِ ۳۰۰ و ۱۵ روز روی خودِ گوشی)؛ پرسشِ شش‌ثانیه‌ای **فقط وقتی صفحهٔ
  گروه جلوی چشم است**. رفتن به پمپِ دیگر گروهِ قبلی را پاک می‌کند. `CACHE` ⇒ `pump-kar-v11`.
- آزمون: `ChatRoomTests` (۱۴ بند) · `dotnet run --project PumpYaqobi.UiTests -c Release -- chatroom <پوشه>`
  (در `scroll-audit.yml`) · بخشِ «گروهِ کارکنان» در `node tools/check-kar-bot.mjs` ·
  و سنجهٔ زندهٔ دستیِ `groupchat <نشانی> <کد> <رمز>` که با سرورِ خانگیِ **واقعی**
  سبز شد (پیامِ گوشی با نقشِ کارمند در برنامه دیده شد).

## 🟡 «حساب ساختم، نه آزمایشی آمد نه روی سرور است — و چراغ می‌گوید وصل است» (از ۱۴۰۵/۰۷/۱۳)

گزارشِ صاحب ریپو با سه عکس: حسابِ تازه، «فعال نشده»، و چراغِ سربرگ «هر دو سرور
وصل‌اند». «اصلاً راست است یا دروغی است؟ … تا عملی ندیدی نگو درست شده.»

با **پشتهٔ واقعی** (پنلِ خانگی + سرورِ حسابِ واقعی + SMTPِ ساختگی) و خودِ برنامه
بازسازی شد (`signuptrial`؛ `PUMP_SIGNUP_SKIP=1`):

- ✅ ثبت‌نامِ کامل (تا گامِ پمپ) **آزمایشیِ ۳۰ روزه می‌دهد** — دو بار پشتِ سرِ
  هم روی یک نصب هم (`PUMP_SIGNUP_TWICE=1`).
- ⛔ **ولی حسابی که گامِ پمپ را «بعداً» زده بود هیچ پمپی نداشت** — و آزمایشی
  داخلِ مجوزی است که فقط با بند شدنِ دستگاه به پمپ صادر می‌شود. پس هرگز
  نمی‌آمد، و پروفایل هیچ راهی برای ساختنِ پمپ نشان نمی‌داد.
- ⛔ **و چراغ نیمه‌حقیقت می‌گفت**: فقط «سرورِ حساب جواب داد؟» را می‌سنجید.

قاعده‌ها:

- ⛔ **چراغِ سرورِ حساب برای دستگاهِ بندنشده زرد است** (`TickCloudDot`:
  `Online when !DeviceBound()`)، با دلیل در `ToolTip`. چراغِ یگانه هم زرد را
  می‌شمارد. «جواب داد» با «ثبت شد» یکی نیست.
  ⚠️ `DeviceBound()` ده ثانیه کَش می‌شود (تیکِ هر ثانیه نباید هر بار دیسک
  بخواند) و با `LicenseChanged` و «ساختنِ پمپ» همان لحظه باطل می‌شود.
- ⛔ **پروفایل کارتِ «⛽ این حساب هنوز پمپی ندارد» دارد** (`NeedsPump`) با
  یک کادرِ نامِ پمپ و دکمهٔ «ساختنِ پمپ» که **همان `FinishPumpAsync`ِ صفحهٔ
  ورود** را می‌زند — راهِ دوم ساخته نشد. پس از ساختن آزمایشیِ ۳۰ روزه همان
  لحظه می‌آید و کارت می‌رود (سنجیده شد، با عکس).
- ⛔ **ردیفِ «سرورِ خانگی»ِ پروفایل از ناشرِ زنده می‌خواند**
  (`PublisherIfStarted.Connected`)، نه از «نشانی در تنظیمات هست».
- ⚠️ **حسابی که امروز در این حال است** (مثلِ همان حسابِ عکس): پس از
  به‌روزرسانی، پروفایل ⇐ «ساختنِ پمپ» — کار دیگری لازم نیست.

### 🧹 نصبِ دوباره «از نو و خالی» — اختیاری، و هرگز پاک کردن

پرسش: «کسی که از سر نصب کند رمز یا اطلاعات داخلش هست؟» — فایلِ نصب هیچ داده‌ای
ندارد (سنجیده شد)، ولی دفتر در `%AppData%\PumpYaqobi` است و حذفِ برنامه عمداً
دستش نمی‌زند؛ پس نصبِ دوباره روی **همان** کامپیوتر همان دفتر و رمز را می‌بیند.

- حالا حذفِ برنامه (فقط حذفِ **دستی**، نه بی‌صدا) یک بار می‌پرسد «از نو و خالی؟»
  با پیش‌فرضِ **«نه»**. «بله» پوشه را با نامِ تاریخ‌دار **کنار می‌گذارد**
  (`RenameFile`) — ⛔ هیچ `DelTree`/`DeleteFile`ی در نصاب نیست و
  `Uninstalling_never_touches_the_users_data` قدغنشان کرده.
- ⚠️ به‌روزرسانی از راهِ حذفِ بی‌صدا نمی‌گذرد و این پرسش را هرگز نمی‌بیند.

آزمون: `SignedInNoPumpTests` (۴ بند) + دو آزمونِ تیزترشده (`InstallerTests`،
`PumpStepLoopTests`)؛ رفتار با `signuptrial` روی پشتهٔ واقعی.

## 🔌 «ثبت نشده» — هر حال جملهٔ خودش، و کلیکِ چراغ واقعاً ثبت می‌کند (از ۱۴۰۵/۰۷/۱۳)

گزارشِ صاحب ریپو با عکسِ چراغِ زرد («سرورِ حساب جواب می‌دهد، ولی این کامپیوتر
هنوز به هیچ پمپی ثبت نشده — در پروفایل پمپ را بسازید»): «ببین الان این وصل
نمی‌شه — با تستِ سالم، نه با حدس.»

سنجهٔ تازهٔ `linkstates` روی **پشتهٔ واقعی** (پنل + سرورِ حسابِ ۲.۱۰.۴ + صندوقِ
ایمیل) پنج حال را می‌سازد و از هر کدام عکس می‌گیرد. پیش از اصلاح **پنج سرخ**:

| حال | پیش از اصلاح |
|---|---|
| نصبِ تازه، بی حساب | چراغ می‌گفت «پمپ بسازید» — بی حساب پمپی ساختنی نیست |
| حساب از ریشه در پنل حذف شد | توکنِ دستگاهِ **پمپِ حذف‌شده** برمی‌گشت ⇒ چراغ **سبز** و پروفایل «فعال» (دروغ) |
| حساب پمپ دارد، این کامپیوتر ثبت نیست | هیچ کارتی در پروفایل، و «پمپ بسازید» |
| کلیکِ چراغ | فقط `/api/health` — هیچ‌وقت ثبت نمی‌کرد |

- ⛔ **`MainViewModel.UnboundWhy` تنها جای جملهٔ «ثبت نیست»** است: بی حساب ⇒
  «وارد حساب نشده‌اید»؛ حسابِ بی‌پمپ ⇒ «نامِ پمپ را بنویسید»؛ حسابِ پمپ‌دار ⇒
  دلیلِ واقعیِ نشدن (`LastBindWhy`) یا «خودش هر دقیقه امتحان می‌کند».
- ⛔ **کلیکِ چراغ همان دورِ کاملِ پس‌زمینه را همین حالا می‌دواند**
  (`StationPublisher.CloudKeepNowAsync` — نشست، ثبتِ دستگاه، مجوز)، و اگر کار
  دستِ کاربر است (ورود یا نامِ پمپ) پروفایل را باز می‌کند. ⛔ **هیچ پمپی
  بی‌خبر ساخته نمی‌شود** — نه این‌جا، نه در حلقه («هر حساب یک پمپ»).
- ⛔ **«پمپ دارد؟» از جوابِ خودِ سرور است** (`CloudLink.AccountHasStation`)، نه
  از مُهرِ `PumpStepDone`؛ مُهر فقط وقتی به کار می‌رود که هنوز نپرسیده‌ایم.
  پروفایل دو کارت دارد: «⛽ ساختنِ پمپ» (`NeedsPump`) و «🔌 ثبتِ همین کامپیوتر»
  (`NeedsBind`، همان دورِ پس‌زمینه).
- ⛔ **نشستِ مرده ⇒ خودِ دستگاه همین حالا پرسیده می‌شود** (`RefreshAsync` در
  `CloudKeepAsync`). فقط جوابِ صریحِ سرور (`device_not_registered`) توکن را
  برمی‌دارد؛ نشستِ نودروزه‌ای که طبیعی مُرد دستگاهِ سالم را پاک نمی‌کند.
- ⛔ **بندهای حساب و پمپ سه‌طرفه نوشته می‌شوند** (`AppSettings.BondFields`):
  خانه‌ای که این نمونه عوض نکرده ولی دیسک عوض شده، از دیسک برداشته می‌شود.
  ریشهٔ «چراغِ سبزِ دروغ» همین بود: `CloudLink`ِ ماندگارِ پروفایل (از زمانِ
  ثبت‌نام زنده) کلِ شیءِ کهنه‌اش را می‌نوشت و توکنِ پاک‌شده را برمی‌گرداند.
  نمونهٔ `new` (بی پایه) مثلِ همیشه کلِ خودش را می‌نویسد.
- ⚠️ چراغ با ورود و خروج **همان لحظه** عوض می‌شود (نه ده ثانیه بعد).
- ⛔ **پس از ورود، گامِ بعد از حقیقتِ سرور است** (`NextStepAfterSignInAsync`):
  هر چهار راهِ ورود (رمز، ثبت‌نام، کدِ ایمیلی، بازیابیِ رمز) از همان می‌گذرند.
  حساب پمپ دارد ⇒ همین کامپیوتر همان لحظه ثبت می‌شود و صفحه تمام؛ ندارد ⇒
  «نامِ پمپ». تا امروز نصبِ دوباره یا بازیابیِ رمز دوباره «نامِ پمپ» می‌خواست.
- ⛔ **پس از شکستِ ثبت، حلقه هر دقیقه دوباره نمی‌زند** (`BindRetryAfterFail`،
  ده دقیقه): `device/bind` سقفِ ده در ربع ساعت برای هر آی‌پی دارد و کامپیوترِ
  جداشده در ده دقیقه آن را پر می‌کرد — پس از آن حتی کلیکِ کاربر «تلاشِ زیاد»
  می‌دید. کلیکِ کاربر (`forceBind`) همیشه همین حالا.
- ⚠️ کامپیوترِ جداشده (`device_revoked`) را از پنل برمی‌گردانند: «مشتری‌ها ←
  پرونده ← کامپیوترهای پمپ ← برگرداندن» (پنل ۱.۵۰.۲۱)، بعد «ثبتِ همین
  کامپیوتر» در پروفایل.

سمتِ سرور، از همین اسکن: `shop` ۲.۱۰.۶ (زدنِ کد سطلِ نرخِ خودش را دارد —
بازیابیِ رمز پس از ثبت‌نام «تلاشِ زیاد» می‌گرفت) و `server` ۱.۵۰.۲۱ (کامپیوترهای
پمپ در پنل، و پرونده برای حسابِ دورهٔ آزمایشی). سنجه‌های `linkstates` حالا نُه
حال‌اند (الف تا ط) و همه سبز.

```
dotnet run --project PumpYaqobi.UiTests -c Release -- linkstates <live.json> [پوشهٔ عکس]
```
(پشته: `node test/signup-stack.mjs <live.json>` در ریپوی `server`؛ در CI نیست،
چون پشتهٔ بیرونی می‌خواهد.) آزمون: `SettingsBondMergeTests` (۳ بندِ رفتاری) و
سه بندِ تازه در `SignedInNoPumpTests`.

⚠️ **ترمزِ ۶۰ ثانیه‌ایِ «کدِ دوباره به همان ایمیل» عمدی است** — سنجه‌ای که
پشتِ سرِ هم با یک ایمیل ثبت‌نام می‌کند باید صبر کند. و **سقفِ نرخِ سرورِ حساب
(۴۲۹) با اجراهای پیاپیِ سنجه پر می‌شود**؛ آن‌وقت پشته را از نو بالا بیاورید،
نه این‌که سنجه را ضعیف کنید.

## 🔔 یک فهرستِ هشدار — زنگ، توستِ میرزا، سرور و بات (از ۱۴۰۵/۰۷/۱۴)

گزارشِ صاحب ریپو با سه عکس (بات «هنوز هیچ هشداری نیامده» و زنگِ داشبورد «۲»):
«دو سه تا هشدار استن اما سرور و برنامه بی‌توجهی می‌کنن… برای قرض‌داری که
حسابش تموم بشه یا قرض‌دار بشه، چرا پیامِ هشدار به میرزا نمیاد؟… بزار برنامه
به سرور و بات بگه و سرور به بات دستور بده و بات لایو توی گروه بره.»

⛔ **ریشه: دو قاعده برای یک چیز.** زنگِ داشبورد مخزن را با «موجودیِ خام زیرِ
حد» می‌شمرد (حتی مخزنی که هیچ خریدی ندارد — همان «۲»ِ عکس روی نصبِ تازه) و
قرض‌داران را **اصلاً**؛ آن‌چه به سرور و بات می‌رفت `StationSnapshot.Alerts` بود،
با قاعدهٔ خودِ بخشِ مخزن (`TankState.IsLow/IsNear`) و با قرض‌داران. پس زنگ یک
چیز می‌گفت و بات چیزِ دیگر، و قرض‌دارِ تمام‌شده هیچ‌جا به میرزا گفته نمی‌شد.

```
AlertWatch (هر ۵ث در حلقهٔ ناشر، ترمزِ Version + کمینهٔ ۵ث)
  ├─▶ زنگِ داشبورد (ApplyAlerts) + توستِ میرزا (OnAlertsChanged)
  └─▶ POST /api/pump/device/state {همهٔ هشدارهای باز, مخزن, خلاصهٔ قرض‌داران?}
         سرورِ حساب مقایسه می‌کند ⇒ «🚨» تازه · «✅ برطرف شد» · بی‌تغییر = هیچ
```

- ⛔ **`Services/AlertWatch.cs` تنها سازندهٔ فهرست است** (از همان
  `StationSnapshot.Alerts`). زنگ، توست و سرور همه از `AppHost.LiveAlerts`
  می‌خوانند. قاعدهٔ جدا ننویسید — `AlertWatchTests` قفلش کرده.
- ⛔ **کلِ فهرست می‌رود، نه فقط تازه‌ها.** `CloudEvents` «تازه» را از حافظه
  حساب می‌کرد و با هر بسته و باز شدنِ برنامه همان هشدارها دوباره می‌رفت — همان
  «حرف‌های تکراری»ِ بات. حالا سرور می‌سنجد. `CloudEvents` فقط برای سرورِ کهنه‌ای
  است که `‎/state‎` را ندارد (`not_found` ⇒ ساعتی یک بار دوباره می‌پرسد).
- ⛔ **هشدار پشتِ هیچ اشتراکی نیست**؛ خلاصهٔ قرض‌داران (برای جست‌وجوی بات) پشتِ
  `Entitlements.Kar` است و دست‌بالا هر ۹۰ ثانیه.
- ⚡ **قانونِ سرعت**: داده عوض نشده ⇒ صفر دستورِ دیتابیس. مخزن فقط ستونِ ‎Sale‎
  را می‌خواند (`ShiftSumsAsync`)، نه همهٔ پارچه‌ها — و `StationSnapshot.TankAsync`
  هم همین شد. قرض‌داران با همان یک ‎GROUP BY‎ی کارت‌ها (`DebtorsLiteAsync`).
- ⚠️ **مخزنِ بی‌خرید دیگر «تمام شده» نیست** (قاعدهٔ `checkLowStock`ِ سایت و خودِ
  بخشِ مخزن). زنگِ نصبِ تازه صفر است تا نخستین خرید ثبت شود.
- ⚠️ عوض شدنِ دفتر (حسابِ دیگر) فهرست را پاک می‌کند (`OnLedgerSwitchedAsync`).
- سمتِ بات (ریپوی `shop`): `docs/TELEGRAM-BOT-fa.md` — شانزده سناریو، هر کدام با آزمون.

⚠️ و باز هم: این سندباکس `dotnet` ندارد (دانلودش هم بسته است)، پس کامپایل و
آزمون‌های C# این‌جا **دویده نشدند** — CI روی PR می‌دواندشان.

## 🟢 چراغ با جوابِ سرور سبز است · «حساب ساخت ⇒ تمام» (از ۱۴۰۵/۰۷/۱۳، نسخه 3.1.179)

دو جملهٔ صاحب ریپو با دو عکس: «وقتی که حساب هم نداشته باشم اون سرور باید بگه که
وصل است و اگه وصل بود باید سبز بشه» و «این ثبتِ همین کامپیوتر و ساختِ حساب
چیه… همین که یارو حسابِ کاربری برای خودش درست کرد تموم حساب درست شده و این قد
پیچیده نکن».

- ⛔ **بندِ «چراغِ سرورِ حساب برای دستگاهِ بندنشده زرد است» (۱۴۰۵/۰۷/۱۳) پس گرفته
  شد.** رنگِ چراغ حالِ **سرور** است: جواب داد ⇒ سبز، با حساب یا بی حساب. «این
  کامپیوتر ثبت نیست» فقط یک خطِ آگاهی زیرِ جملهٔ `ToolTip` است (`UnboundWhy`)
  و جایش پروفایل. و چراغِ یگانه با «سرورِ حساب سبز + سرورِ خانگی **خراب نیست**»
  سبز است؛ خانگیِ پیداشده‌ای که جواب نمی‌دهد (`Pump.Danger`) همچنان زرد می‌کند.
- ⛔ **گامِ «نامِ پمپ» و دو کارتِ «ساختنِ پمپ» / «ثبتِ همین کامپیوتر» رفتند** و
  برنمی‌گردند. `AccountSectionViewModel.EnsureReadyAsync` تنها جای تصمیم است:
  حسابِ بی‌پمپ ⇒ پمپ با نامِ خودش («پمپِ » + نامِ حساب، یا آن‌چه کاربر نوشته)
  از همان `FinishPumpAsync`؛ حسابِ پمپ‌دار ⇒ همان `CloudKeepNowAsync`.
  - ⚠️ فقط از **کارِ خودِ کاربر** صدا زده می‌شود: هر چهار راهِ ورود
    (`NextStepAfterSignInAsync`) و باز کردنِ پروفایل (بی انتظار، `_ = EnsureReadySafeAsync()`).
    ⛔ حلقهٔ پس‌زمینه همچنان هیچ پمپی نمی‌سازد (`StationPublisher` واژهٔ
    `EnsureStationAsync` را ندارد — آزمون قفلش کرده).
  - ⚠️ ترمزِ دودقیقه‌ای فقط برای «نشد» است (سقفِ نرخِ سرور)؛ وصل شد ⇒ برداشته.
  - ⚠️ واردشده همیشه «تمام» است: `LoginStep = SignedIn || f.LoginSkipped ? 4 : 1`.
  - ⚠️ پیامِ ورود (مثلاً «بندهای پمپِ حسابِ دیگر باز شد») زیرِ پیامِ ساختنِ
    خودکار گم نمی‌شود (`cloudlogin` گرفتش).
  - پروفایل جای کارت‌ها فقط **یک خطِ حال** دارد (`LinkingNow` / `LinkingLine`)
    و آن هم فقط وقتی هنوز وصل نیست — بی دکمه.
- ⚠️ **آن ۵۰۰ِ «ثبتِ همین کامپیوتر»** (کدِ پیگیری در عکس) با هیچ سنجه‌ای
  بازسازی نشد: به‌روزرسانی از ۲.۷/۲.۹/۲.۱۰ روی PGliteِ روی دیسک، هر حالِ اشتراک
  از پنل، جداشده/برگشته، و بند شدنِ هم‌زمان. سرورِ حساب ۲.۱۰.۸ حالا گامِ شکسته را
  کنارِ کدِ پیگیری در لاگ می‌نویسد (`@bind.register`…) و شکستِ مجوز دیگر توکنِ
  دستگاه را دور نمی‌ریزد. اگر باز دیده شد: «سرورِ حساب ← لاگ» در پنل، همان کد.
- ✉️ و نامهٔ کد: قالبِ صاحبِ سامانه صفحهٔ وب بود و جیمیل بی‌شکلش می‌کرد؛ از
  سرورِ حساب ۲.۱۰.۸ / پنل ۱.۵۰.۲۳ نسخهٔ جدولیِ همان طرح با تصویرهای درون‌خطی
  می‌رود (شرحش در `CLAUDE.md`ِ `shop`).
- سنجه‌ها: `signuptrial` (هر دو حالت)، `linkstates` (نُه حال)، `cloudlogin`،
  `verify`، `syncui` — همه سبز روی پشتهٔ واقعی؛ `SignedInNoPumpTests` قاعده‌ها را
  روی سورس قفل کرده.

## 🕰️ حسابی که با نسخهٔ پیشین ساخته شده هم آماده می‌شود — بی باز کردنِ پروفایل (از ۱۴۰۵/۰۷/۱۳، نسخه 3.1.181)

گزارشِ صاحب ریپو پس از ۳.۱.۱۷۹: «حسابی که قبلاً آزمایشی نداشت، باز هم داده
نشد؛ و اشتراکی که از سرور دادم به حسابِ همان کاربر نرسید، ولی سرور می‌گوید
فعال شد.»

سنجهٔ تازهٔ `oldacct` همان **تاریخچه‌ها** را روی پشتهٔ واقعی می‌سازد (حساب و
پمپ با خودِ API سرور، و برنامه با همان چیزی که نسخهٔ پیشین روی دیسک گذاشته)،
روی سرورِ حسابِ ۲.۱۱.۰ **و** ۲.۱۰.۴:

| حال | پیش از اصلاح |
|---|---|
| الف) حسابِ واردشده، بی پمپ (نسخهٔ پیشین گامِ پمپ را «بعداً» زده بود) | ❌ تا باز نشدنِ «پروفایل» هیچ پمپی، پس هیچ آزمایشی |
| ب) حسابِ پمپ‌دار، این کامپیوتر وصل نیست | ✅ حلقه خودش وصل کرد، آزمایشی آمد |
| ج) وی‌آی‌پی از پنل، پیش از وصل شدنِ برنامه | ✅ رسید |
| د) وی‌آی‌پی از پنل، برنامه وصل و باز | ✅ در یک دور رسید |

⛔ پس ریشهٔ هر دو شکایت یکی بود: **این کامپیوتر به پمپی وصل نبود**، و آزمایشی
و اشتراک هر دو داخلِ مجوزی‌اند که فقط برای کامپیوترِ وصل‌شده صادر می‌شود.

- ⛔ **یک بار در هر اجرا، پس از همان مکثِ آرامِ ناشر (`StationPublisher.FirstDelay`)،
  همان گامِ «حساب آماده»ِ پروفایل می‌دود** (`Account.EnsureReadyOnOpenAsync` ⇒
  `EnsureReadyAsync`). راهِ دومی ساخته نشد.
  - ⚠️ زیرِ نگهبانِ `_afterSignIn`: عوض شدنِ دفتر آن را دو بار نمی‌کند.
  - ⚠️ نصبِ وصل‌شده هیچ درخواستی نمی‌زند (خطِ اولِ `EnsureReadyAsync`).
  - ⛔ **حلقهٔ پس‌زمینه همچنان هیچ پمپی نمی‌سازد** — این باز شدنِ برنامه به
    دستِ خودِ کاربر است، و «هر حساب یک پمپ» سرِ جایش است (پیش از ساختن از
    سرور می‌پرسد).
- ⚠️ **آزمایشیِ مصرف‌شده برنمی‌گردد و این باگ نیست**: سرورِ حساب هر اشتراکی را
  که روزی نشسته (حتی لغوشده) پایانِ دورهٔ آزمایشی می‌شمارد (قاعدهٔ ۲.۹.۲ی
  `shop`، به خواستهٔ خودِ صاحب ریپو تا «لغو» در ماهِ اول واقعاً ببندد).
- آزمون: `SignedInNoPumpTests.HesabeGhadimi_BiBazKardaneProfile_Amade_Mishavad`
  و `dotnet run --project PumpYaqobi.UiTests -c Release -- oldacct <live.json> [پوشه]`
  (پشته: `node test/signup-stack.mjs <live.json>` در ریپوی `server`؛ نسخهٔ
  کهنهٔ سرورِ حساب با `HLP_ACCOUNT_DIR=<worktree>/server`).

## 📢 هشدارِ دقیق: ۷۰٪ · ۹۰٪ · تمام شد · اضافه داده شد — و «دستورِ کار» (از ۱۴۰۵/۰۷/۱۴)

خواستهٔ صاحب ریپو: «هشدارِ مخزن اومد، نگفت اون مقدار مونده، فقط عدد رو گفت…
یک اعلامیه بده که تیل بگیرید یا به یارو تیل ندید یا از شرکت انقد قرض‌دار
استین… یکم ریس‌مدلی… اگه قرض‌داری به ۷۰ فیصد از حسابش می‌رسید بگه متوجه
باشه، و تا ۹۰، و اگه تموم شد بگه تموم شده، و اگه اضافه داد بازپرسی کنه.»

- ⛔ **`DebtCalculationService.Usage` از همان سنجش‌های `Status` است**
  (`EachCheck` — یک جا). رنگِ کارت و «تمام شد» همچنان فقط از `Status`؛
  `Usage` فقط درصد و مانده را می‌گوید. دو نسخهٔ سنجش ممنوع.
- ⛔ **پله‌ها داخلِ کلید‌اند** (`StationSnapshot.DebtLevel`): ‎d7-stP-w70‎ ⇒
  ‎-w90‎ ⇒ ‎-out‎ ⇒ ‎-over‎. هر پله خبرِ تازهٔ خودش، همان پله دوباره نه.
  ‎out/over‎ حالِ ‎s=out‎ دارند و ‎w70/w90‎ حالِ ‎s=low‎. حسابِ تسویه‌شده
  (حالِ ‎ok‎ با مصرفِ ۱۰۰٪) هیچ هشداری ندارد. عکسِ بی ‎use‎ همان کلیدِ قدیمیِ
  ‎-low/-out‎. ⚠️ سرورِ حساب همین پسوندها را می‌شناسد (`pump-state.subjectOf`)
  — پسوندِ تازه ⇒ آن‌جا هم.
- ⛔ **هر هشدار `a` دارد — دستورِ کار**: «به او دیگر پطرول ندهید»، «بازپرسی
  کنید: چه کسی و چرا اضافه داد؟»، «امروز دیزل سفارش بدهید». از خودِ برنامه،
  چون قاعده این‌جاست؛ بات فقط می‌چیند.
- ⛔ **مخزن می‌گوید چند لیتر مانده و حدِ هشدار چند است** — نه فقط یک عدد.
- **بدهیِ پمپ به شرکت‌ها** (`StationSnapshot.OweAsync`، همان الباقیِ نوارِ
  بالا) با حالِ زنده به سرور می‌رود — با ترمزِ `Version` و فاصلهٔ ۹۰ ثانیه، و
  فقط وقتی عوض شد. بات آن را در «وضعیت» و «📢 اعلامیهٔ صبح» (هر روز ساعتِ ۸
  کابل، ریپوی `shop`) نشان می‌دهد.
- آزمون: `DebtUsageAlertTests`.

## 🔁 نصبی که با کدِ شش‌رقمی فعال شده بود، به پمپِ حسابِ صاحبش می‌رود (از ۱۴۰۵/۰۷/۱۴، نسخه 3.1.183)

گزارشِ صاحب ریپو: «هنوز هم کار نکرد… اشتراک برای حسابِ کاربر دادم، براش نیومده
نه آزمایشی نه اشتراکی که من دادم… با تست بگو با حدس نه، عکس بگیر.»

سنجهٔ `oldacct` حالا شش حالت دارد (الف تا و)، **با زمانِ واقعی** (`real`) و عکسِ
هر کدام. «الف» تا «د» روی نسخهٔ پیشین هم سبز بودند؛ «ه» و «و» گزارشِ او را
بازسازی کردند: نصبی که روزی با **کدِ شش‌رقمی** فعال شده بود روی پمپِ بی‌صاحبِ آن
کد می‌ماند و `HomeFromAccountAsync` فقط «این حساب مالِ پمپِ دیگری است» را
برمی‌گرداند — **هیچ‌جا دیده نمی‌شد**، و VIPی که مدیر به پمپِ حساب داد هرگز
نمی‌رسید.

- ⛔ **`BindAsync(adopt: true)`** فقط برای نصبِ **فعال‌شده** (توکنِ دستگاه دارد) و
  فقط وقتی پمپش با پمپِ حساب یکی نیست. توکنِ کنونی مدرک است و **سرورِ حساب**
  (۲.۱۱.۵، `adoptFromDevice`) تصمیم می‌گیرد: پمپِ بی‌صاحب ⇒ مالِ حساب؛ حسابِ
  پمپ‌دار ⇒ دستگاه با روزهای اشتراکش به پمپِ حساب؛ پمپِ صاحب‌دار ⇒ دست نمی‌خورد.
  - ⛔ **بی نشانِ صریحِ `adopt` در پاسخ، هیچ چیزی روی دیسک عوض نمی‌شود** — حتی
    کلیدِ عمومی (آن سنجش پیش از قفلِ کلید است). سرورِ کهنه ⇒ «سرورِ حساب کهنه
    است… به‌روز کنید».
  - ⛔ **نصبِ فعال‌نشده** که شناسهٔ پمپِ دیگری رویش مانده همان «مالِ پمپِ دیگری
    است» را می‌گیرد و بند نمی‌شود: توکنی ندارد که مدرک باشد.
  - ⚠️ شکستش در `LastBindWhy` با «این کامپیوتر روی پمپِ دیگری است — …» گفته
    می‌شود و همان ترمزِ ده‌دقیقه‌ای را دارد.
- ⛔ **توکنِ کهنه، توکنِ تازه را پاک نمی‌کند** (`DetachDeviceAsync(failed)`): بند
  شدنِ دوباره توکن را عوض می‌کند و سرور قبلی را باطل؛ نمونهٔ دیگری که هنوز توکنِ
  قبلی را داشت `device_not_registered` می‌گرفت و **توکنِ تازهٔ دیسک را خالی
  می‌کرد**. حالا اگر دیسک توکنِ دیگری دارد، همان برداشته می‌شود.
- ⚠️ `CloudLink.ResetReach()` ترمزِ ثبت (`_lastBindFailAt`، استاتیک) را هم صفر
  می‌کند — آزمونی که ثبتِ ناموفق می‌سازد ثبتِ آزمونِ بعدی را ده دقیقه می‌بست.
- **رباتِ دورهٔ آزمایشی سمتِ سرور است** (`shop` ⇒ `lib/trial-sweep.js`، سرِ بالا
  آمدن و هر شش ساعت): هر پمپی که هنوز آغازِ دوره ندارد مُهر می‌خورد. ⛔ برنامه هیچ
  آزمایشی نمی‌سازد و نباید بسازد.
- آزمون: چهار بندِ تازهٔ `CloudSessionTests` (moved · سرورِ کهنه · صاحب‌دار ·
  توکنِ کهنه) و
  `PUMP_OLDACCT_REAL=1 dotnet run --project PumpYaqobi.UiTests -c Release -- oldacct <live.json> <پوشه> real`
  (پشته: `node test/signup-stack.mjs <live.json>` در ریپوی `server`).

⚠️ **برای مشتریِ امروز دو به‌روزرسانی لازم است**: سرورِ حساب ۲.۱۱.۵ (از مرکز
فرمان) و برنامهٔ ۳.۱.۱۸۳. با یکی بی دیگری، برنامه راست می‌گوید چرا نشد.
