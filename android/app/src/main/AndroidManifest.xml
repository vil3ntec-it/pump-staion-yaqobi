<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <!-- «دریافت پیام حتی وقتی برنامه بسته است» همیشه فعال است؛ داخل برنامه هیچ
       گزینه‌ای برای خاموش کردنش نیست و مدیریتش فقط از App Info انجام می‌شود -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <!-- ذخیرهٔ فایلِ دانلودشده در پوشهٔ Downloads. از اندروید 10 (API 29) به بعد
       اصلاً لازم نیست؛ فقط برای گوشی‌های قدیمی‌تر (minSdk 23) گرفته می‌شود. -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
      android:maxSdkVersion="28"/>
  <uses-feature android:name="android.hardware.microphone" android:required="false"/>

  <!-- largeHeap: سهمِ حافظهٔ برنامه از پیش‌فرضِ گوشی بیشتر می‌شود. دفترهای
       چندساله با ده‌ها هزار ردیف باید یک‌جا در حافظه جا شوند، وگرنه اندروید
       صفحه را دور می‌اندازد و برنامه با هر برگشتن از صفر ساخته می‌شود.
       hardwareAccelerated: کشیدنِ صفحه با کارتِ گرافیک، نه با CPU. -->
  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true"
      android:hardwareAccelerated="true"
      android:allowBackup="true"
      android:largeHeap="true">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|uiMode|density"
        android:windowSoftInputMode="adjustResize"
        android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
